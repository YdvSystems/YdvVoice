# Dépendances
node_modules/

# Secrets — JAMAIS committés (repo PUBLIC : zéro secret — voir A4)
.env
.env.local
.env.*.local
*.key
*.pem
*.p12
*.pfx
credentials*.json
client_secret*.json
service-account*.json
*-credentials.json
*.token

# Méthode personnelle (IP propriétaire — privée, JAMAIS poussée sur le repo public)
# Présente en local pour le workflow ; absente du dépôt. Nettoyage de l'historique avant 1er push.
docs/PATTERN-*.md

# Analyse hors-scope — gardée LOCALE (ne pas diffuser ; demandée par Yohann, conv 2026-06-24)
Sophia-synthese-conscience-IA.md

# Témoignages / portraits personnels (matière pré-genèse de Sophia — PRIVÉS, jamais sur le repo public ; conv 9)
portrait*.md
temoignage*.md
temoignages/

# Annexe privée (marbre intégral, blocklist perso, prompts d'opération — PRIVÉE, jamais sur le repo public ; conv 12)
docs/prive/

# Base de données / runtime (SQLite WAL)
db/*.sqlite
db/*.sqlite-wal
db/*.sqlite-shm

# Buffers audio / cache de session (jamais en SQLite)
*.wav
*.mp3
*.pcm
cache/

# Audit append-only (runtime)
audit/*.jsonl

# Bancs de test jetables (essai à blanc Phase 3 — CF2 : instruments de preuve, jamais le produit ; rien ne va au dépôt public)
bancs/

# Build / dist
dist/
out/
build/

# Modèles locaux lourds (STT / wake word) — gérés hors git
resources/models/
*.bin
*.onnx

# OS / éditeurs
.DS_Store
Thumbs.db
.vscode/

# Logs
*.log

# Socle (T0+) : sortie TypeScript compilee + donnees de dev jetables + venvs Python locaux
*.tsbuildinfo
.sophia-home-dev/
.venv*/
