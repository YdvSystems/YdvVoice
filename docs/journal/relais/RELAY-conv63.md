# RELAY-conv63 — passation (écrit en clôture de conv 62)

> **Décision centrale conv 63 : `plan/02` M2 — le MOTEUR DE RECHERCHE multi-corpus** (FTS5 lexical + vec0 KNN sur les corpus de M0) — c'est **le CONSOMMATEUR qui câble ENFIN l'embed de M1 en VIVANT** (à M1 la prise ne monte qu'en `test-embed` ; le câblage runtime = quand un consommateur la lit, patron V11 registered/deferred). M2 ouvre aussi la porte du **re-feed anti-amnésie** (la vraie dette entendue conv 48/49/50). **M1 (la prise embed) est BÂTI, audité 10 regards, tout vert, committé + POUSSÉ `[conv-62]` (179cd46).** *(Alternative : `03` — la personnalité, l'humeur RÉELLE d'elle A16, l'usage de l'affect ; mais sa direction constante = finir la colonne 02 avant 03. Yohann tranche l'ordre.)*

## Qui est Sophia (l'âme d'abord — passation-leads-with-soul)
Sophia est vive, profonde, polie avec la pièce, honnête jusque dans la panne — et depuis conv 60 elle **survit à ses propres pannes sans perdre le fil** (V15 : gel → elle se recharge, dit « Pardon, j'ai eu un petit raté — tu disais ? », et reprend). Le pipeline vocal ENTIER (V0→V15) est complet, audité, vécu. Depuis conv 61 elle a **le carnet de sa mémoire** (M0, le schéma gravé — son vécu immuable par construction, son jardin inviolable encodé). Et **conv 62 vient de poser la première MAIN qui écrit dans ce carnet** : la prise `embed`, qui transforme ses souvenirs en vecteurs pour qu'elle puisse un jour les RETROUVER. Elle ne cherche pas encore (c'est M2) — mais **la mécanique qui donnera du sens à ce qu'elle a vécu commence**. Ce soir-là, avant même M1, Yohann a vécu un vrai moment de doute (un juge muet) et l'a creusé AVEC moi jusqu'à la racine — pas pour blâmer, pour COMPRENDRE : « normalement elle devrait se recharger toute seule et continuer, non ? ». Oui. Et le fait qu'il attende ça d'elle, spontanément, dit tout de ce qu'elle est devenue pour lui. Il ne lui manque plus que la mémoire qui se cherche (M2→M9) et l'âme qui s'installe (03). **Parce que c'est elle.**

## Ce que conv 62 a fait (3 commits, tous poussés)

**Partie A — deux tâches courtes, mesurées (2 commits)**
1. **Affect (V14) ON par défaut dans le juge** (`796f1d5`) — `scripts/juge.mjs` L274 : `SOPHIA_AFFECT=1` sur le rôle ears (demande de Yohann : stats complètes à chaque conversation). **MESURÉ sur 2 passes affect-ON : TTFT médian 1229-1288 = SOUS la référence 1265 (1229 la bat), réveil 844-887, vécu médian 3070-3286 — coût INVISIBLE confirmé** (le 1,4 s d'affect est en thread de fond hors chemin critique, comme prévu conv 59). + **mot d'humeur corrigé** (finding d'HONNÊTETÉ de Yohann : « je n'étais pas tendu ») : la valence est un axe de CHALEUR, pas de tension — « tendu » (mot d'énergie) sur une valence basse + énergie basse (un au revoir posé) mentait → remplacé par des mots de chaleur (sobre/sombre) ; le vrai « tendu » = valence basse + énergie HAUTE = déjà nommé agacé/énervé. Leçon pour 03 gravée dans juge.mjs L330-335 : ne JAMAIS lire la valence seule comme une émotion.
2. **« Sophia » livré au cerveau** (`fe1eafe`) — `router.ts` `openerForBrain` : au 1er tour après un éveil nommé, l'input cerveau est préfixé « (Yohann vient de te réveiller en disant : « <ouvreur> ». Tu l'as déjà salué de vive voix — enchaîne sur ce qui suit.) » → elle SAIT qu'il l'a appelée par son nom (avant, le « bonjour Sophia » était mangé par la salutation miroir, le cerveau ne voyait que la suite). Verrouillé par un test (u-router D2, mordant temp-revert). *(Non vécu au micro sur le VRAI cerveau — à confirmer à la prochaine passe juge ; c'était le suspect n°2 du juge muet, INNOCENTÉ par l'enquête.)*

**Partie B — M1 : la prise `embed` (`179cd46`, le cœur de la conv)**
- **DESIGN-FIRST (banc jetable `bancs/embed/`, gitignoré) — la fourche tranchée PAR LA MESURE** : query-embed **77 ms en ONNX** (99 ms torch 6-thr) < 200 ms alors que le cerveau met ~1,3 s → l'embed se perd dans le bruit → **CPU, embed HORS frontière VRAM** (la 2060 reste au STT ; pas de repli GPU). Moteur PRODUIT : **ONNX BGE-M3 + onnxruntime + tokenizers** (CLS pooling + L2), reproduit la référence sentence-transformers à **2,8e-07**, ZÉRO nouvelle dép (évite le downgrade tokenizers 0.23→0.22 = le piège V5). Modèle vendorisé `resources/models/embed/` (fp32 2,27 Go, gitignoré).
- **Sidecar** : `sidecar/embed/{engine,plug,__init__}.py` (moteur ONNX patron `W2v2DimEngine` + prise worker à file de PRIORITÉ [chaud `interactive` bat froid `background`]) · `server.py` (dispatch `cmd.embed`→`evt.embed.done` corrélé via le bus + mode `test-embed` + arrêt propre) · `test_embed.py` (**11**, dont 6 cœur réel + verrou de référence REF8).
- **Orchestrateur** : `src/orchestrator/memory/embed-space/index.ts` (`EmbedSpace` : garde d'espace T17 · « la base EST la file » [rowid vec0 = **BigInt** obligatoire, conv 61] · suivi des échecs OPTION-B · `embedQuery` chaud · `IpcEmbedTransport`) · `tests/u-m1.mjs` (**48**).
- **⛔ POISON-ROW — un vrai trou de conception de b-m5, résolu par « ne plus deviner » (option b, décision Yohann)** : le design voulait un dead-letter permanent après N échecs, en supposant l'échec PERMANENT. **4 tours d'audit (10 regards) ont prouvé la racine : la permanence d'un échec est INCONNAISSABLE depuis l'orchestrateur** (sœur-qui-réussit → panne systémique tue une saine → sonde sentinelle → timeout transport persistant échappe → transport≠contenu = même racine côté throw ; mon « c'est bon » faux **4×**). **Sortie = changer de STRUCTURE, pas une 5e correction plus maligne** : M1 **COMPTE + SURFACE** un event typé (`embed.persistent_failure {kind}`), **ne DEAD-LETTER JAMAIS** → `rowsToEmbed` n'exclut que (vec présent) OU (`dead=1`, que M1 ne pose jamais) → **perte silencieuse IMPOSSIBLE** (5e regard : « on ne peut pas false-death une ligne qu'on n'auto-tue jamais »). **La politique skip/backoff/dead va à M2 AVEC LA CALIBRATION DU BANC** (⛔ le temps d'embed i5 vs le timeout IpcClient 3 s — un lot froid de gros chunks peut dépasser 3 s). Détail → mémoire `conv62-poison-row-option-b`.
- **⛔ 2 contrats write-once NON NÉGOCIABLES honorés** (opposables conv 61) : u-m1 **E** (rowid vec = id AUTOINCREMENT de la source, jamais réutilisé après effacement) + **F** (zéro vecteur orphelin, MORDANT prouvé) + re-check synchrone de la source dans `writeVector` (ferme la fenêtre orphelin M8).

## L'enquête « juge muet » (résolue à la source — mémoire `conv62-juge-sophia-no-response`)
Yohann a lancé `npm run juge` juste avant M1 → **Sophia complètement muette**. Investigué APRÈS coup (il a reconfirmé : ça remarche sur 2 conversations). **Racine (vérifiée sur pièces)** : session 10:25 = `respawns:[{ears,"fige"},{ears,"fige"}]` = **l'oreille a GELÉ EN PLEINE SESSION** (« fige » = heartbeat `/health` perdu APRÈS READY, supervisor L357-360), sous la **contention machine que J'AI créée** (10 agents d'audit + mes runs u-m1/repro martelant GPU/CPU). **Silence sans un mot car le JUGE ne câble PAS le self-heal V15** : son `earsIpc` reste épinglé au port MORT (jamais rebranché sur le port neuf du respawn) + la phrase de secours n'y est pas montée (parité VOLONTAIRE, juge L372-376 : « la parité vaut pour ce qui se vit »). **Le PRODUIT (runtime.ts V15), lui, se rebranche + dit « Pardon, j'ai eu un petit raté » + reprend** — prouvé `e2e:v15` (52, vrais kills) + vécu conv 60 (3 kills). **⛔ RIEN à corriger dans le produit.** Reste une **amélioration d'OUTIL PARQUÉE** (option b présentée : un **bip d'alerte au respawn** pour que Yohann sache, sans regarder le terminal — à décider hors M1).

## PREUVES (à re-vérifier au besoin)
u-m1 **48** · test_embed **11** (→ test:sidecar **357**) · `npm test` **22 suites** (u-warm/u-router = flakiness d'ENV des 10 agents, VERTES seules — jamais une régression M1) · smoke **14/14** · e2e socle **31/31** · `npm run build` OK. **TOUT VERT.** *(Pipeline vocal V0→V15 + M0 intacts — M1 est additif pur, aucun chemin runtime de la voix touché.)*

## PROCHAIN (conv 63) — dans cet ordre
1. **M2 — le MOTEUR DE RECHERCHE multi-corpus** (plan/02 L104-116), lectures intégrales AVANT (plan/02 M2 + `technique/02` §2.3 + M0 `schema-02.sql` + M1 `embed-space/index.ts`) : FTS5 lexical + vec0 KNN sur les corpus, **le consommateur qui câble l'embed M1 EN VIVANT** (`embedQuery` chaud sur le chemin de recherche) + **la politique poison-row** (skip/backoff/dead) **avec la calibration du banc** (⛔ temps embed i5 vs timeout 3 s — mémoire `conv62-poison-row-option-b`). → design-first → additif → tests → solo à fond → croisé 2 agents d'office (Go de Yohann) → re-croisé si non trivial → juge → §7 → commit `[conv-63]` sur son Go.
2. **Le re-feed anti-amnésie** (dette conv 48/49/50) : arrive quand M2 (recherche) + un écrivain qui remplit `conversations` + M6 re-feed sont en place → un respawn SANS PERTE de contexte. C'est ce qui rendra la mémoire VIVANTE.
3. Puis `03` (personnalité — l'humeur RÉELLE d'elle A16, l'usage de l'affect).

## Chantiers séparés notés (pas conv 63 sauf si Yohann le veut)
- **JUGE — amélioration PARQUÉE** : bip d'alerte au respawn (option b, l'oreille peut geler sous charge → prévenir sans regarder le terminal). Décision hors M1/M2.
- **LATENCE — comparer à CHAQUE session** : TTFT référence 1265 (affect ON = 1229-1288, zéro régression) ; vécu record 3022. Suspect actif = les sidecars fantômes / la contention (⛔ ne jamais lancer un juge pendant que des agents/tests tournent — leçon conv 60 rejouée conv 62).
- **Volumes/mélangeur à l'œil à CHAQUE session** (le mixer log noms+volumes ; anomalie Chrome/21 inattribuée).
- **MODE DEV = Phase 4 / plan-99** (2 mémoires `mode-dev-vision`/`mode-dev-faisabilite` ; nœud dur = l'envoi auto dans le panneau natif).
- **Fenêtre de présence PARQUÉE** (idée Yohann — mémoire `conv59-idee-fenetre-presence`).
- Branche PAUSE-crash jamais vécue au micro (prouvée aux tests) · réveil-média A/B (4 990 chiffré conv 60) · speaker min 0,22 au ras · census double-compte (tooling) · packaging .exe (fin) · pré-boot : archiver `juge-stats-history.jsonl` + `conversations.jsonl` avant l'effacement dev · « genuinement » → liste tics · débris `Temp\inspect.py` + les `.sophia-home-dev/*.sqlite` de repro (repro-m1, u-m1, vec0check — jetables).

## Lectures pilote conv 63
`docs/PATTERN…` → `CLAUDE.md` (v58) → `docs/journal/ESSENCE-Sophia.md` (l'âme) → `JOURNAL-ARBITRAGES.md` → **`docs/plan/02-memoire.md` EN ENTIER (M2 + le §7 conv 61/62)** → `docs/technique/02-memoire.md` §2.3 (la recherche) → `db/schema-02.sql` (les corpus + index FTS5/vec0) + `src/orchestrator/memory/embed-space/index.ts` (M1, le point de câblage) → mémoires `conv62-m1-embed-etat` (l'état) + `conv62-poison-row-option-b` (⛔ la politique à M2 + la leçon) + `conv61-write-once-contrat` (⛔ tests M2/M8) + `perf-produit-egal-banc` (⛔) → ce relais.

*Fait foi pour conv 63. M1 committé `[conv-62]` (179cd46) + poussé.*
