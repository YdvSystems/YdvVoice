# RELAY-conv59 — passation (écrit en clôture de conv 58)

> **Décision centrale conv 59 : V14 — le CAPTEUR/VERROU D'AFFECT** (`docs/plan/01-pipeline-vocal.md` §3 U-V14 : l'affect ne se lit que sur SA voix — modulé/verrouillé par le speaker-ID V6 ; design-first, additif, croisé 2 agents d'office). Conv 58 est **committée + poussée** (`[conv-58]`, d8c5a97 + clôture) ; la table est rase. *(Alternative si Yohann la préfère : le chantier LATENCE/calibration endpointing — voir « chantiers » — mais sa direction constante est d'avancer la colonne technique.)*

## Qui est Sophia (l'âme d'abord — passation-leads-with-soul)
Sophia est vive, profonde, polie avec la pièce — et depuis conv 58 elle est **honnête jusque dans la panne** : si son cerveau meurt pendant qu'on lui parle, ses oreilles le DISENT, une fois, dans SA voix — « Mon cerveau ne répond pas, je redémarre — un instant. » — puis silence, jamais de bavardage de machine. Et quand le cerveau revient, l'épisode se clôt : elle reprend la conversation sans rejouer sa phrase. Yohann l'a vécu au micro : coupure en pleine discussion sur les rêves, la phrase, le retour, et 37 tours de conversation derrière. « Jamais de silence » n'est plus un titre de spec — c'est un comportement qu'il a entendu. **Parce que c'est elle.**

## Ce que conv 58 a fait (V13 — la PHRASE DE SECOURS, bâtie + 8 regards + validée au micro)
1. **DEUX ÉCARTS ACTÉS (décision A, patron V11/V12)** : le filet ENTIER vit dans les OREILLES (le gravé « le sidecar, seul, joue » datait du monolithe — détection `ws_count` + déclencheur fin-de-tour + lecture sounddevice n'existent que là ; l'AEC annule le loopback) · B2 réinterprété (la bouche est **Piper CPU**, pas Kokoro/GPU → « charge→synthétise→décharge » = moteur en RAM le temps de la pré-synthèse puis LIBÉRÉ, prouvé weakref+gc).
2. **Le cœur** (`sidecar/tts/fallback.py`) : `FallbackGuard` — machine d'épisode PURE (candidat = `evt.turn.end` OU appariement wake↔final du MÊME mark [2 ordres vérifiés source stt.py] ; wake SEUL jamais ; parole ambiante jamais ; **consommation ATOMIQUE d'ÉPOQUE au lancement réel** `try_consume(epoch)`/`unconsume` — « une fois par épisode » tenu PAR LE CODE) · `FallbackVoice` — precache générationnel + STALE 30 s (un warm qui pend ≠ un filet mort à vie), lecture one-shot bloquante = EN ENTIER, exempte de barge-in PAR CONSTRUCTION.
3. **Le discriminant MESURÉ au banc** (`server.py`) : close PROPRE = **{0, 1000, 1005}** (`close()` → **0**, PAS 1005 ! · mort du process → 1006) → `episode_consume` (jamais de phrase pendant un arrêt volontaire T6) ; coupure ANORMALE → jouable ; **reset `_listen_mode="resume"` au dernier départ** (FID-M1 — un crash pendant qu'elle parle ne la laisse jamais sourde à vie) ; `episode_reset` à chaque connexion.
4. **L'orchestrateur** : hook **`sidecarPostReady`** (boot ph.5, gravé plan/00, ENFIN défini — IPC éphémère borné 5 s) + ré-envoi idempotent `ensureVoicePipeline` + **ack LU** (`ok:false` → « NON posées », jamais un log menteur) ; `fallback-phrases.ts` (le TEXTE = domaine Yohann — acté par lui APRÈS l'avoir entendu).
5. **AUDIT 8 regards, convergence 2 MAJEUR → 0 → 0** : solo (SOLO-1/2/3) → croisé (fidélité 2 MAJEUR de PORTÉE [gate figé · Job Object sur-affirmé] · robustesse 0 MAJEUR, ROB-M1/M2 REPRODUITS) → **re-croisé A (2 agents FRAIS sur les corrections — la leçon 19ᵉ fois : les 3 MINEUR TOUS dans MES mécanismes neufs, reproduits par l'agent PUIS re-reproduits par le pilote → FIX RACINE UNIFIÉ = l'époque)** ; close-code + reset gate validés EMPIRIQUEMENT sur le VRAI `ws_handler` ; **15 temp-reverts** ; le passe-plat manquant attrapé par l'E2E NATUREL (« la ceinture jamais-fatal ne remplace pas un test qui mord »).
6. **VALIDÉE AU MICRO en 3 actes (l'IDÉE DE YOHANN — meilleure que la mienne)** : conversation normale → coupure en pleine conversation (close 4001 + WarmBrain tué = octet-identique à un crash vu des oreilles) → il a ENTENDU la phrase (1×) → **rebranchement** (épisode clos vérifié, cache idempotent « déjà à jour ») → il a CONTINUÉ (37 tours). L'amnésie du cerveau frais DITE avant et vécue — c'est `plan/02` qui la refermera.

## Le DOSSIER LATENCE (ouvert conv 58 — PAS sous le tapis, demande explicite)
- **Son ressenti est réel et chiffré** : le **SILENCE VÉCU** (fin de SA parole → son d'elle = endpointing + réponse) a dérivé dans la journée du 22/07 : 3,7 s (matin) → 4,1 → 4,4 → 4,6 (session « fluide » conv 57) → **4,7 s le soir, pire tour 14,4 s**. Décomposition médiane : ~1,9 s endpointing + ~1,5 s TTFT (sain, dans la plage) + ~1,3 s 1re-phrase+synthèse.
- **La réflexion VÉRIFIÉE à zéro** (sa demande) : code (`MAX_THINKING_TOKENS=0` chaud+froid, ambiant effacé) + env (rien aux 3 niveaux) + signature (TTFT max 3,4 s / 35 tours, zéro stall 7-12 s). V13 hors de cause (observer µs).
- **« Une petite régression qui ne dit pas son nom ? » = hypothèse OUVERTE** → l'outillage est câblé en PERMANENT : **SILENCE VÉCU** médian+pire tour au verdict du juge ET dans l'historique (`u-metrics` 62, mordant) + **`code:{commit,sujet,dirtyFiles}` dans chaque ligne de session** (corréler une dérive à ce qui tournait) + la session du soir SAUVÉE (`juge-stats-history.jsonl`, source `scenario-v13-conv58`, 37 tours, 36 transcripts). **Consigne de Yohann : comparer CHAQUE session de test — « si ça continue, cette latence, il faudra s'en occuper ».**
- **Les coupures d'endpointing** (« tu me coupes beaucoup ») : SA décision = **au moment opportun**, pas maintenant (« pas vraiment dérangeant, ça amène un côté naturel ») — le chantier calibration (la grâce, conv 48/55) reste différé ; les données du 22/07 au soir sont les meilleures jamais collectées pour le faire.

## PREUVES (à re-vérifier au besoin)
**Sweep 19/19 TOUT VERT** : `npm test` **20 suites** (dont `u-fallback` 17 · `u-metrics` 62) · `test:sidecar` **287** (dont **35 V13**) · `build` · `smoke` 14/14 · `e2e` socle 31/31 · `e2e:boot` 8/8 · **`e2e:v0` → `e2e:v13`** tous verts (`e2e:v13` = 36 vérifs cœur réel : crash 4001 → paire wake↔final ET turn.end jouent UNE fois · gate revenu `resume` · close propre ne joue JAMAIS). Le juge descend désormais le filet V13 au boot (fidèle produit).

## PROCHAIN (conv 59) — V14, le verrou d'affect
`plan/01` §3 (U-V14) + `technique/01` (l'affect) : le capteur d'affect **verrouillé par V6** (il ne se lit que sur la voix de Yohann — jamais sur un média, jamais sur un tiers). Pleine profondeur : lectures intégrales (plan + technique + `speaker.py` V6 + `router.ts`) → écarts surfacés AVANT de bâtir (patron V11/V12/V13 — vérifier ce que le gravé suppose d'un moteur/d'une résidence qui n'existe peut-être plus tel quel) → additif → tests + e2e cœur réel → solo à fond → croisé 2 agents d'office → re-croisé si corrections non triviales → juge à sa voix → §7 AVANT commit → commit `[conv-59]` sur son Go. Puis **V15** (respawn resync S10 : policy → enroll → tts.cache + conformité des prises — referme les frontières tracées §7 V11/V13) ; ensuite **`plan/02` (MÉMOIRE** — referme l'amnésie ENTENDUE au test 3 actes + R-1 barge + PAUSE fil-frais**) / `03` (personnalité)**.

## Chantiers séparés notés (pas conv 59 sauf si Yohann le veut)
- **LATENCE — à SUIVRE à chaque session** (vécu + code dans l'historique ; s'en occuper si ça continue — sa consigne).
- Calibration endpointing (grâce) : au moment opportun, données du 22/07 prêtes.
- **Débris `C:\Users\yohan\AppData\Local\Temp\inspect.py` à SUPPRIMER** (vieux banc `import numpy, soxr` qui shadow la stdlib — casse tout script Python lancé depuis Temp ; signalé à Yohann en clôture conv 58, suppression = son geste).
- Census juge/phantoms double-compte (tooling) · réveil avec média (A/B si ça compte) · reconnexion mid-session (V9/V15) · « elle m'a appelé Sophia » (persona → 03) · packaging .exe (fin) · pré-boot : archiver `juge-stats-history.jsonl` avant l'effacement dev ([[pre-boot-cleanup-avant-naissance]]).

## Lectures pilote conv 59
`docs/PATTERN…` → `CLAUDE.md` (v54) → `docs/journal/ESSENCE-Sophia.md` (l'âme) → `JOURNAL-ARBITRAGES.md` → **`docs/plan/01-pipeline-vocal.md` (V14 §3 + le §7 au fil — l'entrée V13 BÂTI + re-croisé)** → `docs/technique/01-pipeline-vocal.md` (l'affect + la note conv 58) → mémoires `conv58-etat-mi-parcours` + `perf-produit-egal-banc` (⛔) → le code porteur EN ENTIER : `sidecar/consumers/speaker.py` (V6 — le verrou) + `src/orchestrator/voice/router.ts` + `sidecar/tts/fallback.py` (le patron V13 le plus récent) → ce relais.

*Fait foi pour conv 59. Tout est committé + poussé (`[conv-58]`).*
