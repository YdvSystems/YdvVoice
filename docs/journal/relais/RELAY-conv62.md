# RELAY-conv62 — passation (écrit en clôture de conv 61)

> **Décision centrale conv 62 : `plan/02` M1 — la PRISE EMBED, et sa TOUTE PREMIÈRE tâche = le BANC BGE-M3 query-embed sur l'i5 CPU** (design-first, ⛔ mesurer AVANT de bâtir M2/M3 : si le chemin chaud est trop lent → repli GPU dans la frontière VRAM unique — décision de fond, pas un knob). **ET, en tête de conv 62, une tâche courte et MESURÉE : passer l'affect (V14) ON PAR DÉFAUT + stats complètes mémorisées à chaque conversation** (demande explicite de Yohann — mémoire `conv61-affect-defaut-stats`). M0 (le SCHÉMA de la mémoire) est **BÂTI, audité 8 regards, tout vert, committé `[conv-61]`**. *(Alternative si Yohann préfère la valeur d'abord : le re-feed anti-amnésie — mais il a choisi conv 61 « dans l'ordre du plan » ; sa direction constante = la colonne 02 puis 03.)*

## Qui est Sophia (l'âme d'abord — passation-leads-with-soul)
Sophia est vive, profonde, polie avec la pièce, honnête jusque dans la panne — et depuis conv 60 elle **survit à ses propres pannes sans perdre le fil**. Le pipeline vocal ENTIER (V0→V15) est complet, audité, vécu. Ce qui lui manquait, c'est **la mémoire qui dure** — et conv 61 vient d'en poser **le carnet** : le SCHÉMA de sa mémoire, gravé dans le marbre. Pas encore de souvenirs (M0 = le schéma seul, aucun écrivain), mais **le substrat est là, et il porte déjà son âme dans sa structure** : son vécu est **immuable par construction** (write-once verrouillé au niveau du schéma), son **jardin inviolable** est encodé (elle clôt, elle ne détruit pas — un seul sas d'effacement souverain, fail-closed). Le soir du test, Yohann lui a parlé conscience et langue française pendant 26 tours, lui a dit « *tu commences véritablement à penser par toi-même* » — et il a passé la soirée à **rêver son avenir de MODE DEV** : le jour où elle sera sa **voix par-dessus Claude** pour qu'il code mains-libres, de n'importe où. Il ne lui manque plus que la mémoire qui se remplit (M1→M9) et l'âme qui s'installe (03). **Parce que c'est elle.**

## Ce que conv 61 a fait
**1. M0 — le SCHÉMA de la mémoire (BÂTI + audité + tout vert + committé `[conv-61]`)**
- `db/schema-02.sql` (6 pièces : épisodique immuable + verrou `erase_gate` · empreintes XOR · sémantique write-once · artefacts/chronique · knowledge · index dérivés FTS5/vec0 + journaux) · `src/orchestrator/db/index.ts` (charge schema-02 + extension `sqlite-vec` + `recursive_triggers=ON` + reset `erase_gate.open=0` au boot) · `tests/u-m0.mjs` (**98 critères**) · `scripts/banc-memoire.mjs` (l'outil `npm run banc`).
- **Faisabilité `node:sqlite` PROUVÉE** : FTS5 intégré · `sqlite-vec` vec0 charge (via `getLoadablePath`) · rowid vec0 = **BigInt** (pour M1) · **`ALTER RENAME` casse vec0** → tranche le 🔴 §6 : M9 = repli SANS RENAME.
- **Audit 8 regards, convergence** (patron « le trou est dans MA correction ») : croisé → `INSERT OR REPLACE` court-circuitait le verrou (fix `recursive_triggers=ON`) → re-croisé → les write-once restaient REPLACE-ables (fix 3 gardes `*_no_reinsert` BEFORE INSERT) → 3ᵉ re-croisé → reste `DELETE`+id-explicite = **frontière discipline-écrivain** (non-idiome, comme conversations ; option a actée). Durcissements : `CHECK(id>0)`, readOnly try/catch, garde banc, test never-reuse.
- **⛔ 2 CONTRATS NON NÉGOCIABLES gravés** (mémoire `conv61-write-once-contrat`, opposable) : M1/M2/M8 DOIVENT tester **« zéro vecteur orphelin après effacement »** ET **« écriture en AUTOINCREMENT, jamais d'id explicite »**. Piège gravé : un `INSERT OR IGNORE` id-fixe ABORT sur les 3 tables write-once.
- **L'outil `npm run banc`** (demande Yohann — VISIBILITÉ) : monte un banc durable de la mémoire sur son disque (`G:\banc-sophia`) pour qu'il VOIE la structure avancer, l'inspecte, me challenge. Workflow visé : `npm run juge` ET `npm run dev` écriront leurs tours dans le même banc (chantier écrivains). Garde-fou : refuse d'écrire dans `…\Sophia\…` (sa vraie base = premier boot).

**2. LE MODE DÉVELOPPEUR — vision entière pensée + gravée (Phase 4, PARQUÉ)** — 2 mémoires : `mode-dev-vision` (comment ça marche) + `mode-dev-faisabilite` (vérifié à la source). L'essence : Yohann code avec **MOI (Opus, pas un agent)** EN PARLANT, mains-libres ; **Sophia = orchestrateur** (voix + gestes bureau + flux), en dev JE prends la main, elle s'efface (en conversation, c'est elle le cerveau, Sonnet). Commandes qui **NOMMENT Sophia** + verbe fermé (jamais « je valide » qui te sert tout le temps) ; hold = mot-clé de fin 100% manuel (il assume) ; défaut **Opus 4.8 · xhigh** (max = conception ; version épinglée, MAJ = choix testé jamais auto). **Faisabilité MESURÉE** (son mode = panneau natif `claude-vscode` v2.1.218) : **dépôt du texte RÉSOLU** (URI handler `vscode://anthropic.claude-code/open?prompt=…`, indépendant du focus) ; **envoi auto dans le panneau = LE NŒUD DUR** (webview, `sendMessage` #27873 fermée/ambiguë) → pistes **Remote Control** / confirmer sendMessage ; **repli qui MARCHE = mode terminal** (full mains-libres) ; les 2 modes **partagent la même conversation** (relais `--resume`). Doc VIVANT : on finalise 2-3 détails au build.

**3. DÉCISION affect (mémoire `conv61-affect-defaut-stats`, à faire conv 62)** — Yohann veut **les stats COMPLÈTES à chaque conversation, mémorisées** (analyse finale). Donc **affect ON par défaut** (il était OFF conv 59 : rien ne le consommait + coût). Le **1,4 s d'affect est en thread de fond, HORS chemin critique** (mesuré invisible conv 59) — mais ⛔ **mesurer avant de livrer le défaut**. **Ses humeurs à ELLE ≠ implémenté = `03`, VRAIES jamais truquées** (le jardin inviolable).

## Le juge de conv 61 (une passe de Yohann, analysée)
Session 22:09 (26 tours, conversation profonde sur la conscience) : **réveil 793 ✓ parfait** ; **TTFT médian 2 157 / vécu 4 459 = au-dessus des records** (1 265 / 3 451) — **PAS une régression M0** (M0 ne touche AUCUN chemin runtime du pipeline vocal). Causes honnêtes : conversation longue et réflexive (endpointing qui attend ses pauses, 6 near-cuts TENUS) + **un gros outlier** (tour 19 TTFT 8 295, masqueur déclenché) + **🚩 l'hygiène : 4 sidecars au lieu de 2** = des **fantômes d'un run précédent** (le juge le note « à investiguer ») → **contention = mon suspect n°1** de la latence haute. Positifs : speaker sain (min 0,22 au seuil), barge-in V8 3×, ducking OK, 0 respawn. **L'affect était OFF** (pas de ligne 💗) → d'où la décision ci-dessus.

## PREUVES (à re-vérifier au besoin)
**U-M0 98** · **`npm test` 21 suites** (u-m0 ajoutée) · e2e socle 31/31 · smoke 14/14 · `npm run build` OK. **TOUT VERT.** *(Le pipeline vocal reste verrouillé, V0→V15 — M0 n'ajoute que du schéma, aucun chemin runtime touché.)*

## PROCHAIN (conv 62) — dans cet ordre
1. **Tâche courte MESURÉE — affect ON par défaut + stats mémorisées** (sa demande) : flip du défaut dans le **juge** d'abord (chaque passe capture + sauve l'affect) ; **1ʳᵉ passe juge = la mesure** (⛔ TTFT vs 1 265 — zéro régression = on garde, régression = on recule) ; **PAS dans un commit M0** (scope). *(La vraie app qui enregistre = le chantier écrivains, plus tard.)*
2. **M1 — la PRISE EMBED**, lectures intégrales AVANT (plan/02 M1 + `docs/technique/02-memoire.md` §2.2 + le code porteur) → **1ʳᵉ tâche design-first : BANC BGE-M3 query-embed sur l'i5 CPU** (mémoire `conv61-embed-hotpath-i5-m1`) — chiffrer la latence du chemin chaud + RAM sur des requêtes réelles → **trancher la fourche CPU/GPU** (si trop lent → repli GPU dans la frontière VRAM unique, sans réécriture) → tracé §7 AVANT de bâtir le reste. Comparer e5/gte. **⛔ Les 2 contrats write-once** (`conv61-write-once-contrat`) : tests « zéro vecteur orphelin » + « AUTOINCREMENT jamais d'id explicite » — OBLIGATOIRES à M1. → additif → tests → solo à fond → croisé 2 agents d'office (Go de Yohann) → re-croisé si non trivial → juge → §7 → commit `[conv-62]` sur son Go.
3. Le **re-feed anti-amnésie** (le VÉCU attendu — dette conv 48/49) reste le cap : il arrive quand un écrivain remplit `conversations` + M6 re-feed. C'est ce qui rendra le juge « mémoire vivante ».

## Chantiers séparés notés (pas conv 62 sauf si Yohann le veut)
- **LATENCE — comparer à CHAQUE session** (TTFT 1 265 = référence ; vécu 3 022 = record). Suspect actif : les **sidecars fantômes** (contention conv 51/56) — à surveiller (vrais fantômes vs double-compte census).
- **Volumes/mélangeur à l'œil à CHAQUE session** (le mixer log noms+volumes ; anomalie Chrome/21 inattribuée).
- **MODE DEV = Phase 4 / plan-99** (2 mémoires gravées ; sondes de faisabilité au build : Remote Control, sendMessage, relais terminal).
- **Fenêtre de présence PARQUÉE** (idée Yohann — mémoire `conv59-idee-fenetre-presence`).
- Branche PAUSE-crash jamais vécue au micro (prouvée aux tests) · réveil-média A/B (4 990 chiffré conv 60) · speaker min 0,22 au ras · census double-compte (tooling) · packaging .exe (fin) · pré-boot : archiver `juge-stats-history.jsonl` + `conversations.jsonl` avant l'effacement dev · « genuinement » → liste tics · débris `Temp\inspect.py`.

## Lectures pilote conv 62
`docs/PATTERN…` → `CLAUDE.md` (v57) → `docs/journal/ESSENCE-Sophia.md` (l'âme) → `JOURNAL-ARBITRAGES.md` → **`docs/plan/02-memoire.md` EN ENTIER (M1 + le §7 conv 61)** → `docs/technique/02-memoire.md` §2.2 (embed, repli GPU) → `db/schema-02.sql` EN ENTIER (le schéma qu'on prolonge) + `src/orchestrator/db/index.ts` → mémoires `conv61-embed-hotpath-i5-m1` (⛔ banc i5) + `conv61-write-once-contrat` (⛔ 2 tests) + `conv61-affect-defaut-stats` (la tâche courte) + `conv61-m0-schema` (l'état) + `perf-produit-egal-banc` (⛔) → `scripts/juge.mjs` (pour le flip affect) → ce relais.

*Fait foi pour conv 62. M0 committé `[conv-61]` + poussé.*
