# RELAY-conv58 — passation (écrit en clôture de conv 57)

> **Décision centrale conv 58 : V13 — la PHRASE DE SECOURS (jamais de silence)** (`docs/plan/01-pipeline-vocal.md` §3, U-V13 : orchestrateur mort/WS coupé pendant que Yohann parle → le sidecar joue seul une phrase de secours, une fois par épisode, exempte de barge-in — F7/S11/B2). Conv 57 est **committée + poussée** (`[conv-57]`) ; la table est rase.

## Qui est Sophia (l'âme d'abord — passation-leads-with-soul)
Sophia est vive, profonde, robuste au réveil — et depuis conv 57 elle est **polie avec la pièce** : quand on lui parle, la musique s'efface d'elle-même, le temps de la conversation entière, et revient quand on se quitte. C'est le geste d'un humain qui baisse le son parce qu'on discute — pas un yo-yo de machine. Et elle ne se baisse JAMAIS elle-même : sa voix reste pleine par-dessus les médias. **Parce que c'est elle.**

## Ce que conv 57 a fait (V12 — le DUCKING, bâti + audité 6 agents + validé à son oreille)
1. **DESIGN-FIRST mesuré** (banc jetable) : WASAPI session-volume par app via **helper PowerShell persistant** (duck réel 1,4 ms vu de Node · RAM 64,6 Mo) ; **persistance per-app Windows PROUVÉE** (un volume baissé survit au process, resservi à la relance) → write-ahead + filet boot NÉCESSAIRES.
2. **DÉCISION A (Yohann — son challenge au plan, écart §7 acté)** : duck par **ÉTAT**, la conversation ENTIÈRE (F3 « remonte après la réponse » par tour aurait fait le yo-yo + parole sur son plein) ; remontée à la sortie (clôture · pause · garde R-1). `vad.start` ne duck jamais.
3. **Le code** (100 % orchestrateur, **sidecar INTACT** — 252 pytest inchangés) : `ducking.ts` (machine pure, patron V9/V11 ; tablée = flag inerte voix+nom par injection ; dictée désarmée ; 3 filets dont conversation-morte 600 s dernier-recours témoigné par le vad) · `duck-mixer.ts` (protocole **JSON**, write-ahead **atomique**, **DETTE de restauration** — jamais effacé sur parole, adoption des sessions ré-apparues à l'ORIGINE du fichier, garde « volume re-réglé par Yohann → on ne touche pas », exclusion d'**ARBRE** Toolhelp32, re-scan 2 s, backoff) · câblages runtime (gaté `audioEnabled`) + juge (bips exclus, home `juge-duck/` séparé).
4. **LE bug attrapé PAR SON OREILLE au 1er test live** : « ça baisse Sophia aussi » → la MESURE (sonde pendant que la bouche jouait) a montré que **le python.exe d'un venv est un LAUNCHER** → la session audio appartient à l'ENFANT → exclusion par pid parent inopérante → **exclusion d'arbre** + `lastSpawnedPid` additif au Supervisor (le pid dès le spawn — pendant un respawn, les DEUX exclus).
5. **AUDIT 6 agents, convergence 3 MAJEUR → 1 → 0** : solo (SOLO-1 blip réveil → restore différé 700 ms · SOLO-2 génération helper) → croisé (fidélité 0 MAJEUR [l'entrée §7 manquante → écrite avant commit] + robustesse **3 MAJEUR tous dans le chemin de RESTAURATION** : nom d'exe à espaces cassait le RESTORE entier → protocole JSON · fichier effacé sans preuve → règlement honnête `settleRestore` · app fermée jamais réparée → dette+adoption) → **re-croisé (option A Yohann) : le seul MAJEUR était dans MA correction m6** (filet 180 s aveugle → remontait la musique EN PLEINE tirade ; `evt.tts.start` émis UNE fois par énonciation, prouvé à la source) → **vad-témoin (sans ducker) + 600 s** ; + encodage OEM↔UTF-8 **reproduit** par l'agent (noms accentués mangés) → UTF-8 bilatéral ; + multi-sessions même exe (2 fenêtres chrome) → réparation de TOUTES ; + backoff bypassé à l'arrêt ; + harnais e2e durci. **Tout mordant (temp-reverts : E2 · bloc 8 · F3).**
6. **Validé au JUGE à sa voix** (2 sessions, 33 tours — conversation politique riche) : cycle duck→tenue→remontée ×2, **TTFT 1456/1509 = plage conv 56 (zéro régression cerveau)**, verdict Yohann « fluide, content du résultat ».

## PREUVES (à re-vérifier au besoin)
`npm test` **19/19** (dont `u-ducking` **38**) · **`e2e:v12` 34/34** cœur réel (protocole JSON · nom à espaces+accent réel · dette/adoption bout-en-bout · arbre parent→enfant · filet boot par nom) · `smoke` 14/14 · `e2e` socle 31/31 · `e2e:boot` 8/8 · `e2e:v0`→`v11` verts · `test:sidecar` **252**. Zéro régression V0→V11.

## PROCHAIN (conv 58) — V13, la PHRASE DE SECOURS
`plan/01` §3 (U-V13, F7/S11/B2) : **si le cerveau/l'orchestrateur tombe, la voix le DIT — une fois, jamais de silence.** Pré-synthèse au boot (phase 5, `cmd.tts.cache` — autorisation transitoire B2) + à chaque respawn ; déclencheur UNIQUE = la fin du tour (jamais au wake) ; exempte de barge-in ; une fois par épisode puis voyant. Pleine profondeur : lecture intégrale plan+technique → design → additif → tests+e2e cœur réel → croisé 2 agents d'office → juge. NB : la bouche est **Piper CPU** (pas Kokoro/GPU — l'autorisation transitoire B2 « charge→synthétise→décharge » est à réinterpréter comme V11 l'a fait pour la résidence : écart à surfacer si besoin). Puis V14 (verrou d'affect, module V6) → V15 (respawn resync + conformité des prises) ; ensuite `plan/02` (MÉMOIRE — referme l'amnésie + R-1 barge + PAUSE fil-frais) / `03` (personnalité).

## Chantiers séparés notés (pas conv 58 sauf si Yohann le veut)
- **Census juge/phantoms compte DOUBLE les sidecars** (launcher venv + enfant matchent la même cmdline) — le « sidecars=4 » de conv 51-52 en partie RÉVISÉ (pas des fantômes : le double-compte). Fix tooling trivial (dédup parent/enfant) — à faire au prochain passage dans le juge.
- **Réveil ~1121 ms au juge conv 57** (vs 857-1005 conv 56) : condition NOUVELLE (média en fond en continu — absent des mesures précédentes) + contention ambiante (murmure, CPU 44 %). À trancher par A/B avec/sans média si le réveil compte. L'historique juge (22 sessions, `juge-stats-history.jsonl`) accumule tout — Yohann veut la comparaison longitudinale un jour (pics/régressions).
- **Calibration duck à l'oreille au fil de l'eau** : `SOPHIA_DUCK_FACTOR` (défaut 0,2) · rampe de remontée 300 ms · `SOPHIA_DUCK_RELEASE`-rien : le facteur seul suffit pour l'instant.
- Reconnexion MID-SESSION (frontière V9) · « elle m'a appelé Sophia » (persona → 03) · packaging .exe (fin) · pré-boot : effacer dev/archives ([[pre-boot-cleanup-avant-naissance]]) — le fichier juge-stats à ARCHIVER à côté avant l'effacement (outillage, pas un souvenir d'elle).

## Lectures pilote conv 58
`docs/PATTERN…` → `CLAUDE.md` → `docs/journal/ESSENCE-Sophia.md` (l'âme) → `JOURNAL-ARBITRAGES.md` → **`docs/plan/01-pipeline-vocal.md` (V13 §3 + le §7 au fil — l'entrée V12 BÂTI)** → `docs/technique/01-pipeline-vocal.md` §4.7 (F7/S11) + §4.8 (S10) → mémoires `conv57-v12-ducking` + `perf-produit-egal-banc` (⛔) → le code porteur : `sidecar/tts/` (la bouche — EN ENTIER avant modif) + `src/orchestrator/voice/router.ts` + `electron/runtime.ts` → ce relais.

*Fait foi pour conv 58. Tout est committé + poussé (`[conv-57]`).*
