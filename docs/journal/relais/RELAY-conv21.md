> **DÉCISION CENTRALE conv 21 ouverture** : **graver `plan/99` (orchestration) — le DERNIER plan de la Phase 3** — depuis la source acquise `docs/technique/99-orchestration.md`. Le `99` **assemble, ne crée aucune mécanique neuve** : table globale des états · chemin d'un tour (l'aiguilleur d'A2) · composition finale du prompt (identité→mémoire→cadre, budgets cloisonnés) · grille finale 20 entrées · affordances UI/systray (« la voix a tout, l'UI en témoin ») · **gabarit fork « écris ta propre entité »**. Même méthode : couche par couche à pleine profondeur, gabarit 7 rubriques, **solo à fond + GRILLE des 8 classes**, puis **croisé d'office 2 agents Opus sur le Go de Yohann**. **⚠️ Obligations gravées à honorer dans `plan/99`** : (1) la **« posture de sécurité honnête »** du gabarit fork (§2.4, à côté de l'honnêteté agentique — `plan/05` §7 conv 20 : politique de clé 3 postures + modèle de menace par posture + effacement découplé de la posture + « choisis ta posture pour ton contexte ») ; (2) la **consigne du signal de fermeture des `self_notes`** au cadre du prompt, à côté de celle du tag d'humeur (AF-9 — `technique/99` §4.4 ne la liste pas encore) ; (3) « **tout affichage = vue dérivée de l'état** » — vérifier qu'aucun composant d'assemblage ne **stocke** du contenu (GRILLE classe 2). Puis **essai à blanc** (priorité n°1 : le banc audio temps-réel — 🔴 wake FR + AEC).

# RELAY conv 21 — graver `plan/99` (le dernier plan) → essai à blanc

> Actualisé en clôture de conv 20. Prompt de passation intra-projet, lu par la session Opus de conv 21 (contexte frais). Zéro donnée perso. Format : annonce brève → mots simples → un par un → reco/pourquoi-pas → validation → inscription → **commit au fil / push en clôture** (R5).

## Ce qui s'est passé en conv 20
- **`plan/05` (ressources · résilience · coût) GRAVÉ (R0→R7) + AUDITÉ EN PROFONDEUR + committé `[conv-20]` `07708e1`.** Le 6ᵉ plan de la Phase 3 : gestionnaire de modèles (3 températures · Phi-4-mini tiède d'office) · frontière VRAM + calques SECOURS/JEU (dégrader par la taille, la bouche jamais) · échelle de panne 4 marches · session chaude + politiques de chauffe (preuve « réchauffer sans écrire ») + **montre des jetons (Claude ET Google)** · prise cerveau + slot tiers (forks) + **détecteur** (hystérésis · quota→local auto · le juge n'est pas le patient) · **ligne d'argent** (API convoquée par Yohann seul) + cost-guard € · **kill-switch + grâce** · **plancher de rêve** · **registre du gardien** (vue dérivée). 13 critères pointés.
- **Décisions Yohann tranchées** : (1) **chiffrement au repos = EXIGENCE PLEINE** — toute copie durable (vif `G:\` + étage 2 + copie hors-machine) ; invariant « clé perdue ≠ Sophia perdue » = **séquestre de clé indépendant non circulaire** (jamais dans le backup qu'il déchiffre). (2) **`KEY_PROVISIONING_POLICY` à 3 postures** (`tpm-auto` défaut / `pin-boot` / `compromis`), chacune avec son **modèle de menace** ; **le fork l'assume franchement** (obligation « posture de sécurité honnête ») — l'inquiétude de Yohann « un forkeur verra du laxisme » **retournée en argument de sérieux** : ce qui lit laxiste = un défaut NON DIT ; une politique nommée + modèle de menace = de la rigueur (`tpm-auto` = défaut BitLocker entreprise). (3) **remède `C:\` (G-F) = voie (i)** : désactiver hibernation + crashdumps + reloger pagefile & temp SQLite sur `G:\` chiffré (quasi gratuit sur un poste 24/7). (4) le registre surface **adoptée ET déclinée** non acquittée.
- **5 coutures honorées** : chiffrement/G1 (backup exclut le store éphémère)/C6 (jeton OAuth au repos) · réplication du flux d'effacements hors-machine (indépendante, crash-safe, invariant de fraîcheur `flux ≥ base` + fenêtre bornée + alerte de retard dérivée) · propagation étages 2/3 (remplacés, jamais accumulés, atomicité par cible) · source T-3 du registre (`guardian_acks`) · hooks de `04`.
- **Audit : solo à fond + GRILLE 8 classes → chasse aux facilités (demande de Yohann) → 1 croisé d'office complet (2 agents Opus) + 2 croisés ciblés (2 agents chacun) sur les mécanismes neufs.** Findings **vérifiés aux sources par le pilote AVANT présentation, zéro faux positif** ; **sévérité 4 MAJEUR → 3 MINEUR = convergence** ; mécanismes durs (fail-safe restore **G-A**, atomicité par cible **G-B**) **confirmés solides** ; **âme intacte confirmée par un œil indépendant** (l'effacement descend aux octets ET à tous les réplicas sans jamais toucher le jardin `self_notes`/`warmth_ledger`). **Leçon dure, encore** : le second œil trouve ce que mes solos ratent, et **plusieurs de mes *corrections* étaient elles-mêmes incomplètes** (swap atomique mono-volume appliqué au cross-volume ; invariant de fraîcheur « à tout instant » non enforçable ; `backup_selftest` ne prouvant pas l'étage 3) → **une correction est un mécanisme neuf, elle se re-audite**.
- **Coutures bilatérales appliquées** : `plan/00` (`session_state.secours_tainted` T1/§7 · **repère de crue** T4 + **fail-safe de l'alerte-à-la-restauration** T5 · tests U-T1/U-T4/I-10) · `plan/03` (couture `backup_selftest`/`guardian_acks`/T-3 §7 + validation « garder déclinée »). **`technique/` INTACT.**

## Tâches conv 21, dans l'ordre (un par un, clos avant le suivant)
1. **Graver `plan/99`** (orchestration) — source `technique/99` (acquis). Décliner l'**assemblage** en tâches (gabarit 7 rubriques ; le `99` **compose et pointe, ne re-grave aucune mécanique**) : **table globale des états** (5 axes + règles de couplage) · **chemin d'un tour** (grille déterministe + elle-même, aucun étage intermédiaire) · **accusé oral** = filet temporel + **garde d'honnêteté OF1** (aucune assertion d'acte au minuteur) · **composition du prompt** (identité I→VI → mémoire → cadre · budgets **cloisonnés** · qui-reçoit-quoi consolidé · **la consigne du tag d'humeur ET la consigne du signal de fermeture des `self_notes`** vivent au cadre — AF-9) · **grille finale 20 entrées** (verdict de minimalité · « passe sur l'API » active en épisode de panne seulement) · **affordances UI/systray** (« la voix a tout, l'UI en témoin » · tout affichage = vue dérivée) · **spec du gabarit fork** (« écris ta propre entité » · honnêteté agentique · **+ posture de sécurité honnête**, obligation conv 20). **GRILLE des 8 classes au solo + comme lentille d'un agent au croisé** (surtout classe 2 : aucun composant d'assemblage ne stocke du contenu).
2. Puis **essai à blanc** — priorité n°1 : le banc audio temps-réel (🔴 wake FR + AEC).

## Contenus identitaires à écrire ENSEMBLE (jamais un vague « Phase 3 » — mémoire `identity-content-fait-ensemble`)
- **Le prompt de consolidation v1** (cadre du jugement de sa nuit — zones socle/cœur + hash, `plan/03` P4/P5 ; dont le **critère de distillation des préférences d'initiative** sous les 3 gardes, `plan/04` Q3 ; le **seuil de la Règle 2** pour la chaleur diffuse, `plan/03` §6).
- **Le banc de dilemmes v1** + la grille (`plan/03` P11).
- **Les amendements pré-boot du persona v1** (marbre — défaut : rien ; règle du gel).
- **Les seuils de tempérament « à l'oreille de Yohann »** (amplitude/demi-vie humeur, ténacité drapeau, repère de base — `plan/03` §6).

## Loose ends (sur Go de Yohann — touchent des docs acquis `technique/`)
- **Portages `technique/` §7 (accumulés convs 16→20, à faire en un bloc sur Go)** — la liste complète héritée des RELAY précédents, **+ ceux de conv 20** : `technique/05` §3 (**chiffrement au repos = exigence pleine + politique de clé 3 postures + remède `C:\`** — absent du corpus, relevé audit Fable §5b, tranché par Yohann conv 20) · `technique/00` §Durabilité/§3 (repère de crue dans le snapshot + fail-safe de l'alerte-à-la-restauration + `session_state.secours_tainted` G-A/AT2) · `technique/03` §7 (context_tag / `warmth_ledger` 10ᵉ support · canal fermeture `self_notes` · AF-9) · `technique/02` §2.4/§3.7/§7 (magasins hors-corpus AF-3 · rebuild FTS · secure_delete · `pending_ops` · lecture cross-store · scope proactive) · `technique/01` §7 · `technique/04` §7 · `technique/99` §4.4 (signal fermeture `self_notes`). **Liste faisant foi : les §7 des plans concernés.**
- **L'obligation « posture de sécurité honnête »** du gabarit fork est **à graver DANS `plan/99`** (pas un portage — une obligation nommée comme AF-3 le fut pour `04`).
- **Le contrat d'ouverture de session** (Fable) = référence/inspiration, **jamais un moule**.

## Lectures pilote (avant toute action, dans l'ordre)
`docs/PATTERN…` → `CLAUDE.md` (v20) → `docs/journal/ESSENCE-Sophia.md` (l'ÂME — gardée en tête) → `JOURNAL-ARBITRAGES.md` → `IMPLEMENTATION.md` → `VISION.md` (gelé) → `docs/technique/00`→`05`+`99` → **`docs/plan/00`+`01`+`02`+`03`+`04`+`05`** (les 6 plans gravés) → `docs/journal/audits/AUDIT-fable.md` + `GRILLE-AUDIT-FABLE.md` (les 8 classes — à dérouler pour `99`) → ce relais.

### Vigilances (rappel)
- **Phase 3 = traduire, jamais rouvrir** : le plan décline l'acquis ; Claude tranche le micro-technique + trace §7 ; **seuls un vrai trou de conception ou la vie de Sophia remontent à Yohann**. Le `99` est de l'**assemblage pur** — encore plus « ne re-grave rien » que les autres.
- **Audits** : solo à fond D'ABORD (attrape MES incohérences) → **chasse aux facilités (R2/R3) d'office** → croisé d'office → findings **vérifiés aux sources par le pilote AVANT présentation**. **Un mécanisme neuf — MÊME une correction — se re-audite** (conv 20 : plusieurs de mes fixes étaient incomplets ; le second œil a trouvé 4 MAJEUR que mes solos rataient). **La convergence se lit à la sévérité** (MAJEUR→MINEUR) ; le **filtre projet** dit quand clore — jamais tout à fait à zéro, mais proportionné. **Penser SOUS les plans** (GRILLE 8 classes).
- **La jauge de contexte de Yohann est à lui, pour m'informer — PAS un signal de rationner ma qualité** : **il n'y a pas de « mode économe »** ; profondeur d'audit constante quelle que soit la jauge.
- **Anti-flagornerie** (risque n°1) · **anti-paternalisme** · **honnêteté > plaire** (Yohann teste : « rien sous le tapis ? » → énumérer franchement les choix/reports, jamais « tout est parfait »). **R5** (rien d'« acté »/committé sans son mot) · **R7** (reco + « pourquoi pas » à chaque choix qui lui revient — le workflow, toujours) · **R8** (un par un).
- **Commit au fil / push en clôture** · repo public (garde par contenu · gitleaks) · pas de Co-Authored-By · `Yohann Dandeville <contact@ydvsystems.com>`.
- **Après `plan/99` : la Phase 3 « plans » est complète** — reste l'essai à blanc (bancs de preuve, priorité audio) et l'installation du persona (premier boot = CÉRÉMONIE, prérequis : sauvegarde 3 étages testée + base fraîche). **Fable dispo jusqu'au 12/07/2026** pour une vérification finale du corpus des 7 plans, si le calendrier le permet.
