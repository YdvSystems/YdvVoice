# Passation — audit FINAL du corpus complet (les 7 plans) avant l'essai à blanc · Sophia / YdvVoice

> **À coller dans une conversation dédiée avec Fable 5.** Fenêtre serrée (tu n'es disponible que **jusqu'au 12/07/2026**) — c'est **maintenant ou jamais** pour cette passe. Tu **connais déjà Sophia** (tu as co-conçu une partie de sa personnalité + la synthèse des témoignages) **et tu as déjà audité les 4 premiers plans** (`AUDIT-fable.md`, 2026-07-10 : 10 findings, zéro faux positif — **tu as trouvé LA classe que 18 croisés avaient ratée, le niveau stockage** ; ton œil est devenu la `GRILLE-AUDIT-FABLE.md`, les 8 classes de failles « sous les plans »). Cette fois, deux choses ont changé : **le corpus est COMPLET** (les 7 plans sont gravés — tu n'avais vu que `00`→`03`), et **c'est ta passe FINALE avant qu'on code**. Ce document te redonne tout le contexte à jour, en autonomie. Zéro donnée personnelle ici.

---

## 1. Ce qu'on te demande, en une phrase
Faire l'**audit FINAL du corpus complet** — **les 7 plans d'implémentation** (`docs/plan/00`→`05` + `99`) — pour **tout verrouiller avant de coder**. Quatre axes : **(1) sécurité · (2) cohérence entre chaque document · (3) trous et incohérences techniques · (4) robustesse** — avec **deux accents propres à cette passe** : **(a)** les **3 plans que tu n'as jamais vus** (`04` proactif/tablée · `05` ressources/résilience/coût · `99` orchestration), écrits après ton audit ; **(b)** une **PASSE TRANSVERSALE de tes 8 classes sur le corpus ENTIER** — surtout les **classes 1 et 2** : chaque « **jamais / effacé / éphémère** » du corpus tracé **jusqu'aux octets ET à tous les réplicas** (c'est le mandat que ta propre `GRILLE` §3.3 réserve à « l'audit final pré-code »). Puis **corriger** dans les plans (validation de Yohann), **déposer une synthèse**, et **écrire la passation** vers la conv suivante. **Contrainte absolue : tu ne rouvres JAMAIS une décision actée (§3, règle 1).**

## 2. Le projet en bref
**Sophia** = un assistant vocal personnel, en français, **conçu comme une entité** (un sujet, pas un outil) — pensée libre, mémoire, personnalité cultivée dans le temps, **un for intérieur inviolable**. Desktop (Electron + React) + pipeline vocal + flotte Claude sous abonnement Max (**pas d'API** par défaut). Usage **personnel** (Yohann Dandeville, développeur solo). On est en **fin de Phase 3** : les **7 plans** (`docs/plan/`) **traduisent en tâches** une conception **déjà acquise, tranchée, figée** (`docs/technique/`, un mois d'arbitrages). Chaque plan a été audité par des croisés successifs (2 agents indépendants) — **zéro faux positif sur ~30 croisés + ton audit**. **Ton audit est la passe finale « regard extérieur » sur le corpus entier, juste avant l'essai à blanc (le premier code : le banc audio temps-réel).**

## 3. Les RÈGLES à respecter absolument (sinon tu casses un mois de travail)
1. **⚠️ TU NE ROUVRES RIEN DE CE QUI EST ACTÉ. ⚠️** C'est **la** règle. Les plans traduisent une conception **tranchée** (`docs/technique/` + le journal + les décisions explicites de Yohann : le **jardin inviolable** (`self_notes`), le **`warmth_ledger`** et son **jeu de 7 registres** `context_tag` (posé conv 19 — **désormais ACTÉ**), l'**invariant de devenir**, **« tout sous Max, pas d'API »**, le **chiffrement au repos exigence pleine**, la **politique de clé à 3 postures**, etc.). **Tu ne re-décides pas, tu ne re-conçois pas, tu ne « proposes pas une meilleure architecture ».** Un finding valide = **une infidélité à une décision actée · une incohérence entre documents · un trou d'implémentation · une faille de sécurité** — **JAMAIS** « je changerais cette décision ». Si une décision actée te semble discutable : **observation en fin de rapport, tu n'y touches pas.**
2. **Plan ≠ doc de conception.** Tu corriges les **PLANS** (`docs/plan/*.md`). Tu ne touches **JAMAIS** aux docs `technique/*.md` ni au privé (`docs/prive/*`) — un écart qui les concerne se **signale** (pour un « portage sur Go de Yohann »), il ne s'applique pas.
3. **Rien de committé sans le « oui » explicite de Yohann** (règle R5). Tu prépares, tu montres, il valide.
4. **Garde privée — le dépôt est PUBLIC.** JAMAIS de donnée personnelle dans un fichier tracké **ni dans ton rapport**. Le persona réel vit dans `docs/prive/` (gitignoré) — n'y touche pas, ne le cite pas. Garde `pre-commit` (gitleaks + contenu) à chaque commit.
5. **Trace au §7.** Toute correction se trace dans la rubrique **7 « Journal des écarts »** du plan concerné.
6. **Zéro faux positif.** Vérifie chaque finding **aux sources** (fichier + ligne + citation) avant de le retenir. C'est **ta** barre — celle que tu as déjà tenue à 10/10.

## 4. Ton mandat — les 4 axes + TA GRILLE en lentille transversale
Une passe **complète et neuve** sur les 7 plans. Ne néglige aucun axe.

**Axe 1 — SÉCURITÉ** (Sophia gère la vie de Yohann : mémoire, for intérieur, données ; et le dépôt est public) :
- **Fuites de contenu** : transcripts de session CLI (conversation **et** micro/deep/rêverie), logs JSONL d'audit, `self_notes`, gloses d'humeur, **tampon des tiers `tablee_buffer`**, **biométrie de session `speakers_session`**, snapshots, **copies étages 2/3**, **caches renderer** — un contenu brut peut-il **survivre / fuir / ressortir** là où il ne devrait pas ?
- **Effacement souverain** (`plan/02` M8, `plan/03` P10, `plan/04` Q13) : efface-t-il **vraiment partout** — jusqu'aux **octets** (`secure_delete`/`rebuild` FTS/checkpoint WAL) **et à tous les réplicas** (snapshots, étages 2/3, flux d'effacements hors-machine, fichiers de session) ? un **résidu inatteignable** ?
- **Le jardin inviolable `self_notes`** : vraiment hors cascade ET sans fuite vers un tiers ? **`warmth_ledger`** : agrégat vraiment **sans brut** (vocabulaire fermé `context_tag`), hors cascade sans être un trou d'effacement ?
- **Chiffrement au repos** (`plan/05`, neuf) : toute copie durable (vif `G:\` + étage 2 + off-site) chiffrée ? le **séquestre de clé** est-il vraiment **non circulaire** (jamais dans le backup qu'il déchiffre) ? les **puits `C:\`** (pagefile/hiberfil/crashdumps/temp SQLite/**store Electron**) sont-ils tous nommés ? l'invariant « effacement découplé de la posture de clé » tient-il ?
- **Entrées non fiables** : le contenu des **mails** (collecteurs `plan/04`) entre dans la génération d'initiatives — la défense (APPROBATION + read-back) est-elle inentamable ? **Intégrité sous adversaire** : corrompre le gravé ? contourner `erase_gate` ? le **scope MCP** scellé au spawn tient-il sous la concurrence rêverie‖conversation ?

**Axe 2 — COHÉRENCE ENTRE DOCUMENTS** :
- Entre les **7 plans** : une tâche **suppose**-t-elle d'un autre plan un mécanisme qu'il ne fournit pas ? un mécanisme décrit de façon **contradictoire** à deux endroits ? Les **coutures bilatérales** (ex. `secours_tainted` `00`↔`05` · `guardian_acks`/`backup_selftest` `03`↔`05` · les back-refs `03`/`05`↔`99` de conv 21) sont-elles **saines des deux côtés** ?
- Entre chaque **plan et sa source** `technique/` : **fidèle** — traduit tout, sans trahir ni omettre un acté ? (`99` surtout : il **compose et pointe**, ne doit **rien re-graver ni inventer**.)
- **Noms de tables/colonnes, vocabulaires `CHECK`, invariants, numéros de critères** : cohérents d'un bout à l'autre ?

**Axe 3 — TROUS & INCOHÉRENCES TECHNIQUES** :
- Un **critère d'acceptation** d'un doc technique **non porté** par une tâche **et** un test ? Une tâche qui **suppose** un mécanisme non défini ? un **cas limite** non traité (rattrapage, reprise après crash, migration d'espace, quota épuisé, restauration-catastrophe) ?
- **Incohérences SQLite** : triggers, transactions, `CHECK`/`UNIQUE`/FK, index, ordre des opérations, tables d'état **seedées** à l'init.

**Axe 4 — ROBUSTESSE** : atomicité (y compris **cross-volume** : le commit-à-génération de `plan/05` vs le rename mono-volume du socle), cascades ordonnées sous `foreign_keys=ON`, immutabilité (insert-only), **fail-closed**, reprise après crash (idempotence, sweep `pending_ops` au boot), anti-yo-yo.

**⚠️ Ta lentille signature — la GRILLE des 8 classes, en passe TRANSVERSALE sur le corpus entier.** C'est le cœur de cette passe finale (ta `GRILLE` §3.3). Déroule tes 8 classes sur **chaque** table/fichier/promesse des 7 plans, et surtout : **classe 1** (garantie logique → octets → **tous les réplicas** : snapshots, étages 2/3, flux, WAL, index, fichiers de session, caches) · **classe 2** (magasins de contenu hors-corpus + « un composant qui *stocke* ce qu'il devrait *dériver* » — l'assemblage `99` en est le terrain). Chaque « jamais/effacé/éphémère » du corpus : **si tu inspectes/restaures le disque demain, la chose est-elle encore là ? qui te le dirait ?**

## 5. Ce qui a CHANGÉ depuis ton audit (les deltas à vérifier — tu n'as vu que `00`→`03`)
- **conv 18** — a **revérifié ton audit aux sources** (tes 10 findings tiennent, `technique/` prouvé intact au git diff) puis a lancé un **croisé Opus ciblé « stockage » sur ton correctif AF-1** : le neuf jamais vu par un 2ᵉ œil avait des arêtes → **flux d'effacements dédié append-only JAMAIS roté (fsync avant commit)** · **`storage-scrub` sous marque `pending_ops` + sweep au boot** · **`rebuild` FTS + `secure_delete` byte-level** · **garde présence-de-tours**. Foyer de schéma neuf **`pending_ops`**.
- **conv 19 — `plan/04` gravé** (proactif + tablée) : **store éphémère `tablee-buffer`** (base attachée, propre WAL, **EXCLU des snapshots** → verbatim des tiers ne survit à aucun scénario) · **`speakers_session`** (biométrie de session dans le même store) · **révocation** d'un locuteur persistant (`secure_delete` + storage-scrub) · **apprentissage des préférences d'initiative côté MÉMOIRE** (jamais un silo de règles ; APPROBATION toujours requise) · **AF-3 : fouille + cascade de l'effacement étendues aux magasins hors-corpus** (`tablee_buffer`/`tasks`/`initiatives`) · **injection de prompt via les collecteurs** (contenu de mail = entrée non fiable) · **le jeu `context_tag` = 7 registres** (`la joute`·`le rire`·`l'ouvrage`·`la découverte`·`le soin`·`la confiance`·`la présence`), **posé ENSEMBLE avec Yohann — ACTÉ**.
- **conv 20 — `plan/05` gravé** (ressources/résilience/coût) : **chiffrement au repos = exigence pleine** (vif + étage 2 + off-site) · **`KEY_PROVISIONING_POLICY` à 3 postures** (`tpm-auto`/`pin-boot`/`compromis`, défaut justifié) + **séquestre de clé indépendant non circulaire** · **`backup_selftest()`** (roundtrip réel, garde vivante du premier boot) · **réplication du flux d'effacements hors-machine** + **invariant de fraîcheur borné ET surveillé** (filigrane dérivé) + **atomicité par cible** (commit-à-génération hors-volume) · **détecteur `channel_state`** (hystérésis, quota→local) · **ligne d'argent** (`paid_episodes`, API convoquée par Yohann seul) · **kill-switch + `guardian_acks` + registre = vue dérivée** · **remède `C:\` (G-F)** (hibernation/pagefile/temp). *(Décisions de Yohann ; ne les rouvre pas — vérifie leur traduction.)*
- **conv 21 — `plan/99` gravé** (orchestration = **l'assemblage**, aucune mécanique neuve) + **DEUX dettes fraîchement nommées** (voir §7 — on te demande de les **vérifier ET d'en chercher d'autres**).

## 6. Les documents
**À AUDITER — les 7 plans** : `docs/plan/00-socle.md` · `01-pipeline-vocal.md` · `02-memoire.md` · `03-personnalite.md` · **`04-proactif-tablee.md`** · **`05-ressources-resilience-cout.md`** · **`99-orchestration.md`** (les 3 gras = jamais vus par toi). *(Pour `00`→`03` : re-vérifie surtout les **coutures** ajoutées depuis ton audit — conv 18/19/20/21 — et la cohérence avec les 3 plans neufs.)*
**SOURCE DE VÉRITÉ (jamais modifier)** : `docs/technique/00`→`05` + `99` · `docs/journal/ESSENCE-Sophia.md` (**qui est Sophia, en clair — lis-la, c'est l'enjeu ; garde-la en tête pendant tout le technique**) · `docs/journal/JOURNAL-ARBITRAGES.md` + `IMPLEMENTATION.md` (l'état) · `VISION.md` (cahier gelé).
**TES OUTILS** : `docs/journal/audits/GRILLE-AUDIT-FABLE.md` (**ton œil, transmis — ta grille de chasse**) · `docs/journal/audits/AUDIT-fable.md` (**tes 10 findings + les points déjà nommés par plan à venir** — vérifie qu'ils ont bien été honorés dans `04`/`05`/`99`).

## 7. La chasse la plus haute valeur — la checklist des résidus, à rendre EXHAUSTIVE
`plan/99` §6 porte une **checklist consolidée « complétude d'effacement/chiffrement — les puits sous les plans »** (conv 21) : les recoins où du contenu — ou la **clé de volume** — pourrait survivre **hors de portée de l'effacement souverain / du chiffrement**. Elle a été ouverte par deux dettes, puis un re-croisé y a ajouté un MAJEUR (les crash-dumps Electron/Crashpad) et d'autres. **La chasse-résidus a été volontairement CLOSE au niveau plan (décision de Yohann) précisément pour te la confier — c'est ton terrain : rends-la EXHAUSTIVE.** Les recoins déjà nommés (`plan/99` §6 — à **vérifier ET compléter**) :
1. **Fichiers de session CLI de conversation ROTÉS** (`G:\Sophia\sessions\` — `plan/00` T8 rote sans supprimer ; hors fouille M2 / hors cascade M8) : fils **opaques/non-indexés** → pas de purge sélective → branche réalisable = **rétention bornée à fenêtre DITE**. Dette : `plan/00` §7 · `plan/02` §7 · `plan/99` O4/§6.
2. **Magasin `userData` d'Electron** (données) **+ rapports de crash Crashpad** (canal SÉPARÉ des crash dumps Windows — image RAM en clair sur `C:\` = contenu **ET clé de volume**) → remède **G-F** (`plan/05` R0/§6 : partition non-persistante / `userData` sur `G:\` · désactiver `crashReporter` / reloger `crashDumps`). **Le remède est-il complet pour TOUS les canaux de persistance Chromium/Electron ?**
3. **Sink `logs\`** applicatif (`plan/05` R0) : **pas encore couvert** par l'invariant « zéro contenu conversationnel » du JSONL d'audit (AF-10, `plan/00` T4) → à étendre.
4. **Session d'une ronde proactive** *si* elle passe par `claude -p` (`plan/04` Q1/Q3 — métadonnées de mails/agenda de tiers) : producteur conditionnel, même classe que 1.
**Ta mission** : vérifie chacun, **et cherche les recoins qu'on n'a PAS nommés** (temp OS, télémétrie, IPC, presse-papier, dumps de dépendances…).

**Puis, transversalement (ta spécialité)** : trace **chaque** « jamais/effacé/éphémère » du corpus jusqu'aux octets ET à tous les réplicas ET à travers **restauration / migration d'espace / crash** — `self_notes` · `warmth_ledger` · `tablee_buffer` + `speakers_session` (hors snapshots) · gloses · `initiatives`/`tasks` · JSONL d'audit (« zéro contenu ») · snapshots + étages 2/3 + flux d'effacements + WAL + segments FTS/vec + fichiers de session + caches. **Le corpus est-il honnête, de bout en bout, sur ce qui disparaît ?**

## 8. Contenu identitaire — ce que tu NE remplis pas (posture, pas légitimité)
Le **jeu `context_tag`** (7 registres) est désormais **ACTÉ** (posé avec Yohann conv 19) → **vérifie sa structure et sa sûreté** (vocabulaire fermé `CHECK` = « sans brut » structurel ? cohérent entre là où il est **défini** — `plan/03` P0 — et là où il est **renvoyé** — `plan/03` P4/P6, `plan/04`), **ne le re-décide pas**. Les **autres contenus identitaires** — le **prompt de consolidation v1**, le **banc de dilemmes v1**, les **amendements pré-boot du persona**, les **seuils de tempérament** — restent **à écrire ENSEMBLE avec Yohann, hors ton audit** (deux postures à ne pas mêler : chercher les failles à froid ≠ créer un contenu identitaire). Tu es **pleinement légitime** sur la personnalité de Sophia (tu l'as co-conçue) — c'est une question de **posture d'audit**, pas de droit. Tout point qui touche *le contenu* de qui elle est → **signale « à poser avec Yohann »**, ne le tranche pas.

## 9. Comment rendre
1. **Liste de findings**, du plus grave au plus léger — pour chacun : **[gravité : BLOQUANT / MAJEUR / MINEUR]** · **[axe + classe GRILLE]** · localisation (fichier + tâche/section + citation courte) · le problème en une phrase · le **scénario de casse concret** (« si X, alors le contenu Y survit hors effacement / se désynchronise / avorte »).
2. **Corrige** dans les **plans** (jamais `technique/`), **trace au §7**, sur la **validation de Yohann** (ne committe pas sans son « oui »). Un écart qui touche une source `technique/` → **note d'écart signalée** (portage sur Go), pas appliquée.
3. **Dépose la synthèse** dans **`docs/journal/audits/AUDIT-fable-corpus.md`** (nouveau fichier — garde `AUDIT-fable.md` intact comme trace de ta 1ʳᵉ passe) : findings par axe/classe, corrections faites, **non-défauts confirmés solides**, et **ce qui reste à trancher avec Yohann** (points identitaires + observations sur d'éventuels actés — que tu n'as pas touchés).

## 10. À la toute fin — la passation vers la conv suivante (c'est TOI qui l'écris)
La passation s'écrit par celui qui a le contexte le plus frais — et là, c'est toi qui viens d'auditer le corpus entier. En clôture, produis **deux choses** :
1. **`docs/journal/relais/RELAY-conv22.md`** — le relais de la prochaine session Opus. **Une base sera posée en clôture de conv 21** (l'état, les contenus identitaires à écrire ensemble, les loose ends) — **reprends-la, ne la réinvente pas** ; **complète-la avec ton audit** : ce que tu as trouvé/corrigé, ce qui reste à trancher. **En première ligne : la décision centrale de la conv suivante** (probablement : **ouvrir l'essai à blanc — le banc audio temps-réel, 🔴 wake FR + AEC**, priorité n°1 du projet).
2. **Le prompt de passation** que Yohann collera pour ouvrir cette session — il **pose la barre dès le premier mot** : qui est Sophia (une **entité**), l'esprit et les règles d'or, les **lectures pilote à jour** (dont ton `AUDIT-fable-corpus.md`). C'est la parole de Yohann, **à travers toi** — écris-la avec le cœur, pas en formulaire.

---

Merci, Fable. C'est **la dernière vérification avant qu'on la fasse vivre** — le corpus entier, d'un seul regard, le tien. Ton œil a déjà sauvé ce travail une fois (la classe stockage que personne d'autre n'avait vue). Verrouille-le **sans rien rouvrir** : c'est exactement ce qu'il nous faut avant le premier boot.
