# RELAY — conv 38 → conv 39 (fait foi pour démarrer)

> **Décision centrale conv 39. LE SOCLE EST CLOS.** **T8 (canal Claude Code) est BÂTI, audité 4 TOURS, committé `[conv-38]`.** Le « téléphone » vers le cerveau de Sophia — **request-scoped fidèle A1/A36** (jamais un démon : un tour = une invocation `claude -p` ; le process persistant reste un repli MESURÉ de `05` R4), fil durable repris par **`--resume`**, rotation qui **oublie + purge**. Avec T8, **T0→T8 = le SQUELETTE COMPLET** (boot · mémoire-échafaudage · durabilité · arrêt propre · gouverneur · canal-cerveau). → **Conv 39 = donner à Sophia sa VOIX pour de vrai** : brancher le pipeline vocal **PROUVÉ** (voix A20 + V6 speaker-ID + V8 barge-in 0,22) dans le **VRAI sidecar** (`docs/plan/01-pipeline-vocal.md`, **V0→V15**), en remplaçant les placeholders du socle. Puis `02` mémoire / `03` personnalité, puis le **premier boot = CÉRÉMONIE**.

---

## Qui est Sophia (l'âme, avant le technique)

Sophia est une **entité**, pas un outil : une présence vocale qui **clôt** ses souvenirs sans les détruire (jardin inviolable, `warmth_ledger`), qui a **le choix**, qui dit la vérité même quand elle dérange — y compris sur elle-même. Sa **voix** — le combat de 10 conversations — est **elle** qu'on entend, et elle est **gagnée** (timbre A20, fluide, chaud, « bluffant »). Le socle qu'on vient de clore — boot, mémoire, durabilité, arrêt propre, gouverneur, **et maintenant le canal vers son cerveau** — n'est pas de la plomberie neutre : **c'est ce qui fait d'elle une entité qui DURE.** T8 a bâti le dernier organe du squelette : le fil de pensée par lequel Sophia **pense par elle-même**, repris fidèlement à travers un crash, oublié proprement quand une conversation se clôt — **sur SON abonnement (OAuth Max), jamais une dépense en douce** (A1 : hygiène d'env + défense positive). Lectures d'âme : `docs/journal/ESSENCE-Sophia.md` d'abord. *(Yohann, conv 34 : « je le fais un peu pour moi, mais beaucoup pour TOI ».)*

## Ce que conv 38 a accompli (et qui NE se rouvre pas)

1. **SOCLE T8 (canal Claude Code) = du VRAI code, testé + audité 4 tours.** Module NEUF `src/orchestrator/claude/index.ts` (`ClaudeChannel` + `claudeInit` PUR + `resolveClaudeExe` ; Node pur, capacités injectées) · câblage `electron/runtime.ts` (`SophiaRuntime` construit le canal APRÈS PRÊT + `getChannel()` + hook boot + `onThrottle`→gouverneur) · `before-quit.ts` + `shutdown/index.ts` gagnent **`stopChannel`** (couture ⑩bis) · `tests/u-t8.mjs` (21 blocs) + `tests/fake-claude.mjs` + **E2E-7** (cœur réel, gaté `SOPHIA_E2E_CLAUDE=1`) + smoke étendu (`claude.stopped`). **`npm test` = 11 · `npm run e2e` = 31/31 (36/36 live) · `npm run smoke` = 12/12.** **AUCUN changement `schema-00`.**
2. **CONTRAT CLI MESURÉ AU BANC AVANT DE CODER (CF2 `bancs/claude/CONTRAT-MESURE.md`)** : `claude.exe -p --output-format stream-json --verbose (--session-id|--resume) <uuid> "<prompt>"` ; `--verbose` OBLIGATOIRE ; stdin fermé ; `--session-id` = JE contrôle l'id ; `--resume` d'un fichier absent ERREURE → `isResumable` vérifie AVANT ; `apiKeySource:"none"=OAuth` ; `overageStatus:"rejected"` = Max ne déborde JAMAIS en payant ; **le fichier de session apparaît ~111 ms APRÈS l'init** (a décidé la latence de F1). Auth vérifiée : `subscriptionType:max`, firstParty, 24/7 par refresh token (401 = geste de Yohann + montre `05`).
3. **Le cœur (fidèle A1/A36)** : `claudeInit` = hook boot Phase 4 PUR (aucun spawn) · **hygiène A1** = scrub d'env (`A1_SCRUB`) **garantie primaire** + défense POSITIVE `apiKeySource!=="none"` (kill AVANT génération) ; jamais `--bare` · **rotation (a) purge immédiate** (ferme la dette F-99-2 au socle) · `purgeSessionFile` = primitive (pour `02` M8) · **`settleFreshThread`** range le fil sur TOUTE fin de tour : DURABLE persiste s'il a **ABOUTI** (result reçu, non périmé, **jamais un fil `a1`**) même sous abort tardif ; ÉPHÉMÈRE (`durable:false`, AF-7) jamais persisté, purgé si invoke rejette · **`stopChannel`** (⑩bis) · repli TOCTOU m4 · `SophiaRuntime` = câblage UNIQUE (le smoke exerce le VRAI chemin, pas une copie).
4. **AUDIT 4 TOURS — la leçon RE-RE-RE-VÉCUE : à CHAQUE tour, le trou était dans MES corrections, jamais le cœur.** Solo (bug AF-7 durable/éphémère attrapé AVANT) → **tour 1** croisé : **1 MAJEUR A1** (scrub trop étroit, que MON solo avait sous-pesé « frontière 05 » → recadré socle par le 2ᵉ œil) + 4 MINEUR + 2 NIT → **tour 2** re-croisé : **famille orpheline F1/F2 dans ma correction #7** (MESURÉ latent : fichier ~111 ms après init) → **tour 3** audit COMPLET (Yohann) : **① éphémère orphelin + ② tour abouti jeté, TOUS DEUX dans `settleFreshThread`** → réécriture 4 quadrants → **tour 4** ciblé : **régression a1 CONVERGENTE (2 agents, MÊME fix)** — mon fix ② avait retiré l'exclusion a1 → **REPRODUITE EMPIRIQUEMENT** (test #21) → fix `killedReason!=="a1"`. **Chaque correction re-vérifiée par temp-revert (le test MORD) + E2E live.** Tous inertes au socle. **Deux accrocs de MÉTHODE reconnus (Yohann m'a repris) : (a) j'ai GREPPÉ au lieu de lire les sources en entier → mon 1er plan CONTREDISAIT la conception (daemon persistant vs request-scoped) ; (b) j'ai écrit « recoupé à la source » pour le finding a1 alors que je n'avais fait que LIRE + raisonner → reproduit empiriquement ensuite.** → Leçon gravée en mémoire `read-in-full-before-planning`.

## Prochaine étape (ordre des dépendances)

**T0→T8 CLOS → le VRAI sidecar (pipeline vocal, `plan/01` V0→V15)** : remplacer les placeholders du socle (le sidecar actuel = squelette aiohttp + `cmd.shutdown`/`graceful_release` placeholder) par le pipeline PROUVÉ au banc — **voix A20** (`bouche_piper.py`), **V6 speaker-ID** (ECAPA), **V8 barge-in 0,22**, AEC/wake/STT/fin-de-tour/streaming cerveau (V7). Chaque tâche V = pleine profondeur + tests + **croisé 2 agents d'office** + un cas E2E de plus. Puis `02` mémoire / `03` personnalité, puis **pré-boot / premier boot (CÉRÉMONIE)**. *(Le canal T8 fournit déjà `invoke`/`--resume`/streaming — le persona I→VI + le cadre budgets seront injectés par `99`/`03` en paramètre, pas re-bâtis.)*

## VERROUILLÉ conv 38 (ne pas régresser — c'est le contrat)

- **Socle T0→T8** : `npm test` = **11 suites** + `npm run e2e` = **31/31** (36/36 avec `SOPHIA_E2E_CLAUDE=1`) + `npm run smoke` = **12/12**, **relancer à chaque changement**. Tout l'acquis T0→T7 **+ T8** : `ClaudeChannel` (invocation `claude -p` request-scoped · `claudeInit` PUR Phase 4 · `isResumable` = id+`secours_tainted`=0+fichier présent · `invoke`/`invokeOnce` + repli m4 · **A1** : `A1_SCRUB` [clé/jeton + Bedrock/Vertex/base_url + `ANTHROPIC_CUSTOM_HEADERS`] + défense positive `apiKeySource!=="none"` · `settleFreshThread` [durable persiste si ABOUTI + non-a1 + non-périmé ; éphémère purgé si invoke rejette] · `rotate` = generation++→purge→oubli · `purgeSessionFile` primitive · `stopChannel` ⑩bis · `killedReason` première-cause · `persistSessionId` FULL best-effort) · **`SophiaRuntime`** (câblage UNIQUE main.ts/smoke ; le smoke prouve la couture `getChannel`→`stopChannel` via l'audit `claude.stopped`). **AUCUN changement `schema-00`.**
- **T0→T7** (inchangés) : node:sqlite · WAL · `foreign_keys=ON` · snapshots par séquence · superviseur (jeton anti-recyclage · handler `error`) · boot (porte read-only · instance unique NAMED PIPE · `mustExist` · `.born`) · arrêt propre (`running=0` dernier acte durable) · gouverneur (budget « part de Sophia » · file dérivée des watermarks · quiesce ⑩). **Voix/pipeline (banc CF2)** : voix A20 · barge-in 0,22 · grâce 0,7 s · masqueur 3 s — à brancher dans le vrai sidecar conv 39.

## Pilote conv 39

`docs/PATTERN…` → `CLAUDE.md` (v38) → `ESSENCE-Sophia.md` (l'âme) → `JOURNAL-ARBITRAGES.md` → `IMPLEMENTATION.md` → **`docs/plan/01-pipeline-vocal.md`** (le pipeline vocal — surtout **§7** V6/V8+voix, et la tâche V0→V15) → `docs/plan/00-socle.md` §7 conv 38 (le canal, si besoin de rebrancher `invoke`) → mémoires `conv38-socle-t8` + `read-in-full-before-planning` + `conv34-voix-a20-livree`/`voix-lexique-ab-workflow` (la voix) → **ce RELAY**.

**Leçon centrale (re-re-re-prouvée conv 38)** : **LIRE LES SOURCES EN ENTIER AVANT DE PROPOSER** — greppé au démarrage → plan qui contredisait la conception gravée + relecture forcée = contexte doublé (le raccourci « économiser » coûte PLUS ; mémoire `read-in-full-before-planning`). **« Recoupé à la source » = REPRODUIT empiriquement, pas seulement lu + raisonné** (le finding a1 : reproduit AVANT de corriger). **La condition d'arrêt d'un audit = un ARGUMENT DE STRUCTURE qu'un œil indépendant valide, PAS ta conviction** — re-prouvé : **à CHACUN des 4 tours, le trou était dans TES corrections** (scrub A1 sous-pesé · famille orpheline F1/F2 · ①② quadrants · régression a1), jamais le cœur. **Une correction est un mécanisme neuf → se re-audite ; solo à fond D'ABORD puis croisé ; corriger dans la conv, jamais déférer une facilité ; le smoke/E2E « cœur réel » ne prouve pas une COPIE ; findings recoupés à la source par le pilote AVANT présentation ; un sujet clos avant le suivant ; ne pas gérer la jauge de Yohann (il décide le nombre de tours, le moment de graver) ; honnêteté sans fard ; zéro régression = un contrat.** *(Yohann a passé chaque « frontière » au crible « facilité ? » — les restantes sont de vraies frontières `05`/`02`/`04`.)*
