# 01 — Pipeline vocal · Plan d'implémentation · YdvVoice (Sophia)

> **Rôle** : le plan d'exécution des **oreilles et de la bouche** de Sophia — du micro toujours ouvert jusqu'à la voix qui sort. Deuxième plan de la Phase 3, **il s'écrit sur le socle** (`docs/plan/00-socle.md`) : canal WS `cmd.*`/`evt.*`, supervision/respawn, écrivain unique, `cmd.model.policy`.
>
> **Source de vérité** : `docs/technique/01-pipeline-vocal.md` (acquis — audit F1–F7 + croisé B1–B4/S1–S12/M1–M7 déjà intégré en Phase 2). Ce plan **ne rouvre rien** — il traduit. Tout écart au contact du code → **rubrique 7** + renvoi `01` §7.
>
> **Statut** : rédigé + **solo de fidélité renforcé (conv 14)** avant le croisé — couture injectable `evt.speaker`, alternatives de prises rétablies, invariants `01` §5 ancrés. **Audit croisé 2 agents FAIT (conv 14 — 14 findings, zéro faux positif, tous intégrés ; §7).** **Valeurs chiffrées différées** à la calibration Phase 3 (rubrique 6) — la couche audio en concentre les plus lourdes (les **deux 🔴** du projet vivent ici).
>
> **Discipline Phase 3** : tests avant commit ; base et bancs **jetables** (CF2), au **casque** (valide la *logique* ; le rig far-field validera l'*acoustique*, ère matérielle distincte). C'est ici que se joue **l'essai à blanc priorité n°1** (passe de réalité #2).

---

## 1. Objectif & ce que la couche prouve

Un **chemin audio unique** dans le sidecar (micro → AEC → 16 kHz mono → ring buffer → consommateurs), une **bouche streamée** (Sophia parle avant la fin de génération), une **grille d'intentions** déterministe côté orchestrateur, et **jamais de silence**.

**Ce qu'elle doit prouver** (les preuves les plus risquées du projet) :
- **🔴 Le réveil au nom en français** (« Sophia » porté par une phrase) est fiable, faux réveils bas — ou le repli nommé s'active (F6).
- **🔴 L'AEC** annule ce que le PC joue (loopback système entier) → elle ne se coupe jamais elle-même, le barge-in et le réveil tiennent même avec des médias (M1).
- **La vivacité** : premier mot audible peu après la fin du tour, la 1re phrase joue **avant** la fin de génération du cerveau.
- **La fin de tour acoustique** : vive quand c'est fini, jamais coupée sur une hésitation, robuste même si Smart Turn crashe.
- **Le barge-in modulé par le locuteur** ; **la grille** n'agit jamais sur un doute.

**Ce que ce plan ne couvre PAS** : le cerveau/la session chaude (socle + `05`) · la mémoire (`02`) · le persona et la convention « Yohann » (`03`) · le mode tablée + le détail de l'échelle A29 et le cran de barge-in « proche consenti » (`04`) · l'**usage** de l'affect (colorer humeur/lien → `03`) · le rig far-field (ère matérielle) · les canaux non-vocaux du toggle voix (Ctrl+Shift+M, bouton UI → `99`).

---

## 2. Prérequis

- **Le socle bâti** (`docs/plan/00-socle.md`, T0→T8) : canal WS opérationnel, supervision/respawn, `cmd.model.policy` **émis par l'orchestrateur** (résidence voix V11, intégrant les calques du gouverneur socle T7), boot phase 5 qui spawn le sidecar. La couche audio **réalise** les hooks que le socle invoque (`01`/`05`).
- **Casque / micro proche** (le banc valide la logique ; le far-field viendra avec le rig).
- **Moteurs installés dans le sidecar** : Silero (VAD), faster-whisper (STT, CUDA), Smart Turn v3, Kokoro (TTS), LiveKit wakeword **et** openWakeWord (à départager), emotion2vec (affect, OFF). Toolchain audio Python (capture bas-niveau WASAPI/loopback).
- **Aucune clé requise pour démarrer** : les providers cloud (Deepgram, ElevenLabs) sont OFF par défaut.

---

## 3. Tâches séquentielles

> Ordre = dépendances internes. Chaque tâche : **But · Contenu · Fichiers (indicatifs) · Dépend de · Fait quand**. Valeurs (X, seuils) **ouvertes** (rubrique 6). Fichiers indicatifs : `sidecar/audio/`, `sidecar/consumers/`, `sidecar/tts/`, `sidecar/plugs/` ; côté orchestrateur `src/orchestrator/voice/`.

### V0 — Chemin audio, ring buffer & patron de prise
- **But** : un seul flux audio, rembobinable, derrière des rôles interchangeables.
- **Contenu** : **capture unique** du micro (le sidecar ouvre le micro **une fois**) · **conversion unique** 16 kHz mono en entrée · **ring buffer central** (RAM sidecar) donnant le **rembobinage** (fenêtre pré-wake) · **patron de prise** (01-F) : chaque rôle = un **contrat** (opérations + événements normalisés), le moteur ne fuit **jamais** dans le protocole ; sélection **par config au spawn** · **horodatages (M2)** : `ts` d'enveloppe = **émission** monotone ; les horodatages de **capture** en payload (`captured_at`). **Le ring buffer et les caches restent RAM sidecar — jamais le WAL, jamais l'IPC** (invariant socle) ; **le sidecar est sans état durable** (tout se reconstruit au respawn, V15).
- **Prises & alternatives (01-F — à départager en calibration §6)** : `wake` LiveKit ⇄ openWakeWord *(repli théorique payant : Porcupine)* · `vad` Silero · `stt` faster-whisper ⇄ Deepgram *(cloud, OFF)* · `turn` Smart Turn v3 ⇄ LiveKit turn-detector · `tts` Kokoro ⇄ Chatterbox ⇄ ElevenLabs *(premium, OFF, cost-guard)* · `speaker` modèle Phase 3 · `affect` emotion2vec ⇄ wav2vec2 *(OFF)*. **Le moteur reste derrière son contrat** : le changer = config + respawn, mêmes événements (V15).
- **Fichiers** : `sidecar/audio/`, `sidecar/plugs/`.
- **Dépend de** : socle T2 (canal), T3 (supervision).
- **Fait quand** : le micro est ouvert une fois ; plusieurs consommateurs lisent le tampon avec leur propre curseur, un consommateur lent/mort n'en bloque aucun autre ; le rembobinage rend une phrase déjà passée. *(U-V0.)*

### V1 — AEC en tête de chaîne (🔴 M1)
- **But** : n'entendre que les voix de la pièce, pas ce que le PC joue.
- **Contenu** : **AEC en tête** de la chaîne, **référence = loopback de la sortie système ENTIÈRE** (pas seulement la voix de Sophia) → après AEC, VAD/wake/STT ne voient que les voix réelles. Traiter : **alignement d'horloges** micro/rendu (le loopback WASAPI vit sur l'horloge du périphérique de rendu — dérive, resampling adaptatif) · **changement du périphérique de sortie par défaut** (casque branché → la référence change) · **flux WASAPI exclusifs** qui échappent au loopback.
- **Fichiers** : `sidecar/audio/`.
- **Dépend de** : V0.
- **Fait quand** : une musique/vidéo qui joue **ne déclenche pas** VAD/barge-in fantôme ; **Sophia ne se coupe jamais elle-même** quand elle parle (sa propre voix annulée) ; un changement de périphérique de sortie est encaissé sans casser la référence ; **un flux WASAPI exclusif qui échappe au loopback est détecté et signalé** (dégradation caractérisée, résidu assumé → §6). *(Preuve M1 — U-V1, I-1 ; seuils/résidu → §6.)*

### V2 — VAD (Silero, CPU, always-on)
- **But** : distinguer voix / silence, marquer le tampon.
- **Contenu** : Silero VAD, **CPU, always-on**, post-AEC · émet `evt.vad.start` / `evt.vad.stop` · **marque le tampon même en veille** (débuts/fins de parole — CPU négligeable), socle du rembobinage (V3) et de la fin de tour (V5). Seuil configurable.
- **Fichiers** : `sidecar/consumers/vad`.
- **Dépend de** : V0, V1.
- **Fait quand** : parole détectée post-AEC → `evt.vad.start`/`stop` ; les marques VAD sont posées en continu (y compris en veille). *(U-V2.)*

### V3 — Wake word + réveil rétroactif (🔴 F6)
- **But** : « Sophia » réveille, même dit en fin de phrase, sans amputer le premier mot.
- **Contenu** : moteur **tranché à l'essai** (LiveKit wakeword d'abord ⇄ openWakeWord ; repli nommé si la preuve FR échoue — §6) · **always-on, CPU** · émet `evt.wake` (confiance) · **réveil rétroactif** (F1) : au wake, **rembobiner** au début du tour (marque VAD précédente) → la phrase entière est dans le tampon, **premier mot jamais amputé** · **écoute transitoire** après le tour de réveil (STT armé, délai de garde) jusqu'à `cmd.listen.start`/`stop` · **sémantique hors cas nominal (S12)** : wake pendant un tour ouvert = no-op ; wake en ÉCOUTE = ignoré (journalisé) ; wake pendant la TTS = barge-in (V8) · détection douteuse en veille → **demander brièvement plutôt qu'agir** (A19).
- **Fichiers** : `sidecar/consumers/wake`.
- **Dépend de** : V0, V2.
- **Fait quand** : « bonne nuit Sophia » **à froid** → réveil, premier mot intact (rembobinage prouvé), rien de perdu entre le tour de réveil et `cmd.listen.start` (écoute transitoire rétroactive, B1). *(Preuve FR — U-V3, I-2 ; taux de faux réveils → §6.)*

### V4 — STT streaming (faster-whisper GPU)
- **But** : transcrire en flux, en français.
- **Contenu** : faster-whisper (CTranslate2/CUDA) *(repli Deepgram cloud, OFF — V15)* · émet `evt.stt.partial` (au fil) / `evt.stt.final` (du tour) · **français forcé** (langue verrouillée — pas d'auto-détection qui basculerait en anglais) · modèle à calibrer (`medium` ⇄ `large-v3` int8) · prewarm au wake (réflexe, V11).
- **Fichiers** : `sidecar/consumers/stt`.
- **Dépend de** : V0.
- **Fait quand** : parole → `evt.stt.partial` au fil puis `evt.stt.final` ; transcription française sans bascule de langue. *(U-V4 ; modèle/latence/précision FR → §6.)*

### V5 — Fin de tour (Smart Turn v3 + Silero fallback)
- **But** : décider *quand* Yohann a fini de parler — vif, jamais au milieu d'une hésitation.
- **Contenu** : **machine à états unique dans le sidecar**, **émetteur unique de `evt.turn.end`** (reason `smart-turn`|`fallback`) · `evt.vad.start` ouvre le tour · chaque silence = candidat → Smart Turn évalue l'audio : **confiant + court silence → `turn.end` immédiat** ; **pas confiant → attend**, reprise de parole → candidat annulé, **même tour continue** (l'« euh… » ne coupe jamais) ; **plafond de silence → `turn.end` (fallback)** · `evt.stt.final` précède `turn.end`, qui le référence + porte les horodatages du tour (payload, M2) · **le plafond tient même si Smart Turn crashe** (dégradation douce) · **alternative de prise : LiveKit turn-detector** (texte — la prise ne présume pas de la nature du moteur). **Frontière gravée** : fin de tour **acoustique, jamais sémantique** (« attends », « merci Sophia » = grille V10, pas ici).
- **Fichiers** : `sidecar/consumers/turn`.
- **Dépend de** : V2, V4.
- **Fait quand** : Smart Turn confiant → réponse enclenchée peu après la fin réelle ; une hésitation ne coupe jamais la phrase ; **Smart Turn tué → le tour finit quand même au plafond**. *(U-V5 ; seuils + ratio fallback/smart-turn → §6.)*

### V6 — Speaker-ID (prise ; modèle Phase 3)
- **But** : savoir si c'est Yohann qui parle (sert le barge-in et le verrou de l'affect).
- **Contenu** : consommateur du ring buffer, **local, léger** · émet `evt.speaker` (locuteur, confiance) · **ancre = la voix de Yohann** : l'empreinte est **créée par l'enrôlement (doc `04` / premier boot)**, poussée au boot via `cmd.enroll.push` (socle) — **ici, on la consomme** · **`evt.speaker` = couture injectable** (pour tester le barge-in modulé V8 et le verrou d'affect V14 de façon **déterministe**, indépendamment du modèle réel) · **probabiliste** → le doute est géré en aval (honnêteté sociale, barge-in prudent). **L'échelle de confiance complète, l'enrôlement des proches et la vie sociale = doc `04`** ; ici, seulement le consommateur + son événement.
- **Fichiers** : `sidecar/consumers/speaker`.
- **Dépend de** : V0 ; empreinte via socle (`cmd.enroll.push`).
- **Fait quand** : la voix de Yohann est reconnue avec une confiance exploitable par V8/V14 ; un locuteur inconnu est signalé comme tel. *(U-V6 ; modèle + seuil → §6.)*

### V7 — TTS streamé (Kokoro GPU)
- **But** : parler pendant que le cerveau génère encore.
- **Contenu** : `cmd.tts.speak` (id) ouvre · `cmd.tts.push` pousse le texte **au fil du stream du cerveau** · `cmd.tts.end` clôt · le sidecar **découpe en phrases** (prosodie — un TTS nourri en miettes chante faux), synthétise (Kokoro ; *alternatives Chatterbox, ou ElevenLabs premium OFF sous cost-guard*), **joue dès la 1re phrase prête** · **file d'énonciations** (une seule joue) + `cmd.tts.stop` (purge) · cycle `evt.tts.start`/`evt.tts.done`.
- **Fichiers** : `sidecar/tts/`.
- **Dépend de** : V0.
- **Fait quand** : la 1re phrase joue **avant** la fin de génération du cerveau ; la découpe par phrases sonne naturel ; une purge coupe net. *(U-V7, I-3 ; TTS à l'oreille + latence 1re phrase → §6.)*

### V8 — Barge-in modulé + interruption sèche + replay + volume
- **But** : elle s'interrompt quand on lui parle, sans se couper elle-même ni sur un bruit.
- **Contenu** : **barge-in interne au sidecar** (invariant), **modulé par le locuteur** (B3, A29) : le **nom « Sophia »** pendant la TTS (`evt.wake`) → **coupure immédiate** (pas de condition de durée) ; **voix reconnue de Yohann** → coupe **vite** (seuil bas) ; **voix inconnue** → **durée minimale** (anti-faux-barge-in) ; *(cran « proche consenti » en tablée → `04`)* · sur coupure : le sidecar **purge lui-même**, émet `evt.bargein` (id, position, déclencheur) · **interruption sèche (S2)** : après barge-in, transcript « stop »/« chut » → **purge confirmée, zéro appel cerveau** · **replay** : « répète » → `cmd.tts.replay` (cache RAM, **zéro resynthèse** ; cache vide → erreur honnête « je n'ai plus l'audio ») · **volume** (S3) : « moins/plus fort » → gain sortie sidecar, **réglage persisté par l'orchestrateur** (écrivain unique).
- **Fichiers** : `sidecar/tts/bargein`, `src/orchestrator/voice/`.
- **Dépend de** : V7, V3, V6.
- **Fait quand** : « Sophia » pendant qu'elle parle → coupure **immédiate** ; voix de Yohann → coupe vite ; **un bruit bref ne la fait pas taire** ; le **mécanisme** de purge/replay/gain est prouvé ici par injection directe (`cmd.tts.stop` / `cmd.tts.replay` / gain), cache vide → dit honnêtement. **La reconnaissance parlée** (« stop », « répète », « moins fort ») est prouvée à **V10** (grille). *(U-V8 — avec `evt.speaker` injecté pour un barge-in modulé déterministe ; I-3 ; seuils par déclencheur → §6.)*

### V9 — États d'écoute
- **But** : qui écoute quoi, décidé par l'orchestrateur.
- **Contenu** : `VEILLE` / `ÉCOUTE ACTIVE` / `PAUSE` / `MODE DICTÉE` / `APPROBATION` (F5) · **propriété orchestrateur (B1)** : transitions par `cmd.listen.start`/`stop` (`start` **rétroactif** depuis la dernière marque VAD) ; **seule auto-transition sidecar = le tour de réveil** (V3) · **PAUSE** garde le fil (session Claude chaude, socle) · **MODE DICTÉE** = injection au curseur de l'app au focus, silencieuse, **grille réduite à une liste blanche** (S9 ; mode dev = dictée + focus VS Code) · **APPROBATION (S8)** = sous-état de l'orchestrateur pendant ÉCOUTE (rien signalé au sidecar) ; non-match → cerveau, fenêtre maintenue ; **timeout → refus par défaut + annonce**.
- **Fichiers** : `src/orchestrator/voice/states`.
- **Dépend de** : V3, V5 (événements) ; socle (session chaude).
- **Fait quand** : les transitions sont décidées par l'orchestrateur ; l'écoute transitoire est rétroactive ; le **mécanisme** d'injection en dictée et le cycle APPROBATION (transitions, timeout → refus) sont prouvés ici. **Le routage parlé** (dictée liste blanche : « merci Sophia » **écrit, pas exécuté**) est prouvé à **V10** (grille). *(U-V9 ; délai de garde + timeout APPROBATION → §6.)*

### V10 — Grille d'intentions (01-G — 20 entrées)
- **But** : reconnaître les intentions *système* sur le transcript final ; tout le reste = conversation → cerveau.
- **Contenu** : **config versionnée de l'orchestrateur** (pas une table SQLite) · **les 20 entrées** (réveil/ouverture · sollicitation · demande directe à froid · interruption sèche · suspension · annulation · pause · clôture · rappel · replay · volume · silencieux/voix · dictée · dev · statut · sessions · interrupteur proactif · kill-switch rêverie · **convocation API** · approbation) · **règles** : **match = énoncé entier normalisé** (S1, normalisation bornée : politesse, mots-outils, ordre) ; **match → l'acte d'un coup** (zéro cerveau) ; **pas de match → cerveau** ; **flou → cerveau, jamais d'action système** ; **« d'un coup » ≠ sans approbation** (S5 : les actes à conséquence gardent la fenêtre APPROBATION) ; transcript vide → **redemande honnête, zéro cerveau** (S4) ; **dictée → liste blanche** (S9) ; intentions contextuelles **inertes hors état** ; **« passe sur l'API » = active en épisode de panne du canal seulement** (99-D) · **convention « Yohann »** (gravée `03`) · supersession cahier B4 (injection = dictée explicite).
- **Fichiers** : `src/orchestrator/voice/grid`.
- **Dépend de** : V4, V9.
- **Fait quand** : les 20 intentions reconnues sur énoncé entier ; **« merci Sophia, c'est parfait, continue » → cerveau** (pas pause) ; « ok/go/fonce » inertes hors APPROBATION ; **« bonsoir » vs « bonne nuit » Sophia distingués** (S6) ; **transcript vide/inintelligible → redemande honnête, zéro cerveau** (S4 — jamais de silence) ; « passe sur l'API » inerte hors épisode de panne. *(U-V10 ; tolérance du mapping + faux matchs → §6.)*

### V11 — Résidence des modèles côté voix (01-E, S7)
- **But** : charger/décharger les modèles selon l'état, sans jamais dépasser la politique.
- **Contenu** : **trois axes, un seul émetteur** : (1) **mode voix** (groupe VEILLE vs CONVERSATION, dérivé des états V9 par l'orchestrateur → `cmd.model.policy` à chaque transition) ; (2) **calques du gouverneur** (SECOURS, JEU — descendus via socle/`05`) ; (3) **autorisations transitoires** (`cmd.tts.cache`, B2) · **réflexes chauds armés par la politique, tirés localement** (wake → prewarm Whisper ; `turn.end` → Kokoro monte ; reprise → Whisper remonte) · **résidence alternée** (écouter/parler jamais simultanés ; l'inactif en cache RAM) · **remontée** `evt.model.loaded/unloaded` (+ VRAM) · **le sidecar ne dépasse jamais la politique** (allocation refusée → dégrade et rapporte, jamais de crash silencieux) · `embed` (couche 2) autorisé dans tous les groupes (CPU, hors frontière VRAM) · **une seule frontière VRAM** arbitrée voix ↔ cerveau-de-secours (socle/`05`) · **priorité interactive absolue (socle) : les réflexes voix servent l'échange en cours, jamais une tâche de fond**.
- **Fichiers** : `src/orchestrator/voice/residence`, `sidecar/audio/models`.
- **Dépend de** : V9 ; socle T7 (gouverneur/calques).
- **Fait quand** : le set résident suit l'état voix ; le prewarm au wake est tiré localement (zéro aller-retour) ; une allocation VRAM refusée est rapportée sans crash. *(U-V11 ; co-résidence Whisper+Kokoro, swap RAM→VRAM, coût prewarm → §6 + doc `05`.)*

### V12 — Ducking (F3 — armé par l'état)
- **But** : baisser les médias quand il faut, jamais en yo-yo.
- **Contenu** : **armé par l'état** — VEILLE : seul `evt.wake` duck ; conversation (ÉCOUTE/APPROBATION) : `evt.vad.start` duck, remonte après ; **TABLÉE : sa voix + son nom seulement** (`evt.tts.start` + `evt.wake`), jamais le VAD ambiant (AT10, politique `04`) ; **DICTÉE : désarmé** (S9) · **systématique et non désactivable dans son périmètre**, **strictement orthogonal au toggle voix** · mécanisme côté **orchestrateur** (mixer Windows). Supersède le « à toute parole » du cahier (M4).
- **Fichiers** : `src/orchestrator/voice/ducking`.
- **Dépend de** : V9 ; le process orchestrateur (socle T0 — le mixer Windows est piloté depuis là ; le contrôle du mixer est bâti ici).
- **Fait quand** : les médias baissent selon l'état (au wake en veille, au VAD en conversation), remontent après ; désarmé en dictée ; indépendant du toggle voix ; **l'armement tablée (sa voix + son nom seuls) est prouvé ici par injection** de `evt.tts.start` + `evt.wake` (le VAD ambiant ne duck pas) — **la politique *quand* activer la tablée reste `04`**. *(U-V12.)*

### V13 — Panne du cerveau : jamais de silence (F7, B2)
- **But** : si le cerveau tombe, la voix le dit — une fois.
- **Contenu** : **pré-synthèse** au boot (**phase 5 du boot socle, après readiness sidecar, avant PRÊT**) et à chaque respawn → `cmd.tts.cache` (**autorisation transitoire** B2 : charge Kokoro → synthétise les phrases de secours → décharge → retour au set résident) · **phrase de secours (S11)** : **déclencheur unique = la fin du tour** (jamais au wake) ; **exempte de barge-in** (courte, prioritaire) ; **une fois par épisode de panne**, puis silence + voyant systray.
- **Fichiers** : `sidecar/tts/fallback`, `src/orchestrator/voice/`.
- **Dépend de** : V7.
- **Fait quand** : orchestrateur mort / WS coupé pendant que Yohann parle → la phrase de secours joue **une fois, en entier**, exempte de barge-in ; ensuite voyant ; **après la pré-synthèse (`cmd.tts.cache`), Kokoro est déchargé et le set résident restauré** — l'autorisation transitoire se referme, VRAM non dépassée (B2). *(U-V13, I-4 ; liste des messages + durée d'épisode → §6.)*

### V14 — Capteur d'affect (01-H — la prise, OFF par défaut)
- **But** : lire l'état affectif de Yohann dans sa voix — signal doux, jamais une étiquette.
- **Contenu** : consommateur du ring buffer, **CPU/intermittent** · **une évaluation par tour** (à `evt.turn.end`), `evt.affect` **attaché au tour** · **signal doux** (valence, énergie, **confiance**) — **jamais d'étiquette catégorielle** · **verrouillé sur l'ancre vocale** (n'évalue que si locuteur = Yohann) · **muet dans le doute** (confiance basse modèle **ou** locuteur → rien) · **OFF par défaut** (emotion2vec — *alternative wav2vec2-emotion* — branché à son essai, calibré sur la ligne de base de Yohann). **L'usage (colorer humeur/lien) = doc `03`.**
- **Fichiers** : `sidecar/consumers/affect`.
- **Dépend de** : V0, V6 (verrou locuteur).
- **Fait quand** : `evt.affect` **muet** si confiance basse ou locuteur ≠ Yohann ; **jamais d'étiquette** nulle part ; l'activation ne change pas le protocole (`evt.*` extensible). *(U-V14 — avec `evt.speaker` injecté pour vérifier le verrou locuteur ; modèle + seuils + coût CPU → §6.)*

### V15 — Respawn resync + suite de conformité des prises (S10, 01-F)
- **But** : après un respawn, tout se reconstruit ; changer de moteur ne change rien en aval.
- **Contenu** : **resync dans l'ordre** (S10) — `cmd.model.policy` (la politique courante) → `cmd.enroll.push` (empreintes) → `cmd.tts.cache` (phrases de secours) · **énonciations en vol au crash = échec terminal** (leurs `evt.tts.done` n'arrivent jamais → l'orchestrateur les clôt, **pas de re-énonciation auto**, notif honnête) · caches RAM repartent vides (replay → erreur normalisée) · **suite de conformité par contrat** : mêmes tests pour toute implémentation d'un rôle ; **cloud (replis)** : clés **par l'environnement au spawn** (jamais sur le WS), **OFF par défaut**, chaque appel payant → événement de coût → **cost-guard orchestrateur** ; **échec provider cloud → retour automatique au local + notification honnête**.
- **Fichiers** : `sidecar/plugs/`, `src/orchestrator/voice/`.
- **Dépend de** : V0–V14 ; socle T3 (respawn).
- **Fait quand** : après kill du sidecar → resync complète (politique, empreintes, secours), wake de retour ; changer de moteur = config + respawn, **mêmes événements en sortie**, suite de conformité passe (**prise `affect` V14 comprise**) ; **un échec de prise cloud (stub) → retour automatique au local + notification honnête** ; aucune clé requise pour démarrer. *(U-V15, I-5.)*

---

## 4. Tests

> Aucune tâche « faite » sans son test vert. Les seuils (< X) sont des **cibles à calibrer** (§6) — le test vérifie d'abord le **comportement**.

**Unitaires (par tâche)** : U-V0 capture unique + ring buffer + rembobinage · U-V1 AEC (pas de barge-in fantôme sur médias · pas d'auto-coupure · changement de périphérique encaissé) · U-V2 marques VAD post-AEC · U-V3 réveil rétroactif (premier mot intact) + sémantique wake hors cas nominal · U-V4 partial/final + français forcé · U-V5 fin de tour (confiant/hésitation/plafond + Smart Turn tué → plafond tient) · U-V6 evt.speaker (Yohann reconnu, inconnu signalé) · U-V7 découpe en phrases + 1re phrase avant fin de génération + purge · U-V8 barge-in par déclencheur *(evt.speaker injecté)* + bruit bref ignoré + stop/chut sans cerveau + replay + cache vide honnête + volume · U-V9 transitions orchestrateur + dictée liste blanche + APPROBATION (non-match → fenêtre maintenue, timeout → refus) · U-V10 les 20 intentions + énoncé entier + « bonsoir/bonne nuit » (S6) + **transcript vide → redemande zéro cerveau (S4)** + « passe sur l'API » conditionnée · U-V11 set résident dérivé de l'état + prewarm local + allocation refusée rapportée · U-V12 ducking armé par l'état + orthogonal au toggle · U-V13 phrase de secours (fin de tour, exempte barge-in, une fois) · U-V14 affect muet dans le doute + jamais d'étiquette *(evt.speaker injecté)* · U-V15 resync ordonnée + moteur interchangeable + **échec de prise cloud (stub) → reprise locale + notif**.

**Intégration (transverses)** :
- **I-1** **🔴 AEC bout-en-bout** : médias jouent + Sophia parle → aucun barge-in fantôme, aucune auto-coupure (M1 prouvée).
- **I-2** **🔴 réveil FR** : « Bonjour/Dis-moi/bonne nuit Sophia » depuis la pièce (au casque) → détection + premier mot intact ; taux de faux réveils mesuré.
- **I-3** **vivacité + barge-in** : wake → premier mot audible peu après `turn.end`, 1re phrase **avant** fin de génération (cerveau-stub) ; « Sophia » pendant qu'elle parle → coupure immédiate.
- **I-4** **jamais de silence** : orchestrateur tué pendant un tour → phrase de secours jouée une fois, en entier.
- **I-5** **résilience** : kill sidecar en pleine conversation → respawn + resync complète (V15), wake de retour < X.
- **I-6** **le banc bout-en-bout** (l'essai à blanc priorité n°1) : micro → AEC → VAD/wake/STT → fin de tour → cerveau-stub → TTS streamé, **au casque**, latence wake→premier mot mesurée.

---

## 5. Critères d'acceptation

> **Pointés vers `01` §6 (les 13) — la source, jamais réinventés.** *(Les invariants de `01` §5 — audio hors IPC, sidecar sans état durable, état d'écoute à l'orchestrateur, fin de tour acoustique, grille sur énoncé entier, injection en dictée seulement, moteur hors protocole, barge-in modulé, affect muet-dans-le-doute, ducking armé par l'état, une frontière VRAM, priorité interactive — sont ancrés dans les tâches V0–V15 et leurs tests.)*

1. **Réveil** (« Bonjour/Dis-moi Sophia » depuis la pièce, accueil < X, faux réveils < Y/j) → V3 / I-2.
2. **Adresse naturelle d'un coup** (« bonne nuit Sophia » à froid, premier mot intact, rien perdu) → V3, V10 / I-2.
3. **Fin de tour** (confiant < X ms · hésitation ne coupe pas · plafond tient si Smart Turn crashe) → V5 / U-V5.
4. **Vivacité** (premier mot < X s après `turn.end` ; 1re phrase avant fin de génération) → V7, V11 / I-3, I-6.
5. **Barge-in** (« Sophia » immédiat · Yohann < X ms · « stop » sans cerveau · jamais d'auto-coupure · bruit bref ignoré) → V8, V1 / I-1, I-3.
6. **Replay** (à l'identique, zéro cerveau/resynthèse ; cache vide dit honnêtement) → V8 / U-V8.
7. **Prises** (changer de moteur = config + respawn, mêmes événements ; conformité ; aucune clé pour démarrer) → V0, V15 / U-V15.
8. **Résilience** (kill sidecar → respawn + resync complète, wake < X ; orchestrateur mort → phrase de secours une fois) → V15, V13 / I-4, I-5.
9. **Affect** (`evt.affect` muet si confiance basse/locuteur ≠ Yohann ; jamais d'étiquette) → V14 / U-V14.
10. **Grille** (flou → cerveau jamais d'action ; forme dans une phrase plus longue → cerveau ; « ok/go/fonce » inertes hors APPROBATION ; timeout → refus) → V10, V9 / U-V10, U-V9.
11. **Ducking** (baisse dès la parole en conversation, au wake en veille, désarmé en dictée ; **armement tablée voix+nom seuls testé par injection en V12, politique tablée → `04`** ; indépendant du toggle) → V12 / U-V12.
12. **Erreur d'oreille** (transcript vide → redemande honnête, zéro cerveau, jamais de silence) → V10 / U-V10.
13. **Dictée** (« merci Sophia » dicté est écrit, pas exécuté ; hors dictée, rien n'est jamais tapé) → V9, V10 / U-V9.

---

## 6. Preuves de calibration Phase 3

> Les valeurs laissées ouvertes ci-dessus. Depuis `01` §7. **Zéro chiffre inventé.** *La couche audio porte les deux 🔴 du projet.*

- **🔴 Wake word FR** : LiveKit ⇄ openWakeWord sur la qualité FR de « Sophia » porté par des phrases variées + taux de faux réveils. **Tension F6** : « Sophia » (2 syllabes) = cible plus courte que « Dis-moi Sophia » → **repli nommé** si la preuve échoue (nom-en-phrase à chaud seulement, formules longues à froid). Entraînement avec la voix réelle de Yohann.
- **🔴 AEC (M1 — le vrai dur)** : alignement d'horloges micro/rendu (loopback WASAPI, dérive, resampling) · changement du périphérique de sortie · flux WASAPI exclusifs · latence et résidu d'annulation. Prérequis du barge-in **et** du réveil fiable en médias.
- **Fin de tour** : seuils (confiance Smart Turn, court silence, plafond, fenêtre de fusion d'hésitation) + **ratio `fallback`/`smart-turn`** (jauge de santé FR).
- **Barge-in modulé** : seuils par déclencheur (nom immédiat · Yohann bas · inconnu durée minimale) — sans avaler un mot bref porteur d'intention.
- **STT** : modèle Whisper (`medium` ⇄ `large-v3` int8), latence streaming réelle sur la 2060, précision FR.
- **TTS** : Kokoro ⇄ Chatterbox **à l'oreille** (avec le timbre A20) ; latence 1re phrase ; naturel de la découpe.
- **VRAM** (avec `05`) : co-résidence Whisper+Kokoro ou alternance stricte ; temps de swap RAM→VRAM ; coût du prewarm ; coût de `cmd.tts.cache` au boot.
- **Ring buffer** : taille de la fenêtre pré-wake (« bonne nuit Sophia » entier, marge comprise).
- **Écoute transitoire** : durée du délai de garde après le tour de réveil. **APPROBATION** : timeout avant refus.
- **Speaker-ID** (A29) : modèle + seuil (conditionne le verrou affect **et** la modulation barge-in).
- **Affect** : emotion2vec sur la ligne de base de Yohann ; seuils d'émission ; coût CPU/tour.
- **Grille** : tolérance du mapping (variantes réelles) ; taux de faux matchs (→ 0) ; **cas obligatoire « bonsoir » vs « bonne nuit » Sophia** (S6).
- **Volume (S3)** : pas de réglage du gain (marches), bornes.
- **Far-field** : d'abord au **casque** (valide la logique) ; le rig validera l'acoustique (passe #5).
- **Latence bout-en-bout** : wake → premier mot de réponse (le chiffre du critère de succès du cahier).
- **Phrases de secours** : liste exacte + déclencheurs + durée de l'épisode de panne.

---

## 7. Journal des écarts (code ↔ `01`)

> Vide au départ. Tout écart découvert au contact du code est **inscrit ici ET renvoyé au `01` §7**, jamais contourné.

- **[Emprunt tracé — audit croisé conv 14, FID-7]** V11 mentionne « `embed` (couche 2) autorisé dans tous les groupes (CPU, hors frontière VRAM) » : exact, mais c'est une notion de **`02` §2.2 / `05` §2.1** (embed dans le vocabulaire de `cmd.model.policy`), au-delà de la source `01`. Conservée (utile à la résidence voix) et tracée ici comme emprunt.
- **[Note d'audit]** Audit croisé 2 agents conv 14 (fidélité FID-1→7 · robustesse ROB-1→7) : **14 findings, zéro faux positif, tous intégrés**. **Deux findings (FID-2, FID-4) ont porté sur le plan du socle** (cohérence socle↔audio) → corrigés dans `docs/plan/00-socle.md` (T5 phase 5 nomme `cmd.tts.cache` ; T7 nomme le calque JEU). Non-défauts confirmés : couture injectable `evt.speaker` réellement branchée · les deux 🔴 (wake FR I-2, AEC I-1) adossés à un test · zéro chiffre inventé.
- **[Note ← AUDIT FABLE (intermède, 2026-07-10)]** Audit complet des 4 plans (sécurité · cohérence · trous · robustesse) : **ce plan est sain — zéro finding le concernant** (fidélité aux 13 critères de `01` §6 revérifiée, invariants §5 ancrés, grille 20 entrées conforme). Les findings de l'audit portent sur `plan/00`/`plan/02` (classe stockage + coutures) — cf. `docs/journal/audits/AUDIT-fable.md`.
- **[Back-refs de confirmation ← gravure de `plan/04` (conv 19), posées par l'audit corpus Fable (2026-07-11, FC-9 — symétrie stricte conv 21)]** trois coutures que `plan/04` §7 promettait « Renvois `plan/01` §7 (sur Go) » sont **confirmées bilatérales** (la mécanique était déjà saine — les renvois amont vivent dans le corps ; seule la trace manquait) : **V8** — l'échelle du barge-in gagne le cran **« proche consenti »** (reconnu, seuil modéré anti-cross-talk — politique `plan/04` Q10) ; **V12** — la **politique de ducking tablée** (armé par `evt.tts.start` + `evt.wake` seuls, jamais le VAD ambiant — posée `plan/04` Q10, mécanique ici) ; **V14** — le **verrou d'affect sur Yohann vaut aussi en tablée** (`plan/04` Q7). *(Le corpus ferme ses boucles — décision de symétrie stricte, conv 21.)*

- **[Preuve de banc — Temps 1 du banc AEC, conv 22 (2026-07-11) — validée par Yohann]** premier essai à blanc (banc **jetable** `bancs/aec/`, Python 3.13, PyAudioWPatch loopback WASAPI). **La dérive d'horloge micro↔loopback — la part classiquement la plus dure de V1/M1 — est NÉGLIGEABLE dans le chemin normal.** Sur 90 s, l'écart `loopback − micro` reste **constant** (+480 frames ≈ 10 ms), dérive mesurée **+3.4 ppm sous un plancher de ±10 ppm → horloges VERROUILLÉES**. Cause : en **mode partagé WASAPI**, le moteur audio de Windows cadence les deux flux sur une **horloge commune** (rééchantillonnage par périphérique masqué en interne) → micro et référence loopback arrivent **déjà alignés**. **Conséquence V1** : le **resampling adaptatif** anticipé par `01` §7/§V1 **n'est PAS nécessaire dans le chemin normal** ; reste un **délai fixe** (~10 ms) que l'AEC estime **une fois** (facile — pas de la dérive). **Limites, à prouver (Temps 2 / suite)** : vaut en mode **partagé** — les **flux exclusifs** (déjà nommés `01` §7) court-circuitent le moteur → dériveraient ET échapperaient au loopback (cas à part) ; l'**annulation elle-même** (WebRTC AEC3 ⇄ SpeexDSP) et le **changement de périphérique de sortie** restent à prouver au Temps 2. Environnement confirmé : Node 24.13 · Python 3.13/3.14 · RTX 2060 6 Go · Build Tools 2022. Renvoi `01` §6 (AEC/M1 = la preuve prioritaire).

- **[Preuve de banc — Temps 2 du banc AEC (l'annulation), conv 23 (2026-07-11) — validée par Yohann]** Second essai à blanc (banc **jetable** `bancs/aec/`, `03_aec_cancel.py`, gitignoré). **La preuve I-1 est acquise, en conditions réelles** (barre de son HDMI + micro USB, sortie réelle de Yohann) :
  - **Écho seul** (média fort) : après AEC, résidu au bruit de fond (activité résiduelle ~0 %, pic −33 dBFS), **ERLE ~30 dB** → **elle ne se coupe pas elle-même, zéro barge-in/VAD fantôme**.
  - **Double-parole** : voix de Yohann **préservée à −3,5 dB (67 %)**, média annulé — **confirmé à l'oreille par Yohann** → **elle l'entend par-dessus le média**.
  - Temps réel trivial (~0,4 ms/trame pour 10 ms de budget).

  **Moteur benché : SpeexDSP** (via `pyaec`, DLL SpeexDSP compilée prête). **Aucun binding maintenu de WebRTC APM/AEC3 n'existe** sur Windows/Python 3.13 (seul paquet PyPI cassé + périmé AECM ; vrai AEC3 = build C++ from-source Meson/abseil = **dette de maintenance**, contre R3).

  **Écart / raffinement (renvoi `technique/01` §2.1) :** le design pose « micro → AEC → PUIS conversion 16 kHz ». **Empiriquement, SpeexDSP sur-supprime le proche à 48 kHz** (perte ~22 dB au passthrough référence-zéro ; propre à 16 kHz). Speex est conçu pour 8/16 kHz → **pour un moteur famille-Speex, la conversion 16 kHz doit se faire À/AVANT l'AEC**. (AEC3 encaisserait le 48 k.) Le harnais tourne l'AEC à 16 kHz.

  **Findings :** barre de son = **~150 ms de latence** (réf→écho) → **queue de filtre AEC ≥ 200 ms** (trop court = écho raté). L'AEC mérite d'être une **prise `aec` formelle** au §2.3 (absente aujourd'hui — signalé, non modifié).

  **Décision technique (micro-technique tranchée par Claude — garde-fou Phase 3 pt 2 ; pas une réouverture : le moteur n'a jamais été gravé, laissé à l'essai comme A8/A5) : moteur AEC primaire = SpeexDSP à 16 kHz** (robuste, maintenable, coût ~nul, prouvé suffisant pour M1 sur le cas réel un-locuteur + barre de son). Prise `aec` laissée **ouverte pour un AEC neuronal (ONNX/onnxruntime)** en upgrade — SOTA double-parole, s'intègre au sidecar neuronal existant, maintenable — **si** un besoin réel mesuré apparaît, au coût d'une part de VRAM (frontière `05`). **AEC3 non retenu** (algorithme respecté, intégration non maintenable dans ce stack). **CF2 : rien du banc n'entre dans le produit** ; l'audit croisé viendra au vrai sidecar.

- **[Cas durs M1 — banc AEC, conv 23 (2026-07-11) — validée par Yohann]** Les deux limites nommées de V1, **confirmées empiriquement** :
  - **Flux exclusif — échappement confirmé** (`04_exclusive_escape.py`, **avec témoin**) : ton en **partagé** capté (−42 dBFS) ; même ton en **exclusif** NON capté (**−97 dBFS = silence, 0,2 %**). L'exclusif court-circuite le moteur partagé → **hors référence de l'AEC → non annulable** → **le sidecar doit détecter l'usage exclusif et signaler la dégradation** (résidu assumé, conforme V1/§6).
  - **Changement de périphérique de sortie — confirmé** (`05_device_change.py`, sur la Focusrite USB de Yohann) : allumer un 2ᵉ périphérique bascule la sortie/loopback par défaut ([10] TV/[13] → [15]/[20] Focusrite) ; l'ancien loopback reste **lié à l'ancienne sortie (périmé)**. → **le sidecar doit détecter le changement (notifications Core Audio IMMNotificationClient) et RÉ-OUVRIR le loopback** sur la nouvelle sortie. **Piège** : PyAudioWPatch cache l'énumération à l'init → re-init/notifications nécessaires (pas un simple re-query).
  - Handling des deux = **code sidecar produit** (API Core Audio), hors du banc jetable.

- **[Preuve de banc — wake word FR « Sophia » (🔴 n°2 · V3/F6), conv 24 (2026-07-12) — validée par Yohann]** **Dérisqué et PROUVÉ** au banc jetable `bancs/aec/` (venv py3.13, `livekit-wakeword` 0.2.1 + VoxCPM2, torch 2.6+cu124 / RTX 2060). *(Micro-technique tranchée par Claude — garde-fou Phase 3 pt 2 ; moteur non gravé, laissé à l'essai comme A8.)*

  **Moteur & install :** **`livekit-wakeword`** (conv-attention, ONNX ; A8 confirmé). Mur d'install franchi : `editdistance` sans wheel cp313 → compilé sous `vcvars64` + `DISTUTILS_USE_SDK=1` + `--no-build-isolation`. *(triton 3.7 incompatible torch 2.6 → `torch.compile` inutile ; sans effet sur la vitesse, voir ci-dessous.)*

  **Méthode de génération FR = « E » (conditionnement par référence) :** VoxCPM déduit la langue du **texte** → « Sophia » seul (court, ambigu) sort en anglais/accent aléatoire (**constaté à l'oreille de Yohann**). Solution : générer une **référence française** (phrase complète) puis « Sophia » en **continuation** (`prompt_wav_path`+`prompt_text`) → voix/accent/genre **français** hérités. **`retry_badcase=False` + 6 pas de diffusion** → **~6 s/clip** (le `retry`, faux-positif sur mot court, coûtait ~40 s).

  **Modèle final :** 1000 clips synthétiques FR **+ 231 clips de la VRAIE voix de Yohann ×3** (77 uniques, conditions près/doux/loin — A8) ; négatifs FR avec **confusables proches sur-représentés** (Sonia/Sophie/sosie, ~50 %, défense F6). Éval synthétique : AUT=0,0017 · FPPH=0,00 @ 0,5 (n_neg≈30k / ~17 h).

  **Preuve à la voix de Yohann (le vrai juge) :** **6/6 des « Sophia » détectés à seuil ~0,25–0,30, ZÉRO faux** (parole normale + confusables Sonia/symphonie/sosie plafonnent à ~0,06). L'enrichissement par la voix réelle (A8) a **remonté la courbe de recall de ~0,15–0,20** (6/6 à 0,30 au lieu de 0,12 sans elle).

  **Décision (tracée) : F6 = PASSÉ — repli nommé NON nécessaire** (reste spécifié `01` §6/§7, non activé). « Sophia » (2 syllabes) est fiable à ta voix, faux réveils quasi nuls. **Seuil de détection retenu ≈ 0,25–0,30** (calibration par utilisateur, `01` §6 — à affiner à l'usage réel ; en environnement FR réel les faux paraissent très improbables, marge ~5× sous le seuil).

  **Périmètre :** prouvé = reconnaissance du nom **à la voix de Yohann, au micro**. Le **far-field acoustique** (réverbération/distance réelles, beamforming/fusion) reste la **passe #5 / ère matérielle** (rig micros) — le modèle est déjà entraîné sur des « Sophia » lointains. **Précision Yohann (post-clôture conv 24)** : son usage réel = **micro porté au cou** (near-field constant qui bouge avec lui — l'équivalent « casque » du design, §7) → la preuve « à la voix, au micro » **EST représentative de l'usage réel** ; le **rig far-field (passe #5) devient OPTIONNEL, pas un bloquant** (utile seulement s'il veut un jour se passer du micro porté). Testé « plus loin que la chaînette » avec bon résultat ; bruit de chaînette absorbé (AEC + marge 0,06 vs seuil 0,25). **CF2 : rien du banc n'entre dans le produit** ; le vrai sidecar (prise `wake`, §2.3) ré-implémentera, audit croisé alors. Renvoi `01` §6 (wake FR = preuve prioritaire) + §7 (F6).

- **[Preuve de banc — STT FR (V4), conv 25 (2026-07-12) — validée par Yohann]** Tournoi à la voix de Yohann au banc jetable `bancs/aec/06_stt.py` (**faster-whisper 1.2.1 / ctranslate2 4.8.1**, py3.13 / CUDA 12.4 / RTX 2060). *(Micro-technique tranchée par Claude — garde-fou Phase 3 pt 2 ; modèle non gravé, laissé à l'essai comme A5.)*

  **Fix Windows** : ctranslate2 charge cuDNN9/cuBLAS/cudart **depuis `torch/lib`** (`os.add_dll_directory` avant l'import) — pas de doublon `nvidia-*`. **Langue verrouillée `fr`** ; décodage `temperature=0` + `condition_on_previous_text=False` ; **VAD en amont indispensable** — Whisper **hallucine sur le silence pur** (« Merci d'avoir regardé cette vidéo »), le VAD (Silero, V2) l'élimine (fichiers benchés avec `vad_filter`).

  **Deux passes à la voix de Yohann (le vrai juge)** : (1) **lecture** de 3 phrases FR ; (2) **parole spontanée** (débit réel). Modèles comparés sur le **même audio** : `large-v3-turbo`, `large-v3`, `medium`, **+ fine-tunés FR** (`bofenghuang/whisper-large-v3-french` complet + `…-distil-dec16`, CT2).

  **VRAM mesurée (RTX 2060, int8_float16 sauf mention)** : turbo **1,15 Go** · large-v3 **2,1 Go** · large-v3 **float16 3,9 Go** · medium 1,15 Go → **`float16` écarté** (+1,8 Go pour un gain nul : `int8_float16` ne quantifie que les poids, calculs en fp16).

  **Justesse** : **`large-v3` le plus propre ET complet** (parfait en lecture ; élisions/ponctuation nettes ; capte « anthropique »). turbo **quasi** (rate 2 mots durs : « ronronnent »→« rouronnent » ; « Anthropic »→« entropique »). medium : 1 erreur de sens. **Fine-tunés FR = aucun gain** sur la voix de Yohann (même « anthropique » que large-v3 ; le complet a **perdu des mots** en fin de phrase) → *hypothèse « un spécialiste FR sera plus juste » INFIRMÉE au banc*.

  **Vitesse** : GPU tous largement temps réel (turbo RTF ~0,04 · large-v3 ~0,13, silences coupés). **CPU seul (i5-9600KF — mode JEU, GPU saturé)** : turbo RTF **~0,3–0,65**, medium ~0,45–0,9 → **plancher CPU viable** (phrase courte ≈ 2 s, réponse vocale maintenue).

  **Décision (tracée)** : **STT par défaut = `large-v3` int8_float16 sur GPU** (optimal justesse ; 2,1 Go, la 2060 le porte large en usage normal). **Cran de dégradation = `large-v3-turbo`** selon la **VRAM libre** — GPU en jeu lourd, **CPU si GPU saturé** (prise `stt` par paliers = gestionnaire de modèles, **doc `05` / mode JEU** : « dégrader en vitesse, jamais en canal »). **Fine-tuné FR non retenu.** **Filet transverse** : les erreurs mineures sont **absorbées par le cerveau** (le LLM comprend l'intention, pas les lettres) et **tout acte à conséquence est confirmé avant exécution** (A26).

  **Périmètre** : prouvé **à la voix de Yohann, au micro porté** (near-field = usage réel, cohérent §7 wake). **CF2 : rien du banc n'entre dans le produit** ; le vrai sidecar (prise `stt`, §2.3) ré-implémentera, audit croisé alors. Renvois `01` §6 (STT = calibration) · doc `05` (paliers VRAM / mode JEU).

- **[Preuve de banc — Fin de tour (V5), conv 25 (2026-07-12) — validée par Yohann]** Banc jetable `bancs/aec/07_turn.py` (**Smart Turn v3.2-cpu ONNX** + **Silero VAD**, `onnxruntime` CPU, py3.13). *(Micro-technique tranchée par Claude — garde-fou Phase 3 pt 2 ; moteur non gravé, laissé à l'essai comme A6.)* **Deux étages (A6 / `01` §4.3)** : Silero VAD = garde-fou (silence + plafond) ; **Smart Turn v3.2** = le cerveau (waveform/**intonation**, pas le texte).

  **Chaîne (vérifiée à la source — `inference.py` Pipecat)** : audio 16 kHz → **8 s** (dernières, pad zéros à gauche) → mel-spectrogram Whisper (`WhisperFeatureExtractor(chunk_length=8)`, 80×800, **`do_normalize=True`**) → ONNX → **la sortie EST une probabilité** (sigmoïde intégrée ; le tensor `logits` est trompeur — **PAS de sigmoïde à rajouter**, sinon tassement vers 0,5 : bug attrapé en lisant la source). Modèle `smart-turn-v3.2-cpu.onnx` (dernier au repo, > la v3.1 que citait le code). **~40 ms/appel sur l'i5** (annonce 12 ms ; écart = i5 ancien + FE non optimisé — négligeable pour la fin de tour).

  **Français DÉRISQUÉ à la voix de Yohann** (la doc dit seulement « multilingual » sans lister le FR → **le test tranche, comme le wake**). **Mode fichier** (parole spontanée) : 4 vraies fins de demande captées (0,57–0,99), respirations/« Ah, et… » internes ignorées (≤ 0,12). **Mode live** : **le cas dur réussi** — un « euh… » suspendu → **0,214, ne coupe pas** → puis 0,936 à la vraie fin ; 4 fins nettes (0,94–0,99), pauses internes ≤ 0,005. **Zéro coupure prématurée, zéro fin ratée.** **Latence ~200 ms** après la fin réelle (150 ms silence Silero + ~40 ms Smart Turn) → vif.

  **Décision (tracée)** : **fin de tour = Smart Turn v3.2-cpu + Silero VAD** ; **seuil ≈ 0,5** (fossé énorme : fins > 0,93 / hésitation ~0,21 / pauses < 0,01 → marge large, calibrable 0,3–0,7) ; `min_silence` Silero ~150 ms avant d'évaluer. **Machine à états unique = émetteur unique de `evt.turn.end`** (V5). **Frontière gravée tenue** : fin de tour **acoustique, jamais sémantique** (le sens = grille V10). **Alternative de prise (LiveKit turn-detector, texte) non nécessaire.**

  **Non exercé (honnête)** : le **plafond/fallback** (3 s) ne s'est **jamais déclenché** — Smart Turn a tranché à chaque fois (bon signe de fiabilité) → garde-fou spécifié et codé, mais non prouvé empiriquement ici (à voir sur un cas ambigu, ou Smart Turn tué → le plafond tient, `01` §4.3). **CF2 : rien du banc n'entre dans le produit** ; le vrai sidecar (prise `turn`, §2.3) ré-implémentera, audit croisé alors. Renvoi `01` §6 (fin de tour = seuils + ratio fallback/smart-turn).

- **[Preuve de banc — Voix / timbre TTS (V7 · A20), conv 25 (2026-07-12) — validée par Yohann]** Tournoi de voix FR au banc jetable (`bancs/aec/tts_out/`). *(Le moteur/timbre TTS n'est pas gravé — laissé à l'essai, A9 ; et le TIMBRE relève de Yohann, A20.)*

  **Tournoi à l'oreille de Yohann (le juge — A20)** : **Kokoro `ff_siwis`** (seule voix FR, native mais **trop froide/robotique**) · **Chatterbox** (cloneur — **sans référence, voix par défaut MASCULINE non-FR** → inutilisable tel quel, écarté) · **VoxCPM2** (déjà présent pour le wake) = **la meilleure voix FR douce**. Kokoro/Chatterbox montés sur **venvs py3.12 isolés** (mur spaCy py3.13 + conflit `transformers` → isolation ; écartés).

  **Écart A9 assumé et tracé** : A9 listait **Kokoro (défaut) / Chatterbox / ElevenLabs** ; le retenu = **VoxCPM** (hors A9). Motif : à l'épreuve du FR réel, les candidats A9 ne donnent pas la voix voulue ; VoxCPM oui.

  **Timbre CHOISI par Yohann (A20)** : voix **douce, suave, naturelle, féminine FR**, intonation juste (« très bien… compliqué de faire mieux »). **Zéro clonage réel** (voix **synthétique** — personne de réel → esprit A20 respecté : on sculpte la sienne). **Recette** (`SOPHIA_VOIX_recette.md`) : VoxCPM2 · référence `SOPHIA_voix_reference_v1.wav` (seed synthétique) + son `prompt_text` · **continuation** (`prompt_wav_path`) · `cfg_value=2.0` · `inference_timesteps=16` · **débit natif** (le time-stretch dégradait/saccadait — abandonné). Démo validée : portrait de 42,7 s.

  **DÉFI OUVERT — vitesse (conv 26)** : VoxCPM ≈ **10× le temps réel** (RTF ~10, RTX 2060) → **trop lent pour le TTS streamé** (V7 / A9 / §6). **Piste** : reproduire CE timbre avec un moteur **rapide** en lui donnant `SOPHIA_voix_reference` comme **voix cible** (Chatterbox/XTTS sur GPU — les cloneurs redeviennent l'outil, la voix cible étant synthétique et choisie). **Le choix esthétique est acquis ; la vitesse reste à prouver.**

  **CF2** : la référence de voix = **asset « timbre choisi » à préserver** (banc, hors dépôt public) ; le vrai sidecar TTS ré-implémentera (prise `tts`, §2.3), audit croisé alors. Renvois `01` §6 (TTS = à l'oreille + latence 1re phrase) · doc `03`/A20 (le timbre porte le caractère) · A9 (écart moteur).

- **[Preuve de banc — Voix RAPIDE / moteur TTS streamé (V7 · A9 · A20), conv 26 (2026-07-12) — validée par Yohann]** Le timbre choisi conv 25 (VoxCPM) était **~10× le temps réel** → défi vitesse. **VoxCPM accéléré écarté par la mesure** : plancher **RTF ~8,2** même à `inference_timesteps=4` (goulot = **backbone autorégressif** ~1,3 s/trame sur la 2060, pas la diffusion) → inutilisable en streamé. **Solution retenue = cloner le timbre** (`SOPHIA_voix_reference_v1.wav`) avec un moteur rapide. Chatterbox **multilingue** clone vite (RTF ~1,2) mais prosodie FR imparfaite (prénom, montée des questions, respiration). → **Chatterbox-FRANÇAIS** (`Thomcles/Chatterbox-TTS-French` — fine-tune du Chatterbox **anglais** sur ~1400 h Emilia FR ; on charge la base `ChatterboxTTS.from_pretrained` puis on **injecte le T3 français** via `hf_hub_download(...,'t3_cfg.safetensors')` → `model.t3.load_state_dict`) : **RTF ~0,87** (sous le temps réel → streamable), prosodie FR nettement meilleure (validée à l'oreille). **Prénom = lexique de prononciation** « **Yohann** » (mémoire/texte, intact) → « **Yoan** » (à l'entrée du moteur seulement) : « Yohann » brut **non fiable**, « Yohan » (avec h) échouait, « **Yoan** » fiable sur 5 contextes → mécanisme **général** (réglage produit ; tout mot mal dit peut recevoir une entrée). **Réglages** (oreille de Yohann, A20) : `exaggeration=0.5`, `cfg_weight=0.4`, `temperature=0.7` ; SR **24 kHz** ; watermark Perth **inaudible** (désactivable au vrai sidecar). **La ponctuation pilote la prosodie** (virgules=pauses ; `:`/`…`→virgules ; le cerveau ponctuera naturellement). **Génération phrase-par-phrase** (= le streaming V7). **Écart A9** (ouvert conv 25) : moteur rapide = **Chatterbox français** (Chatterbox était listé A9 ; fine-tune FR hors liste, esprit tenu) clonant le timbre **synthétique** choisi (A20, zéro personne réelle). **Premium = ElevenLabs** (chemin nommé A9, « quand revenus ») : la **prise `tts`** (01-F/V0) rend le moteur **interchangeable** → bascule = config, **zéro reprise**. **Validé Yohann : voix v1** (+ démo de présentation partageable, montage soigné). **Non encore mesuré** : latence bout-en-bout **wake→premier mot** (I-6, dominée par le cerveau) = prochaine étape. **CF2** : banc jetable, rien n'entre au produit ; le vrai sidecar (prise `tts`, §2.3) ré-implémentera, audit croisé alors. Renvois `01` §6 (TTS à l'oreille + latence 1re phrase) · A9 (moteur/écart) · A20 (timbre).

- **[Preuve de banc — banc bout-en-bout I-6 (assemblage live), conv 27 (2026-07-12) — validée par Yohann]** **I-6 PROUVÉ dans ses deux temps.** Toutes les briques réunies pour la 1re fois en une boucle live (micro → AEC → portier → STT → fin de tour → cerveau → TTS streamé → voix). Bancs jetables `bancs/aec/oreilles_live.py` (chaîne d'écoute + coordinateur, `.venv` py3.13) + `bancs/aec/bouche_live.py` (service TTS Chatterbox-FR, `.venv-cbg` py3.12), reliés par **socket localhost** (l'audio ne transite jamais par l'IPC). Deux venvs incompatibles (numpy/py) → boucle **multi-process**, fidèle à la frontière orchestrateur↔sidecar. *(Micro-technique tranchée par Claude ; CF2 : rien n'entre au produit, le vrai sidecar ré-implémentera, audit croisé alors.)*

  - **(a) cerveau-stub** : boucle prouvée live, elle répond à voix haute à l'adresse de Yohann. Latence **fin de phrase → 1er mot ≈ 3–3,8 s** (plancher plomberie : STT `large-v3` ~1,3 s + génération 1re phrase TTS ~2 s ; cerveau ≈ 0).
  - **(b) VRAI Claude** (`claude -p`, Max, « pas d'API ») : **elle a donné une vraie réponse (Spinoza) à voix haute, validée par Yohann.** Latence **≈ 7 s** (STT ~1,3 + **Claude à FROID ~4–5** + TTS ~2), **dominée par le cerveau** (conforme au cahier). **Dépendance Anthropic (vigilance n°1) tenue en réel.** Détails : cerveau lancé depuis un **dossier neutre** (sinon il charge le CLAUDE.md *du chantier* → « ravi de reprendre l'aventure de Sophia » !) · **Sonnet** · consigne « assistant vocal, bref, oral, sans markdown ni émojis » · fil gardé par `--resume` · **`claude.CMD` résolu via `shutil.which`** (Windows : `claude` nu = FileNotFound depuis Python) · **jamais de cerveau sur un transcript vide** (garde).

  - **DÉCISION MAJEURE (Yohann) — l'éveil par PHRASE, portier VAD+STT (écart produit tracé).** Le **wake-model conv-24 s'est révélé trop faible en conditions réelles** : il **ratait « Dis-moi Sophia »** (~0,29, sous seuil) et **déclenchait sur « Sophie »** (0,45) — un « Sophia » noyé dans une phrase naturelle score plus bas qu'un « Sophie » isolé ; l'AEC-**preprocess** l'aggravait (denoise/AGC hors distribution → coupé pour le wake) ; l'intégration temporelle n'y suffit pas (un confusable clair tient aussi longtemps qu'un vrai). **→ wake-model ÉCARTÉ du banc ; portier = VAD (entend la parole) + STT (lit la PHRASE, distingue Sophia/Sophie de façon fiable).** **Éveil par phrase (« OK Siri », pas « Siri ») : Bonjour / Bonsoir / Salut / Dis-moi Sophia** (+ **Bonne nuit Sophia** = éveil-clôture) ; **conversation active multi-tours sans re-nommer** ; **clôture « Merci Sophia, à plus tard »** (le « à plus tard » lève l'ambiguïté — un « merci Sophia » seul ne ferme pas) + rendormissement sur silence. **Prouvé (mode listen) : 3 phrases → 3 issues nettes** (ÉVEIL / CLÔTURE / IGNORÉ « Sophie »). **Écart produit** : conv-24 insuffisant pour la détection *en phrase* → produit = **meilleur modèle wake (ré-entraîné)** OU **STT-toujours-actif** (acceptable en usage perso local/privé) — renvoi `01` §6 (wake) + §7 (F6, repli nommé désormais ADOPTÉ sous forme de phrases).

  - **RESTE = optimisations CONNUES, pas des inconnues (priorité conv 28)** : **l'accusé (O2)** — elle parle instantanément (« mmh… ») pendant qu'elle réfléchit → masque la latence (**Yohann l'a identifié seul**) ; **la session chaude (A36)** — garder UN Claude allumé → ~1–2 s au lieu de ~5 (le gros du délai = le redémarrage à froid, pas la réflexion). Plus : **barge-in absent** (V8) ; **attaque avalée sur phrase très courte** (Chatterbox rêche sur le très court — recette conv 26, montage best-of). Renvois `99` O2 (accusé) · `05` R4/A36 (session chaude) · `01` V8 (barge-in) · `01` §6 (latence 1re phrase).

- **[Preuve de banc — conv 29 (2026-07-13) — validée par Yohann À L'USAGE RÉEL (micro porté, vrai Claude)]** Ordre du plan (V1→V4→V8→session chaude) exécuté au banc jetable `bancs/aec/` (CF2). **V1 (AEC live, sonde `08_v1_diag`, M0-M4)** : câblage prouvé (le son joué ressort dans le loopback = réf AEC ; délai TV ~110 ms, dans la queue 200 ms) · **média en fond = 0 tour fantôme** · **silence = 0 tour**, plancher micro **-82 dBFS** · alignement des files sain (garde anti-backlog jamais déclenchée). Le **résidu de sa PROPRE voix** déclenche le VAD (l'AEC plafonne **~8-10 dB** au micro porté — écho FAIBLE *floor-limité*, pas le « média fort » de conv 23 ; **ni** le preprocess/suppresseur de résidu **ni** la compensation du délai 110 ms ne débloquent) → **tranché : ce résidu n'est PAS un trou de V1** (l'AEC annule ce qu'il peut), c'est **V8/speaker-ID** (sa voix synthétique ≠ Yohann), **dormant** dans la boucle tour-par-tour. **V4 (anti-hallu)** : le « Sous-titrage… » = hallu Whisper sur silence, **tuée par `vad_filter`** (sonde `09`) ; média → le **VAD bloque le tour** (0 déclenchement) ; edge « bruit bref → phrase inventée » → **filet** (liste de phrases-fantômes + `no_speech_prob`, sonde `10`, **11/11**) branché dans `oreilles_live`, **prouvé live** (ignore « Sous-titrage ST' 501 » deux fois). **V8 étape 1** : `_await` devient **dual-poll** (elle écoute pendant la TTS, **fil unique** = pas de course, invariant « émetteur unique ») + **ids d'énonciation uniques → la fuite de jalons de conv 28 est CORRIGÉE** (0 temps négatif en réel) ; mécanisme de coupure prouvé **par injection** (couture injectable, comme `V6`/`V8` le prévoient) ; **la vraie DÉTECTION audio du barge-in = V8 étape 2, NON faite**. **Session chaude (A36 · renvoi `05` R4)** : cerveau `claude` **persistant** (`--input-format stream-json --output-format stream-json --verbose`, sous Max, pas d'API) → **~4 s d'allumage puis ~1,4 s/tour** (vs 5-13 s à froid ; sonde `11`) ; intégré (`WarmBrain` + pré-allumage au boot + **repli à froid** robuste) → **fini le backlog cerveau-froid**. **Clôture** (spec Yohann) : « Merci Sophia, à bientôt » → « À bientôt Yohann » → veille + reste muette. **Bug corrigé** : le **plafond de silence** (V5) comptait AUSSI pendant une phrase continue > 3 s (le VAD ne « pointe » qu'au début de la parole) → coupure à 3 s en plein milieu ; corrigé (compteur suspendu tant que `in_speech`).
  **DEUX MURS restants = problèmes de CONCEPTION, PAS des patchs (→ conv 30)** : **① l'endpointing / fin de tour (V5)** — la pile **Silero VAD + Smart Turn v3.2 + plafond** n'est **pas fiable** pour la parole naturelle de Yohann (phrases longues, pause avant de nommer « …entre Kant » *puis* « et Hegel », politesse finale « …, s'il te plaît ») : **elle le coupe en plein milieu** et la réactivité est **inconstante**. *Écart tracé (renvoi `01` §6 fin de tour) : la **fin de tour ACOUSTIQUE seule ne suffit pas** ; pistes à concevoir — **endpointing sémantique** (la phrase est-elle *complète* ?) · meilleur modèle · autre mode d'interaction.* **② la latence de la voix (V7)** : le temps avant le **1er mot prononcé** est de **7→41 s et CROISSANT** au fil de la conversation (**accumulation dans le tuyau de la bouche — à MESURER avant de fixer** ; + réponses trop longues vs consigne « 1-3 phrases »). **Leçon de méthode (Yohann) : ces murs se CONÇOIVENT, ils ne se bricolent pas** (chaque rustine — grâce, seuil — a régressé la réactivité). **CF2 : rien du banc n'entre au produit** ; le vrai sidecar ré-implémentera, audit croisé alors. Renvois `01` §6 (fin de tour = seuils + endpointing · latence 1re phrase) · `05` R4/A36 (session chaude) · V8 (détection barge-in).

- **[Preuve de banc — STREAMING CERVEAU (V7) + diagnostic ENDPOINTING (V5), conv 31 (2026-07-14) — validée par Yohann À L'USAGE RÉEL (micro porté, vrai Claude, 2 conversations)]** *Prémisse conv 30 (tracée au RELAY, pas au §7) : le blanc = **le cerveau** (~7,7 s sur les longues) ; Piper quasi-gratuit ; **PAS d'accumulation** (le « mur croissant 7→41 s » de conv 29 = ABSENT). Voix retenue = **Piper/« Jessica »** (rapide, prénom IPA `[[joˈann]]`, prise `tts` interchangeable).* Bancs jetables `bancs/aec/` (CF2). *(Micro-technique tranchée par Claude — garde-fou Phase 3 pt 2.)*

  - **① STREAMING CERVEAU (V7) — PROUVÉ.** `WarmBrain.ask()` attendait la réponse **entière** (blocage sur `result`) avant de parler → c'était ça, le blanc. Rendu **incrémental** : **`--include-partial-messages`** ajouté à la session chaude (schéma **mesuré d'abord**, banc jetable `20_brain_stream.py` : le texte au fil = `stream_event` → `event.content_block_delta` → `delta.text_delta.text` ; l'événement `assistant` n'arrive qu'à la **fin**, inutile pour le streaming). Nouveau générateur **`WarmBrain.ask_stream()`** : accumule les `text_delta`, **découpe en phrases au fil** (`_split_stream` : terminateur `.?!…` **suivi d'un blanc** → ne coupe pas « 3.14 » ni « M. » ; reliquat flushé à `result`), **rend chaque phrase dès qu'elle est complète** ; `ask()` = `" ".join(ask_stream())` (un seul parseur ; `prewarm`/repli à froid conservés) ; repli à froid SI le flux meurt **avant** toute phrase, sinon on finit avec ce qu'on a (jamais de double-voix). `_respond` consomme le flux et **`mouth.speak()` par phrase, au fur et à mesure** (le train d'avance `gen_q`→`play_q` de la bouche enchaîne). **Preuve live (8 tours, vraie conversation)** : **`trous=[0.0]` sur TOUS les tours** (zéro blanc *entre* les phrases — le doute central de Yohann, tranché par la mesure : le cerveau écrit plus vite que Piper ne joue → le train reste plein) ; **présence ~3 s** (meilleur cas warm : tours courts) vs **~7–9 s** conv 30 ; **1re phrase ~1,6–2,0 s plus tôt** sur les réponses multi-phrases (gain **nul** sur les réponses d'1 phrase — rien à streamer en avance). **Le blanc résiduel n'est PLUS le streaming** : c'est `STT` (1–2,8 s) + la **1re-phrase du cerveau** (TTFT ~2–4 s, jusqu'à 12 s sur 2 tours = **charge API transitoire**, `rate_limit_event` vu, revenu à ~4 s — pas notre code). Nouvelles mesures : `brain_first_sentence` (cerveau→1re phrase) vs `brain_done` (flux fini).
    - **ÉCART doc↔banc (V7) tracé** : la conception V7 **découpe les phrases dans le SIDECAR** (`cmd.tts.push` au fil du cerveau → un splitter, toutes les voix). Le **banc découpe dans le COORDINATEUR** (oreilles) — bouche **inchangée** (protocole `speak`), les 3 bouches (piper/xtts/live) non touchées. **Les deux nourrissent le TTS en phrases ENTIÈRES** (prosodie juste — « un TTS nourri en miettes chante faux », V7). Le banc prouve la **latence**, pas l'archi finale ; le vrai sidecar re-partitionnera (prise `tts`, §2.3), audit croisé alors.

  - **② ENDPOINTING (V5) — DIAGNOSTIQUÉ (mesuré AVANT de toucher, leçon conv 29 : concevoir, pas bricoler).** Instrumentation posée dans `oreilles_live.py` (à **chaque silence** : score Smart Turn + décision `✓ FIN`/`… continue` ; à la fin : mécanisme déclencheur + **tous** les candidats). **Constat sur 5 vraies phrases** : **Smart Turn ne coupe JAMAIS à tort** — 0 faux positif ; il **tient les pauses** (mi-phrase : 0,01 · 0,04 → « continue ») et note haut les **vraies fins** (0,92–0,98). Le **seuil `TURN_THR=0.5` est bon → NE PAS y toucher.** **Le vrai acteur = le PLAFOND (3 s)**, qui entre en jeu quand Smart Turn est *incertain* : **(a) latence** — sur une fin *douce* ratée par Smart Turn (« …non ? » scoré **0,04**), le plafond attend **3 s** avant qu'elle réponde ; **(b) coupure** — sur une **pause-réflexion > 3 s** en plein milieu, le plafond tranche à la place de Smart Turn (qui, lui, dit « attends »). **Tension de CONCEPTION** (un plafond fixe ne peut être *à la fois* court-après-vraie-fin et long-à-travers-une-pause), **pas un bug**. **Le « elle me coupe » de Yohann = le plafond (a/b), PAS le seuil Smart Turn.** *Fix réel = endpointing **sémantique** (« l'énoncé est-il complet ? ») — mais ça touche la **frontière GRAVÉE V5** (« fin de tour **acoustique, jamais sémantique** » ; l'**alternative de prise** « LiveKit turn-detector texte » y est nommée) → **décision qui revient à Yohann**, et elle **converge avec la V4** (transcrire en continu = juger la complétude).* **Décision : on n'y touche pas** (mécanisme sain, bords du plafond rares) ; **instrumentation gardée = boîte noire** (si une vraie coupure arrive en usage réel → score fautif capturé). Renvoi `01` §6 (fin de tour = seuils + endpointing).

  - **PROCHAIN LEVIER = V4 (STT streaming, entrée) — reporté conv 32 (démarrage frais, conception d'abord).** Le `STT` (1–2,8 s, mesuré) est le dernier gros grain **sur chaque tour** (court comme long). faster-whisper n'étant **pas** nativement incrémental, la V4 *faite bien* = **streaming Whisper** (fenêtres qui se recouvrent pendant la parole + **commit du préfixe stable / « local agreement »**) → STT **hors chemin critique** ; elle **rouvre proprement** la porte au sémantique du plafond (②). *(Bouton rapide alternatif noté, PAS la V4 : `large-v3-turbo` → STT ~1 s, au prix de la précision choisie conv 25.)* **CF2 : rien du banc n'entre au produit.** Renvois `01` §4 (V4 STT · V5 fin de tour · V7 TTS streamé) · §6 (latence 1re phrase · seuils fin de tour) · `05` R4/A36 (session chaude).

- **[Preuve de banc — V4 STT STREAMING (entrée), conv 32 (2026-07-14) — validée par Yohann À L'USAGE RÉEL (micro porté [12], vrai Claude, conversation Breaking Bad 5 tours)]** *Décision centrale conv 32. Le STT (`large-v3`) transcrivait UNE fois, sur TOUT le tour, APRÈS la fin de tour → 1–2,8 s de blanc sur le chemin critique à CHAQUE tour (mesuré conv 31). Rendu INCRÉMENTAL (la V4 « faite bien » = streaming Whisper « local agreement »).* Bancs jetables `bancs/aec/` (CF2). *(Micro-technique tranchée par Claude — garde-fou Phase 3 pt 2 ; modèle/méthode non gravés, laissés à l'essai comme A5.)*

  - **① CONCEPTION + MESURE D'ABORD (banc `22_stt_stream.py`, avant tout code — leçon conv 29/31).** Options servies à Yohann (R7) : **A** local-agreement (re-transcrire une fenêtre bornée, committer le préfixe stable, LocalAgreement-2 / whisper_streaming) · **B** chunké sur pauses VAD (écarté : coupe le contexte → risque la justesse gravée conv 25) · **C** bouton `large-v3-turbo` (~1 s mais troque la justesse, reste un blanc — repli, PAS la V4) · **D** cloud Deepgram (écarté : « pas d'API ») · **E** moteur nativement streamé (écarté : jette la gravure justesse-FR conv 25 ; la prise `stt` le garde pour + tard). **Reco = A, gardée par une mesure.** Yohann : « A + mesure ».

  - **② PORTE DE DÉCISION passée (banc 22, sur de vrais tours de la voix de Yohann).** Plancher par appel `large-v3` int8_float16 = **~600 ms** (coût encodeur, plat jusqu'à 4 s ; `word_timestamps` +~50 ms). Prototype local-agreement en temps réel simulé : **(a)** tient le temps réel — charge **48–69 %** sur des tours de 9,6–43 s (fenêtre bornée par rognage) ; **(b)** justesse **WER 0,0 %** vs one-shot (les rares diffs = un pluriel, un « bah » que le streamé capte MIEUX) ; **(c)** résidu de fin **~0,6 s** (le plancher `large-v3` — ne descend PAS sous ~0,6 s ; sous 0,6 s = compromis turbo, décision Yohann) ; **(d)** zéro hallucination (VAD en amont + accord inter-fenêtres + filet). Réglages tombés de la mesure : **cadence 1,5 s · fenêtre bornée 5 s · beam 1 partiels / beam 5 one-shot & finalisation · entrée bornée VAD**.

  - **③ INTÉGRATION dans `oreilles_live.py` (`StreamingSTT` ; smoke de câblage `23_smoke_stream_integ.py`).** Point d'archi tranché : le STT streamé vit sur un **fil worker dédié** (un appel `large-v3` ~0,6 s bloquerait la boucle VAD/Smart Turn) ; **tous les appels du modèle sur ce seul fil** (pas de course ctranslate2). Protocole : `start_turn(préroll)` → `feed` **pendant la parole seulement** (`in_speech` → pas de silence de queue à halluciner) → `end_turn` + `get_final` (bloque ~le résidu). **Replis (R3)** : worker mort → one-shot ; worker figé → tour ignoré (jamais deux appels concurrents). **LEÇON (le smoke a attrapé une régression refusée à la livraison)** : re-transcrire un **fragment de queue ISOLÉ** le prive de contexte de tête → un mot dur se dégrade (« ronronnent » → « rouronnent », le mot exact écarté chez turbo conv 25) + duplications aux frontières. **Principe sain, général** : ne JAMAIS re-transcrire un fragment isolé — la finalisation = un dernier pas sur la **fenêtre de travail entière** (plein contexte), le committé (déjà juste, transcrit AU FIL avec contexte) préservé tel quel. Plusieurs passes, **chacune diagnostiquée par la mesure**, pas des rustines (pas du bricolage conv 29).

  - **④ PREUVE AU MICRO (Yohann, vrai Claude, 5 tours Breaking Bad).** **STT = 0,7–1,5 s** (moyenne ~1,0 s) là où c'était 1–2,8 s, et **constant** (`ctx` 0k→2k, aucune dérive) ; transcripts justes, zéro hallucination ; `trous=[0.0]` partout ; Smart Turn 0,95–0,99, **jamais le plafond, jamais coupé** (endpointing sain reconfirmé en réel). **Objectif V4 atteint.** ÉCART doc↔banc : la V4 gravée émet `evt.stt.partial`/`final` dans le SIDECAR (prise `stt`) ; le banc réalise le streaming dans le coordinateur — le vrai sidecar re-partitionnera, audit croisé alors.

  - **⑤ FINDING HONNÊTE : le STT n'est plus le mur.** Après V4, la latence *ressentie* (fin de phrase → 1er son) = **~3,8–4,9 s sur les tours 2+**, dominée par le **TTFT du cerveau (2,4–4,0 s, côté Anthropic)**, PAS le STT (~1 s). Preuve dans les données : au tour 1 la phrase de recherche masque l'attente (présence 1,48 s) ; sans elle (tours 2+), 3,8–4,9 s. **Prochain levier de latence ressentie = l'accusé (O2, doc 99)** — une présence honnête instantanée (garde OF1 : vraie par construction, aucune assertion d'acte) qui masque le TTFT à CHAQUE tour (généralise ce que la phrase de recherche fait au tour 1). Renvois `99` O2/OF1 · `01` §6 (latence 1re phrase) · `05` R4/A36 (session chaude). **CF2 : rien du banc n'entre au produit.**

- **[Preuve de banc — VOIX rendue NATURELLE + latences masquées, conv 32 suite (2026-07-14) — validée par Yohann À L'USAGE RÉEL (micro porté, vrai Claude, plusieurs conversations Maupassant / H.G. Wells)]** *Après V4, longue session d'optimisation de la voix XTTS (timbre A20 cloné) et des latences ressenties. Bancs jetables `bancs/aec/` (CF2 — l'implémentation vit AU BANC + mémoire ; le vrai sidecar ré-implémentera, audit croisé alors). Mesuré AVANT de coder (leçon conv 29/31) ; micro-technique tranchée par Claude (garde-fou Phase 3 pt 2).*

  - **① LATENCE RÉVEIL réglée à ~0,65 s (vs 3,9 s), décomposée par la mesure.** La voix est instantanée (salut en cache, TTFB 0,01 s) → le délai était 100 % en AMONT (endpointing). **B** = plafond COURT au réveil (`WAKE_PLAFOND=0,8 s`, `active=False` seulement) au lieu du plafond 3 s. **A** = **lecture rapide one-shot** au VAD-end (`_transcribe(beam_size=1)`, gardée par `parle < STT_HOP` = worker streamé prouvé au repos → un seul appelant ctranslate2) → salut dès que l'ouvreur matche, sinon repli B. *(L'A « sur transcript committé streamé » NE TIRAIT JAMAIS — le committé retarde trop ; remplacé par la lecture rapide, mesuré.)* **ZÉRO régression sur les vrais tours (`active=False`, prouvé au log).**

  - **② PHRASES FIXES FIABLES (salutations + clôture) — la PORTE DE QUALITÉ.** Le cache était un tirage stochastique GELÉ → XTTS lâchait parfois le prénom (« Bonjour » + trou + blip → le trim enlevait « Yohann »). Fix = re-tirer (temp basse) jusqu'à un clip PROPRE (`_max_internal_gap ≤ 0,15` sur le BRUT + durée plausible). **Clôture** : « Yohann » irrécupérable en clôture (test de graphies = démoniaque ~11/12, XTTS ne sait pas dire le prénom en fin) → **« Avec grand plaisir »** (sans prénom), CLIP choisi à l'oreille + **FIGÉ sur disque** (`CLOSE_CLIP`). Leçon : une phrase fixe qui doit être PARFAITE (chaleur) se SÉLECTIONNE à l'oreille + se FIGE (la porte garantit « pas d'artefact », pas « beau »).

  - **③ QUALITÉ DE LA VOIX EN RÉPONSE — le COUPLE verbosité × température.** Session longue mesurée : réponses de **11-14 phrases / ~1400 car** → beaucoup de génération XTTS → **charabia** (« mots plus en français ») + démoniaque, ET attentes longues. `VOICE_SYSPROMPT` resserré (**cap 2-4 phrases, droit à l'essentiel**) → charabia MORT. Ça a démasqué une voix **robotique** = la temp basse (`0,45`, mise pour tuer le démoniaque ; mesurée : sur-génère la durée, 35 s vs 20 s pour la même tirade). **INSIGHT : verbosité courte ⇄ marge pour REMONTER la temp** → `STREAM_TEMP=0,55` (choisi à l'oreille banc `33`, stabilité vérifiée) = chaleur revenue SANS charabia. **Vitesse** : `STREAM_SPEED=1,08` (banc `28`, contre « ça traîne »).

  - **④ MASQUEUR « je réfléchis » (l'accusé CONDITIONNEL, cf. O2/doc 99).** « Donne-moi une petite minute » jouée UNE fois SI le cerveau tarde > **4 s** sans 1re phrase (sinon rien) — déclenché depuis la boucle de poll de `ask_stream` (fil unique), NON compté comme réponse, clip caché. **Prouvé : déclenché sur les tours à 10-15 s de TTFT, JAMAIS sur ~3 s** (« pas trop facilement », exigence Yohann). *(Écart doc 99 : O2 = accusé À CHAQUE tour ; ici masqueur CONDITIONNEL sur seuil — Yohann : l'accusé systématique « gâche tout ».)*

  - **⑤ ENDPOINTING (troncature sur pause de réflexion) — TRANCHÉ PAR LA MESURE.** Instrumentation `[VETO?]` : **Smart Turn gère DÉJÀ les pauses** (réflexion 0,01-0,02 → retenues ; vraies fins 0,77-0,99 → fin) ; la coupure « d'Arthur » = cas RARE d'une pause qui *sonne finie*. **Piste « veto sémantique sur le transcript » ÉCARTÉE par la mesure** : le committé retarde ~13 mots → un veto retiendrait TOUS les tours (latence partout). **Décision Yohann : E (Smart Turn suffit) + barge-in (V8) en filet plus tard ; PAS de veto.** La frontière V5 « acoustique, jamais sémantique » tient.

  - **⑥ FINDING HONNÊTE + PROCHAIN = « la dernière ligne droite » (décidé Yohann, clôture conv 32).** Observation-clé : **la voix sonne mieux en SOLO (bancs, hors-cerveau) qu'en RÉEL** ; données : DANS une réponse `trous=0` (fluide) mais BLANC au DÉBUT (TTFT cerveau + `trou` jusqu'à 5,4 s) + file `qplay` jusqu'à 51 ; + défauts de prosodie XTTS neufs (accélération au début, montée soudaine dans les aigus). **Conv 33 = optimiser prosodie + fluidité + naturel, ZÉRO régression** (investiguer le gap solo-vs-réel : rythme du cerveau streamé + charge concurrente du vrai pipeline, absente en solo) — AVANT de reprendre le plan. Renvois `99` O2 (accusé/masqueur) · `01` §4 (V5 fin de tour · V7 TTS streamé) · §6 (latence 1re phrase). **CF2 : rien du banc n'entre au produit.**

- **[Preuve de banc — CARREFOUR VOIX : XTTS écarté, décision « entraîner une voix A20 sur Piper », conv 33 (2026-07-14) — exploration validée à l'oreille de Yohann]** *« La dernière ligne droite » a viré au carrefour : le gap solo-vs-réel + le « démoniaque » ont mené à une exploration EXHAUSTIVE des moteurs de voix. Bancs jetables `bancs/aec/` (CF2). Mesuré AVANT de conclure ; décision structurante prise à l'oreille.*

  - **① EXPÉRIENCE MODÈLES (quasi-scientifique — même script « Les Misérables » + 2 relances, 6 configs, capture chiffrée + moniteur GPU/CPU).** Haiku (témoin) · Sonnet 4.6 low/medium/high/max · Sonnet 5 medium. **Trois verdicts** : (a) **la latence ne dépend NI du modèle NI de l'effort** (~2,5 s TTFT partout ; Haiku ≈ Sonnet) → **le plancher est l'API** ; aucun modèle ne rend plus vif (confirmé pour de bon). (b) **SURPRISE (ma prédiction inverse était fausse) : plus d'effort = réponses PLUS COURTES** (low 63 s → high/max 29-32 s de parole) SANS coûter de latence — le haut effort respecte mieux la consigne de brièveté. (c) **le « démoniaque » de Haiku = verbosité × VRAM tendue** : 14,7 ph / 85 s → une phrase à 44 s de gén (RTF 1,9) → **14 s de sous-alimentation** ; **VRAM 5,8/6 Go (95 %) = le goulot** (pas la chauffe 58°C, ni le CPU) ; tous les Sonnet : 0 sous-alim, 0 trou. **Gagnant mécanique = Sonnet 4.6 max / Sonnet 5 medium**, convergent avec l'oreille — MAIS le vrai choix de modèle est **DIFFÉRÉ AU PERSONA** (le cerveau = paramètre de banc ; observation de Yohann que mes métriques ne voient pas : tous ont pris Javert sauf 4.6 high / S5 medium → Valjean = signal de CONTENU, l'axe du persona). Instrumentation neuve : capture JSONL taguée par config · `mon_perf.py` (GPU/VRAM/CPU) · verbosité reformulée **« CANAL pas contenu »** (`VOICE_SYSPROMPT` : conversation orale, PAS de cap-nombre qui muselle — décision Yohann « ne pas brider Sophia ») · `--effort` optionnel + `BRAIN_MODEL/EFFORT` env.

  - **② CARTE DES MOTEURS DE VOIX (fluidité vs timbre A20).** **XTTS** = clone A20 (sa voix) MAIS le **« démoniaque » est INHÉRENT au moteur** (artefacts dans les silences, **VRAM-INDÉPENDANT** — Yohann m'a repris : PAS la saturation ; d'où le trim anti-artefact) + pas fluide sur les longues. **Piper/« Jessica »** = **fluide, CPU (GPU libre : VRAM 2,3 Go vs 5,8, GPU moy 3 %), propre** MAIS timbre **générique** (Yohann a fait le tour : seule upmc/« jessica » correcte en FR ; le « site plus beau » = même voix, diff = chaîne de lecture, confirmé WAV vs WAV — `diag_jessica.py`). **PocketTTS (Kyutai)** = clonage CPU **mais ANGLAIS SEULEMENT** (français = presets ; « fantine » écartée à l'oreille ; RTF ~1,0 sur son CPU, pas les « 6× » d'un MacBook M4). **Une GPU 12 Go ne réglerait PAS le démoniaque** (inhérent XTTS) ni les blancs du cerveau (API) — écarté honnêtement.

  - **③ DÉCISION STRUCTURANTE (Yohann).** **Aucun moteur CPU ne clone A20 EN FRANÇAIS de façon fluide aujourd'hui.** → La seule voie vers « timbre A20 + fluide + CPU + gratuit » = **ENTRAÎNER une voix Piper sur A20** (recette KNOWN — Chatterbox-FR clone → ~1300 phrases → fine-tune Piper ; on A DÉJÀ Chatterbox-FR conv 26). **Performance = Piper PAR CONSTRUCTION** (RTF 0,1, CPU, propre) ; seule variable = le timbre. **Fait dans une conversation SÉPARÉE, HORS-PLAN** (banc CF2) — prompt de passation écrit (`bancs/aec/PROMPT-voix-A20-piper.md`) ; dataset le soir, fine-tune de nuit (2060, UNE fois), inférence CPU. XTTS / A20 premium / ElevenLabs restent des reports (A9).

  - **④ BARGE-IN : écart de conception ÉVITÉ (Garde-fou Phase 3 exercé).** J'allais bâtir un détecteur de barge-in par **seuil d'énergie** (contournement de l'AEC faible du micro PORTÉ) — **Yohann m'a arrêté (« vérifie que ça ne contredit pas le plan »)**, et la vérif à la source (technique §4.4 / plan V8) a confirmé la **divergence** : le design V8 = **barge-in modulé par le LOCUTEUR (speaker-ID V6)** + invariant « ne se coupe jamais elle-même » **garanti par l'AEC (F2)**, pas par une heuristique ; mon énergie-gate rouvrait conv 29 (dormant → speaker-ID) et ne tenait pas l'invariant. **Écarté. V8 reste tel que gravé (dépend de V6).** Seul changement gardé du jour = la verbosité « canal ». Renvois `01` §4 (V8 barge-in · V5/V7) · technique §4.4. **CF2 : rien du banc n'entre au produit.**

- **[Preuve de banc — V6 (SPEAKER-ID) + V8 (BARGE-IN modulé par le locuteur) PROUVÉS OFFLINE ET LIVE ; endpointing « grâce de fin » + voix A20 validés live, conv 33-34 (2026-07-15) — validé à l'oreille de Yohann]** *Même milestone « le pipeline vocal VIT ». Bancs jetables `bancs/aec/spk/` + `oreilles_live.py`/`bouche_piper.py` (CF2). Mesuré/prouvé AVANT de conclure ; la voix A20 (fine-tune Piper de conv 33, hors-plan) est EN PLACE.*

  - **① V6 speaker-ID (précondition gravée de V8) — ECAPA-TDNN CPU, isolation stricte prouvée.** `spkrec-ecapa-voxceleb` (SpeechBrain), **CPU strict** (`CUDA_VISIBLE_DEVICES=""`, mono-thread, priorité basse), venv dédié `.venv-speaker`, service socket SÉPARÉ (`spk/v6_service.py`, port 8767) — pour NE PAS toucher le venv d'oreilles ni le fine-tune parallèle (isolation prouvée par mesure GPU/RAM/CPU AVANT toute charge). Ancre = **centroïde de la voix de Yohann** (ses clips wake propres) ; score = cosinus. **Séparation Yohann / voix-de-Sophia = EER 0 % à 3 s d'intégration** (offline), **RE-CONFIRMÉE sur la VRAIE voix A20** (`verify_a20.py`, `fr_FR-a20-e400`) : A20 moy **0,165**, EER 0 %.
  - **② V8 barge-in modulé par le locuteur — PROUVÉ LIVE (branché conv 34).** Dans `_await` : gate d'énergie → accumulation → speaker-ID → `mouth.stop()` (bouche pas touchée, `{"type":"stop"}` existait ; `V6Client`, constantes `BARGE_THR/FLOOR/MIN_SPEECH/WIN`). **Preuve live (conversation Voltaire, 8 tours, micro porté, vrai Claude) : seuil `SOPHIA_BARGE_THR=0,22` → 6/6 barge-ins déclenchés (scores 0,23-0,39), 0 fausse coupe.** Distribution mesurée : voix-qui-coupe 0,23-0,39 (min 0,23) ; résidu de Sophia -0,10 à +0,10, **pic 0,21** → marge SERRÉE ~0,02. Le seuil offline 0,30 était trop haut (live = voix mélangée au résidu → 0,22 = le bon point live ; diagnostic par MESURE, pas au jugé). Log `[barge?]` gardé. **Peaufinage (anti-rebond, fenêtre plus courte) analysé PUIS écarté avec Yohann : l'acquis 6/6 / 0 faux tient, la fenêtre courte élargirait la variance (risque de régression des DEUX côtés) → on FIGE 0,22.**
  - **③ Endpointing « grâce de fin » (`ENDGRACE=0,7 s`) — validé live (conv 33).** Smart Turn CONFIANT (phrase complète + court silence) coupait à l'instant → Yohann, qui enchaînait, était coupé. Fix : laisser 0,7 s pour reprendre (VAD-start → grâce annulée, tour continue ; sinon fin). **SCOPÉ conversation ACTIVE** (le réveil reste vif ; `SOPHIA_ENDGRACE`). Preuve live (monologues 42/32/24 s) : **~10 coupures évitées** (ex. tour 42 s : confiant à 17 s (0,99) et 37 s (0,96) → il a continué 2×, fin à 41,8 s), coût +0,7 s invisible, **0 régression réveil**. Masqueur `FILLER_AFTER=3,0 s` (« Donne-moi une petite minute » si TTFT > 3 s ; jamais sur une question simple).
  - **④ Voix A20 (`fr_FR-a20-e400`, speaker 0 « sophia ») — validée LIVE bout-en-bout.** Piper entraîné sur A20 dans le pipeline complet (voix + cerveau, micro porté) : **fluide** (`trous ~0`, RTF ~0,06), **zéro démoniaque**, GPU quasi libre, tient sur les longues réponses (13-16 phrases / 1300+ car.). Latence ~3 s = cerveau/API, pas la voix. **Verdict Yohann : « bluffant » — le combat de la voix (10 conversations) est GAGNÉ.**
  - **ÉCART PRODUIT (tracé)** : V6/V8 + grâce + voix réalisés au **coordinateur de banc** (`oreilles_live.py`/`bouche_piper.py`, CF2) ; le **vrai sidecar ré-implémente** V5 (fin de tour + grâce) / V6 (speaker-ID) / V7 (TTS A20) / V8 (barge-in modulé locuteur) — **audit croisé À CE MOMENT-LÀ**. Renvois `01` §4 (V5/V6/V7/V8) · technique §4.4. **CF2 : rien du banc n'entre au produit.**

- **[V0 BÂTI DANS LE PRODUIT — conv 39 (2026-07-17) — validé par Yohann]** Première tâche du pipeline vocal **passée du banc au CODE PRODUIT** (`sidecar/audio/`, `sidecar/plugs/`, câblage `sidecar/server.py`). Fin de la phase « prouvé au banc » pour V0 ; début du vrai sidecar (l'écart produit ci-dessus commence à se combler). *(Micro-technique tranchée par Claude — garde-fou Phase 3 pt 2.)*
  - **① VENV UNIQUE prouvé au RUNTIME (mesure design-first — le plan/socle « même venv » RESPECTÉ).** Les deux murs du banc (Piper py3.12 · torch CPU-vs-CUDA du speaker) **sont tombés** : `piper-tts 1.4.2` a un wheel `cp39-abi3` (py3.13) ; `speechbrain` accepte le torch **CUDA** partagé (le speaker force CPU au runtime, `CUDA_VISIBLE_DEVICES=""`). Prouvé : voix A20 (synthèse) + ECAPA (encode) + faster-whisper + silero + smart-turn + pyaudiowpatch chargent ENSEMBLE sur `.venv-sidecar` py3.13, zéro conflit. **Pas d'écart** (le multi-process du banc était un artefact d'époque). **Assets sécurisés** hors du `bancs/` jetable → `resources/models/{voice,voice-anchor}/` (voix A20 e400/e390 + ancre vocale de Yohann ; irremplaçables ; gitignorés).
  - **② Le chemin audio produit** : `ring.py` (RingBuffer **SPMC** : 1 écrivain, N lecteurs à curseurs indépendants, overrun=drop-oldest signalé, rembobinage pré-wake, horodatage M2 à **marques déterministes en ms**) · `capture.py` (callback WASAPI **RT minimal** → file → thread de conversion : mono + **rééchantillonnage STREAMING À ÉTAT via `soxr`** — pas de transitoire de bord ni de FIR redessiné par bloc) · `plugs/base.py` (patron de prise 01-F : contrat + `evt.*` normalisés, moteur derrière). **Câblage `server.py`** : opt-in `SIDECAR_AUDIO=1`, lazy, non-fatal (pas de micro → vit sans oreilles) ; `graceful_release` libère le micro (T6, non bloquant via executor) ; `/debug` sonde le ring + les pertes.
  - **③ ÉCARTS / décisions tracés** : moteur de rééchantillonnage = **`soxr` streaming** (au lieu de `scipy.resample_poly` par-bloc — la conversion PAR BLOC dans un flux crée des transitoires ~17 % d'amplitude, mesuré ; `to_16k_mono` scipy reste pour le one-shot) · **`test:sidecar` = script SÉPARÉ** de `npm test` (qui reste **portable Node** — les tests dépendant de `.venv-sidecar` sont hors de `npm test`, comme `npm run e2e`) · `RING_SECONDS=30` (valeur `§6`-ouverte « fenêtre pré-wake ») · `captured_at` en **ms** (aligné sur le `ts` d'enveloppe socle) · `evt.plug.overrun` (nommage dans la famille `evt.*`).
  - **④ FRONTIÈRES V1 (vraies, pas facilités)** : lock tenu pendant la copie de lecture (négligeable à hop 160 ; à revoir quand le STT lira 1 s d'un coup) · ancrage **ADC précis** de `captured_at` (`time_info.input_buffer_adc_time`, pour l'alignement AEC) · flush `soxr` (`last=True`) + reset overlap sur `src_rate`-change en vol (device-change V1).
  - **⑤ AUDIT — croisé 2 agents d'office + RE-CROISÉ des corrections (méthode conv 34/35/37).** Croisé (fidélité + robustesse) : **0 MAJEUR** (cœur SPMC validé sain), 13 findings corrigés (soxr R#2 · fuite PyAudio R#1 · `captured_at` déterministe/ms R#5 · garde capacité R#7 · graceful non bloquant R#4 · status overflow R#3 · idempotence · `evt.plug.overrun` · stop borné). **RE-CROISÉ** (ciblé sur les corrections) : **0 MAJEUR**, **5 MINEUR TOUS dans mes corrections** (C#1 garde double-start du *vrai* écrivain · C#2 reset d'état de conversion en `stop()` · C#3 compteur d'erreurs de conversion · S#1 idempotence concurrente via `dict.pop` atomique — ma 1ʳᵉ correction gardait une race, re-corrigée · S#2 réfs figées dans `/debug`) — corrigés + testés. **La leçon re-re-vécue : le trou est dans les corrections, jamais le cœur ; le cœur validé sain par 4 agents.**
  - **⑥ PREUVES** : `pytest sidecar` **27** (concurrence SPMC · rembobinage · overrun · resample streaming = one-shot · fuite · double-start · captured_at déterministe · exception moteur · idempotence concurrente) · **E2E-V0** cœur réel (`SIDECAR_AUDIO=test`, source synthétique 48 k → soxr → ring 16 k, 0 perte, `cmd.shutdown` libère) · contrat socle **INTACT** (`npm test` 11 · `e2e` 31 · `smoke` 12) · capture prouvée **live au vrai micro** (ring alimenté, 0 perte/erreur). **Renvois** `01` §3 (V0) · §4 (U-V0) · §5/§6.

- **[V1 BÂTI DANS LE PRODUIT — l'AEC, le premier 🔴 — conv 40 (2026-07-17) — validé par Yohann]** L'annulation d'écho **au CODE PRODUIT**, greffée sur le chemin V0 : Sophia n'entend que les voix de la pièce, jamais ce que le PC joue (invariant F2 — elle ne se coupe jamais elle-même, un média ne déclenche aucun VAD/barge-in fantôme). **Prouvé LIVE (I-1) au vrai setup** (micro USB porté + Focusrite 44,1k / TV 48k) : écho annulé (**25 dB** média fort · **10,3 dB** ciblé aligné, délai Focusrite **166 ms** absorbé par la queue 200 ms) · double-parole **voix préservée** (bande-voix 49 %→**87 %**) · rates-mixtes 44,1k/48k alignés · device-change **récupéré** (Focusrite éteinte → `degraded` → ~2 s → `reopens=1` sur la TV, micro jamais décroché). *(Micro-technique tranchée par Claude — garde-fou Phase 3 pt 2.)*
  - **① Le code V1** : `aec.py` (`EchoCanceller` = SpeexDSP via **`pyaec==1.0.1`**, `Aec(160,3200,16000, enable_preprocess=False)` derrière une **interface swappable** — l'AEC neuronal ONNX reste un swap config+respawn ; garde de longueur de trame) · `capture.py` (`WasapiDuplexSource` = micro + loopback système sur **UNE** PyAudio ; `AecCapture` = deux `_MonoStream` soxr → appariement **160-trames** → AEC → ring **POST-AEC** ; ERLE observable ; garde anti-backlog ref bornée à la queue de filtre) · câblage `server.py` (`SIDECAR_AUDIO=1` prod · `=test-aec` E2E-V1 · `graceful_release` libère les deux sources).
  - **② DÉCISIONS / ÉCARTS tracés** : **preprocess OFF FERME** (le denoise/AGC SpeexDSP distord l'entrée du wake, `oreilles_live` l.65 ; conv 29 : le suppresseur de résidu n'aidait pas) · **conversion AVANT l'AEC** (AEC à 16 kHz : SpeexDSP sur-supprime le proche à 48 k, conv 23 — écart de placement déjà tracé, ici IMPLÉMENTÉ) · **soxr streaming pour LES DEUX sources** (near ET ref ; jamais `resample_poly` par-bloc) · **prise `aec` formelle §2.3 ABSENTE du doc technique acquis** : swappabilité bâtie en CODE, **doc non touché** (la ligne formelle = décision Yohann) · **exclusif = RÉSIDU ASSUMÉ** (le loopback est **ÉVÉNEMENTIEL** — 0 trame à l'idle — donc un flux exclusif est indistinguable de « rien ne joue » : aucune détection propre sans COM ; `01` §6, signalé honnêtement, jamais maquillé).
  - **③ DEVICE-CHANGE (V1.c) — mesuré/corrigé LIVE** : `IMMNotificationClient` (le mécanisme nommé au plan) **mesuré CAPRICIEUX** (COM apartment : MTA+sleep ET STA+pompe n'ont pas délivré les callbacks) → **différé §7 (upgrade)**. À la place : sur la **MORT** du loopback (`is_active()` False, **signal PROUVÉ fiable**), **re-init COMPLÈTE** (terminate PyAudio + fraîche → ré-ouvre micro+loopback sur la sortie courante). **Pourquoi tout re-init** : l'énumération PortAudio reste **FIGÉE tant qu'UNE PyAudio vit** (mesuré : une PyAudio fraîche nested est AVEUGLE ; un poll par nom ne voit rien) → seul un terminate complet re-énumère. **FRONTIÈRE / BACKLOG §7 (M-D)** : le switch de sortie **« à chaud »** (nouvelle sortie par défaut alors que l'ancienne reste ALLUMÉE — ex. brancher un casque) laisse une référence PÉRIMÉE non détectée → **exige le COM** → **backlog §7** (le cas courant « éteindre la Focusrite » EST couvert ; Sophia n'est pas prévue au casque — décision Yohann).
  - **④ AUDIT — solo + croisé 2 agents + RE-CROISÉ (méthode conv 34-39). La leçon RE-VÉCUE : le trou était dans MES corrections, jamais le cœur AEC/SPMC (validé sain par 4 agents sur deux tours).** **Solo** : SOLO-1 (le micro VITAL re-glitché en boucle quand le loopback est durablement absent → backoff exponentiel borné ~5 min). **Croisé (fidélité + robustesse)** : **3 MAJEUR** (M-A l'écrivain unique du ring meurt EN SILENCE sur une exception de conversion soxr — régression de l'invariant C#3 de V0, surdité + `/health` vert = pas de respawn ; M-B la ref se désynchronise du micro sans se re-caler → l'écho fuit ; M-C use-after-free — `is_active()` sans lock sur un stream que `close()` a libéré, confirmé à la source pyaudiowpatch) **+ 2 MINEUR** (#5 pas de garde de longueur de trame → OOB C pyaec ; #6 fuite de stream loopback si `start_stream()` lève) **+ M-D** (ci-dessus → backlog) — **TOUS reproduits à la source** (M-A/M-B empiriquement, M-C/#5 au niveau C). Corrigés à la racine : M-A garde extérieure (l'écrivain ne meurt jamais, erreur comptée) · M-B backlog ref borné à ~la queue de filtre (re-calage near-maintenant ↔ ref-récente, **après** l'appariement) · M-C `loopback_active()` lit un booléen en cache sous lock (sonde C isolée) · #5 garde de longueur · #6 fermeture. **RE-CROISÉ (ciblé sur les corrections)** : **1 MAJEUR — dans MA correction SOLO-1** (M-A2 : le backoff confondait « micro fermé » [à récupérer VITE] et « loopback absent, micro VIVANT » [à espacer] → prolongeait la surdité du micro jusqu'à ~5 min au power-cycle de la Focusrite) → **séparé en deux branches** (micro mort = retenter agressivement) ; **les 5 autres corrections validées SANS RÉSERVE par les 2 agents**. **1 MINEUR ACCEPTÉ** : `degraded` sur `/debug` retarde de ≤2 s une mort de périphérique (le cache M-C) — **prix assumé de la sûreté mémoire** ; la **récupération** utilise la sonde FRAÎCHE, pas le cache. **Convergence : croisé 3 MAJEUR (code d'origine) → re-croisé 1 MAJEUR (ma correction) → fermé.**
  - **⑤ PREUVES** : `pytest sidecar` **49** (EchoCanceller annule/double-parole/passthrough/garde de longueur · appariement 160 + reste conservé · ref starvation · **écrivain increvable M-A** · **backlog borné M-B** · **use-after-free cache M-C** · device-change re-init + backoff SOLO-1/**M-A2** · degraded honnête) · **E2E-V1** cœur réel (`SIDECAR_AUDIO=test-aec` : near=écho+voix / ref=far-end → **ERLE > 6** dans le vrai sidecar, voix préservée, `cmd.shutdown` libère) · **I-1 LIVE** (ci-dessus) · contrat socle+V0 **INTACT** (`npm test` 11 · `e2e` 31 · `e2e:v0` 9 · `smoke` 12). **Renvois** `01` §3 (V1) · §4 (U-V1) · §5/§6 · technique `01` §2.1/§3.2 (ring post-AEC) · §2.3 (prise `aec`, trou signalé).

- **[V2 BÂTI DANS LE PRODUIT — le VAD (détection d'activité vocale) — conv 41 (2026-07-17) — validé par Yohann]** La 1ʳᵉ brique d'**ANALYSE** du pipeline passe au CODE PRODUIT : un **CONSOMMATEUR du ring POST-AEC** (curseur indépendant) qui détecte la parole et émet `evt.vad.start`/`evt.vad.stop` vers l'orchestrateur — la brique d'entrée de **V3** (réveil rétroactif : rembobiner à la marque VAD précédente) et **V5** (fin de tour). C'est le **PREMIER émetteur d'`evt.*`** du projet (V0/V1 ne faisaient que remplir le ring). *(Micro-technique tranchée par Claude — garde-fou Phase 3 pt 2.)*
  - **① Le code V2** : `sidecar/consumers/vad.py` (`SileroVadEngine` = Silero **torch JIT** + hystérésis derrière une **interface INJECTABLE** `VadEngine` ; `VadPlug(ConsumerPlug)` = fenêtrage **512**, normalisation int16→float32÷32768, marques + discontinuité **auto-détectée** [écart de position du curseur → reset moteur + clôture du segment]) · `sidecar/bus.py` (le pont NEUF **thread-de-fond → boucle asyncio → WS** : `publish_threadsafe`→`call_soon_threadsafe`→file **bornée par abonné**, drop-oldest + signal agrégé) · câblage `server.py` (bus au startup ; `ws_handler` s'abonne+draine avec `send_lock` ; `_start_audio` monte le VAD en prod `=1` + mode `=test-vad` ; `/debug` expose `audio.vad` + `bus`).
  - **② DÉCISIONS / ÉCARTS tracés** : **backend = torch JIT** (`load_silero_vad()` défaut — la voie **recoupée au CODE du banc** conv 25 `07_turn.py`/`oreilles_live.py`, PAS onnx ; l'ONNX reste un swap) · **hystérésis RÉIMPLÉMENTÉE, équivalente à VADIterator** (N1 — le plan T-A disait « + VADIterator » ; **remplacée** pour obtenir prob **ET** index exact en **UN SEUL appel modèle** [un 2ᵉ appel coûterait **×2,14**, mesuré], reproduisant `utils_vad.py:528-549` à l'identique ; **prouvée équivalente par test**, index compris ET **y compris le reset**) · **marque `pos` = index padé** (speech_pad 30 ms AVANT la fenêtre — la marque exacte pour V3 « premier mot jamais amputé » ; `pos = _fed_base + idx`) · `evt.vad.stop` porte `prob` (N3, extension bénigne d'un protocole extensible) · **`test-vad` passe par le VRAI `AecCapture` (POST-AEC, PARITÉ PROD)** — la 1ʳᵉ isolation sur V0 était une **facilité** (attrapée par Yohann + prouvée : parole 0,99 franchit l'AEC **5/5**, vs 0,6 → 2/5) · **seuil** `SOPHIA_VAD_THRESHOLD` au spawn (§6 calibration ; défaut 0,5 gravé banc) · **bus** = infra de transport neuve, le **protocole `evt.*` gravé est inchangé** (`IpcClient.on` prêt).
  - **③ DESIGN-FIRST (mesuré avant de coder)** : cohabitation **torch CPU-only** + `silero-vad` avec V0/V1 prouvée (import ensemble 2,2 s ; cuda inutile — le VAD tourne CPU ; CUDA reste un choix mesuré pour V6) · **~0,8 ms/fenêtre de 512** (~2,5 % d'un cœur → négligeable always-on) · **silence/bruit ne déclenchent PAS Silero** (proba ~0,01/0,06 → un média/bruit ne fait pas de faux `vad.start`, F2-adjacent, testable déterministe avec le VRAI moteur).
  - **④ AUDIT — solo + croisé 2 agents + RE-CROISÉ. La leçon RE-VÉCUE : le trou était dans MES corrections, jamais le cœur (validé sain par 4 agents).** **Croisé** (fidélité + robustesse) : **0 MAJEUR**, FIDÈLE + SOLIDE ; 5 MINEUR (marque tardive · `dropped` bus non exposé · erreurs moteur non comptées · `ws_count` préexistant socle · couverture concurrente) + NITs → **7 fixes** — dont **NIT-5 = MA facilité, prouvée par mesure** (test-vad isolé sur V0 → passé POST-AEC). **RE-CROISÉ (ciblé sur les corrections)** : **1 MINEUR dans MA correction `reset()`** — l'**état RNN Silero non réinitialisé** (≠ `VADIterator.reset_states` → `self.model.reset_states`, `utils_vad.py:501`) → décisions post-overrun biaisées, **reproduit Δ 0,28-0,47**, **caché exactement là où le test d'équivalence ne regardait pas** → corrigé (reset RNN, parité pleine) + **test qui MORD** (temp-revert → échec) ; les 5 autres corrections **SANS RÉSERVE**. **Convergence : trous d'origine → 7 fixes → 1 MINEUR (ma correction) → fermé.**
  - **⑤ PREUVES** : `pytest sidecar` **69** (dont **20 V2** : logique moteur scripté · smoke Silero réel · **équivalence VADIterator** [décisions+index] · **reset efface l'état RNN** · hystérésis pure zone-grise/pad · alignement/discontinuité · bus drop-oldest agrégé · `_stop_audio` concurrent avec vad) · **E2E-V2** cœur réel (`SIDECAR_AUDIO=test-vad` : POST-AEC → ring → prise → **VRAI Silero** → bus → WS ; `evt.vad.*` avec `pos`/`captured_at`/`prob`/`duration_ms` ; `aec_frames>0` ; cycle d'abonnement 1→0) · contrat socle+V0+V1 **INTACT** (`npm test` 11 · `e2e` 31 · `e2e:v0` 9 · `e2e:v1` 12 · `smoke` 12). **Renvois** `01` §3 (V2) · §4 (U-V2) · §5/§6 (seuil = calibration) · JOURNAL A7 (Silero).

- **[V3 BÂTI DANS LE PRODUIT — le RÉVEIL RÉTROACTIF — conv 42 (2026-07-17) — validé par Yohann]** Au signal d'éveil, Sophia **rembobine** un curseur du ring jusqu'à la MARQUE que V2 pose → la phrase entière est dans le tampon, **premier mot jamais amputé** (invariant F1). C'est le MÉCANISME du réveil ; l'éveil PAR PHRASE (le wake-model conv 24 écarté conv 27) sera branché dessus par le portier STT en V4. **Coupe V3/V4 tranchée (RELAY, validée Yohann) : V3 SEUL cette conv, éveil INJECTÉ par couture** (comme `evt.speaker` injecté pour V6/V8 avant leur source — patron du projet). *(Micro-technique tranchée par Claude — garde-fou Phase 3 pt 2.)*
  - **① Le code V3** : **`RingCursor.seek_to(pos)`** (+ `RingBuffer._clamp_seek`) — place un curseur à une position ABSOLUE (la marque), clampé à `[oldest, write_pos]`, **retourne la troncature** (marque hors fenêtre = garde d'honnêteté F1, jamais « premier mot intact » à tort ; `pos<0` borné à 0) · **`sidecar/consumers/wake.py`** (`WakeGate` = **porte réactive SANS thread** : `observe` suit la marque VAD via l'**emit wrappé** de `server.py` — **le `VadPlug` verrouillé n'est PAS touché** ; `on_wake(mark)` rembobine à la marque FOURNIE + émet `evt.wake {pos, captured_at, truncated}` + rend le curseur rétroactif ; **S12** « seule auto-transition sidecar = le tour de réveil » → 2ᵉ éveil = no-op ; `release()`) · câblage `server.py` (mode **`test-wake` DISTINCT** [e2e-v2 INTACT] · hook `/debug/wake?pos=` sous `TEST_HOOKS` injecte l'éveil, **JAMAIS en prod** · `/debug audio.wake` · `evt.wake` dans `EVT_TYPES` · pop atomique du wake dans `_stop_audio`). **Zéro changement orchestrateur** (`IpcClient` route `evt.wake` génériquement).
  - **② ÉCARTS doc↔plan tracés (F-2)** : le plan §3 V3 disait « émet `evt.wake` (**confiance**) » et listait `wake` comme une **prise** ; le code = **`evt.wake` porte des POSITIONS + la garde d'honnêteté, PAS de « confiance »** (wake-model écarté → plus de score de wake) · **`WakeGate` = porte réactive, PAS un `ConsumerPlug`** (pas de 2ᵉ thread/curseur — fidèle « ne pas doubler le CPU » conv 41) · **la détection « Sophia » vs « Sophie » est relocalisée en V4** (STT-phrase, écart fondateur déjà tracé conv 27). **Mode nominal = marque FOURNIE** (vise le bon segment même si un nouveau a démarré) ; mode générique `mark=None` = dernière marque suivie, réservé au `cmd.listen.start` rétroactif de V9.
  - **③ FRONTIÈRES V4/V9 (gravées dans le code — contrats, pas des bugs V3)** : **R-1** — l'écoute transitoire (`_armed`) n'a pas de filet en V3 ; un `release()` oublié par l'aval (STT V4 / orchestrateur V9) laisserait `_armed`=True → Sophia SOURDE → **contrat gravé dans `release()` : V9 DOIT porter la deadline de garde** (pas de timer creux maintenant, rien à piloter — crible facilité). **R-2** — `truncated` est un INSTANTANÉ au réveil ; si le STT tarde et le ring avance, la vraie perte apparaît en `overrun` **au read** (reproduit : `truncated=0` puis `overrun=400`) → **contrat gravé : V4 vérifie l'`overrun` au read EN PLUS de `truncated`**.
  - **④ AUDIT — solo à fond + croisé 2 agents (fidélité + robustesse). 0 MAJEUR × 2 — le cœur validé SAIN par 4 regards** (invariants tenus, `VadPlug` intact, F1 rembobinage fidèle + garde solides, S12 correct, tests qui mordent, cœur réel = VRAIE marque Silero anti-NIT-5, E2E = vrai chemin). **Corrections à la racine — TOUTES reproduites à la source** : **R-4** (`seek_to(-5)` gonflait `truncated` → `max(0, …)` + test) · **F-1** (borne du test cœur réel +8000 → resserrée +3500, mord si la marque dérive) · **R-6** (`/debug/wake` renvoyait un réveil antérieur sur no-op) · **F-3** (import inutilisé). **NITs bénins assumés** (captured_at best-effort · release au teardown · fenêtre transitoire /debug [fermer = imbriquer les locks = deadlock, S12 prime] · marque future via le hook). **Re-croisé des corrections : jugé inutile par Yohann** (corrections triviales, reproduites avant/après) — **décision de Yohann, pas mon jugement « c'est assez ».**
  - **⑤ POINT HONNÊTE (mesure)** : le vrai Silero pose la marque **~174 ms APRÈS** le début de la parole SYNTHÉTIQUE (attaque molle, voyelles seules) — **propriété de V2** (délai Silero + speech_pad 30 ms, audité conv 41), plus courte en parole réelle (attaques consonantiques ; le banc conv 27 le prouvait). **V3 rembobine FIDÈLEMENT à la marque** (octet-à-octet). « Premier mot jamais amputé » = invariant CONJOINT V2 (pose) + V3 (respecte). **AUCUNE latence ajoutée** (le rembobinage est instantané dans un tampon déjà capturé).
  - **⑥ PREUVES** : `pytest sidecar` **82** (dont **13 V3** : seek_to + clamp + garde négative · observe · rembobinage nominal + cas dur marque-fournie · S12 + release · mode générique + honnêteté · troncature · emit-fail · **cœur réel vrai Silero premier-mot-intact** · `_stop_audio`+wake concurrent) · **E2E-V3** **16** cœur réel (`SIDECAR_AUDIO=test-wake` : parole → AEC → ring → VAD → injection éveil → `WakeGate` → `evt.wake` → bus → WS) · contrat socle+V0+V1+V2 **INTACT** (`npm test` 11 · `e2e` 31 · `e2e:v0` 9 · `e2e:v1` 12 · `e2e:v2` 16 · `smoke` 12). **Renvois** `01` §3 (V3) · §4 (U-V3) · §5/§6.

- **[V4 BÂTI DANS LE PRODUIT — le STT streaming + le PORTIER d'éveil PAR PHRASE — conv 43-44 (2026-07-18) — validé par Yohann]** Sophia **entend son nom dans une phrase et se retourne** : le VRAI déclencheur sur V3 (l'éveil n'est plus injecté par un hook — le portier lit le transcript et distingue « Sophia » de « Sophie », wake-model conv 24 écarté conv 27). *(Micro-technique tranchée par Claude — garde-fou Phase 3 pt 2.)*
  - **① Le code V4** : `sidecar/consumers/stt.py` (NEUF) — `FasterWhisperEngine` (large-v3 int8_float16 **GPU**, fr forcé, temp 0, `condition_on_previous_text=False`, fix DLL `torch/lib`, **interface injectable** `SttEngine`) · `HypoBuffer` (LocalAgreement-2, porté du banc conv 32, WER 0) · **`SttPlug` = prise pilotée-VAD** (worker UNIQUE = seul appelant ct2 [**mono-fil**], curseur ring, `seek_to` la marque = rembobinage F1, **accumulation de GROUPE** [micro-pause = 1 seul transcript], réglages banc conv 32 : cadence 1,5s / fenêtre 5s / beam 1) · **portier** (`match_opening`/`match_closing`/`is_goodnight`/`is_hallucination`, purs, portés du banc conv 27) → `wake.on_wake(mark)` / `release`. Câblage `server.py` (`_observing_emit` VAD→bus+`wake.observe`+`stt.on_vad` · mode `test-stt` · `/debug audio.stt` · `_stop_audio` pop atomique) · `WavLoopSource` (`test_source.py`) · `wake.py` (+ `armed` lu par le portier) · `requirements.txt` **torch 2.13.0+cu126** (V2 §7 anticipé, V2 INTACT) + faster-whisper 1.2.1 + ct2 4.8.1.
  - **② Le RÉVEIL à la perf (banc conv 32) + le FIX à la VRAIE VOIX (conv 44)** : **lecture rapide** au vad-stop (Sophia dort + tour court → one-shot beam 1 → réveil VIF sans attendre le silence) + **plafond différencié** (`WAKE_PLAFOND_S`=0,8s au réveil / `GROUP_SILENCE_S`=3,0s en conversation). **Réveil conv 43 : 2663→834 ms.** **FIX conv 44** (cause trouvée en instrumentant À SA VOIX — [[perf-produit-egal-banc]]) : sa « Bonjour Sophia » naturelle fait **~0,9 s**, SOUS le seuil de lecture rapide qui valait `STT_MIN_WIN_S`=1,0 (celui du STREAMING) → « fast SKIP » → réveil lent + INCOHÉRENT → **seuil DÉDIÉ `WAKE_MIN_WIN_S`=0,4s** → **CONFIRMÉ à sa voix : 3 réveils cohérents 784/764/649 ms, tous par lecture rapide** (avant : #1 vif, #2/#3 lents). **Garde `_last_call_end` retirée** de la lecture rapide (le banc à 2 fils la gardait contre un ct2 concurrent ; ici mono-fil → sûr, prouvé). **Warmup STT** (→ entrée PERF conv 44). **FAUSSE PISTE reconnue + revertée** (« streaming armé-seulement » régressait son #1 → leçon : instrumenter À SA VOIX tôt, pas raisonner sur un WAV propre).
  - **③ Corrections d'audit à la RACINE (F-SOLO + F-1 + F-2)** : **F-SOLO-2** (solo) — les marqueurs de clôture à APOSTROPHE (« à tout à l'heure », « on s'arrête ») ne fermaient JAMAIS (`_norm` transforme l'apostrophe du transcript mais pas celle du marqueur brut comparé tel quel) → `OPEN_PHRASES`/`CLOSE_MARKERS` normalisés à la construction · **F-1** (croisé) — `_audio` (buffer de travail) croissait SANS BORNE sur un groupe long (parole très longue / VAD figé) → **compaction** (`_compact` + `HypoBuffer.shift`) qui jette l'audio **DÉJÀ transcrit**, **le texte committé INTACT** (l'invariant de Yohann : « elle a tout le contexte de ce que je dis » — transparence PROUVÉE octet-pour-octet par test moteur positionnel) + garde absolue (cas VAD figé, aucune parole committée perdue) · **F-2** (croisé) — la file de commandes VAD fuyait sans borne si le worker STT meurt (moteur KO, VAD vivant) → `queue.Queue(maxsize=256)` drop-oldest + compteur.
  - **④ AUDIT — solo à fond + croisé 2 agents + RE-CROISÉ 2 agents. La leçon RE-VÉCUE : le trou était dans MES corrections, jamais le cœur (validé SAIN par 4 regards — mono-fil, SPMC, R-1/R-2, distinction Sophia/Sophie sur le VRAI moteur, les 3 fixes conv 44, faux-fix reverté absent).** **Solo** : F-SOLO-2 (portier apostrophe, reproduit à la source) + F-SOLO-1 (commentaire faux). **Croisé (fidélité + robustesse)** : **0 MAJEUR**, **2 MINEUR = les 2 fuites F-1/F-2** (reproduites à la source : `_audio` 0,96 MB et grimpe · `_cmds`=10000) + NITs écartés (silence inter-segment fail-safe **dé-risqué au vrai moteur** · lecture rapide sans borne haute = efficacité, pas latence). **RE-CROISÉ (ciblé sur MES corrections)** : **0 MAJEUR, transparence de la compaction PROUVÉE octet-pour-octet par les 2 agents INDÉPENDAMMENT** ; findings = broutilles DANS mes corrections (commentaire `MAX_AUDIO_S` sur-promet · test aveugle au contenu → **test de transparence ajouté** · 3 NIT triviaux) → tous corrigés. **Convergence : 2 fuites (origine) → 0 (mes corrections) → fermé. 4 agents, sévérité qui chute.**
  - **⑤ ÉCARTS / FRONTIÈRES tracés** : **groupe vs tour** (le groupe se ferme à un plafond de silence simple ; la **fin de tour FINE = V5** Smart Turn) · **portier gère éveil + clôture** ; l'écoute active + la deadline de garde = **V9** (R-1) · **R-2 overrun au read** honoré · torch CPU→CUDA (décision V2 §7) · **silence inter-segment** dans la fenêtre lue (le sidecar lit par plage, hors de la config prouvée-WER du banc) = **fail-safe vérifié empiriquement au vrai moteur** (jamais de faux réveil ; test de couverture 2-segments ajouté).
  - **⑥ PREUVES** : `pytest sidecar` **106** (dont **24 V4** : portier Sophia/Sophie + **apostrophe qui mord** · HypoBuffer + **shift transparent** · SttPlug scripté · R-1/R-2 qui mordent · lecture rapide vive + **< 1s conv 44** + après-streaming · **compaction bornée + transparente octet-pour-octet** · **file bornée worker mort** · replis · **cœur réel vrai faster-whisper** : réveil / ignore Sophie / 2-segments) · **E2E-V4** cœur réel (source WAV → AEC → VAD → **vrai STT** → portier → `evt.wake` **SANS injection**) · contrat socle+V0+V1+V2+V3 **INTACT** (`npm test` 11 · `e2e` 31 · `e2e:v0` 9 · `e2e:v1` 12 · `e2e:v2` 16 · `e2e:v3` 16 · `smoke` 12). **Renvois** `01` §3 (V4) · §4 (U-V4) · §5/§6 · JOURNAL (portier conv 27).

- **[PERF conv 44 (2026-07-18) — cerveau (recette V7) + warmup STT (V4) + RÈGLE NON NÉGOCIABLE — validé par Yohann]** Investigation perf design-first (Yohann « en max »). **RÈGLE GRAVÉE, NON NÉGOCIABLE, opposable à TOUTES les conversations : le produit ne doit JAMAIS être moins performant que le banc, pour TOUT (réveil, streaming, temps de réponse) ; et si on peut améliorer sans rien casser, on améliore.** (→ CLAUDE.md garde-fou · mémoire `perf-produit-egal-banc` · `conv44-perf-cerveau-v7`.)
  - **① Cerveau — mesuré fidèlement (`claude-sonnet-4-6`, OAuth Max `apiKeySource=none`)** : (a) **l'effort n'affecte PAS le TTFT** (low 4264 / medium 4849 / high 4720 ms — indistinguables dans le bruit ; confirme conv 33 « latence = API ») → **le choix d'effort se fait pour sa PENSÉE, jamais la vitesse** ; (b) **A/B agent-complet vs chat-nu** (`--system-prompt` REMPLACE le défaut + `--strict-mcp-config` + `--tools ""`) : **le chat nu COUPE le TTFT à froid de moitié** (1er token **4447 → 2253 ms**) — le harnais d'agent (MCP + outils + gros prompt système) était le frein ; **c'est ça, l'écart navigateur-vs-CLI** ; (c) **à CHAUD (persistant), nu ≈ agent** (~1926 vs 2108 ms — le cache de prompt amortit le prefill) → **le chat nu gagne surtout à FROID** (1er tour + replis) ; (d) le banc (`oreilles_live.WarmBrain`) était un **process claude PERSISTANT** (~2 s chaud, ne re-payait PAS la plomberie) → **le socle T8 request-scoped, coincé à froid ~4,5 s, est une RÉGRESSION latence vs le banc** ; ~2 s chaud = **plancher modèle/API** (on l'ÉGALE, on ne le bat pas franchement).
  - **② RECETTE V7 (gravée)** : cerveau = **WarmBrain PERSISTANT** (→ plan/05 R4 : **n'est PLUS un simple repli — REQUIS pour ne pas régresser vs le banc**) **+ chat NU** (sa persona I→VI = le `--system-prompt` ; 0 MCP / 0 outil pour le **dialogue** — l'agent complet, outillé, reste pour **agir** sur le bureau = invocation SÉPARÉE, « un seul guichet ») **+ streaming** (`--include-partial-messages`, parle dès le 1er token, comme le banc conv 31). Effort medium/high au choix de sa pensée. **La qualité conversationnelle du chat nu reste à valider quand V7 sera bâti** (l'A/B n'a mesuré que la latence).
  - **③ Warmup STT AJOUTÉ à V4** (`FasterWhisperEngine.warm`, conv 44) : une inférence JETABLE (1 s de bruit très faible, 16 kHz, deterministe) compile les noyaux CUDA au démarrage du worker → le **PREMIER réveil ne paie plus la compilation** (mesuré : 1re inférence 556 ms vs à chaud ~425 ms). **Parité banc** (`mesure_v4_designfirst.py` warmait avant de mesurer ; sans ça le produit était plus lent que le banc au 1er réveil = interdit). Best-effort (un warmup KO ne bloque jamais le modèle). `pytest sidecar` **99** (inchangé), **ZÉRO régression** (`npm test` 11 · `e2e` 31 · `e2e:v0-v3` · `smoke` 12).
  - **④ Chiffres de banc = PLANCHERS (jamais descendre en dessous)** : réveil ~650 ms (ta voix — produit chaud **445 ms < 650**, à confirmer à TA voix) · STT flux ~1,0 s (produit ~858 ms, **déjà mieux**) · cerveau **chaud** ~2 s (froid ~4,5 s = à corriger en V7) · voix A20 **CPU** ~instant (RTF 0,06 — GPU dédié STT, sa bouche ne vole rien à ses oreilles). **Reste (conv 44+)** : contention réveil à chaud (mesurer l'effet du warmup en contexte) + **le JUGE à TA voix au micro** (le WAV de test est du TTS, pas toi). Outils : `bancs/aec/perf/`.

- **[V5 BÂTIE DANS LE PRODUIT — la FIN DE TOUR FINE (Smart Turn v3.2) — conv 45 (2026-07-18) — validé par Yohann]** Sophia décide **quand Yohann a VRAIMENT fini de parler** — vive quand la phrase sonne finie, **jamais coupée sur un « euh… » ou une pause** (l'invariant SACRÉ). En V4 le groupe se fermait sur un plafond de silence SIMPLE (3,0 s) ; V5 apporte le VIF : Smart Turn (l'intonation) raccourcit le plafond, le 3,0 s reste le FALLBACK. *(Micro-technique tranchée par Claude — garde-fou Phase 3 pt 2 ; moteur A6 laissé à l'essai, prouvé conv 25.)*
  - **① Le code V5** : `sidecar/consumers/turn.py` (NEUF) — `SmartTurnEngine` (onnxruntime **CPU** ; preprocessing `WhisperFeatureExtractor` REPRODUIT avec `torch.stft` + une **matrice mel VENDORISÉE** — **PROUVÉ BIT-À-BIT au banc**, design-first : max|diff proba|=**0,0** sur 7 signaux ; **ZÉRO dep `transformers` au runtime**) derrière une **interface injectable** `TurnEngine` · logique de décision **PURE** (`hold_reason` gardes A/B, `effective_plafond` hiérarchie) · `TurnDetector` (moteur + logique ; un moteur qui lève → fallback plafond) · constantes EXACTES du banc. **Intégration dans `SttPlug`** (`stt.py`, D1 : le groupe = le tour, le contrat `stt.final`→`turn.end` impose un seul chemin ordonné ; `turn=None` → **V4 EXACT**) : `_turn_audio` (audio continu du tour, **pré-attaque 0,4 s** `_read_preattack`, borné 8 s) · `_turn_check` au candidat de silence (conversation) · `_emit_turn_end` **après** `_emit_final` (ordre gravé, scopé conversation via `_armed_at_open`) · `_on_overrun` reset l'état V5. Câblage `server.py` (`EVT_TYPES += evt.turn.end` · prod = `TurnDetector(SmartTurnEngine())` · mode `test-turn` E2E-V5 · `test-stt`/V4 INTACT). Modèle `smart-turn-v3.2-cpu.onnx` + `mel_filters_80.npy` VENDORISÉS `resources/models/smart-turn/` (gitignorés, offline au runtime). Reqs += `onnxruntime==1.27.0` (déjà présent, dép faster-whisper).
  - **② FIDÈLE AU BANC `oreilles_live.py` (l'endpointing validé à l'oreille conv 32-34), pas au `07_turn` naïf** : constantes EXACTES (`TURN_THR`=0,5 · `MIN_SPEECH_END`=1,2 · `HELD_PLAFOND`=0,8 · `HELD_CONF`=0,85 · `ENDGRACE`=0,7 · `PLAFOND`=3,0 · `WAKE_PLAFOND`=0,8 · liste `HANGING` au caractère près) · la hiérarchie de décision (confiant→grâce de fin 0,7 / court+très-confiant→grâce courte 0,8 / retenu→3,0 / pas-confiant→3,0) 1:1, frontières `>`/`<=` identiques · **pré-attaque 0,4 s** (banc `recent[-0.4s:]`, ajoutée conv 45 sur demande de Yohann « coller au banc »).
  - **③ ÉCARTS assumés + tracés** (Yohann : « un vrai problème technique empêche le 100 % ? » → non) : **(a) `parle` en temps-audio** `(seg_stop-mark)/rate` au lieu de l'horloge murale du banc — **même valeur** en temps réel (le banc l'approxime), déterministe (testable bit-pour-bit) — **choix (A) validé par Yohann** ; **(b) threading** : le banc mono-fil, le produit multi-prises — ne change PAS les décisions (elles dépendent des entrées, pas de qui-tourne-quand) ; **(c) au RÉVEIL, Smart Turn NE tourne PAS** (D3 : la lecture rapide conv 44 gère l'ouvreur — protège le réveil que tu as gagné) ; **(d) `evt.turn.end`** = ADDITION produit (le banc monolithe agissait sur place) ; **(e) garde B** = seul franchissement acoustique→sémantique, **validé conv 32**. **Le seul juge du « 100 % en décision » = TA voix, REPORTÉ à V7** (pipeline complet, comme le banc — décision Yohann : tester avec la voix, tous les deux).
  - **④ AUDIT — solo à fond + croisé 2 agents (fidélité + robustesse). Le trou dans MES ajouts, jamais le cœur.** **Solo à fond** : **S-1** (`_on_overrun` ne réinitialisait pas l'état V5 → audio périmé / `parle` négatif) · **S-7** (`warm()` mi-chargé, `_sess` posé avant la mel → crash `predict` au retry) · **S-11** (garde env `SOPHIA_TURN_THREADS`) · **S-12** (la pré-attaque débordait la marque si près d'`oldest` → doublon d'audio) — tous corrigés + **tests qui MORDENT**. **Croisé (fidélité + robustesse) : 0 MAJEUR ×2** — fidélité mappée 1:1 ; robustesse reproduite empiriquement (aucun crash worker sur AUCUN audio dégénéré, `_turn_audio` borné, thread-safety, **zéro régression V4**, scoping/ordre corrects). 3 MINEUR → **M2** (`_turn_check` gardé sur l'arme courant `_armed_view` au lieu du rôle `_armed_at_open` → Smart Turn sur du VIDE pour l'ouvreur-armé-en-cours) **corrigé** (les 3 gardes V5 cohérentes, restaure V4, réveil intouché) · **NaN/inf** (contrat `predict→[0,1]` non gardé à la frontière injectable) **corrigé** (garde `isfinite`→fallback) — **tous 2 MORDENT** · overrun-ne-reset-pas-`_seg_stop` **assumé** (bénin, artefact déjà présent en V4). **Re-croisé SKIPPÉ par Yohann** (fixes triviaux, testés MORD, re-relus — calibration Yohann, précédent conv 42).
  - **⑤ FIDÉLITÉ #2 (design-first)** : `transformers` (le dep du banc) downgraderait `tokenizers` 0.23→0.22 dont **faster-whisper dépend** → écarté pour **protéger V4** ; le preprocessing reproduit avec `torch.stft`+mel prouvé bit-à-bit → **venv minimal, zéro risque V4**. **Jitter V0-V3 fermé EMPIRIQUEMENT** (e2e:v5 : `overruns=0`/`resyncs=0` avec Smart Turn qui tourne — le ring 30 s absorbe le pic ~40 ms). Latence Smart Turn ~32 ms (≥ banc), threads onnxruntime = défaut (fidèle banc, configurable `SOPHIA_TURN_THREADS`).
  - **⑥ PREUVES** : `pytest sidecar` **126** (dont **20 V5** : logique pure gardes/hiérarchie · intégration scriptée [confiant→ENDGRACE · **hésitation ne coupe PAS** · reprise annule la grâce · court+confiant→HELD · garde B retient · crash→fallback · ordre `stt.final`<`turn.end` · turn.end conversation-seulement] · **fidélité NON-SKIPPABLE bit-à-bit** · cœur réel vrai Smart Turn · pré-attaque 0,4 s + jamais-au-delà-de-la-marque · reset overrun · warm mi-chargé · gardes M2/NaN — **tous MORDENT**) · **E2E-V5** cœur réel (vrai Smart Turn → `evt.turn.end` après `stt.final`, `turn_errors=0`, jitter fermé) · contrat socle+V0→V4 **INTACT** (`npm test` 11 · `e2e` 31 · `e2e:v0` 9 · `e2e:v1` 12 · `e2e:v2` 16 · `e2e:v3` 16 · `e2e:v4` · `smoke` 12). **Renvois** `01` §3 (V5) · §4 (U-V5) · §6 (fin de tour = seuils + ratio fallback/smart-turn) · §7 (preuve banc Smart Turn conv 25). **PROCHAIN : V6 (speaker-ID, ECAPA, banc conv 34) → V7 (1ʳᵉ parole produit — voix A20 + WarmBrain + chat nu + streaming ; corrige la régression latence T8 ; c'est là que la fin de tour se JUGE à ta voix).**

- **[V6 BÂTI DANS LE PRODUIT — le SPEAKER-ID « qui parle ? » (ECAPA CPU) — conv 46 (2026-07-18) — validé par Yohann]** Sophia sait désormais **si c'est Yohann qui parle** : un consommateur du ring POST-AEC (curseur indépendant, piloté-VAD comme le STT) qui score chaque segment de parole contre l'empreinte de Yohann et émet **`evt.speaker {locuteur, score, mark, captured_at, speech_ms}`**. Précondition gravée de **V8 (barge-in modulé)** + **V14 (verrou d'affect)** ; **couture injectable** (moteur ET centroïde) pour tester V8/V14 déterministe. L'écart produit annoncé conv 33-34 (V6/V8 au coordinateur de banc) se comble pour V6. *(Micro-technique tranchée par Claude — garde-fou Phase 3 pt 2 ; moteur A29 laissé à l'essai, prouvé banc conv 33-34.)*
  - **① Le code V6** : `sidecar/consumers/speaker.py` (NEUF) — `EcapaEngine` (SpeechBrain `spkrec-ecapa-voxceleb`, **CPU** via `run_opts={"device":"cpu"}` — JAMAIS `CUDA_VISIBLE_DEVICES=""` qui aveuglerait le STT GPU du MÊME process, mesuré conv 46 ; modèle VENDORISÉ offline `resources/models/speaker/`, charge 0,27 s sans réseau ; `embed` = copie fidèle `v6_service.emb`) derrière une **interface injectable** `SpeakerEngine` · logique PURE `cosine`/`decide` · `build_centroid` (moyenne normalisée des 3 clips d'ancre raw_near/raw/raw_soft, `v6_service` EXACT, construit au RUNTIME dans la même instance ECAPA → cohérence centroïde↔scoring, pas de couplage à un `.npy`) · `SpeakerDetector` (**gardes HONNÊTES** : audio<0,4s / moteur qui lève / NaN → **None** [n'émet rien], JAMAIS un faux « inconnu » ; un vrai score bas → `("inconnu", score)`) · `SpeakerPlug(ConsumerPlug)` (worker unique + curseur indépendant [ECAPA lourd → thread dédié, ne bloque jamais VAD/STT] ; `on_vad`→file bornée [F-2] ; `seek_to` la marque + **R-2 overrun** ; cadence du banc `v8_bargein` : 1er score à MIN_SPEECH 0,75s puis chaque EVAL_EVERY 0,5s sur les dernières MAX_WIN 1,5s, plafonné CAP 3,0s ; buffer BORNÉ ; garde `_tick` + `tick_errors` = zéro mort silencieuse). Câblage `server.py` (`evt.speaker` · mode `test-speaker` · fan-out `_observing_emit(bus, wake.observe, stt.on_vad, speaker.on_vad)` · `_stop_audio` pop atomique · `/debug audio.speaker`) · `requirements.txt` `speechbrain==1.1.0` (version banc `.venv-speaker` vérifiée ; **ZÉRO downgrade** du stack, mesuré `--dry-run`). Modèle ECAPA + négatifs A20 de test VENDORISÉS (gitignorés).
  - **② FIDÈLE AU BANC** (`v6_service.py` calcul + `v8_bargein.py` cadence ; croisé fidélité : **cœur 1:1, 0 MAJEUR/0 MINEUR**) : embed/centroïde/cosinus/decide/constantes/`load16` = copies exactes. **BARRIÈRE DE FIDÉLITÉ design-first (AVANT de coder, règle perf produit≥banc)** : le venv produit reproduit le banc — **EER 0 % à l'intégration** (L≥1,5s), A20 moy **0,152** ≈ banc 0,165 ; ancre + modèle **octet-pour-octet** identiques au banc/HF (hashes vérifiés). **BARRIÈRE LÉGER** : **137 ms/eval** CPU, **jitter=0 PROUVÉ** à l'e2e (`vad.resyncs=0` / `speaker.overruns=0` — l'ECAPA ne ralentit pas V0-V5 ; parité du gate jitter V5).
  - **③ ÉCARTS assumés + tracés** : **(a)** seuil défaut **0,22** = valeur LIVE prouvée du banc (résidu post-AEC ~0,21 / Yohann barge-in 0,23-0,39, conv 34), **PAS** le fossé offline plein niveau (A20 0,30-0,42) car en runtime V6 ne voit la voix de Sophia qu'**APRÈS l'AEC** → **dépendance réelle V6→V1/AEC (F2)** gravée ; le vrai juge = ta voix LIVE (test offert dès V6, pas reporté). **Facilité corrigée par la MESURE** : mon plan disait « seuil = milieu du fossé offline mesuré » → faux (clips A20 pleins ≠ résidu post-AEC) → seuil = valeur live du banc. **(b)** `evt.speaker` porte le **score cosinus BRUT** (pas une proba [0,1] inventée ; échelle = doc 04 ; miroir de l'écart V3). **(c)** gate = **V2 Silero** (le banc offline gatait RMS ; V2 = meilleur gate, patron produit STT ; le barge-in par énergie fut ÉCARTÉ conv 33). **(d)** `cmd.enroll.push` = couture V15 (centroïde runtime des clips vendorisés pour l'instant). **(e)** un échec de calcul n'émet RIEN (≠ faux « inconnu »).
  - **④ AUDIT — solo à fond + croisé 2 agents + RE-CROISÉ 2 agents (4 agents). La leçon RE-VÉCUE : le trou est dans MES corrections, jamais le cœur (validé sain par 4 regards).** **Solo** : plafond CAP bloquait le verdict sur un gros read (le banc évalue PUIS break) · warm silencieux · `import queue`. **Croisé (fidélité+robustesse) : 0 MAJEUR ×2** (cœur 1:1 + solide : mono-fil, bornes, gardes, machine à états, zéro régression, jitter) ; NITs → **NIT-1** `_tick` non gardé (mort silencieuse latente) **corrigé** (garde + `tick_errors`) · **NIT-2** marque périmée **corrigée** (marque = position CLAMPÉE, honnête si scroll hors ring). **RE-CROISÉ ciblé : 0 MAJEUR ×2 — le trou dans MA correction NIT-1 = sa RIGUEUR** : garde `_tick` **SANS test qui MORD** → test ajouté + **prouvé par temp-revert** (sans la garde : `RuntimeError` remonte) ; `_discard_cmds`/`seek_latest` non gardés → passés sous la garde. **Convergence : croisé 0/0 → re-croisé 0/0 (que des NIT couverture/latents) → fermé.**
  - **⑤ PREUVES** : `pytest sidecar` **149** (dont **23 V6** : logique pure + gardes honnêtes [échec≠inconnu, NaN, MIN_SAMPLES] · plomberie [MIN_SPEECH · cadence/CAP · gros-read-past-CAP · overrun R-2 · file bornée · reprise · stop-seul · warm_failed · marque clampée · **tick-guard qui MORD**] · **cœur réel = séparation EER 0 % à l'intégration** [Yohann `raw_far` held-out ≫ A20]) · **E2E-V6** cœur réel (source `raw_far` → AEC → VAD → **vrai ECAPA** → `evt.speaker` **locuteur=yohann score 0,495 > 0,22**, SANS injection ; jitter=0 ; arrêt propre) · contrat socle+V0→V5 **INTACT** (`npm test` 11 · `e2e:v0-v5` · `smoke` 12). **Renvois** `01` §3 (V6) · §4 (U-V6) · §6 (modèle+seuil = calibration ; juge à ta voix) · §7 (preuve banc ECAPA conv 33-34). **PROCHAIN : V7 (1ʳᵉ parole produit — voix A20 CPU + WarmBrain persistant + chat nu + streaming ; corrige la régression latence T8 ; c'est là que la fin de tour V5 se JUGE à ta voix, pipeline complet) → V8 (barge-in 0,22, modulé par V6).**

- **[V7 BÂTI DANS LE PRODUIT — la 1ʳᵉ PAROLE PRODUIT (voix A20 + cerveau chaud + routeur) + ARCHI 2 PROCESS — conv 47 (2026-07-18) — validé au juge à ta voix]** Sophia **parle enfin pour de vrai dans le sidecar** : le tour se ferme (V5) → elle répond en STREAMING → sa voix A20 le prononce. Deux vraies conversations (Nietzsche/Zarathoustra ; « tu me construis avec moi, pas pour moi ») — vive, présente, lucide sur elle-même. **Le vrai livrable de V7 = elle respire ; les millisecondes ne sont là que pour ne pas l'étouffer.** *(Micro-technique tranchée par Claude — garde-fou Phase 3 pt 2 ; recette cerveau = PERF conv 44 ci-dessus.)*
  - **① Le code V7 (3 morceaux)** : **(A) la BOUCHE** `sidecar/tts/` (`TtsPlug` = VRAI Piper A20 **CPU** `resources/models/voice/fr_FR-a20-e400.onnx`, découpe en phrases, train gen/play, sortie audio ISOLÉE ; `cmd.tts.speak/push/end/stop` → `evt.tts.start/done` ; lexique prénom `[[joˈann]]`) · **(B) le CERVEAU CHAUD** `src/orchestrator/resources/warm/` (`WarmBrain` = `claude -p` **PERSISTANT** + chat **NU** [`--system-prompt` persona, 0 MCP/0 outil] + **streaming** [`--include-partial-messages`] — corrige la RÉGRESSION latence du socle T8 request-scoped, recette conv 44 ; `ask` streame via `onDelta`, ne rejette jamais par contrat) · **(C) le ROUTEUR** `src/orchestrator/voice/router.ts` (le FIL oreilles↔voix : `evt.wake`→salutation miroir ; `evt.turn.end`→cerveau chaud→`cmd.tts` streaming ; clôture→au revoir ; GATE anti-auto-écoute). Le canal T8 (request-scoped) reste pour l'ACTION outillée ; le WarmBrain sert le DIALOGUE (« un seul guichet »).
  - **② ARCHI 2 PROCESS (la décision structurante — journalée Phase 3, comme le banc conv 34)** : le monolithe **étouffait la voix** (contention PROUVÉE `scratchpad/diag_contention.py` : 6 cœurs sur-souscrits → synth Piper **×3 sous charge** → fil de sortie audio affamé → voix « lente/monotone » + latence). **Fix = séparer** : `sidecar/server.py` `SIDECAR_ROLE` (`ears` = AEC+VAD+réveil+STT+fin de tour, **V6 OFF** défaut [`SOPHIA_SPEAKER=1` rallume] ; `mouth` = Piper + sortie ISOLÉE ; **sans rôle = monolithe inchangé, ADDITIF**) · **gate anti-auto-écoute CROSS-PROCESS** `cmd.listen.mute/resume` (le routeur les pose sur les oreilles quand la bouche parle — remplace `tts.is_speaking` qui n'est plus in-process) · routeur 2 canaux `earsIpc`/`mouthIpc` (`ipc` seul = monolithe, rétro-compat). **Ma facilité conv 39 reconnue** (« un venv » → « un process »). Détail → JOURNAL (arbitrage Phase 3 « archi 2 process »).
  - **③ PROUVÉ AU JUGE (`scratchpad/juge_v7.mjs` : 2 sidecars ears/mouth + 2 canaux + test 2 PASSES)** — meilleur que le monolithe, dans la fourchette du banc : réveil silence→son **759 ms (P1) / 796 ms (P2 retour après pause)** [monolithe 867 ; banc 650-830] · cerveau TTFT médian **1389 ms (P1) / 1628 (P2)** [monolithe 2688 ; banc ~1276] · auto-écho (inconnu) **0** [monolithe 1]. **Le réveil est enfin dans la fourchette du banc ; le retour après pause reste CHAUD** (WarmBrain vivant + prewarm au 2ᵉ réveil → le cas « bonjour Sophia 1 h après » est géré). 19 tours, streaming 18/19, 0 secours, 0 froid. **Clôture EXACTE du banc restaurée** (`« Avec grand plaisir. »`, sans prénom — j'avais dérivé vers « …, Yohann. À bientôt. » = 5,3 s + son parasite).
  - **④ FIX FLUSH + AUDIT 4 agents (0 MAJEUR)** : le gate anti-auto-écoute (l'équivalent produit du `_flush_audio` du banc) affiné — **MAJEUR-1** (un final RÉSIDUEL de sa voix jamais ré-apparié comme ouvreur d'un éveil futur) · **M1** (deadline de LECTURE large ≠ deadline « moteur mort » : une réponse longue ne rouvre plus le gate en plein discours) · **N1/N2** (timers post-stop / logger qui lève). Audité 4 agents, **0 MAJEUR** ; corrections dans MES corrections (leçon centrale). Auto-écho **RÉGLÉ** (juge : 1 → 0).
  - **⑤ ÉCARTS / FRONTIÈRES tracés** : **routeur = EMBRYON** (la grille des 20 intentions + `cmd.listen.*` complet = V9/V10) · le GATE V7 ne coupe pas l'écoute à la SOURCE (V9 `cmd.listen.stop`) · le BARGE-IN intentionnel (Yohann coupe, via speaker-ID V6) = **V8** · phrases fixes = MÉCANISME V7, CONTENU = personnalité 03 (placeholder « Tu es Sophia ») · **persona I→VI fixé au spawn du WarmBrain** (`sysprompt`), l'injection par tour = frontière 03 · **RECONNEXION sur respawn** d'un sidecar = frontière V9 (le juge tourne sur des sidecars stables) · **MIGRATION du vrai runtime (2 superviseurs, `electron/runtime.ts`) = conv 48** (à conv 47, seul le juge tourne en 2 process ; le runtime tourne ENCORE en monolithe — la voix y serait étouffée).
  - **⑥ PREUVES** : `pytest sidecar` **188** · `npm run build` OK · `u-router` vert *(u-warm flake sous le runner parallèle, passe SEUL — WarmBrain non touché ; même famille que le flake e2e:v5)* · **E2E-V7** cœur réel (la BOUCHE : `cmd.tts.*` → VRAI Piper A20 → `evt.tts.*`, cycle start/done + purge, sans jouer de son) · **juge live 2 passes** (③ ci-dessus). Committé **`[conv-47]`**. **Renvois** `01` §3 (V7) · §4 (U-V7, I-3, I-6) · §6 (voix + latence 1re phrase) · JOURNAL (arbitrage « archi 2 process ») · PERF conv 44 (recette cerveau). **PROCHAIN : conv 48 = MIGRER LE RUNTIME (2 superviseurs) → affiner le réveil (STT) → endpointing V5 (a coupé Yohann 1× sur une pause) → V8 (barge-in, réactive V6).**

---

*Plan 01 — Pipeline vocal. Traduit `01-pipeline-vocal.md` (A5–A9 + A32-étendu + affect + B4 + part couche 1 d'A29/A35) en tâches V0→V15 + tests + critères pointés. S'appuie sur `docs/plan/00-socle.md`. Suite (ordre des dépendances) : `docs/plan/02-memoire.md`. Les deux 🔴 du projet (wake FR, AEC) se prouvent ici. **Conv 33-34 : V6 speaker-ID + V8 barge-in modulé par le locuteur PROUVÉS live (seuil 0,22, 6/6, 0 faux, vraie voix A20) + grâce de fin (0,7 s) + voix A20 « bluffante » — le pipeline vocal VIT ; écart produit tracé (ré-implémentation au vrai sidecar). Conv 39 : V0 (chemin audio) BÂTI. Conv 40 : V1 (AEC, le 1ᵉʳ 🔴) BÂTIE + prouvée LIVE + auditée 3 tours. Conv 41 : V2 (le VAD, Silero, 1ᵉʳ émetteur d'`evt.*` via un BUS thread→boucle→WS) BÂTI dans le VRAI sidecar ; hystérésis réimplémentée équivalente à VADIterator (prob + index en 1 appel, prouvée par test) ; audité croisé + re-croisé (0 MAJEUR ; le 1 MINEUR du re-croisé — reset RNN non effacé — était dans MA correction, corrigé + test qui mord) ; pytest 69 + E2E-V2 cœur réel POST-AEC + socle intact. Conv 42 : V3 (le réveil rétroactif) BÂTI — `RingCursor.seek_to` (rembobine à la marque VAD, garde d'honnêteté sur la troncature) + `WakeGate` (porte réactive sans thread, observe la marque via l'emit wrappé SANS toucher le VadPlug verrouillé, `evt.wake {pos,captured_at,truncated}`, S12) ; V3 seul (éveil injecté, comme evt.speaker pour V6/V8) ; audité croisé 2 agents 0 MAJEUR ×2, cœur sain ×4 regards, corrections R-4/F-1/R-6/F-3 reproduites à la source + frontières V4/V9 gravées (R-1 release, R-2 overrun). Conv 43-44 : V4 (le STT streaming + le portier d'éveil PAR PHRASE) BÂTI — elle entend son nom et se retourne (le VRAI déclencheur sur V3, distingue Sophia/Sophie) ; réveil rendu VIF et COHÉRENT à SA VOIX (`WAKE_MIN_WIN_S`=0,4s : sa « Bonjour Sophia » 0,9s ratait le seuil streaming 1,0s → 784/764/649 ms) ; audité 4 agents (solo + croisé [2 fuites mémoire F-1 compaction transparente + F-2 file bornée] + re-croisé [transparence prouvée octet-pour-octet, le trou dans MES corrections] — cœur sain) ; pytest 106 (24 V4) + E2E-V4 cœur réel SANS injection + socle+V0-V3 intact. Conv 45 : V5 (la FIN DE TOUR FINE, Smart Turn v3.2) BÂTIE — elle laisse Yohann parler avec ses pauses sans le couper (`evt.turn.end` fin, plafond 3,0s = fallback) ; moteur Smart Turn PROUVÉ BIT-À-BIT au banc (torch.stft+mel vendorisée, zéro dep transformers) + pré-attaque 0,4s (fidèle banc) ; audité solo à fond (S-1/S-7/S-11/S-12) + croisé 2 agents (0 MAJEUR ×2 ; M2 + NaN corrigés + MORD, overrun assumé) ; pytest 126 (20 V5) + E2E-V5 cœur réel + socle+V0-V4 intact ; `parle`=temps-audio validé (A), le JUGE à ta voix reporté à V7. Conv 46 : V6 (le SPEAKER-ID, ECAPA CPU) BÂTI — Sophia sait si c'est Yohann qui parle (`evt.speaker {locuteur, score}`, consommateur piloté-VAD, seuil 0,22 = valeur live du banc) ; barrières design-first (EER 0 % reproduit dans le venv produit, jitter=0 mesuré, ancre+modèle octet-pour-octet) ; audité 4 agents (0 MAJEUR ×4 ; le trou dans la RIGUEUR de MA correction NIT-1 — garde `_tick` sans test qui MORD, corrigé+prouvé temp-revert) ; pytest 149 (23 V6) + e2e:v6 (yohann 0,495) + socle+V0-V5 INTACT. Conv 47 : V7 (la 1ʳᵉ PAROLE PRODUIT — voix A20 Piper CPU + cerveau chaud WarmBrain + routeur) BÂTI + **ARCHI 2 PROCESS** (la voix retrouve sa propre voie, comme le banc conv 34 : le monolithe l'étouffait, contention prouvée diag_contention — synth Piper ×3 sous charge ; oreilles/bouche séparées, gate anti-auto-écoute cross-process `cmd.listen.*`, V6 en veille ; ma facilité conv 39 « un venv = un process » reconnue) + fix flush + audité 4 agents (0 MAJEUR) ; PROUVÉ au juge à ta voix (réveil 759 ms dans la fourchette banc, auto-écho 0, retour chaud après pause, 2 vraies conversations) ; pytest 188 + E2E-V7 + u-router + `npm run build` ; committé `[conv-47]`. **Migration du vrai runtime (2 superviseurs `electron/runtime.ts`) = conv 48** (à conv 47, seul le juge tourne en 2 process) — la ré-implémentation produit AVANCE (V0→V7).***
