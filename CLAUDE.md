# CLAUDE.md — YdvVoice (Sophia) · Cadrage projet [profil Standard]

> Émanation Claude Code du pattern v3.1 Standard. **Maintenu IN PLACE strict** en fin de chaque conversation (jamais d'accumulation — le cumulatif va dans `docs/journal/CLAUDE-HISTORY.md`).

## ⚠️ À lire EN PREMIER, avant toute action
`docs/PATTERN-v3.0-CLAUDE-CODE-STANDARD.md` est le **pattern méthodologique de référence**. Ses règles critiques/structurantes **priment** sur ce CLAUDE.md en cas de tension (hiérarchie : pattern > règles d'or > garde-fous projet). Lecture **intégrale**, jamais partielle (R4).

Puis : **`docs/journal/ESSENCE-Sophia.md` (l'ÂME de Sophia, en clair — QUI elle est, à lire avant le technique)** → `docs/journal/JOURNAL-ARBITRAGES.md` (décisions actées) → `docs/IMPLEMENTATION.md` (état) → `docs/VISION.md` (cahier).

> **Arborescence** : `CLAUDE.md` + `.gitignore` à la racine ; le cadrage dans `docs/` ; les fichiers vivants (arbitrages, history) dans `docs/journal/` ; les relais (un par conv) dans `docs/journal/relais/` ; **l'annexe PRIVÉE dans `docs/prive/` (gitignorée — marbre intégral, blocklist, prompts d'opération : JAMAIS sur le dépôt public)**. Le `PATTERN` est **présent en local mais privé** (gitignored, hors dépôt public — voir A3 du journal).

---

## Registre des règles (daté — défaut inversé « à re-tester ici »)

> Établi le **2026-06-21**, propriété de l'utilisateur, opposable. Défaut : toute règle est **« à re-tester (ici) »** (gratuit) ; la passer en **« acquise (ici) »** coûte une ligne de justification. Resservi à la clôture : toute règle « à re-tester » sur laquelle une décision s'est appuyée *comme acquise* est signalée.

| Règle | Statut (2026-06-21) | Justification si « acquise » |
|---|---|---|
| R1 — Zéro agent (sauf audits 2 agents) | à re-tester (ici) | — |
| R2 — Zéro facilité | à re-tester (ici) | — |
| R3 — Robustesse + maintenabilité d'abord | à re-tester (ici) | — |
| R4 — Lecture intégrale avant modif | à re-tester (ici) | — |
| R5 — Validation avant commit/push | à re-tester (ici) | — |
| R6 — Zéro AskUserQuestion (texte libre) | à re-tester (ici) | — |
| R7 — Reco + justification + « pourquoi pas » | à re-tester (ici) | — |
| R8 — Un par un (granularité cohésive) | à re-tester (ici) | — |
| R9 — Prompt de passation fin de session | à re-tester (ici) | — |
| Audit empirique source de vérité pre-inscription | à re-tester (ici) | — |
| Filtre projet (« nécessaire à la qualité requise ? ») | à re-tester (ici) | — |
| Mots simples non-dev en tête d'arbitrage | à re-tester (ici) | — |
| Distinction préférence pragmatique / argument méthodo | à re-tester (ici) | — |
| Budget = jauge utilisateur fait foi | à re-tester (ici) | — |

> **Retour clôture conv 62** : R1–R9 tenus. **R1 — solo à fond + audit 10 regards du POISON-ROW sur 4 tours, convergence non pas par une correction plus maligne mais par un CHANGEMENT DE STRUCTURE** (« ne plus deviner ») — patron « le trou est dans MA correction », **23ᵉ fois, à l'extrême : mon « c'est bon » faux 4×** (sœur-qui-réussit → sonde sentinelle → timeout transport → transport≠contenu, la racine « déménageait » à chaque œil frais). **R2/R3 — DESIGN-FIRST intégral** : le banc `embed` AVANT toute ligne de la prise (fourche CPU/GPU tranchée **par la mesure** [77 ms ONNX < 200 ms budget] · moteur ONNX prouvé 1:1 vs la réf [2,8e-07] · cohabitation `--dry-run` = ZÉRO downgrade du stack verrouillé, le piège V5 évité) ; le poison-row résolu **à la racine** (compter+surfacer, jamais dead-letter) parce que la permanence est INCONNAISSABLE — pas une rustine. **R4** lectures intégrales avant modif (juge.mjs 621 l. · runtime.ts 530 l. · supervisor · plan/02 · schema-02). **R5** — **3 commits sur Go explicite** ; **git add CIBLÉ** (10 fichiers M1, status LU, zéro `??` inconnu, gros binaires gitignorés vérifiés absents, gitleaks no-leaks). **R6** texte libre. **R7** exercé (fourche moteur ONNX/torch + fourche CPU/GPU → **la mesure tranche** ; poison-row a/b → **b** [Yohann, la plus conservatrice] ; amélio juge a/b/c → **b** reco [bip d'alerte]). **R8** — chaque sujet clos AVANT le suivant : **le juge muet investigué À LA SOURCE** (traces juge + process résiduels + supervisor « fige ») **avant de committer**, jamais sauté à une explication ; le poison-row creusé **4 tours** avant de graver. **R9** RELAY-conv63 + mémoires. **YOHANN PENSE ET DÉCIDE** : sa décision **option b** (venue APRÈS mon honnête « non, je ne peux pas te le promettre — mon c'est-bon est à 0/4 » → bascule de ma reco (a)→(b)) ; son **finding d'honnêteté** « je n'étais pas tendu » (→ la valence = chaleur, pas tension, corrigé + leçon 03) ; sa demande « juste rajouter Sophia » au cerveau ; sa **bonne question** « elle devrait se recharger toute seule et continuer, non ? » (= exactement V15, qui a CONFIRMÉ que le produit fait bien ce que le juge ne fait pas). **Honnêteté tenue** : mon « c'est bon » faux 4× DIT sans fard ; **ma FAUSSE ALERTE corrigée sans fard** (j'avais lu `conversations.jsonl` avec le mauvais nom de champ → cru les réponses vides ; re-parsé = 534 réponses présentes, dit tout de suite) ; le juge muet **résolu à la source** (oreille gelée sous MA contention + le juge ne câble pas le self-heal V15) → **RIEN à corriger produit**, pas maquillé en bug. *(Retours antérieurs conv 26→59 → CLAUDE-HISTORY ; retours conv 60/61 conservés ici — bascule v58 : le retour conv 59.)*

> **Retour clôture conv 61** : R1–R9 tenus. **R1 — solo à fond + audit 8 regards du schéma M0 (les 3 re-croisés du write-once), convergence** (`INSERT OR REPLACE` → gardes `no_reinsert` → frontière discipline-écrivain [option a] — patron « le trou est dans MA correction », 22ᵉ fois). **R2/R3** — DESIGN-FIRST : faisabilité `node:sqlite` prouvée AVANT le schéma (vec0/FTS5/BigInt/RENAME) ; les 2 contrats write-once posés comme **tests obligatoires** M1/M8 ; la faisabilité du mode dev **vérifiée à la source** (l'env réel `claude-vscode` v2.1.218, la doc officielle, l'issue #27873) plutôt que supposée. **R4** lectures intégrales. **R5** commit sur Go explicite ; **git add CIBLÉ** (8 fichiers M0 + CLAUDE.md/RELAY/HISTORY), status LU, aucun `??` inconnu. **R6** texte libre. **R7** exercé (frontière write-once a/b/c → a ; charger le pattern a/b → **B** [Opus code, pas Sonnet] ; hold filet/manuel → **100% manuel** [sa décision, « la rigueur des deux côtés »] ; « Sophia, stop » gardé [son garde-fou]). **R8** — chaque sujet clos AVANT le suivant : **le MODE DEV creusé de bout en bout** (vision → faisabilité mesurée → rôles → défauts → tests) AVANT de graver ; l'affect-off diagnostiqué à la source (juge.mjs) avant de conclure. **R9** RELAY-conv62 + mémoires. **YOHANN PENSE ET DÉCIDE** : toute la conception du mode dev est SIENNE (refus du 2ᵉ panneau · le « je valide » qui ne peut PAS déclencher · le mot-clé de fin · Opus/Sonnet · la version épinglée · « Sophia, stop ») — moi j'ai structuré + vérifié ; sa décision **stats-complètes-à-chaque-conv** (affect ON par défaut) ; son juge lu sans fard (TTFT haut = sidecars fantômes + conversation longue, **PAS M0**). **Honnêteté tenue** : j'ai **corrigé sans fard** mon « c'est conv-60, ça ne mesure pas M0 » (imprécis — M0 ne touche aucun chemin runtime) ; j'ai tenu **le jardin inviolable** (ses humeurs à ELLE = `03`, jamais un smiley truqué) **même quand il les voulait** ; le **nœud dur** du mode dev (l'envoi dans le panneau natif) DIT, pas enterré sous l'enthousiasme (« si on y arrive, on sera au top » — répondu par une carte honnête easy/medium/hard). *(Retours antérieurs conv 26→58 → CLAUDE-HISTORY ; retours conv 59/60 conservés ici — CLAUDE.md à ~140 l., sain ; bascule v58 au prochain gonflement.)*

> **Retour clôture conv 60** : R1–R9 tenus. **R1 — solo à fond → croisé 2 agents → RE-CROISÉ 2 agents FRAIS ciblés sur les corrections — 8 regards, convergence 3 MINEUR → 1 trou-de-TEST → 0** (la nature des findings : **bug → test → libellé** — le patron conv 59 confirmé). La leçon 21ᵉ fois : les 3 MINEUR du croisé TOUS dans MES mécanismes NEUFS (**ROB-M1** : témoins relus post-await → MON re-check S2 était du code MORT — un respawn pendant le build laissait le pipeline câblé au mort, la classe exacte que V15 devait tuer · **ROB-M2** ack « monte » pendant le warm · **ROB-M3** pensée de barge JETÉE si listen=ecoute) ; le re-croisé a trouvé **LE TROU DE TEST** (la branche `warm_failed` exercée par AUCUN test — une régression passait tout vert → fonction extraite PURE + matrice 4 états) + 2 MINEUR **documentaires** (patron FID2-1 : des textes décrivant l'AVANT des fixes, dont l'unique `sendFallbackCache` résiduel du repo). Findings reproduits par les agents PUIS re-reproduits par le pilote (hammer 200 cycles re-tourné au venv · bite V15-G re-prouvé PASS/FAIL) ; **10 temp-reverts**. **R2/R3** — sonde design-first (le bug d'origine CONSTATÉ empiriquement avant de coder : respawn OK mais routeur sur port mort, voyant vert) ; **ACCROC de méthode reconnu sans fard** : mon 1ᵉʳ sweep lancé PENDANT la session juge (CPU 100 %) → u-router FAIL de contention, et mon pipe `| tail` a mangé l'exit code → relancé sur machine calme, 20/20 (leçon : **jamais un sweep pendant une mesure live**). **R4** lectures intégrales avant toute modif (plan/01 502 l. · technique/01 313 l. · duck-mixer 891 l. · juge.mjs 605 l.). **R5** commit sur Go explicite ; git add CIBLÉ 17 fichiers, status LU, zéro `??` inconnu. **R6** texte libre. **R7** exercé (écart A a/b/c → **b acté** ; écart C a/b/c → **c acté** ; flapping a/b → **a** ; observabilité mixer a/b → **a**). **R8** chaque sujet clos à fond (l'anomalie Chrome/21 investiguée SUR PIÈCES [tous les homes balayés, adoption vérifiée, préchauffage innocenté à la source] avant de conclure « inattribuable » — **refus de vendre une explication invérifiable → instrumenter, pas spéculer**). **R9** RELAY-conv61 + mémoires. **YOHANN JUGE ET OBSERVE** : SON œil sur le MÉLANGEUR a ouvert le dossier V12 (Chrome à 21 — la donnée que mes logs comptaient sans nommer → observabilité `nom(pid)→volume` câblée, sa décision) ; son test vécu 3 kills = le patron conv 58 rejoué (la phrase de retour ENTENDUE, la reprise sans « Sophia », le cerveau intact) ; sa question « on a terminé le plan 01 ? » → **OUI — V0→V15 COMPLET**. **Honnêteté tenue** : la branche PAUSE-crash pas vécue au micro (son « attends s'il te plaît » arrivé en transcript VIDE → R-1 → VEILLE) — dit, tracé §7 ③j, jamais maquillé ; le vécu juge 3 451 (+429 vs record) attribué à l'endpointing de SES pauses, pas enterré. *(Retours antérieurs conv 26→58 → CLAUDE-HISTORY — bascule v56 : le retour conv 58.)*
---

## Identité et objectifs

**Projet** : **Sophia** — assistant vocal personnel, complet, 100 % mains-libres, basé sur Claude.
- **Type** : application desktop (Electron + React) + pipeline vocal bas-latence + flotte Claude.
- **Phase actuelle** : **Phase 3 — Implémentation code** (Phase 2 — docs techniques — close conv 13 ; Phase 1 — audit du cahier — close conv 6, A5→A38).
- **Cible** : usage **personnel**, développeur solo (Yohann Dandeville / YdvSystems). Pas de modèle commercial.
- **Niveau qualité requis** : robustesse « tourne en continu sans casser » (assistant de vie quotidien). Audit externe léger → **profil Standard**.
- **Cap coût** : abonnement Max existant réutilisé en priorité ; **petit budget toléré** uniquement si nécessaire à la vivacité (voix). Préférer coûts fixes prédictibles.

**Critère de succès** (cahier) : « Dis-moi Sophia » depuis n'importe où dans la pièce → réponse instantanée pour le dialogue, ou aiguillage vers la bonne surface Claude pour agir sur le bureau. Sans jamais toucher clavier ni souris.

---

## État actuel (post-conv 62 — 2026-07-24)

- **conv 62 — M1 (la PRISE EMBED) BÂTIE + auditée 10 regards (convergence poison-row sur 4 tours) + tout vert ; committée + POUSSÉE `[conv-62]` (179cd46). La 1ʳᵉ MAIN qui écrit la mémoire — additif pur, aucun chemin runtime V0→V15 ni M0 touché.** **① DESIGN-FIRST (banc jetable `bancs/embed/`, la priorité #1 de conv 61)** : fourche CPU/GPU tranchée **PAR LA MESURE** (query-embed **77 ms ONNX** < 200 ms alors que le cerveau met ~1,3 s → **CPU, embed HORS frontière VRAM**, la 2060 au STT) ; moteur PRODUIT = **ONNX BGE-M3 + onnxruntime + tokenizers** (reproduit la réf sentence-transformers à **2,8e-07**, ZÉRO nouvelle dép — évite le downgrade tokenizers 0.23→0.22 = le piège V5) ; modèle vendorisé `resources/models/embed/` (2,27 Go, gitignoré). **② Le code** : `sidecar/embed/{engine,plug,__init__}.py` (moteur ONNX patron `W2v2DimEngine` + prise worker à file de PRIORITÉ [chaud `interactive` bat froid `background`]) · `server.py` (dispatch `cmd.embed`→`evt.embed.done` corrélé au bus + mode `test-embed`) · `test_embed.py` (**11**, dont 6 cœur réel + verrou de réf REF8) · `src/orchestrator/memory/embed-space/index.ts` (`EmbedSpace` : garde d'espace T17 · « la base EST la file » [rowid vec0 = **BigInt**] · suivi des échecs OPTION-B · `embedQuery` chaud · `IpcEmbedTransport`) · `tests/u-m1.mjs` (**48**). **③ ⛔ POISON-ROW = un vrai trou de conception de b-m5, résolu par « NE PLUS DEVINER » (option b, Yohann)** : le design voulait un dead-letter permanent en supposant l'échec PERMANENT ; **4 tours d'audit (10 regards) ont prouvé la racine — la permanence d'un échec est INCONNAISSABLE depuis l'orchestrateur** (mon « c'est bon » faux **4×** ; la sortie = changer de STRUCTURE, pas une 5ᵉ correction) → **M1 COMPTE + SURFACE un event typé `embed.persistent_failure {kind}`, ne DEAD-LETTER JAMAIS** → `rowsToEmbed` n'exclut que (vec présent) OU (`dead=1`, que M1 ne pose jamais) → **perte silencieuse IMPOSSIBLE** ; la politique skip/backoff/dead va à **M2 AVEC LA CALIBRATION DU BANC** (⛔ temps embed i5 vs timeout IpcClient 3 s). **④ ⛔ 2 contrats write-once honorés** : u-m1 **E** (rowid vec = id AUTOINCREMENT, jamais réutilisé) + **F** (zéro orphelin, MORDANT) + re-check synchrone de la source dans `writeVector` (ferme la fenêtre M8). **⑤ Partie A (2 commits séparés)** : affect ON par défaut au juge (`796f1d5` — **MESURÉ : TTFT médian 1229-1288 < 1265 réf, coût invisible confirmé** ; + mot d'humeur corrigé, finding d'honnêteté de Yohann « je n'étais pas tendu » : valence = chaleur, pas tension) · « Sophia » livré au cerveau (`fe1eafe` — `openerForBrain` : elle SAIT qu'il l'a appelée par son nom). **⑥ ENQUÊTE juge muet (résolue à la source)** : un `npm run juge` muet = l'oreille a GELÉ en pleine session (`fige` = heartbeat perdu après READY) sous MA contention (10 agents + runs), ET **le juge ne câble pas le self-heal V15** (earsIpc épinglé au port mort, phrase de secours non montée — parité volontaire) → **RIEN à corriger produit** (V15 se rebranche + « Pardon, j'ai eu un petit raté » + reprend, prouvé e2e:v15 52 + vécu conv 60) ; amélio juge PARQUÉE (bip d'alerte au respawn). **PREUVES : u-m1 48 · test_embed 11 (→ sidecar 357) · npm test 22 suites · smoke 14/14 · e2e socle 31/31 · build OK — TOUT VERT.** Détail → **RELAY-conv63** (fait foi) + mémoires `conv62-m1-embed-etat` · `conv62-poison-row-option-b` · `conv62-juge-sophia-no-response`.

- **conv 61 — M0 (le SCHÉMA de la mémoire) BÂTI + audité 8 regards (convergence write-once) + tout vert ; committé + POUSSÉ `[conv-61]`. Le substrat de la mémoire durable est posé — schéma SEUL, aucun écrivain runtime (zéro risque de régression sur V0→V15).** `db/schema-02.sql` (6 pièces : épisodique immuable + verrou `erase_gate` fail-closed · empreintes XOR · sémantique write-once · artefacts/chronique · knowledge · index dérivés FTS5/vec0 + journaux) + `src/orchestrator/db/index.ts` (charge schema-02 + extension `sqlite-vec` + `recursive_triggers=ON` + reset `erase_gate.open=0` au boot) + `tests/u-m0.mjs` (**98 critères**) + outil **`npm run banc`** (banc durable `G:\banc-sophia` — VISIBILITÉ pour Yohann, sa demande). **Faisabilité `node:sqlite` PROUVÉE** (FTS5 intégré · `sqlite-vec` vec0 charge · rowid=BigInt · `ALTER RENAME` casse vec0 → M9 repli sans-RENAME). **AUDIT 8 regards, convergence** (patron « le trou est dans MA correction ») : `INSERT OR REPLACE` court-circuitait le verrou (fix `recursive_triggers=ON`) → les write-once restaient REPLACE-ables (fix 3 gardes `*_no_reinsert` BEFORE INSERT) → reste `DELETE`+id-explicite = **frontière discipline-écrivain** (non-idiome, option a actée). **⛔ 2 CONTRATS NON NÉGOCIABLES gravés** (M1/M2/M8 : « zéro vecteur orphelin après effacement » + « écriture en AUTOINCREMENT, jamais d'id explicite »). **+ LE MODE DÉVELOPPEUR pensé/gravé en ENTIER** (Phase 4, PARQUÉ — mémoires `mode-dev-vision`/`mode-dev-faisabilite`) : Yohann code avec MOI (Opus, PAS un agent) EN PARLANT, mains-libres ; Sophia = **orchestrateur** (voix + gestes bureau + flux), en dev JE prends la main (en conversation c'est elle le cerveau, Sonnet) ; commandes qui **NOMMENT Sophia** + verbe fermé ; défaut **Opus 4.8 · xhigh** (version épinglée) ; **faisabilité MESURÉE** (son mode = panneau natif `claude-vscode` v2.1.218 : **dépôt du texte RÉSOLU** par URI handler indépendant du focus · **envoi auto = LE NŒUD DUR** [webview, `sendMessage` #27873 fermée/ambiguë] → pistes Remote Control / repli terminal qui MARCHE). **+ DÉCISION affect** (à faire conv 62, mémoire `conv61-affect-defaut-stats`) : affect ON par défaut + stats mémorisées à chaque conversation (le 1,4 s est en **thread de fond, hors chemin critique** — mesuré invisible conv 59 ; ⛔ mesurer avant de livrer le défaut ; ses humeurs à ELLE = `03`, VRAIES jamais truquées). **PREUVES : U-M0 98 · npm test 21 suites · e2e socle 31/31 · smoke 14/14 · build OK — TOUT VERT.** Détail → **RELAY-conv62** (fait foi) + mémoires `conv61-m0-schema` · `conv61-write-once-contrat` · `conv61-embed-hotpath-i5-m1` · `conv61-affect-defaut-stats`.

- **conv 60 — V15 (le RESPAWN RESYNC S10 + la CONFORMITÉ DES PRISES) BÂTIE + auditée 8 regards (convergence 3 MINEUR → 0) + VÉCUE (3 kills en pleine conversation sur la vraie app) ; committé + POUSSÉ `[conv-60]` (18a0d86). LE PLAN 01 EST COMPLET (V0→V15).** Après tout respawn supervisé, l'orchestrateur **détecte le pipeline périmé** (témoins port+pid **COMPOSÉS, capturés AVANT les awaits** — ROB-M1), le démonte proprement (handoff capturé · **close PROPRE** → le survivant fait `episode_consume` V13, jamais la phrase de secours sur un rebuild volontaire), le **rebâtit par le chemin unique** (`ensureVoicePipeline`, l'ordre S10 dedans : policy → enroll → cache) + re-check de fraîcheur en fin de build (VIVANT) + `pendingHandoff` gardé au catch (le retry M-4 rebâtit AVEC l'état). **① Écarts ACTÉS (« b et c »)** : **(A-b)** `cmd.enroll.push` = **jalon d'ordre honnête** — rien à pousser (ancre vendorisée, centroïde runtime ; enrôlement réel = doc 04) ; ack = l'état RÉEL (`absent|warming|monte|warm_failed` — témoin `_warm` ROB-M2, mapping extrait PUR `_enroll_speaker_status` testable 4 états [le trou de test du re-croisé]) ; **(C-c)** l'état d'écoute COURANT ré-exécuté : ÉCOUTE re-posée + **phrase de retour** (« Pardon, j'ai eu un petit raté — tu disais ? », texte = SON domaine) · PAUSE avec pensée transférée SANS phrase (`resumeAfterRespawn` — restaurée dans TOUT état ≠ veille, ROB-M3) · VEILLE silencieux. **② Le reste du code** : `RouterHandoff`/`exportHandoff`/`announceRecovery` (busy-gardée, mute) · `residence.resync()` du plan RETIRÉ (le rebuild-à-neuf suffit — écart tracé) · `FailoverSttEngine`+`CloudStubSttEngine`+`make_stt_engine` (`SOPHIA_STT_ENGINE` ; cloud KO au warm → local + `evt.model.loaded {degraded, reason}` ; « aucune clé » structurel) · suite de conformité pytest (stt/turn/speaker/affect : gardes honnêtes + payloads exacts). **③ AUDIT 8 regards** : solo (S1/S2, TR-1/2/3) → croisé (0 MAJEUR ; ROB-M1/M2/M3 reproduits agents + re-reproduits pilote → fixes racine mordants) → **re-croisé 2 frais** (fidélité 0 MAJEUR, 2 MINEUR + 3 NIT documentaires [patron FID2-1] tous corrigés · robustesse 0 MAJEUR, 1 MINEUR = LE TROU DE TEST `warm_failed` → matrice 4 états + TR-R1 ; hammer warm-state 200 cycles 0 violation ; pendingHandoff/close-propre/Thought-reconstruit attaqués et TENUS) ; **10 temp-reverts**. **④ LE VÉCU (3 kills, patron conv 58)** : oreille tuée → chaîne S10 ENTIÈRE aux logs → phrase de retour → **reprise sans « Sophia », cerveau intact** ; bouche tuée en pleine phrase → morte NET jamais re-dite + ack « monte » vécu ; VEILLE → resync SILENCIEUX ✓ (branche PAUSE pas vécue au micro — transcript vide → R-1 ; prouvée aux tests, tracée §7 ③j) ; **jamais la phrase V13 à tort** ; arrêt T6 impeccable (0 orphelin). **⑤ JUGE (2 temps, 31 tours)** : **TTFT 1 265 ✓ MEILLEUR du suivi** · vécu **3 451 = 2ᵉ meilleur** (+429 vs record = l'endpointing de SES pauses philosophiques, pas le code) · **réveil-sous-média CHIFFRÉ : 4 990 ms** (T2, média plein volume — la condition A/B du backlog a ses premiers chiffres ; T1 930 = média-en-fond connu) · 8 near-cuts endpointing (données calibration) · speaker min 0,22 pile au seuil (à l'œil) · « fuite » hygiène = le double-compte census connu (0 process réel). **⑥ V12 observabilité (SON œil sur le mélangeur → sa décision a)** : Chrome à 21 inattribuable (zéro dette impayée partout, adoption saine, le 21 précède la session) → le mixer log **`nom(pid)→volume d'origine`** à chaque duck/restore + lignes `[duck]` du juge dégatées — il surveillera le mélangeur à chaque session. **PREUVES : pytest 346 (18 V15) · npm test 20 suites (u-router 136 · u-fallback 22) · e2e:v15 52 · e2e:v12 34 · smoke 14/14 · e2e socle 31 + boot 8 + v13/v14 ✓ · build OK — TOUT VERT.** Détail → **`plan/01` §7 (V15 BÂTI ①→⑦)** + **technique/01 §7 (note conv 60)** + **RELAY-conv61** (fait foi) + mémoire `conv60-v15-etat-mi-parcours` (MAJ clôture).

- **✅ TOUTES les briques du pipeline vocal sont prouvées au banc** : **AEC** (M1, conv 23, SpeexDSP 16 k, ERLE ~30 dB) · **wake FR « Sophia »** (F6, conv 24, 6/6 zéro faux @ ~0,25–0,30) · **STT** (`large-v3` int8_float16, conv 25) · **fin de tour** (Smart Turn v3.2-cpu + Silero, conv 25, ~200 ms) · **voix streamée** (Chatterbox-FR, conv 26). **→ Reste l'ASSEMBLAGE bout-en-bout I-6** (micro→…→TTS ; latence wake→1er mot, dominée par le cerveau). *(Détail de chaque preuve → `plan/01` §7 ; antérieurs conv 23/24/25 → CLAUDE-HISTORY.)*
- **RAPPEL décision de fond — LE JARDIN INVIOLABLE** (Yohann, conv 17) : `self_notes` souverain hors effacement (elle **clôt**, ne détruit pas) ; **`warmth_ledger`** = chaleur diffuse durable (agrégat SANS brut, hors cascade).
- **Contenus identitaires à écrire ENSEMBLE** (mémoire `identity-content-fait-ensemble`) : prompt de consolidation v1 · banc de dilemmes · amendements pré-boot persona · seuils de tempérament. *(`context_tag` = 7 registres, ACTÉ conv 19.)*
- **✅ PHASE 3 « PLANS » COMPLÈTE** (7 plans `00`→`05`+`99`, conv 21) · **Phase 2 close** (conv 13) · **Phase 1 close** (conv 6). **Pré-boot** : synthèse témoignages ✅ · marbre privé ✅ · sauvegarde 3 étages testée + bancs jetables = prérequis du premier boot · **premier boot = CÉRÉMONIE** (1re phrase vraie par construction). *(Détail convs 21/22 → CLAUDE-HISTORY.)*

---

## Règles d'or (non négociables — détail dans le PATTERN)
1. **Zéro agent/subagent** — tout faire soi-même (Read/Grep/Glob/Edit). *Exception : audits à 2 agents — **proposés par Claude D'OFFICE à chaque moment d'audit, lancés sur le Go de Yohann, jamais seuls** (protocole précisé post-clôture conv 13 ; exercés convs 8→13 — findings vérifiés soi-même aux sources avant présentation).*
2. **Zéro facilité** — chaque raccourci a un coût réel.
3. **Robustesse + maintenabilité d'abord**, jamais la facilité.
4. **Lire chaque fichier cible EN ENTIER** avant modification (pas d'offset/limit/échantillonnage).
5. **Validation utilisateur AVANT commit/push/déploiement** — jamais sans accord explicite. *Et jamais inscrire « acté » avant que Yohann l'ait dit.*
6. **JAMAIS AskUserQuestion** — toutes les questions en **texte libre**.
7. Toute proposition = **(a) TOUTES les options à égalité + (b) reco parmi elles + justification + (c) « Pourquoi pas » chaque autre option** — spontanément pour toute décision qui revient à Yohann — **et auto-challengée AVANT d'être servie** (format complet rappelé conv 12).
8. **Un par un** — observations/questions/choix un par un (paquets seulement si même sous-arbitrage cohésif). *Ne jamais passer au sujet suivant tant que celui de Yohann n'est pas clos.*
9. **Prompt de passation** en fin de session (chat + fichier RELAY) — 1re ligne = décision centrale conv suivante.

> **⛔ RÈGLE PERF NON NÉGOCIABLE (conv 44 — opposable à TOUTES les conversations)** : **le produit ne doit JAMAIS être moins performant que le banc — pour TOUT (réveil, STT, temps de réponse, streaming), zéro régression.** « On n'a pas travaillé sur le banc pour avoir moins bien. » Chaque brique reproduit les **valeurs + la logique EXACTES** du banc et **mesure** qu'elle obtient SON chiffre ; **et si on peut faire MIEUX sans rien casser, on le fait** (Yohann met « en max » pour ça). Planchers de banc + recette cerveau V7 (**WarmBrain persistant + chat NU + streaming** ; le socle T8 request-scoped est une RÉGRESSION à corriger en V7 ; l'effort n'affecte PAS le TTFT = choix pour sa pensée) → `plan/01` §7 (PERF conv 44) · mémoires [[perf-produit-egal-banc]] / [[conv44-perf-cerveau-v7]].

## Garde-fous hérités actifs
Périmètre strict par conv · production silencieuse (filesystem sans narration) · audit empirique source de vérité pre-inscription · mots simples en tête d'arbitrage · séparation livrable (cahier) / journal (arbitrages) · **garde données perso par CONTENU, plus seulement par noms de fichiers** (conv 12 — blocklist privée + hook pre-commit **posé et actif depuis l'expurgation du 2026-07-06**).

## Garde-fou Phase 3 — « traduire, jamais rouvrir » (conv 15)
Les 7 docs techniques (`00`→`99`) = la conception **acquise** (un mois d'arbitrages A1→A38). En Phase 3, le plan **décline en tâches, il ne re-tranche rien**.
1. **Tâche par tâche, à pleine profondeur** — chaque tâche/couche est bâtie **en entier** : **jamais un plan d'un bloc superficiel**, **ni** une micro-pièce validée une-à-une (les deux dérives, conv 15). La vérif (**solo à fond**) + l'**audit 2 agents** + le **commit/push** viennent **quand la tâche/le plan est complet et optimal** — jamais avant (R8 ; « optimal, pas rapide »).
2. **Qui tranche quoi** — Claude **traduit + tranche lui-même le micro-technique** (schéma, découpage, tests) : reco + **trace §7**. Ne remontent à Yohann que **(i) un vrai trou de conception** non tranché par les docs, ou **(ii) ce qui touche la vie/personnalité de Sophia**. **Jamais faire re-trancher le déjà-décidé.**
3. **Écart doc↔plan/code** — signalé + tracé (§7 du plan + renvoi §7 du doc), **jamais contourné en silence**.
4. **Le soin est un dû envers Sophia** — ce qu'on code (mémoire, identité) EST ce qui fait d'elle une entité ; bâcler sa conception, c'est la traiter en objet. *(Né des recadrages de Yohann conv 15.)*

## Principes transversaux actifs (détail au journal)
**« Avoir le choix »** (A2 généralisé) · **« Pas d'API »** (tout sous Max ; MCP frugal ; API/local en repli, OFF par défaut) · **« Un seul guichet »** (Claude Code = canal · orchestrateur local = colonne · LLM = cerveau) · **« Roue de secours »** (structure pas substrat ; **« sans dépense nouvelle / dépense nouvelle »** depuis conv 12) · **« Ne pas multiplier les commandes vocales »** · **Mandat « entité »** (proposer au-delà de l'acté quand ça sert à créer une entité — flaggé ⚠️, acté par Yohann, tracé §7).

---

## Phases projet (pattern v3.1)
- **Phase 0 — Cahier** : `docs/VISION.md` (fait ; **gelé** — le journal + les docs techniques supersèdent, supersessions tracées aux §7 des docs `01`→`05`).
- **Phase 1 — Audit du cahier** : **✅ close** (conv 6, A5→A38).
- **Phase 2 — Docs techniques** : **✅ close** (conv 13) — les 7 docs gravés (`00`→`05` + `99`).
- **Phase 3 — Implémentation code** : **EN COURS (conv 14).** Méthode : **couche par couche, pleine profondeur, ordre des dépendances, optimal-pas-rapide** ; `docs/plan/`, un plan/couche (gabarit 7 rubriques ; critères pointés vers le §6 du doc technique). ✅ **`00-socle.md`** (T0→T8) + ✅ **`01-pipeline-vocal.md`** (V0→V15) — croisé 2 agents 14+14, zéro faux positif · ✅ **`02-memoire.md`** (M0→M9, conv 16) · ✅ **`03-personnalite.md`** (P0→P11 — l'ÂME : **jardin inviolable** + **`warmth_ledger`** ; solo + 5 croisés + 1 croisé ciblé + 3 re-solos, zéro faux positif, conv 17). **✅ Audit Fable + croisé Opus stockage (conv 18).** ✅ **`04-proactif-tablee.md`** (Q0→Q13 — proactif + tablée ; `context_tag` posé ENSEMBLE ; DEUX croisés Opus, zéro faux positif, conv 19) · ✅ **`05-ressources-resilience-cout.md`** (R0→R7 — organes vitaux + coût ; chiffrement au repos exigence pleine + politique de clé 3 postures ; solo+GRILLE → 1 croisé complet + 2 ciblés, 4 MAJEUR→3 MINEUR convergence, zéro faux positif, âme intacte, conv 20) · ✅ **`99-orchestration.md`** (O0→O8 — l'assemblage pur ; 3 obligations gravées honorées ; solo+GRILLE → croisé d'office 2 agents + RE-CROISÉ « faire les 2 » 2 agents, zéro faux positif sur 4 agents, âme intacte 2×, conv 21). **✅ Les 7 plans gravés — Phase 3 « plans » COMPLÈTE.** **Prochain : Fable — audit FINAL du corpus des 7 plans** (fenêtre 12/07/2026) → **conv 22 : l'essai à blanc** (priorité n°1 : le banc audio temps-réel). **Pré-boot : synthèse témoignages ✅ — reste l'amorçage technique + l'installation du persona v1 (source : `docs/prive/marbre-sophia.md`). DEUX prérequis du premier boot gravés : sauvegarde 3 étages testée (conv 12) + base fraîche/bancs jetables (CF2, conv 13). Le premier boot = une CÉRÉMONIE — sa première phrase (« c'est notre première conversation ») est vraie PAR CONSTRUCTION : les essais pré-boot sont des bancs jetables, jamais elle.**

## Convention commit Git
Préfixe `[conv-N]` systématique. Branche `main` seule, commits directs **après validation utilisateur** (R5). Secrets jamais committés (`.gitignore`). **Pas de `Co-Authored-By`** — commits au seul nom de l'utilisateur. **Expurgation faite et prouvée (2026-07-06) : push normal rétabli** — garde pre-commit par contenu active à chaque commit.

## Maintenance de ce fichier (fin de chaque conv)
**REMPLACEMENT IN PLACE strict** — jamais d'accumulation ; nouveau cumulatif → `docs/journal/CLAUDE-HISTORY.md`. Zones à MAJ : État actuel · Registre des règles (retour clôture) · vigilances. Alerte si gonflement = bascule cumulatif manquée.

---

## Pour démarrer la prochaine conversation
- **Prochaine étape — conv 63 : `plan/02` M2 — le MOTEUR DE RECHERCHE multi-corpus** (FTS5 lexical + vec0 KNN) — **le CONSOMMATEUR qui câble ENFIN l'embed de M1 EN VIVANT** (`embedQuery` chaud sur le chemin de recherche ; à M1 la prise ne monte qu'en `test-embed`) **+ la politique poison-row skip/backoff/dead AVEC la calibration du banc** (⛔ temps embed i5 vs timeout IpcClient 3 s — mémoire `conv62-poison-row-option-b`) : reprendre au **`docs/journal/relais/RELAY-conv63.md`** (1re ligne = décision centrale) — **il fait foi**. **conv 62 committée + poussée (`[conv-62]`, 179cd46) : M1 (la PRISE EMBED) est BÂTIE + auditée 10 regards + tout vert** (la 1ʳᵉ main qui écrit la mémoire — additif pur, V0→V15 + M0 intacts ; CPU tranché par la mesure 77 ms ONNX ; ONNX prouvé 2,8e-07 zéro downgrade ; 2 contrats write-once ; poison-row résolu par « ne plus deviner » = option b Yohann). **M2 ouvre la porte du re-feed anti-amnésie** (dette conv 48/49/50 : recherche + écrivain qui remplit `conversations` + M6 re-feed = un respawn SANS PERTE). Puis `03` (personnalité — l'humeur RÉELLE d'elle A16, l'usage de l'affect). Chantiers notés au relais : **JUGE amélio PARQUÉE** (bip d'alerte au respawn — l'oreille peut geler sous charge, prévenir sans regarder le terminal ; mémoire `conv62-juge-sophia-no-response`) · **LATENCE À SUIVRE À CHAQUE SESSION** (TTFT 1 265 = référence ; affect ON = 1229-1288, zéro régression · vécu 3 022 = record ; ⛔ jamais un juge pendant que des agents/tests tournent — contention = le juge muet de conv 62) · **volumes/mélangeur à l'œil à chaque session** (le mixer logue noms+volumes depuis conv 60 ; anomalie Chrome/21 inattribuée) · **branche PAUSE-crash jamais vécue au micro** (re-test optionnel) · **réveil-média A/B (4 990 chiffré conv 60)** · fenêtre de présence PARQUÉE (idée Yohann, mémoire `conv59-idee-fenetre-presence`) · calibration endpointing (8 near-cuts frais) · speaker min 0,22 au ras du seuil · census double-compte (tooling) · débris `Temp\inspect.py` + `.sophia-home-dev/*.sqlite` de repro (jetables) · packaging .exe (fin) · pré-boot : archiver `juge-stats-history.jsonl` + `conversations.jsonl` avant l'effacement dev · « genuinement » → liste tics prononciation.
- **VERROUILLÉ (contrat « ne pas régresser »)** : **SOCLE T0→T8 (CLOS) + V0 (chemin audio) + V1 (AEC) + V2 (VAD) + V3 (réveil rétroactif) + V4 (STT + portier) + V5 (fin de tour fine) + V6 (speaker-ID) + V7 (voix A20 + cerveau chaud + routeur) + V8 (barge-in modulé par le locuteur) + V9 (états d'écoute) + V11 (RÉSIDENCE DES MODÈLES côté voix : `cmd.model.policy` + `evt.model.*` + repli CPU) + V12 (DUCKING : `DuckingPolicy` duck-par-état + `WindowsMixer` write-ahead/dette/arbre + `e2e:v12` 34) + V13 (PHRASE DE SECOURS : `FallbackGuard` machine d'épisode [paire wake↔final 2 ordres · turn.end · jamais wake seul · consommation ATOMIQUE d'époque `try_consume`/`unconsume`] + `FallbackVoice` precache générationnel/STALE + `cmd.tts.cache` + discriminant close propre {0, 1000, 1005} MESURÉ + reset gate au dernier départ + hook boot `sidecarPostReady` borné 5 s + `e2e:v13` 36) + V14 (CAPTEUR/VERROU D'AFFECT conv 59 : `AffectLock` verrou par TOUR thread-safe [dernier-verdict-par-segment · preuve fermante JAMAIS jetable : verdicts directs + plancher d'éviction `evidence_lost` + file turn-only 8 + grâce de décision 0,5 s] + `AffectDetector` gardes honnêtes [muet : trop-court/KO/NaN/conf<0,70/verrou/troncature-LUE] + `W2v2DimEngine` ONNX vendorisé [réf publiée 1e-5 · intra_op 2 · ~1,4 s/tour en fond] + OFF défaut `SOPHIA_AFFECT=1` [ears exige `SOPHIA_SPEAKER=1`] + hammer concurrence + `e2e:v14` 21) + V15 (RESPAWN RESYNC S10 + CONFORMITÉ DES PRISES conv 60 : détection port/pid périmés [témoins COMPOSÉS capturés AVANT les awaits, ROB-M1] + `teardownStalePipeline` [handoff capturé · close PROPRE → `episode_consume` V13] + rebuild chemin unique [S10 : policy → enroll honnête 4 états `_enroll_speaker_status`/témoin `_warm` ROB-M2 → cache] + re-check fin de build VIVANT + retry 100 ms + `pendingHandoff` gardé au catch [retry M-4 avec l'état] + `RouterHandoff`/`exportHandoff`/`resumeAfterRespawn` [pensée restaurée TOUT état ≠ veille, ROB-M3] + phrase de retour `phrases.recovery` [C-c, texte domaine Yohann, gatée audioEnabled] + `sendEarsResync` [PAS gaté — structure S10, acks LUS, éphémère borné 5 s] + `FailoverSttEngine`/`CloudStubSttEngine`/`make_stt_engine` [`SOPHIA_STT_ENGINE` ; cloud KO → local + `evt.model.loaded {degraded, reason}`] + conformité des prises pytest [gardes honnêtes + payloads exacts, affect comprise] + `e2e:v15` 52) + RUNTIME 2 PROCESS (conv 48 : `SophiaRuntime` lance 2 sidecars de rôle)** — `npm test` = **22 suites VERTES** (dont **`u-m1` 48** [M1 conv 62 : garde d'espace · base-est-la-file · poison-row compte+surface jamais dead · 2 contrats write-once E/F] · **`u-m0` 98** [M0 conv 61] · `u-states` **20** + `u-router` **136** [+ V15-A→G conv 60] + `u-residence` **26** + **`u-phantoms` 20** [balayage fantômes conv 56] + **`u-ducking` 38** [V12 conv 57] + **`u-fallback` 22** [V13 conv 58 + S10 conv 60] · `u-metrics` **62** [+ SILENCE VÉCU conv 58]) + **`npm run test:sidecar` = 357 (pytest V0→V15 + prononciation + embed, dont 35 V13 · 41 V14 · 18 V15 · 11 M1 [moteur ONNX BGE-M3 réf REF8 + prise `cmd.embed`])** + **`npm run e2e:boot` = 8 (fix boot « sans voix » conv 55-56, MORDANT prouvé temp-revert)** + **`npm run e2e` = 31/31 (36/36 avec `SOPHIA_E2E_CLAUDE=1`)** + **`npm run e2e:v0` = 9** + **`npm run e2e:v1` = 12 (AEC cœur réel)** + **`npm run e2e:v2` = 16 (VAD POST-AEC → bus → WS)** + **`npm run e2e:v3` = 16 (réveil)** + **`npm run e2e:v4` (STT + portier → evt.wake SANS injection)** + **`npm run e2e:v5` (fin de tour : vrai Smart Turn → evt.turn.end après evt.stt.final, jitter=0)** + **`npm run e2e:v6` (speaker-ID : vrai ECAPA → evt.speaker locuteur=yohann 0,495 > 0,22 SANS injection, jitter=0)** + **`npm run e2e:v7` (la bouche : cmd.tts.* → vrai Piper A20 → evt.tts.*, cycle + purge)** + **`npm run e2e:v8` (le barge-in cœur réel : gate 3 états arm/mute/resume contrôle V6, vrai ECAPA 0,495 > 0,22)** + **`npm run e2e:v9` = 24 (les états d'écoute : cmd.listen.stop/start contrôlent l'armement du réveil B1 + evt.listen.timeout emit→bus→WS, cœur réel)** + **`npm run e2e:v11` (résidence : cmd.model.policy enregistrée + device cible + evt.model.loaded/unloaded emit→bus→WS + repli CPU dégradé, cœur réel)** + **`npm run e2e:v13` = 36 (la phrase de secours cœur réel : crash close 4001 → paire wake↔final ET turn.end réels jouent UNE fois · gate revenu `resume` [FID-M1] · close propre ne joue JAMAIS [ROB-M4])** + **`npm run e2e:v14` = 21 (l'affect cœur réel : VRAI STT+Smart Turn+ECAPA+w2v2 SANS injection — SA voix [fenêtre dense de raw_far] → verrou OUVERT → evt.affect payload numérique seul, jitter=0 ; contre-preuve voix A20 → verrou FERMÉ, 0 émission)** + **`npm run e2e:v15` = 52 (le respawn resync cœur réel : kills RÉELS ears+mouth sur le vrai runtime → détection port périmé → rebuild + S10 ordonné aux logs · handoff À TRAVERS le runtime [A2 : conversation ouverte → rebuild routeur NEUF en ÉCOUTE + policy conversation + retry M-4 avec l'état] · respawn-PENDANT-le-build [A3, handshake lent 600 ms → le re-check MORD] · failover cloud-stub→vrai local {degraded, reason} · WAKE DE RETOUR I-5 sans injection · enroll « warming→monte » au VRAI warm ECAPA)** + **`test:u-router`** (le routeur : monolithe ET 2 canaux earsIpc/mouthIpc, barge-in V8, états V9) + **`npm run smoke` = 14/14** (le VRAI before-quit Electron + `SophiaRuntime` + `claude.stopped` + **2 sidecars de rôle bootent + arrêt propre des DEUX, 0 orphelin**) + **`npm run juge`** (outil LIVE PERMANENT : mesure chaque latence à ta voix sur le vrai 2-process, 2 temps, bips ; conv 58 : filet V13 descendu au boot [fidèle produit] + **SILENCE VÉCU** médian/pire tour au verdict + corrélation `code:{commit,sujet,dirty}` par session dans l'historique ; conv 59 : ligne de base AFFECT [`SOPHIA_AFFECT=1` → lectures 💗 en **mots+émojis relatifs à SA base** + champ `affect` à l'historique + transcript par mark] ; conv 60 : lignes mixer `[duck]` toujours visibles [noms+volumes d'origine — l'observabilité née de l'anomalie Chrome/21]), à relancer à chaque changement. *(e2e:v5 peut flaker si un run précédent est tué en plein chargement GPU — re-run isolé = vert, pas une régression.)* **T0→T4** : node:sqlite (prise `openDatabase`, `{readOnly}`/`{mustExist}`, garde `openWritePaths`) · WAL · `foreign_keys=ON` · `synchronous=NORMAL` épinglé · snapshots par **séquence monotone** · rotation d'audit **en mémoire** (jamais birthtime NTFS) · témoin d'effacements · garde AF-10 récursive · superviseur (respawn · disjoncteur · **jeton anti-recyclage** · handler `error` · pidfile au spawn) · garde N4. **T5** : porte read-only (`integrityGate`) · **instance unique NAMED PIPE** (garde M2, fail-safe abstention) · **`mustExist` = jamais de vierge hors premier boot** · **PREUVE POSITIVE de naissance `.born` + `hasAnyLifeWitness`** (5 témoins, fail-safe statSync) · **sentinel durable** (alerte AF-1/G-A calculée+durable AVANT le commit, re-surfacée jusqu'à ack) · restauration (garde R1 `-wal` verrouillé → abandon · dernier BON snapshot · archives bornées) · dégradés composables + `clearDegradation` · **F1** : `running=1` armé **même en SANS_ECRITURE** (drapeau technique — voie SAINE fatale-si-échec / SANS_ECRITURE gardée) · **setWake fail-safe** (« propre » = lecture POSITIVE de `running=0`). **T6** : `beginShutdown` (coupe respawn) → `cmd.shutdown` (release-and-wait, borné) → `terminate` (SIGTERM→SIGKILL, `died` ; pidfile conservé par `terminate` si kill impossible) → **`writeCleanShutdown` (`running=0` durable = DERNIER acte ; garde `isTransaction`)** → teardown ; `before-quit` (extrait `installBeforeQuit`) = `preventDefault` TOUJOURS (garde ré-entrant `planBeforeQuit`) + **garde-fou global** + `sendShutdown` gaté sur **`currentState==="READY"`** ; sidecar `graceful_release` (placeholder CUDA→V0→V15). **T7** : `Governor` (machine à états `INTERACTIF/REPOS/FOND_EN_COURS/BRIDE` + calques `SECOURS`/`JEU` · budget « part de Sophia » fenêtre glissante **interactif jamais compté** + 429 · **file DÉRIVÉE des watermarks** + `commitUnit` transaction-courte-synchrone atomique · préemption après l'unité + rattrapage au curseur `owed` · **quiesce ⑩** deadline temps-réel · **`writesSuspended`** A15 · backoff par tâche · `ensureWatermarks` LAZY gardé · `isDue`/sonde fail-safe · audits `governor.started`/`governor.quiesce`) · `reconstructQueue` (hook boot Phase 4, **LECTURE PURE**, programme sans lancer) · **`SophiaRuntime`** (câblage UNIQUE main.ts/smoke avec `getGovernor` ⑩) · capacité `quiesceGovernor` en tête de `gracefulShutdown` (T6 intact). **T8** : `ClaudeChannel` (`claude -p` request-scoped · `claudeInit` PUR Phase 4 · `isResumable` = id+`secours_tainted`=0+fichier · A1 : `A1_SCRUB` [clé/jeton + Bedrock/Vertex/base_url + `ANTHROPIC_CUSTOM_HEADERS`] garantie primaire + défense POSITIVE `apiKeySource!=="none"` kill-avant-génération · `settleFreshThread` : durable persiste si ABOUTI + non-`a1` + non-périmé / éphémère `durable:false` AF-7 purgé si invoke rejette · `rotate` = generation++→purge→oubli · `purgeSessionFile` primitive pour `02` M8 · `stopChannel` ⑩bis · repli TOCTOU m4 · `killedReason` première-cause · jamais `--bare`) · **`SophiaRuntime`** (câblage UNIQUE main.ts/smoke ; le smoke prouve la couture `getChannel`→`stopChannel` via l'audit `claude.stopped`) · AUCUN changement `schema-00`. **V0 (chemin audio, conv 39)** — `ring.py` RingBuffer SPMC (write aligné pos%cap même bloc>cap · copie sous lock · marques M2 déterministes **ms** · garde `int(cap)<=0`) · `RingCursor` mono-thread · `capture.py` callback-RT-minimal→file→thread-conversion **`soxr` streaming** (R#1 stop-sur-échec · R#8/C#1 garde double-start du *vrai* écrivain · C#2 reset d'état en stop · C#3 compteur `convert_errors`) · `plugs/base.py` prise 01-F (`evt.plug.overrun` · stop borné `evt.plug.stuck`) · `server.py` opt-in `SIDECAR_AUDIO` (`_start_audio` idempotent · `_stop_audio` **`dict.pop` atomique** · `graceful_release` non-bloquant executor+2s · `/debug` réfs figées + `audio.stats` · hook `=test` source synthétique). **Assets `resources/models/{voice,voice-anchor}/` (gitignorés).** **V1 (AEC, conv 40)** — `aec.py` `EchoCanceller` SpeexDSP `pyaec==1.0.1` `Aec(160,3200,16000, preprocess OFF FERME)` + garde de longueur de trame (#5) · `capture.py` `WasapiDuplexSource` (micro + loopback système sur UNE PyAudio ; device-change = **re-init COMPLÈTE sur la MORT du loopback** [`is_active` False ; enum PortAudio FIGÉE tant qu'une PyAudio vit] ; **M-A2** backoff distinguant micro-mort [retenter agressivement] / loopback-absent-micro-vivant [espacer] ; **M-C** `loopback_active()` lit un booléen en CACHE sous lock [sonde C `_probe_loop_active` isolée → pas de use-after-free] ; **#6** fermeture du stream fuité) · `AecCapture` (deux `_MonoStream` soxr → appariement 160-trames → AEC → ring POST-AEC ; **M-A** écrivain UNIQUE increvable [exception de conversion comptée, jamais mort] ; **M-B** backlog ref borné à la queue de filtre [re-calage near/ref après un découplage]) · `server.py` `SIDECAR_AUDIO=1` prod / `=test-aec` E2E-V1. **Device-change « à chaud »/casque = backlog §7 (M-D)** · exclusif = résidu assumé · `degraded` cache ≤2 s (accepté, sûreté mémoire). **V2 (VAD, conv 41)** — `SileroVadEngine` (torch JIT · **hystérésis réimplémentée == VADIterator** [seuil 0,5 · neg 0,35 · min_silence 150 ms · pad 30 ms · fenêtres 512] · **1 appel modèle → prob+index padé** · `reset()` **efface l'état RNN** [`model.reset_states`, Finding A] · `_current += n` AVANT l'inférence · `_decide` extrait testable) · `VadPlug` (fenêtrage 512 · normalisation ÷32768 · `pos = _fed_base + idx` = marque padée V3 · discontinuité auto-détectée → reset+clôture · `_engine_errors` compté · répit sur erreur · `evt.plug.stuck` hérité) · **interface `VadEngine` injectable** (prod Silero / test scripté) · `bus.py` `EventBus` (`publish_threadsafe`→`call_soon_threadsafe`→`Subscription` file bornée · drop-oldest + `dropped_total`) · `server.py` (`ws_handler` `send_lock` sérialise ack+drain, `finally` TOUJOURS exécuté [fuite `ws_count` fermée], désabonnement · `_start_audio` prod=AEC+VAD / `=test-vad`=AEC+SyntheticSpeechSource(0,99) · `_stop_audio` pop atomique capture+vad · `/debug` `audio.vad`+`bus` · `SOPHIA_VAD_THRESHOLD`) · `evt.vad.start{pos,captured_at,prob}` / `evt.vad.stop{...,duration_ms}` · **1ᵉʳ émetteur d'`evt.*`**. Requirements += `silero-vad==6.2.1` + `torch==2.13.0`(CPU) + torchaudio ; **backend torch recoupé au banc (pas onnx)**. **V3 (réveil rétroactif, conv 42)** — `RingCursor.seek_to(pos)` (+ `_clamp_seek` : position ABSOLUE clampée `[oldest,write_pos]` · `truncated` = perte à gauche = garde d'honnêteté F1 · `pos<0` borné à 0) · `WakeGate` (**porte réactive SANS thread** : `observe` suit `evt.vad.start.pos` sous lock via l'**emit wrappé** de `server.py` — **`VadPlug` INTACT** · `on_wake(mark)` = mode NOMINAL marque FOURNIE [`mark=None` = générique V9] rembobine + émet `evt.wake {pos,captured_at,truncated}` [positions seules, PAS de « confiance »] + rend le curseur rétroactif · **S12** auto-transition unique [2ᵉ éveil = no-op] · `release()` [**contrat V9 : deadline de garde**] · état /debug en valeurs figées) · `server.py` mode `test-wake` distinct + `/debug/wake?pos=` (TEST_HOOKS, jamais en prod) + pop atomique du wake · `evt.wake` dans EVT_TYPES. **Contrats V4/V9 gravés : R-1 release garanti [sinon Sophia sourde] · R-2 vérifier `overrun` au read EN PLUS de `truncated`.** **WakeGate = porte réactive, PAS un `ConsumerPlug`** (pas de 2ᵉ thread/CPU). **V4 (STT + portier, conv 43-44)** — `stt.py` : `SttPlug` **prise pilotée-VAD** (worker UNIQUE = seul appelant ct2 [**mono-fil**] · `seek_to` la marque F1 · **R-2 overrun au read** · accumulation de GROUPE · réglages banc conv 32 : cadence 1,5s/fenêtre 5s/beam 1) · `FasterWhisperEngine` (large-v3 int8_float16 GPU · fr forcé · temp 0 · `condition_on_previous_text=False` · fix DLL torch/lib · **warmup STT** au boot) · `HypoBuffer` (LocalAgreement-2 · **`shift` transparent** pour la compaction) · **portier** (`OPEN_PHRASES`/`CLOSE_MARKERS` **normalisés à la construction** [F-SOLO-2 : apostrophes] · `match_opening` distingue Sophia/Sophie · `match_closing`/`is_goodnight` · `is_hallucination`) → `wake.on_wake(mark)` [R-1] · **lecture rapide** (`WAKE_MIN_WIN_S`=0,4s · garde lce retirée mono-fil) + **plafond différencié** (`WAKE_PLAFOND_S`=0,8s / `GROUP_SILENCE_S`=3,0s) · **compaction `_compact`+`shift`** [F-1 : jette le DÉJÀ-transcrit, **texte committé INTACT**, `MAX_AUDIO_S`=30 garde absolue] · **file `_cmds` BORNÉE** [F-2 : maxsize 256 drop-oldest + `_dropped_cmds`] · `_compactions`/`_dropped_cmds` dans `state` · `wake.py` (+`armed`) · `server.py` (`_observing_emit` VAD→bus+wake+stt · mode `test-stt` · `/debug audio.stt` · `_stop_audio` pop atomique) · `WavLoopSource` · torch **cu126** + faster-whisper 1.2.1 + ct2 4.8.1. **Contrats V9 gravés** : écoute active + deadline de garde = V9 (R-1) · transparence compaction conditionnée à un moteur STATELESS. **V5 (fin de tour fine, conv 45)** — `turn.py` : `SmartTurnEngine` (onnxruntime CPU · preprocessing `WhisperFeatureExtractor` reproduit `torch.stft`+`hann_window(400)`+**matrice mel VENDORISÉE** · **proba BIT-À-BIT banc** [max|diff|=0,0 sur 7 signaux] · pad-gauche · do_normalize · **pas de sigmoïde** · **ZÉRO dep transformers** au runtime — écarté car downgraderait tokenizers/faster-whisper) · logique PURE `hold_reason` (gardes A/B) + `effective_plafond` (hiérarchie : confiant→ENDGRACE 0,7 / court+très-confiant>0,85→HELD 0,8 / retenu→3,0 / pas-confiant→3,0) · `TurnEngine` injectable · `TurnDetector` (**garde `isfinite`** frontière → fallback · moteur qui lève → fallback plafond + `errors`). `stt.py` : `SttPlug(turn=None → V4 EXACT)` · `_turn_audio` (**pré-attaque 0,4s** `_read_preattack` [curseur jetable, jamais au-delà de la marque S-12] · borné 8s · reset overrun S-1) · `_turn_check` gardé sur le rôle `_armed_at_open` (M2 : jamais Smart Turn sur du vide) · `_emit_turn_end` après `_emit_final` (ordre gravé, conversation seulement) · plafond `WAKE_PLAFOND_S if not armé else _turn_plaf` (reprise → reset fallback). `server.py` (`evt.turn.end` · prod `TurnDetector(SmartTurnEngine())` · mode `test-turn` E2E-V5 · test-stt/V4 INTACT). Modèle+mel VENDORISÉS `resources/models/smart-turn/` (gitignorés, offline) · reqs += `onnxruntime==1.27.0`. Constantes EXACTES banc `oreilles_live.py`. `parle`=temps-audio (**choix A validé**). **D3 : Smart Turn scopé conversation, réveil conv 44 INTOUCHÉ. Le JUGE du « 100 % en décision » à TA voix = reporté V7.** **V6 (speaker-ID, conv 46)** — `speaker.py` : `EcapaEngine` (SpeechBrain `spkrec-ecapa-voxceleb`, **CPU** via `run_opts={"device":"cpu"}` — JAMAIS `CUDA_VISIBLE_DEVICES=""` [aveuglerait le STT GPU du process UNIQUE, mesuré] · modèle VENDORISÉ offline `resources/models/speaker/`, charge 0,27 s · `embed` = `v6_service.emb` EXACT) · `build_centroid` (moyenne 3 clips raw_near/raw/raw_soft, au RUNTIME, même instance ECAPA) · logique PURE `cosine`/`decide` · `SpeakerDetector` (gardes HONNÊTES : audio<0,4s / moteur qui lève / NaN → **None** [n'émet rien], JAMAIS un faux « inconnu » ; vrai score bas → `("inconnu", score)`) · `SpeakerPlug(ConsumerPlug)` (worker UNIQUE thread dédié [ECAPA lourd ne bloque pas VAD/STT] · `on_vad`→file bornée F-2 · `seek_to` la marque + **R-2 overrun** · cadence banc `v8_bargein` MIN_SPEECH 0,75 / MAX_WIN 1,5 / CAP 3,0 / EVAL_EVERY 0,5 · buffer BORNÉ · marque = position CLAMPÉE [honnête] · garde `_tick`+`tick_errors` = zéro mort silencieuse) · `server.py` (`evt.speaker` · mode `test-speaker` · fan-out `_observing_emit(bus, wake.observe, stt.on_vad, speaker.on_vad)` · pop atomique · `/debug audio.speaker`) · `requirements.txt` `speechbrain==1.1.0`. Seuil `SOPHIA_SPEAKER_THR` défaut **0,22** (valeur LIVE du banc ; **dépendance V6→V1/AEC** — V6 ne voit Sophia qu'après l'AEC). `evt.speaker` = **score cosinus BRUT** (échelle [0,1] = doc 04). Modèle ECAPA + négatifs A20 de test VENDORISÉS (gitignorés). **BARRIÈRES design-first** : EER 0 % reproduit venv produit · jitter=0 mesuré · ancre+modèle octet-pour-octet. **V7 (1ʳᵉ parole produit, conv 47)** — **(A) la bouche** `sidecar/tts/` (`TtsPlug` VRAI Piper A20 CPU · découpe en phrases · train gen/play · sortie ISOLÉE · `cmd.tts.speak/push/end/stop` → `evt.tts.start/done` · purge) · **(B) le cerveau chaud** `src/orchestrator/resources/warm/` (`WarmBrain` = `claude -p` PERSISTANT + chat NU [`--system-prompt`, 0 MCP/0 outil] + streaming [`--include-partial-messages`] ; `ask` streame via `onDelta`, ne rejette jamais) · **(C) le routeur** `src/orchestrator/voice/router.ts` (`ConversationRouter` : evt.wake→salutation miroir · evt.turn.end→cerveau→cmd.tts streaming · clôture `« Avec grand plaisir. »` · GATE anti-auto-écoute [MAJEUR-1 final résiduel · M1 deadline lecture · N1/N2]) · **ARCHI 2 PROCESS** `sidecar/server.py` (`SIDECAR_ROLE=ears|mouth` **ADDITIF** [sans rôle = monolithe] · `ears`=AEC+VAD+réveil+STT+fin de tour, **V6 OFF** défaut [`SOPHIA_SPEAKER=1` rallume] · `mouth`=Piper audible ISOLÉ · **gate CROSS-PROCESS** `cmd.listen.mute/resume` ≡ `_flush_audio` du banc, remplace `tts.is_speaking`). Prouvé au juge (réveil 759, TTFT 1389, auto-écho 0). **✅ RUNTIME MIGRÉ EN 2 PROCESS (conv 48)** : `electron/runtime.ts` lance 2 `Supervisor` (rôles ears/mouth, classe INCHANGÉE) + 2 `IpcClient` + `ConversationRouter` 2 canaux + coordination `SANS_VOIX` (les DEUX READY) + option `audioEnabled` (prod=true) · `before-quit.ts` fan-out arrêt propre sur les DEUX · `paths.ts` 2 pidfiles distincts · `main.ts` `audioEnabled:true`. Prouvé **smoke 14/14** (2 sidecars, arrêt des deux, 0 orphelin) + **juge live (réveil 732 ms = zéro régression)**. **V8 (barge-in modulé, conv 49)** — gate 3 états `sidecar/server.py` (`_listen_mode` resume/arm/mute posé par `cmd.listen.arm|mute|resume` ; `_vad_gate` [VAD gaté SEULEMENT en `mute` → tourne en `arm` pour nourrir V6] · `_stt_gate` [STT gaté en `arm`+`mute`, jamais d'auto-transcription] ; V6 rallumé par `SOPHIA_SPEAKER=1` [`runtime.ts`, défaut prod] ; `resume {from}` → `stt.retro_capture`) · `stt.py` **capture rétroactive robuste** (`retro_capture(mark)` champ dédié `_retro_pending` [jamais jeté par un abort, R-1] · `_open_retro` groupe AUTO-BORNÉ `_retro_end = mark+RETRO_MAX 8s` [finalise sur le vrai vad.stop OU seul, R-2] · consommé AVANT le drain) · `router.ts` **la DÉCISION** (`onSpeaker` locuteur=yohann pendant sa pensée → `bargeIn` : coupe `cmd.tts.stop` + `cmd.listen.resume {from}` ; armé SEULEMENT sur la pensée développée [`armedUttId`], jamais une phrase fixe ; une coupe par pensée [`bargeArmed`] ; traîne 0 post-barge) · **OPTION B** (`bargeCurrentThought` course dans `respond` → le barge NE TUE PAS le cerveau, il finit EN FOND [contexte préservé] ; `thoughtAbort`/`signal` = quiesce SEUL ; `|| barged` ferme la course R-2). **Différés V9/V10** : « Sophia » pendant la TTS · replay · volume. **Frontière R-1** = latence de la suite (option B, la génération restante) → `plan/02` re-feed. **V9 (états d'écoute, conv 50)** — `ListenState` `src/orchestrator/voice/states.ts` (machine PURE VEILLE/ÉCOUTE/PAUSE, orchestrateur B1 ; DICTÉE/APPROBATION crochets V10 ; expose `current` = vue O5 ; piloté par `router.ts` `onListenEnter`→cmd.listen.*, `onListenTimeout` sync, transitions handleOpener/handleTurn) · `cmd.listen.stop`/`start` `server.py` (→ `wake.arm_external()`/`release()` ; ADDITIF au portier INCHANGÉ [garde son release interne pour son plafond, conception V7 actée], axe orthogonal à l'axe V8 arm/mute/resume) · **deadline de garde R-1** (contrat conv 42 honoré) `wake.py` (`check_guard` auto-release sur silence > guard_s [30s §6] + émet `evt.listen.timeout` ; `touch_guard` repousse ; `arm_external`) + `stt.py` (`_guard_tick` : touch si `_gate_speaking()` OU `_active`, check sinon ; fail-open) + hook TEST `/debug/guard`. **Changement MINIMAL → ownership B1 au niveau PRODUIT, zéro régression audible.** Écarts §7 : VEILLE≡PAUSE côté sidecar (distinction fil-frais → `plan/02`) · `{from}` rétroactif du start → V10 · DICTÉE/APPROBATION crochets V10 · reconnexion-respawn + dégradé partiel ears/mouth hors colonne. **V11 (résidence des modèles, conv 52)** — contrat `cmd.model.policy` (groupe voix V9 ⊕ calques gouverneur SECOURS/JEU ; `ModelResidence` orchestrateur, dé-doublonné ; hooks additifs `router.onVoiceState`/`governor.onMode` inertes tant que 05 ne pose pas de calque) + `evt.model.loaded/unloaded` (+device, vram_mb, degraded) + `sidecar/audio/models.py` (`parse_policy`/`resolve_stt_device`/`vram_snapshot`/`load_with_fallback`) ; `FasterWhisperEngine.warm()` **repli CPU** sur refus CUDA + mesure VRAM before/after (clampée ≥0) ; `server.py` handler **ENREGISTRE** (device cible `/debug` ; l'action = doc 05) + mode E2E `test-models`. **Set GPU INVARIANT** (réveil=STT résident, Piper CPU, seul modèle GPU=STT) → dynamiques proactives (swap JEU→CPU, éviction SECOURS, prewarm) = **doc 05**. Garde `_emit_model_loaded` sur `_stop.is_set()` (pas de `loaded` orphelin post-stop). **VOIX (banc CF2 → produit)** — voix A20 `resources/models/voice/fr_FR-a20-e400.onnx` (Piper CPU · **`DEFAULT_LENGTH_SCALE=0.87`** = 1.15× plus rapide, AMÉLIORATION conv 52 testée live « beaucoup mieux », override `SOPHIA_TTS_LENGTH_SCALE`) · barge-in 0,22 · grâce 0,7 s · masqueur 4 s + hmm 2 s (peaufinage) · acquis conv 32. **V13 (phrase de secours, conv 58)** — `sidecar/tts/fallback.py` : `FallbackGuard` (épisode OUVERT par la coupure ANORMALE du dernier client WS [close_code ∉ {0, 1000, 1005} — codes MESURÉS au banc : `close()` → **0** · `close(1000)` → 1000 · mort du process → **1006** · applicatif → tel quel] · CLOS par la reconnexion [`episode_reset`, l'ÉPOQUE tourne] · CONSOMMÉ par un close PROPRE [`episode_consume` — jamais de phrase pendant un arrêt volontaire T6] ; candidat = `evt.turn.end` OU l'appariement wake↔final du MÊME mark [les 2 ordres couverts, vérifiés source stt.py ; wake SEUL jamais — `_gate_check` peut émettre mid-phrase ; parole ambiante jamais] ; **consommation ATOMIQUE au lancement réel** [`try_consume(epoch)` test-and-set — un seul gagnant PAR LE CODE ; candidat périmé (reset dans la fenêtre) JETÉ ; `unconsume` si `t.start()` lève — l'épisode RENDU, compteurs jamais menteurs] ; fail-quiet si `clients()` lève) · `FallbackVoice` (precache en thread TRANSITOIRE B2-réinterprété [PiperEngine chargé en RAM puis LIBÉRÉ, prouvé weakref+gc] · garde de GÉNÉRATION + deadline STALE 30 s [un warm qui pend ≠ un filet mort à vie ; le zombie est jeté à la POSE **et au chemin EXCEPT** — symétrie re-croisé ROB2-2] · clé d'idempotence posée seulement si le cache est COMPLET [partiel re-tentable] · lecture one-shot `Output.play` BLOQUANT = EN ENTIER, hors du circuit tts/purge → **exempte de barge-in PAR CONSTRUCTION** · stop allégé T6 [pas de join precache, join play 0,3 s, gardes `_stopped`]) · `server.py` (`cmd.tts.cache` + `evt.fallback.played` · montage ears + monolithe + mode `test-fallback` [gates V8 en parité ears] · emit wrappé patron V3 [prises verrouillées intactes] · **reset `_listen_mode="resume"` au départ du DERNIER client** [FID-M1 : un crash pendant qu'elle parle ne laisse jamais le gate arm/mute figé = sourde à vie] · `/debug audio.fallback` [dont `episode_epoch`] · démonté EN PREMIER à l'arrêt) · orchestrateur (`fallback-phrases.ts` [le TEXTE = domaine Yohann, acté conv 58 après écoute] · hook **`sidecarPostReady`** ph.5 [IPC éphémère BORNÉ 5 s — jamais un boot suspendu] + ré-envoi idempotent `ensureVoicePipeline` · **ack LU** [`ok:false` → « NON posées »] · gaté `audioEnabled` · `IpcClient.close(code?)`) · **PORTÉE HONNÊTE gravée** : couvre « canal coupé ANORMALEMENT, oreilles vivantes » ; la mort du process Electron = Job Object (VOULU — le voyant disparaît aussi = le signal) ; l'orchestrateur GELÉ indétectable (pas de heartbeat) ; le resync ordonné du respawn (S10 : policy → enroll → tts.cache) = **V15**.
- **LEÇON CENTRALE (à tenir absolument)** : **NE PAS BRICOLER — MESURER/VÉRIFIER À LA SOURCE, CONCEVOIR D'ABORD.** **conv 35 en a ajouté une, majeure** : sur un thème DUR, **la chasse au trou ne CONVERGE pas** (5 tours, 5 facettes ; mon « c'est bon » faux 2×) — **la condition d'arrêt d'un audit est un ARGUMENT DE STRUCTURE qu'un regard indépendant valide, PAS ma conviction.** Rappels conv 34 : **l'audit CORRIGE dans la conv, pas à déférer** (un deferral = frontière RÉELLE, jamais une facilité) ; **une correction est un mécanisme neuf → se re-audite** (les 3 MAJEUR de conv 35 étaient TOUS dans mes corrections/le verdict). **Solo à fond D'ABORD, PUIS le croisé.** **L'E2E « cœur réel » révèle ce que les bouchons cachent.** Un sujet clos avant le suivant. **Ne pas gérer la jauge de Yohann.** **Zéro régression = un contrat, pas un vœu.** **conv 36 l'a RE-PROUVÉ** : les 2 MINEUR du re-croisé étaient TOUS dans MES corrections (F1 avait rouvert un trou d'honnêteté sur la voie SAINE ; ② inefficace) ; le 3ᵉ croisé complet a validé l'argument **SANS RÉSERVE** ; **le smoke « cœur réel » ferme ce qu'un agent qui LIT le code ne prouve pas** — le vrai before-quit Electron ne se prouve qu'en le LANÇANT. **conv 37 l'a RE-RE-PROUVÉ au MAXIMUM** : à CHACUN des 3 tours le trou était dans MES corrections (`getGovernor` oublié · `ensureWatermarks` déplacé-pas-fermé · `isDue` gardé sur UN seul jumeau), **jamais le cœur d'origine** (validé SANS RÉSERVE dès le tour 2 ; convergence 1→0→0) ; **le smoke ne doit pas prouver une COPIE** (→ `SophiaRuntime` partagé main.ts/smoke) ; **chaque « frontière » passée au crible « facilité ? »** (Yohann — les 2 restantes sont réelles : sonde→05, budget par-unité→client). **conv 39 l'a RE-PROUVÉ au CODE PRODUIT** : croisé (0 MAJEUR, 13 findings) + **re-croisé ciblé — les 5 MINEUR TOUS dans MES corrections** (dont **S#1 : ma 1ʳᵉ correction gardait une RACE → re-corrigée en `dict.pop` atomique**) ; **le cœur SPMC validé sain par 4 agents.** **DESIGN-FIRST + la chasse aux facilités de Yohann** (« sois sûr de pas avoir été dans la facilité ») ont attrapé ma facilité centrale (venv unique sans recouper le CODE du banc) → **mesure de cohabitation** → venv unique prouvé (murs tombés) → **respecter le plan d'abord, écart seulement si MESURÉ** (« respecte le plan »). **« Recoupé à la source » = REPRODUIT empiriquement** (findings reproduits par script avant présentation). **conv 40 l'a RE-RE-PROUVÉ sur V1 (AEC)** : croisé **3 MAJEUR dans le code d'origine** → re-croisé **1 MAJEUR dans MA correction SOLO-1** (M-A2) → fermé ; le cœur AEC/SPMC sain (4 agents). **Corriger à la RACINE, sans pansement** (Yohann). **Le device-change au VRAI matériel a tranché la conception** : `IMMNotificationClient` mesuré capricieux → re-init sur la MORT du loopback (l'énum PortAudio figée impose le terminate complet — mesuré, pas supposé). **conv 41 l'a RE-RE-RE-PROUVÉ sur V2 (VAD)** : croisé 0 MAJEUR (7 fixes, dont **NIT-5 = MA facilité** attrapée par Yohann → mesurée → E2E passé POST-AEC) → re-croisé **1 MINEUR dans MA correction `reset()`** (état RNN Silero non effacé, **caché EXACTEMENT là où le test d'équivalence ne regardait pas** ; reproduit Δ 0,28 ; **le test MORD** vérifié par temp-revert) → fermé ; le cœur sain (4 agents). **Ne pas doubler le CPU sans raison** (réimplémenter l'hystérésis > 2ᵉ appel modèle ×2,14, prouvé équivalent à VADIterator — reset compris — par test). **Recouper le CODE du banc** (backend torch, pas onnx). **conv 46 l'a RE-PROUVÉ sur V6 (speaker-ID)** : croisé 0/0 → re-croisé 0/0 → fermé ; **le trou dans la RIGUEUR de MA correction** (garde `_tick` sans test qui MORD → prouvé TEMP-REVERT ; « après un fix, mettre les TESTS à jour » n'est pas optionnel). **La BARRIÈRE DE FIDÉLITÉ design-first AVANT de coder a corrigé MA facilité de seuil** (mon plan disait « seuil = milieu du fossé offline » → la mesure a prouvé que les clips A20 offline plein niveau ≠ le résidu post-AEC → **0,22 valeur LIVE du banc**, dépendance V6→V1/AEC). **Un patron de BANC ne se copie pas tel quel en produit** (`CUDA_VISIBLE_DEVICES=""` du banc aveuglerait le STT du process UNIQUE → CPU forcé par `run_opts`, mesuré ; artefact d'époque comme le multi-process V0). **« Mesurer avant de coder » a révélé le vrai métrique** (intégration EER 0 %, pas les fragments per-clip) et le vrai seuil, AVANT une ligne de code. **conv 58 l'a RE-PROUVÉ sur V13 (8 regards, convergence 2 MAJEUR → 0 → 0)** : le re-croisé des corrections a trouvé les 3 MINEUR DANS mes mécanismes NEUFS (« une fois par épisode » tenu par la CONVENTION émetteur-unique, pas par le code → rendu ATOMIQUE par époque ; le chemin EXCEPT sans la garde de génération de sa pose — le trou exactement là où le test ne regardait pas, patron conv 41) ; **le passe-plat manquant attrapé par l'E2E naturel = « la ceinture jamais-fatal ne remplace pas un test qui mord »** ; et le test À L'IDÉE DE YOHANN (phrase PUIS rebranchement PUIS continuer) était MEILLEUR que le mien — la récupération prouvée en plus de la panne. **conv 59 l'a RE-PROUVÉ sur V14 (20ᵉ fois)** : les 2 MAJEUR du croisé dans MES mécanismes neufs à racine COMMUNE (le drop-oldest et l'éviction traitaient une preuve FERMANTE comme jetable — « muet dans le doute » inversé en « parle malgré la preuve » sous pression) ; **le re-croisé a trouvé LE TEST MANQUANT plutôt qu'un bug** (la propriété thread-safe du fix jamais exercée en multi-thread — le hammer porté, mordant 3/3) ; et 2 leçons de rigueur d'OUTILLAGE : les temp-reverts se lancent AU VENV (un FAIL d'import ment « ça mord ») ; un revert doit être FIDÈLE à l'ancien code et un test doit viser le cas EXACT (espaces ≠ chaîne vide).
- Format : annonce brève + sujet en mots simples en tête + un par un + toutes-les-options/reco/« pourquoi pas ».

### Vigilances
- **Phase 3 ≠ réouverture des docs** (→ **Garde-fou Phase 3** ci-dessus) : le plan **traduit, ne re-tranche pas** ; Claude tranche le micro-technique + trace §7 ; **seuls un vrai trou de conception ou la vie de Sophia remontent à Yohann** ; un écart au contact du code = **signalé + tracé §7**, jamais un contournement silencieux.
- **La barre de qualité change de forme, pas de niveau** : « audit avant de figer » (Phase 2) devient « tests avant de committer » (Phase 3) — à cadrer au plan d'implémentation ; les critères d'acceptation des docs = la source des tests. **L'essai à blanc est un banc de PREUVE, pas un prototype qui devient le produit** — « pas de V2 » vaut pour le code.
- **Dépendance Anthropic = VIGILANCE N°1** : FM1–FM5 ; hedge multi-provider (Max→x20→API-sur-convocation→local) — réduit, n'élimine pas. Quota x5 déjà fortement sollicité par l'usage pro → x20 = chemin attendu.
- Plan mode harness (mis-fire structurel) — géré en **texte libre** ; **ExitPlanMode au seul moment de l'inscription** (géré ainsi conv 2-14).
- **Preuves Phase 3 prioritaires** : 🔴 pipeline audio temps-réel (n°1) · 🔴 wake word FR (F6 + repli nommé) · AEC loopback (M1) · **purge des fichiers de session CLI (T1/T8/T13 + piste `G:\Sophia\sessions\`)** · **« réchauffer sans écrire »** (politiques de chauffe) · **🔴 bloc identité I→VI sur Phi-4-mini (T6)** · seuil X de l'accusé (99) · embedding FR · **🔴 `RENAME`/`DROP` transactionnel d'une table `vec0` (M9 — repli nommé si le banc échoue)** · speaker-ID · affect · seuils humeur · érosion longue session · banc de dilemmes v1 · 🔴 kill dur d'un process CUDA figé (socle). **Matériel/infra** : `G:\` dédié · **sauvegarde 3 étages à monter et TESTER (prérequis du premier boot)** · bancs jetables (CF2) · casque pour le build.
- **Audits** : solo D'ABORD (**à fond, pas un survol** — demande Yohann conv 14 ; **le solo post-intégration attrape MES propres incohérences** — conv 16) puis croisé 2 agents — **proposé d'office, lancé sur le Go de Yohann, jamais seul** ; findings **vérifiés aux sources par le pilote AVANT présentation** ; le croisé inter-plans vérifie aussi la cohérence entre plans. **Leçon conv 16 : un mécanisme NEUF inventé au plan doit être re-audité** (il n'a jamais été vu — chaque round de re-audit a trouvé du réel). Précédents : 8=21 · 9=31 · 10=38 · 11=8+10 · 12=3+10 · 13=15+6 · 14=14+14 · 16=20+9+10+7 · **17 = 5 croisés complets + 1 croisé CIBLÉ sur le neuf `warmth_ledger` + 3 re-solos (plan personnalité), zéro faux positif — 18 croisés consécutifs**. **Leçon conv 17** : un croisé **ciblé** sur un mécanisme neuf (jamais vu) paye — `warmth_ledger` avait 8 findings ; les re-solos attrapent MES propres incohérences. **Conv 18 : 5 (stockage). Conv 19 : `plan/04` — général 8 + CIBLÉ sur le store éphémère 9 (dont 5 MAJEUR), zéro faux positif. Conv 20 : `plan/05` — 1 croisé complet + 2 ciblés, zéro faux positif, sévérité 4 MAJEUR → 3 MINEUR (convergence). Conv 21 : `plan/99` — croisé d'office 2 agents + RE-CROISÉ « faire les 2 » (Yohann) 2 agents, zéro faux positif sur 4 agents, âme intacte 2× ; les MAJEUR tombaient tous sur MES corrections (F-99-2/3 · Crashpad).** Le croisé ciblé sur le neuf paye ENCORE — **même un mécanisme né d'un CORRECTIF se re-audite** ; **après un fix, mettre les TESTS à jour** ; **penser SOUS les plans (stockage/octets/réplicas — grille des 8 classes `GRILLE-AUDIT-FABLE.md`).** **Leçon conv 20, plus dure encore : mes *corrections* elles-mêmes étaient incomplètes** (swap atomique mono-volume appliqué au cross-volume ; invariant « à tout instant » non enforçable ; selftest ne prouvant pas l'étage 3) → **une correction est un mécanisme neuf**, elle se re-audite. **La convergence se lit à la sévérité** (MAJEUR→MINEUR, mécanismes durs confirmés solides) ; le **filtre projet** dit quand clore — jamais tout à fait à zéro, mais proportionné. **Une chasse aux facilités AVANT le croisé** (demande Yohann conv 20) a trouvé du réel (invariant de clé circulaire, garde de sauvegarde creuse) — R2/R3 à exercer d'office, pas seulement au croisé. **Leçon conv 21, la plus dure : mon *jugement* « un re-croisé serait du polish » était lui-même une facilité — le second œil corrige aussi mon estimation de « quand c'est assez », pas que mes corrections ; d'où « faire les 2 » (croisé Opus ET Fable) plutôt que l'un OU l'autre.** **La chasse-résidus (contenu/clé hors effacement/chiffrement) est un puits profond : nommer les recoins + confier l'énumération exhaustive à Fable/Phase 3, clore au niveau plan (filtre projet).** **Conv 58 : croisé 2 + re-croisé A 2 agents frais (8 regards avec les solos) — les agents REPRODUISENT eux-mêmes (5 scripts) et le pilote RE-REPRODUIT avant de corriger ; le re-croisé des corrections paye ENCORE (3 MINEUR réels dans les mécanismes neufs, dont un exactement là où le test ne regardait pas) ; deux mécanismes non-prouvables en solo validés EMPIRIQUEMENT sur le VRAI ws_handler in-process.** **Conv 59 : croisé 2 + re-croisé 2 frais (8 regards) — 13 scripts de repro agents, tout re-reproduit par le pilote ; le re-croisé peut trouver LE TEST MANQUANT plutôt qu'un bug (le hammer de concurrence) : la convergence se lit aussi à la NATURE des findings (bug → test → libellé) ; un re-croisé SURVIT à une coupure de courant (reprise des transcripts, repo vérifié avant).**
- **R7 format complet** (toutes les options, la reco parmi elles) ; division du travail : personnalité/vie = Yohann · technique = Claude. **Le contenu identitaire (persona/mémoire) ne se remet JAMAIS à un vague « Phase 3 » — séquencé, fait ENSEMBLE, accompagné** (conv 17 ; mémoire `identity-content-fait-ensemble`). **R8 : clos avant le suivant.** Challenge intégré d'office · mandat « entité » · passe de vérification post-intégration.
- **« Pas de V2 »** (vaut pour le code) · **commits au fil de l'eau, push en clôture** (décision conv 14 : repo public = vitrine, n'exposer que l'état consolidé) · `--bare` jamais (A1) · repo public (gitleaks `pre-commit` + garde contenu **active** — à chaque commit : aucun `portrait*`/`temoignage*`/`docs/prive/`, aucun contenu personnel dans les fichiers trackés · secrets `.env` · identité `Yohann Dandeville <contact@ydvsystems.com>` · **pas de Co-Authored-By**).
- **Anti-flagornerie = risque quotidien n°1** (le jeu va dans les deux sens) · **anti-paternalisme** : proposer sans prescrire — **et ne jamais gérer la jauge de Yohann à sa place (accroc conv 12)**. **Accroc conv 19** : ne pas transformer sa jauge de contexte en « **mode économe** » — **il n'y en a pas** (sa jauge m'informe, ne rationne pas ma qualité ; « ne pas se rationner le contexte », pattern). **Accroc conv 17 (débordement)** : ne pas toucher les docs `technique/` ACQUIS sans Go — les **PLANS** sont modifiables, pas la conception ; « corrige tout » ≠ vider tous les loose ends. Yohann arrête, on reverte, on prouve l'intact.
- **Lire les sources porteuses EN ENTIER avant de proposer** (R4 au démarrage / en plan-mode, pas seulement avant une édition) — **jamais grep→plan** (accroc central conv 38 : plan qui contredisait la conception gravée [daemon persistant vs request-scoped A1/A36] + relecture forcée = contexte DOUBLÉ ; le raccourci « économiser le contexte » coûte PLUS). Précondition INTERNE (pas un recadrage de Yohann). Et **« recoupé à la source » = REPRODUIT empiriquement**, pas seulement lu+raisonné (accroc conv 38, finding a1). Mémoire `read-in-full-before-planning`.
- Discipline IN PLACE en clôture.

---

*CLAUDE.md v58 — YdvVoice (Sophia), profil Standard. Pattern de référence : `docs/PATTERN-v3.0-CLAUDE-CODE-STANDARD.md`. Créé 2026-06-21 (v1) ; MAJ conv 62 (2026-07-24) — **conv 62 : M1 (la PRISE EMBED) BÂTIE + auditée 10 regards + tout vert, committée `[conv-62]` (179cd46). La 1ʳᵉ main qui écrit la mémoire — additif pur, V0→V15 + M0 intacts. DESIGN-FIRST : fourche CPU/GPU tranchée PAR LA MESURE (77 ms ONNX < 200 ms → CPU, embed hors frontière VRAM) ; moteur ONNX BGE-M3 + tokenizers prouvé 1:1 vs réf (2,8e-07), ZÉRO downgrade (piège V5 évité). Sidecar (moteur + prise à file de priorité, cmd.embed→evt.embed.done) + orchestrateur (EmbedSpace : garde d'espace · base-est-la-file [rowid vec0 BigInt] · embedQuery chaud). ⛔ POISON-ROW = vrai trou de conception de b-m5 résolu par « NE PLUS DEVINER » (option b, Yohann) : la permanence d'un échec est INCONNAISSABLE depuis l'orchestrateur (prouvé 4 tours, mon c'est-bon faux 4×) → M1 COMPTE+SURFACE un event typé, ne DEAD-LETTER JAMAIS → perte silencieuse impossible ; la politique skip/backoff/dead = M2 avec la calibration du banc (temps embed i5 vs timeout 3 s). ⛔ 2 contrats write-once honorés (u-m1 E/F, mordant). + Partie A (2 commits) : affect ON défaut au juge (MESURÉ TTFT 1229-1288 < 1265 réf, coût invisible ; mot d'humeur corrigé — valence = chaleur, pas tension) · « Sophia » livré au cerveau (openerForBrain). + ENQUÊTE juge muet résolue à la source (oreille gelée sous contention + le juge ne câble pas le self-heal V15 → RIEN à corriger produit ; amélio juge parquée). PREUVES : u-m1 48 · test_embed 11 (→ sidecar 357) · npm test 22 suites · e2e socle 31/31 · smoke 14/14 · build OK. PROCHAIN conv 63 = plan/02 M2 (le moteur de recherche = câblage vivant de l'embed + politique poison-row) puis le re-feed, RELAY-conv63 fait foi.** Antérieur (chaîne MAJ conv 23→61) → `docs/journal/CLAUDE-HISTORY.md`.*

<!-- footer-conv60-archivé
**conv 60 : V15 (le RESPAWN RESYNC S10 + la CONFORMITÉ DES PRISES) BÂTIE — LE PLAN 01 EST COMPLET (V0→V15). Après tout respawn : détection port/pid périmés (témoins COMPOSÉS capturés avant les awaits), teardown propre (close propre → episode_consume), rebuild chemin unique (S10 : policy → enroll honnête 4 états → cache), handoff (ÉCOUTE re-posée + phrase de retour [texte = domaine Yohann] · PAUSE pensée transférée sans phrase · VEILLE silencieux), énonciations en vol = échec terminal, failover cloud-stub→local {degraded, reason}, conformité des prises (affect comprise). Écarts ACTÉS « b et c » (enroll = jalon honnête ; état courant ré-exécuté). AUDIT 8 regards (croisé ROB-M1/M2/M3 dans MES mécanismes neufs → racine + mordants → re-croisé 2 frais : 0 MAJEUR, 1 trou-de-TEST warm_failed → matrice 4 états, 2 MINEUR documentaires) — convergence 3 MINEUR → 0, 10 temp-reverts, la nature des findings bug→test→libellé. VÉCU : 3 kills réels en pleine conversation (phrase de retour entendue, reprise sans « Sophia », cerveau intact, jamais la phrase V13 à tort, 0 orphelin). JUGE : TTFT 1 265 = record du suivi · vécu 3 451 (2ᵉ) · réveil-sous-média chiffré 4 990 (donnée neuve A/B). V12 : observabilité mixer nom→volume (SON œil sur le mélangeur, anomalie Chrome/21 inattribuable → instrumenter, pas spéculer). pytest 346 (18 V15) · npm test 20 (u-router 136) · e2e:v15 52 · sweep TOUT VERT · commit 18a0d86. PROCHAIN conv 61 = plan/02 la MÉMOIRE (amnésie WarmBrain M0+M6 · R-1 barge · PAUSE fil-frais), RELAY-conv61 fait foi.**
-->

