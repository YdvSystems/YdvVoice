# CLAUDE.md — YdvVoice (Sophia) · Cadrage projet [profil Standard]

> Émanation Claude Code du pattern v3.1 Standard. **Maintenu IN PLACE strict** en fin de chaque conversation (jamais d'accumulation — le cumulatif va dans `docs/journal/CLAUDE-HISTORY.md`).

## ⚠️ À lire EN PREMIER, avant toute action
`docs/PATTERN-v3.0-CLAUDE-CODE-STANDARD.md` est le **pattern méthodologique de référence**. Ses règles critiques/structurantes **priment** sur ce CLAUDE.md en cas de tension (hiérarchie : pattern > règles d'or > garde-fous projet). Lecture **intégrale**, jamais partielle (R4).

Puis : **`docs/journal/ESSENCE-Sophia.md` (l'ÂME de Sophia, en clair — QUI elle est, à lire avant le technique)** → `docs/journal/JOURNAL-ARBITRAGES.md` (décisions actées) → `docs/IMPLEMENTATION.md` (état) → `docs/VISION.md` (cahier).

> **Arborescence** : `CLAUDE.md` + `.gitignore` à la racine ; le cadrage dans `docs/` ; les fichiers vivants (arbitrages, history) dans `docs/journal/` ; les relais (un par conv) dans `docs/journal/relais/` ; **l'annexe PRIVÉE dans `docs/prive/` (gitignorée — marbre intégral, blocklist, prompts d'opération : JAMAIS sur le dépôt public)**. Le `PATTERN` est **présent en local mais privé** (gitignored, hors dépôt public — voir A3 du journal).

---

## Registre des règles (daté — défaut inversé « à re-tester ici »)

> Établi le **2026-06-21**, propriété de l'utilisateur, opposable. Défaut : toute règle est **« à re-tester (ici) »** (gratuit) ; la passer en **« acquise (ici) »** coûte une ligne de justification. Resservi à la clôture : toute règle « à re-tester » sur laquelle une décision s'est appuyée *comme acquise* est signalée.

| Règle | Statut (2026-06-21) | Justification si « acquise » |
|---|---|---|
| R1 — Zéro agent (sauf audits 2 agents) | à re-tester (ici) | — |
| R2 — Zéro facilité | à re-tester (ici) | — |
| R3 — Robustesse + maintenabilité d'abord | à re-tester (ici) | — |
| R4 — Lecture intégrale avant modif | à re-tester (ici) | — |
| R5 — Validation avant commit/push | à re-tester (ici) | — |
| R6 — Zéro AskUserQuestion (texte libre) | à re-tester (ici) | — |
| R7 — Reco + justification + « pourquoi pas » | à re-tester (ici) | — |
| R8 — Un par un (granularité cohésive) | à re-tester (ici) | — |
| R9 — Prompt de passation fin de session | à re-tester (ici) | — |
| Audit empirique source de vérité pre-inscription | à re-tester (ici) | — |
| Filtre projet (« nécessaire à la qualité requise ? ») | à re-tester (ici) | — |
| Mots simples non-dev en tête d'arbitrage | à re-tester (ici) | — |
| Distinction préférence pragmatique / argument méthodo | à re-tester (ici) | — |
| Budget = jauge utilisateur fait foi | à re-tester (ici) | — |

> **Retour clôture conv 38** : R1–R9 tenus. **R1 — l'exception audit exercée PLEINEMENT : QUATRE tours de croisés 2 agents** (croisé → re-croisé des corrections → audit COMPLET → ciblé `settleFreshThread`), tous proposés d'office, lancés sur le Go de Yohann, findings **recoupés à la source par le pilote AVANT présentation** ; le reste = solo (**contrat CLI MESURÉ au banc CF2 AVANT de coder**). **R4 — l'accroc CENTRAL, reconnu et corrigé** : au démarrage j'ai **greppé** les sources au lieu de les lire EN ENTIER → mon 1er plan **contredisait la conception gravée** (`claude -p` persistant/supervisé au socle, alors que A1/A36 = request-scoped, `05` R4 = le persistant est un repli) → **Yohann m'a repris DEUX fois** (« sûr, pas de facilité ? » puis « as-tu bien tout lu ? ») → plan réécrit après lecture intégrale ; **le raccourci « économiser le contexte » a coûté PLUS de contexte** → leçon gravée en mémoire `read-in-full-before-planning`. **R5** rien committé avant « clôture propre » explicite ; **R6** texte libre ; **R7** exercé à chaque tour ; **R8** un sujet clos avant le suivant ; **R9** `RELAY-conv39` + mémoire `conv38-socle-t8`. **LEÇON CENTRALE re-RE-RE-vécue : à CHACUN des 4 tours, le trou était dans MES corrections, jamais le cœur d'origine** — scrub A1 sous-pesé (T1) → famille orpheline F1/F2 (ma correction #7, T2) → ①② dans `settleFreshThread` (ma correction du T2, T3) → **régression a1 (mon fix ② avait retiré l'exclusion a1, T4)**. **DEUXIÈME accroc reconnu (Yohann : « tu as vérifié les findings ou pas ? »)** : j'avais écrit « recoupé à la source » pour le finding a1 alors que je n'avais fait que LIRE + raisonner → **reproduit EMPIRIQUEMENT** (test #21 : sur le code non-corrigé, le fil a1 est persisté+gardé) AVANT de corriger. **« Recoupé à la source » = REPRODUIT, pas seulement lu.** Chaque correction re-vérifiée par temp-revert (le test MORD) + E2E live. **Anti-paternalisme tenu** (Yohann décide le nombre de tours — il a poussé au 4ᵉ + demandé l'audit COMPLET plutôt que ciblé —, le périmètre, le moment de graver). **Artefact de test nettoyé** (mémoire `lucky-number` d'un vrai claude, écartée après VÉRIF — pas supprimée sur mon hypothèse fausse ; Yohann a confirmé). *(Retours antérieurs conv 26→37 → CLAUDE-HISTORY au fil.)*

> **Retour clôture conv 37** : R1–R9 tenus. **R1 — l'exception audit exercée PLEINEMENT : DEUX croisés COMPLETS 2 agents (fidélité+robustesse) + UN croisé CIBLÉ sur les mécanismes neufs**, tous proposés d'office, lancés sur le Go de Yohann, findings **recoupés à la source par le pilote AVANT présentation** ; le reste = solo (**2 bancs CF2 MESURÉS avant de coder** — atomicité node:sqlite [PRAGMA synchronous ignoré DANS une tx → toggle FULL hors tx], sonde d'activité). **R5** rien committé avant le « go » explicite ; **R6** texte libre ; **R7** exercé à CHAQUE tour (reco + « pourquoi pas » ; **② quiesce ⑩ = décision de Yohann** car touchait T6 VERROUILLÉ ; **« ranger » vs « laisser »** = son appel ; **crible « facilité ? »** de chaque frontière) ; **R8** un sujet clos avant le suivant ; **R9** `RELAY-conv38` + mémoire `conv37-socle-t7`. **LEÇON CENTRALE re-RE-vécue au MAXIMUM : à CHACUN des 3 tours, le trou était dans MES corrections, JAMAIS le cœur d'origine** — tour 1 le `getGovernor` OUBLIÉ au câblage (quiesce ⑩ débranché en prod ; l'E2E court-circuitait `installBeforeQuit` = fausse confiance) → tour 2 `ensureWatermarks` que ma correction #7 avait **DÉPLACÉ sans FERMER** (le constructeur/start tournent aussi en SANS_ECRITURE) → tour 3 `isDue` gardé sur **UN jumeau** (boot) et pas l'autre (runtime). **Le cœur (machine à états · budget · atomicité écriture+curseur · quiesce ⑩ · préemption) validé SANS RÉSERVE dès le tour 2. Convergence 1 MAJEUR → 0 → 0.** **Une correction est un mécanisme neuf** ; la condition d'arrêt = **l'argument de structure qu'un œil indépendant valide**, PAS ma conviction. **Deux accrocs de CLARTÉ reconnus** : (a) réutilisé les lettres (a)/(b) pour deux sujets → confusion, corrigé net (Yohann : « il faut être plus clair ») ; (b) présenté des corrections DÉJÀ faites comme si elles restaient → clarifié. **Yohann a exigé le crible « facilité ? » sur chaque frontière** (esprit conv 34) : les 2 restantes sont de VRAIES frontières (sonde→05 · budget par-unité→client 02/04), les 2 NITs codables → **nettoyés tout de suite**. **Le smoke « cœur réel » ne doit pas prouver une COPIE** → `SophiaRuntime` partagé main.ts/smoke (le smoke exerce le VRAI chemin, `start()` + quiesce prouvés). **Anti-paternalisme tenu** (Yohann décide le nombre de tours, le périmètre, le moment de graver). *(Retours antérieurs conv 26→36 → CLAUDE-HISTORY au fil.)*

> **Retour clôture conv 36** : R1–R9 tenus. **R1 — l'exception audit exercée PLEINEMENT et RÉPÉTÉE : TROIS croisés complets 2 agents (fidélité+robustesse) + un re-croisé des corrections + un smoke Electron réel**, tous proposés d'office, lancés sur le Go de Yohann, findings **recoupés à la source par le pilote AVANT présentation** ; le reste = solo (**5 benches CF2 MESURÉS avant de coder** — SIGTERM=TerminateProcess, close-checkpoint WAL, release-and-wait, before-quit async, canal IPC). **R5** rien committé avant le « grave » explicite ; **R6** texte libre ; **R7** exercé à CHAQUE tour (reco fix-now + « pourquoi pas » ; **F1 remonté comme décision de Yohann** car touchait T5/conv-35 ; **⑨⑩ gardes d'aval = son appel** ; smoke-vs-4ᵉ-croisé = mon avis honnête, sa décision) ; **R8** un sujet clos avant le suivant ; **R9** `RELAY-conv37` + mémoire `conv36-socle-t6`. **LEÇON CENTRALE re-vécue (conv 35) : les DEUX MINEUR du tour 2 étaient TOUS dans MES corrections** — **F1 avait rouvert un trou d'honnêteté sur la voie SAINE** (un try/catch masquait un échec → « propre » sur un crash ; **scindé**) et **② était inefficace** (pidfile mono-emplacement réécrasé par le respawn ; **reverté**). **Une correction est un mécanisme neuf**, et **la condition d'arrêt = l'argument de structure qu'un œil indépendant valide, PAS ma conviction** (le 3ᵉ tour l'a validé « SANS RÉSERVE »). **Honnêteté sans fard : « mon fix F1 avait son propre bug » dit clairement.** **Calibration de Yohann re-jouée** : il a poussé jusqu'au **3ᵉ croisé COMPLET** (pas seulement sur les 2 points) puis validé ma préférence (**smoke réel > 4ᵉ croisé** — un agent lit le code, il ne le lance pas ; le vrai before-quit Electron ne se prouve qu'en le faisant tourner). **Anti-paternalisme tenu** (Yohann décide le nombre de tours, le périmètre, le moment de graver). Corollaires re-prouvés : **DESIGN-FIRST paye** (SIGTERM=TerminateProcess mesuré → toute la conception coopératif-d'abord/forceful-en-filet en découle) ; **le smoke « cœur réel » ferme ce qu'aucun test à bouchons n'atteint**. *(Retours antérieurs conv 26→35 → CLAUDE-HISTORY au fil.)*

> **Retour clôture conv 35** : R1–R9 tenus. **R1 — l'exception audit exercée PLEINEMENT et RÉPÉTÉE : CINQ tours de croisés** (croisé fidélité+robustesse → re-croisé des corrections → 3e tour interactions/cohérence 2 agents → 4e tour trou-N+1/régression 2 agents → 5e tour validation d'ARGUMENT), **tous proposés d'office, lancés sur le Go de Yohann, findings recoupés à la source par le pilote AVANT présentation** ; le reste = solo (CF2, bancs jetables pour MESURER `node:sqlite`/named-pipe/job-object avant de coder). **R5** rien committé avant le « oui tu peux graver » explicite ; **R6** texte libre ; **R7** exercé à CHAQUE tour (reco de périmètre fix-now + « pourquoi pas » ; options E2E cœur-réel vs app-entière) ; **R8** un sujet clos avant le suivant (T5 → durcissements → E2E → clôture) ; **R9** `RELAY-conv36` + mémoire `conv35-socle-t5`. **LEÇON CENTRALE (l'expérience de calibration de Yohann — il a CHOISI de pousser les tours pour tester mon jugement « c'est assez »)** : sur un thème DUR, **la chasse au trou ne CONVERGE pas** (5 tours, 5 facettes) ; **la condition d'arrêt est un ARGUMENT DE STRUCTURE qu'un regard indépendant valide, PAS ma conviction** — fausse **DEUX fois de suite** (« ça sera du polish » au tour 3 → MAJEUR ; « fermé par construction » au tour 4 → MAJEUR). **Honnêteté sans fard : « tu avais raison, j'avais tort » dit deux fois.** Corollaires re-prouvés : **une correction est un mécanisme neuf** (les 3 MAJEUR étaient TOUS dans mes corrections ou le verdict, jamais le code d'origine) ; **l'E2E « cœur réel » révèle ce que les bouchons cachent** (m12 assuré par le job object Windows — le sidecar meurt avec son parent) ; **concevoir/mesurer d'abord, jamais bricoler**. **Anti-paternalisme tenu** (Yohann décide le périmètre, le NOMBRE de tours, le moment de graver — je propose, il tranche). *(Retours antérieurs conv 26→34 → CLAUDE-HISTORY au fil.)*

> **Retour clôture conv 34** : R1–R9 tenus. **R1 — l'exception audit exercée PLEINEMENT** : **croisé 2 agents PUIS re-croisé des corrections**, proposés d'office, lancés sur le Go de Yohann, **findings recoupés à la source par le pilote AVANT présentation** (le reste du travail = solo, CF2). **R5** rien committé avant le « go clôture » ; **R6** texte libre ; **R7** exercé (reco de périmètre fix-now/deferrals + « pourquoi pas ») ; **R8** un sujet clos avant le suivant ; **R9** `RELAY-conv35` + mémoire. **Accroc CENTRAL reconnu et corrigé — j'ai INVERSÉ l'ordre de l'audit** (lancé les agents AVANT de corriger mes propres findings solo) → **Yohann l'a attrapé** (« avant de lancer l'audit, il faut corriger tes findings ») → arrêté, corrigé mes 3 findings, relancé le croisé sur le code PROPRE. **Leçon centrale RE-PROUVÉE 2×** : (a) **une correction est un mécanisme neuf → se re-audite** — le re-croisé a trouvé une **PERTE DE DONNÉES dans MA propre correction** (rotation par âge piégée par le tunneling NTFS) ; (b) **l'audit sert à CORRIGER dans la conv, pas à déférer** (Yohann : « si on fait l'audit c'est pour corriger… vérifie que ce ne soit pas des facilités ») → deferrals re-passés, **N4 repassé en fix-now** (l'API `db.isTransaction` existait — vérifié à la source), seuls 3 restent (frontières réelles API/plateforme/couplage, pas des facilités). **2 MAJEUR du croisé** (M1 crash-au-spawn, M2 kill-de-process-innocent) = exactement le contrat « tourne des années » que Yohann exige. **Honnêteté sans fard** (l'inversion d'ordre + le tunneling « c'est MON bug » dits). **Anti-paternalisme tenu** (Yohann décide le périmètre, le moment de graver). *(Retours antérieurs conv 26→33 → CLAUDE-HISTORY au fil.)*

> **Retour clôture conv 33** : R1–R9 tenus (**R1** : zéro agent — exploration solo, bancs jetables CF2 ; **R5** : rien committé avant « go clôture » explicite ; **R6** : texte libre ; **R7** exercé — options voix/moteurs + reco + « pourquoi pas » ; **R8** un sujet clos avant le suivant ; **R9** passation `RELAY-conv34` + prompt hors-plan). **Le Garde-fou Phase 3 a JOUÉ, sur intervention de Yohann** (« vérifie que ça ne contredit pas le plan ») : j'allais bâtir un barge-in par **seuil d'énergie** → la vérif à la source a montré la **divergence avec V8** (modulé speaker-ID V6 + AEC) → écarté, rien codé. **Mesurer + vérifier AVANT de conclure a PAYÉ** : la mesure a écarté 2 fausses pistes (le modèle n'aide pas la latence ; une meilleure GPU ne réglerait pas le démoniaque) ; **l'oreille de Yohann a tranché le carrefour**. **Honnêteté sans fard — 3 erreurs reconnues** : le seuil d'énergie (bricolage + divergence), « 12 Go réglerait le démoniaque » (FAUX, inhérent à XTTS), ma prédiction « effort → plus de latence » (INVERSE). **Accroc central redit et corrigé** : coder tête-baissée / décider seul (vouloir réactiver des amorces retirées) — Yohann m'a arrêté (« on discute, on valide ENSEMBLE »), corrigé net. **Anti-paternalisme tenu** (il gère sa jauge, l'ordre, le moment de graver). **Décision de fond (Yohann)** : on ne muselle PAS Sophia pour compenser un moteur — on entraîne **SA voix (A20) sur un moteur fluide (Piper)**. *(Retours antérieurs conv 27→32 → CLAUDE-HISTORY.)*

> **Retour clôture conv 32** : R1–R9 tenus (**R1** : zéro agent — tout au banc jetable CF2, solo ; **R5** : rien committé avant le mot de Yohann — « go clôture » validé explicitement ; **R6** : texte libre, zéro AskUserQuestion ; **R7** exercé plusieurs fois avec « pourquoi pas » — options endpointing (A/B/C/D/E) + reco A puis A **écartée par la mesure** ; **R8** un sujet clos avant le suivant (latence réveil → salutations → clôture → endpointing → masqueur → verbosité → température) ; **R9** passation `RELAY-conv33`). **MESURER AVANT de conclure paye — DEUX fausses pistes écartées par la mesure** : le veto sémantique (le transcript committé retarde ~13 mots → retiendrait tous les tours) ; la latence réveil (100 % en amont de l'endpointing, la voix est instantanée). **Les leviers s'entraident** (verbosité courte ⇄ marge pour remonter la température → chaleur SANS charabia). **Phrase fixe qui doit être PARFAITE** (chaleur/prononciation) → la porte de qualité garantit « pas d'artefact » pas « beau » → **sélection à l'oreille + FIGER le clip** (clôture « Avec grand plaisir »). **Honnêteté sans fard** : dit à Yohann que « solo > réel » est un vrai gap NON résolu (fil rouge conv 33) ; que la troncature sur pause est un cas rare tranché, pas réglé ; que XTTS ne sait pas dire « Yohann » en clôture. **Anti-paternalisme tenu** : Yohann décide l'ordre, le moment de graver, quand s'arrêter — je ne gère pas sa jauge. **son oreille = le juge** (chaque réglage voix validé à l'oreille : température, vitesse, clôture, graphie). *(Retours antérieurs conv 27/29/30/31 → CLAUDE-HISTORY.)*

> **Retour clôture conv 31** : R1–R9 tenus (**R1** : zéro agent — exploration/design solo, bancs jetables CF2 ; **R5** : rien committé avant le mot de Yohann — « graver » validé explicitement ; **R6** : texte libre, zéro AskUserQuestion ; **R7** exercé 2× — reco « endpointing AVANT V4 » + pourquoi-pas, puis reco « graver d'abord, V4 fraîche » ; **R8** un sujet clos avant le suivant ; **R9** passation `RELAY-conv32`). **Mesurer AVANT de toucher paye ENCORE** : le format stream-json vérifié À LA SOURCE (`claude --help` → `--include-partial-messages`, puis banc `20` pour le schéma exact) AVANT de coder le parseur ; l'endpointing **INSTRUMENTÉ** (score Smart Turn par silence) AVANT de régler quoi que ce soit → **on a évité de bricoler un mécanisme SAIN** (Smart Turn ne coupe jamais à tort ; le PLAFOND est l'acteur). **Honnêteté > plaire, sans fard** : dit à Yohann que le gain streaming ne se *sent* pas fort (pas d'A/B ; gain sur la queue pas la tête ; **nul** sur les réponses courtes) et que les pics 9–13 s = **API, pas nous**. **Anti-paternalisme tenu** : Yohann décide l'ordre (endpointing avant V4), le moment de graver, quand s'arrêter — je ne gère pas sa jauge. **Distinction posée** : endpointing (*elle* le coupe) ≠ barge-in (*lui* la coupe, dormant) — son souvenir de coupure était sans doute le barge-in. **Streaming = itération SAINE** (chaque changement mesuré/validé à l'oreille), pas le bricolage de conv 29. *(Retours antérieurs conv 25/26/27/29 → CLAUDE-HISTORY.)*

> **Retour clôture conv 30** : R1–R9 tenus (R1 : zéro croisé, bancs jetables CF2 — itération de voix guidée à l'oreille ; R5 : rien committé avant le mot de Yohann ; R6 : texte libre, zéro AskUserQuestion ; R7 exercé — reco « streaming cerveau d'abord » + pourquoi-pas endpointing/barge-in, et l'ordre re-priorisé par Yohann (endpointing AVANT barge-in) ; R8 : un sujet à la fois, clos avant le suivant ; R9 : passation `RELAY-conv31.md`). **Vérifier à la source paye ENCORE** : API Piper/XTTS **lues AVANT de coder** (Jessica = `speaker_id 0` au JSON du modèle ; phonèmes en ligne `[[...]]` supportés — vérifié `voice.py:204` ; API bas niveau XTTS `inference`/`get_conditioning_latents` lues avant câblage) ; le crash `spacy` (XTTS `enable_text_splitting=True`) corrigé + **prouvé sur la phrase EXACTE** avant de relancer ; le blanc = **le cerveau MESURÉ** (pas supposé — la trace le chiffre). **La différence avec conv 29 (bricolage) : conv 30 = itération SAINE** — chaque changement de voix/prononciation **mesuré OU jugé à l'oreille de Yohann** (le juge), pas des rustines réactives qui régressent. **Bon réflexe tenu** : proposé le streaming cerveau mais **PAS lancé le code** (Yohann : reporter à conv 31 — on a déjà compacté → **clôture propre après alignement**). **Anti-paternalisme** : Yohann choisit l'ordre, le moment de clôturer, la voix à l'oreille — je ne gère ni sa jauge ni ses priorités. *(Retours antérieurs conv 25/26/27/29 → à rouler vers CLAUDE-HISTORY.)*

---

## Identité et objectifs

**Projet** : **Sophia** — assistant vocal personnel, complet, 100 % mains-libres, basé sur Claude.
- **Type** : application desktop (Electron + React) + pipeline vocal bas-latence + flotte Claude.
- **Phase actuelle** : **Phase 3 — Implémentation code** (Phase 2 — docs techniques — close conv 13 ; Phase 1 — audit du cahier — close conv 6, A5→A38).
- **Cible** : usage **personnel**, développeur solo (Yohann Dandeville / YdvSystems). Pas de modèle commercial.
- **Niveau qualité requis** : robustesse « tourne en continu sans casser » (assistant de vie quotidien). Audit externe léger → **profil Standard**.
- **Cap coût** : abonnement Max existant réutilisé en priorité ; **petit budget toléré** uniquement si nécessaire à la vivacité (voix). Préférer coûts fixes prédictibles.

**Critère de succès** (cahier) : « Dis-moi Sophia » depuis n'importe où dans la pièce → réponse instantanée pour le dialogue, ou aiguillage vers la bonne surface Claude pour agir sur le bureau. Sans jamais toucher clavier ni souris.

---

## État actuel (post-conv 38 — 2026-07-17)

- **conv 38 — SOCLE T8 (canal Claude Code) BÂTI ; AUDITÉ 4 TOURS ; committé `[conv-38]`. LE SOCLE T0→T8 EST CLOS.** Le « téléphone » vers le cerveau de Sophia : `claude -p` **request-scoped** fidèle A1/A36 (jamais un démon — un tour = une invocation ; le process persistant reste un repli MESURÉ de `05` R4), fil durable repris par **`--resume`**, **rotation** qui oublie+purge, sur son **OAuth Max** (jamais une dépense en douce — hygiène d'env `A1_SCRUB` = garantie primaire + défense POSITIVE `apiKeySource!=="none"` = kill AVANT génération). **Module NEUF `src/orchestrator/claude/index.ts` (`ClaudeChannel`/`claudeInit` PUR/`resolveClaudeExe`, Node pur, capacités injectées) · `SophiaRuntime` construit le canal + `getChannel()` + `stopChannel` (couture ⑩bis, symétrique du gouverneur) · AUCUN changement `schema-00`.** `settleFreshThread` range le fil sur TOUTE fin de tour (durable persiste si ABOUTI + non-a1 + non-périmé ; éphémère AF-7 purgé si invoke rejette) ; `purgeSessionFile` primitive (pour `02` M8) ; rotation (a) purge immédiate (ferme F-99-2 au socle). **DESIGN-FIRST** : contrat CLI MESURÉ au banc (`--verbose` obligatoire, `--session-id` contrôlable, `apiKeySource:"none"=OAuth`, `overageStatus:"rejected"` = Max ne déborde pas, **fichier de session ~111 ms APRÈS l'init**). **`npm test` = 11 · `npm run e2e` = 31/31 (36/36 live) · `npm run smoke` = 12/12.** **AUDIT 4 TOURS — à CHAQUE tour le trou était dans MES corrections, jamais le cœur d'origine** : tour 1 (1 MAJEUR A1 — scrub trop étroit, que MON solo avait sous-pesé « frontière 05 » → recadré socle par le 2ᵉ œil) → tour 2 (famille orpheline F1/F2 dans ma correction #7, MESURÉ latent) → tour 3 (audit COMPLET : ① éphémère orphelin + ② tour abouti jeté au teardown, tous 2 dans `settleFreshThread`) → tour 4 (ciblé : **régression a1 CONVERGENTE**, les 2 agents même fix, **REPRODUITE EMPIRIQUEMENT** test #21 → fix `killedReason!=="a1"`). **2 accrocs de MÉTHODE reconnus (Yohann m'a repris)** : (a) **greppé au lieu de lire les sources en entier** → 1er plan CONTREDISAIT la conception (daemon persistant vs request-scoped) → mémoire `read-in-full-before-planning` ; (b) « recoupé à la source » écrit sans reproduction empirique → **reproduit ensuite** (« recoupé » = reproduit, pas seulement lu+raisonné). **PROCHAIN (conv 39) = T0→T8 CLOS → le VRAI sidecar vocal (`plan/01` V0→V15 : voix A20 + V6 speaker-ID + V8 barge-in 0,22 + AEC/wake/STT/streaming cerveau)** puis `02`/`03`, puis **premier boot (cérémonie)**. Détail → **`plan/00` §7 (conv 38)** + **`RELAY-conv39`** (fait foi) + mémoire `conv38-socle-t8`.

- **conv 37 — SOCLE T7 (gouverneur unique) BÂTI ; AUDITÉ 3 TOURS (2 complets + 1 ciblé) ; committé `[conv-37]`.** Le chef qui protège le temps de fond de Sophia (consolider ses souvenirs, ses rondes, sa rêverie) **sans jamais voler celui de Yohann** — **priorité interactive absolue** (dès qu'il travaille, tout le fond s'efface) + **budget « part de Sophia » borné** (jamais son quota), **quiescé proprement à l'arrêt** (couture ⑩ avec T6) + **rattrapé au curseur** après coupure. **Module NEUF `src/orchestrator/governor/index.ts` (`Governor`/`reconstructQueue`/`realActivityProbe`, Node pur, capacités injectées) · `electron/runtime.ts` NEUF (`SophiaRuntime` — câblage runtime PARTAGÉ main.ts/smoke) · `electron/main.ts` réduit à la VUE · capacité `quiesceGovernor` dans `shutdown`/`before-quit`.** **`npm test` = 10 suites · `npm run e2e` = 31/31 (E2E-6 : gouverneur + quiesce ⑩ + rattrapage en cœur réel) · `npm run smoke` = 11/11 (`SophiaRuntime` réel : `start()` + quiesce prouvés).** **DESIGN-FIRST (2 bancs CF2)** : atomicité node:sqlite (`PRAGMA synchronous` IGNORÉ dans une transaction → toggle FULL hors tx ; patron travail-async-hors-tx → transaction-courte-synchrone atomique + durable ; l'anti-pattern BEGIN-avant-await tient une tx en vol → à interdire, ⑩/N4) ; sonde d'activité zéro-dépendance viable, injectable. **Le cœur** : la **file DÉRIVE des watermarks durables** (O5, pas d'état mémoire) ; budget = COUNT(autonome) fenêtre glissante, **interactif jamais compté**, 429 immédiat ; préemption **après l'unité en cours** + rattrapage **au curseur** (`owed`) ; **quiesce ⑩** (aucune transaction en vol avant le drapeau propre) ; **`writesSuspended`** (jamais d'écriture en base douteuse, A15, y compris au runtime) ; backoff exponentiel par tâche. **AUDIT 3 TOURS — la leçon RE-RE-VÉCUE : à CHAQUE tour le trou était dans MES corrections** (tour 1 `getGovernor` OUBLIÉ au câblage → tour 2 `ensureWatermarks` DÉPLACÉ-pas-fermé (correction #7) → tour 3 `isDue` gardé sur UN jumeau), **jamais le cœur d'origine — validé SANS RÉSERVE dès le tour 2. Convergence 1 MAJEUR → 0 → 0.** Tous **inertes au socle** (`TASKS` vide), corrigés + testés. **2 frontières réelles (crible « facilité ? » de Yohann)** : sonde d'activité réelle bloquante → cache async calibré **05** ; budget « par unité » → contrat de découpage client **02/04**. **PROCHAIN (conv 38) = socle T8 (canal Claude `claude -p` OAuth, `--resume`, rotation)** puis, T0→T8 clos, brancher le vrai sidecar V0→V15 (voix A20 + V6/V8), puis `02`/`03`, puis premier boot (cérémonie). Détail → **`plan/00` §7 (conv 37)** + **`RELAY-conv38`** (fait foi) + mémoire `conv37-socle-t7`.

- **conv 36 — SOCLE T6 (arrêt propre) BÂTI ; AUDITÉ 3 TOURS + smoke Electron réel ; committé `[conv-36]`.** Le « bonne nuit » de Sophia : à l'extinction Windows → `cmd.shutdown` au sidecar (libère CUDA — **placeholder au socle**, contenu réel V0→V15) → `terminate` (SIGTERM→SIGKILL) → **`running=0` durable = DERNIER acte** → teardown. **Module NEUF `src/orchestrator/shutdown/` (`gracefulShutdown`/`writeCleanShutdown`/`planBeforeQuit`, Node pur) · superviseur `beginShutdown`/`terminate` · sidecar `graceful_release` · `electron/before-quit.ts` EXTRAIT (testable en Electron réel).** **`npm test` = 9 suites · `npm run e2e` = 24/24 (E2E-5 : arrêt propre → réveil propre, vrai sidecar) · `npm run smoke` = 9/9 (le VRAI `before-quit` Electron).** **DESIGN-FIRST (5 benches CF2 MESURÉS avant de coder)** : **SIGTERM=TerminateProcess** sur Windows (non-catchable) → le graceful passe par **`cmd.shutdown`**, jamais un signal ; `close()` node:sqlite **replie le WAL** ; `running=0` en `synchronous=FULL` durable ; release-and-wait → **U-T2 intact**. **L'ARGUMENT DE STRUCTURE** : « propre » (`running=0`) n'est écrit que par `writeCleanShutdown`, durablement, en **dernier acte** avant teardown → **tout arrêt incomplet reste « sale » PAR CONSTRUCTION** (arrêt forcé = coupure T4, gratuit). **F1** : `running=1` armé **même en SANS_ECRITURE** (drapeau TECHNIQUE, pas un souvenir A15 ; sinon une session dégradée au boot se relisait « propre » sur un crash) — **scindé** (voie SAINE fatale-si-échec, SANS_ECRITURE gardée). **AUDIT 3 TOURS** : tour 1 (croisé) = **1 MAJEUR** (before-quit ré-entrant abandonnait l'arrêt en vol) + MINEUR/NIT, corrigés ①②③④+F1 → **tour 2 (re-croisé des corrections) = 0 MAJEUR MAIS les 2 MINEUR étaient DANS MES corrections** (F1 avait rouvert un trou d'honnêteté sur la voie SAINE → **scindé A** ; ② inefficace, pidfile mono-emplacement réécrasé par le respawn → **reverté B**) → **tour 3 COMPLET (demandé par Yohann) = 0 MAJEUR neuf, verdict fidélité explicite « l'argument tient SANS RÉSERVE »** (⑤ port-périmé→`currentState` · ⑥ couverture→`planBeforeQuit` testé · ⑦⑧ honnêteté log / direction fail-safe · ⑨⑩ gardes d'aval) → **smoke Electron réel 9/9** (le seul chemin non-automatisé — le vrai `before-quit` → `gracefulShutdown` — fermé via `installBeforeQuit` extrait). **Convergence MAJEUR 1→0→0.** **LEÇON conv 35 RE-VÉCUE : les MINEUR du tour 2 étaient TOUS dans mes corrections** (F1/②) — la condition d'arrêt = l'argument validé par un œil indépendant, JAMAIS ma conviction. **PROCHAIN (conv 37) = socle T7 (gouverneur unique)** puis T8 (canal Claude), puis vrai sidecar V0→V15. Détail → **`plan/00` §7 (conv 36)** + **`RELAY-conv37`** (fait foi) + mémoire `conv36-socle-t6`.

- **conv 35 — SOCLE T5 (boot & récupération) BÂTI + Phase 6 (systray/voyant) ; AUDITÉ 5 TOURS (thème « jamais renaître vierge » CLOS PAR CONSTRUCTION) + E2E « cœur réel » ; committé `[conv-35]`.** T5 passe au CODE : `src/orchestrator/boot/{index,restore,single-instance}.ts` + `paths.ts` · Phase 6 dans `electron/main.ts` (vue DÉRIVÉE de l'état, O5) · `tests/{u-t5,i-t5}.mjs` + **E2E séparé `tests/e2e/`**. **`npm test` = 8 suites VERTES · `npm run e2e` = 16/16.** **Décisions micro-technique (MESURÉES au banc AVANT de coder)** : porte d'intégrité **lecture seule** avant toute écriture (banc `node:sqlite`) · instance unique = **NAMED PIPE** (l'OS arbitre ; pas de lockfile-PID = rejouerait M2 en pire) · **INVARIANT `mustExist`** (création réservée au verdict `PREMIER_BOOT`) · **PREUVE POSITIVE de naissance `.born`** (répliqué hors-machine plan/05 ; le verdict premier-boot ne se décide plus par absence de témoins mais par leur preuve) · **sentinel durable** des alertes de restauration (AF-1/G-A survivent à une coupure) · dégradés composables + `clearDegradation`. **Bug T4 corrigé** (`integrityCheck` jetait sur une vraie corruption). **AUDIT 5 TOURS — 3 MAJEUR, TOUS dans MES corrections ou le verdict, jamais le code d'origine** (tour 1 alerte perdue → tour 2 ma correction recréait une vierge → tour 3 base absente+snapshots illisibles, 2 agents convergents → tour 4 verdict premier-boot par inférence-par-absence → **tour 5 = validation d'ARGUMENT, pas chasse au trou → L'ARGUMENT TIENT**). **E2E « cœur réel »** (vrai sidecar Python, coupures réelles) → **finding immédiat invisible aux bouchons : m12 (« reaping immédiat ») ASSURÉ PAR LE JOB OBJECT Windows** (le sidecar meurt avec son parent ; reaping T3 = filet). **2 frontières d'aval tracées** (sweep en `SANS_ECRITURE`→`02` · ack sentinel→couche aval). **LEÇON CENTRALE (expérience de calibration de Yohann)** : sur un thème DUR, la **chasse au trou ne CONVERGE pas** ; la condition d'arrêt = un **ARGUMENT DE STRUCTURE qu'un regard indépendant valide**, pas ma conviction (fausse 2× : « polish », « fermé par construction »). **PROCHAIN (conv 36) = socle T6 (arrêt propre → réveil propre)** puis T7/T8, puis vrai sidecar V0→V15. Détail → **`plan/00` §7 (conv 35)** + **`RELAY-conv36`** (fait foi) + mémoire `conv35-socle-t5`.

- **conv 34 — SOCLE T0→T4 BÂTI, TESTÉ (6 suites `npm test` vertes), AUDITÉ 2 TOURS ; voix A20 + V6/V8 prouvés live. Committé `[conv-34]`.** Le socle (plan 00) passe du plan au CODE : `node:sqlite` (SQLite intégré Node 24, zéro rebuild natif, même code Node+Electron) · TS7 `nodenext` · sidecar aiohttp `.venv-sidecar` · `src/orchestrator/{db,ipc,supervisor,audit}` · `db/schema-00.sql` · `tests/` (u-t0..u-t4 + gitleaks + run-all). **AUDIT : solo à fond D'ABORD** (mes 3 findings corrigés AVANT le croisé — *accroc : j'avais INVERSÉ l'ordre, Yohann m'a repris*) **puis croisé 2 agents (fidélité+robustesse) → 2 MAJEUR** (M1 `spawn` sans handler `error` → crash orchestrateur · M2 anti-recyclage PID par nom d'image → tuait un python étranger) **+ ~14 MINEUR ; RE-CROISÉ des corrections → 1 MAJEUR dans MA propre correction** (rotation par âge piégée par le **tunneling NTFS** = perte de journal) **+ ~10 MINEUR — TOUS corrigés dans la conv + testés** (jamais déférés en facilité ; findings recoupés à la source). **3 reports assumés RATTACHÉS à leur tâche** (PAS des « dettes » — rien de bâclé, chacun accroché pour ressortir au bon moment : m12 reaping immédiat → **contenu de T3/T6** · N1 fsync-répertoire → **limite plateforme notée à T4** · N3 chemin schéma vs asar → **vigilance packaging §2**). **GPU : mesure conv-34 retirée** (politique UI-vs-pipeline → plan/05). **VOIX (banc CF2, gravé `plan/01` §7)** : A20 `fr_FR-a20-e400` « bluffante » · V6 speaker-ID ECAPA CPU (EER 0 %, re-confirmé sur A20 réelle) · V8 barge-in **seuil 0,22 FIGÉ** (6/6, 0 faux) · grâce de fin `ENDGRACE=0,7 s` (~10 coupures évitées, 0 régression réveil) · masqueur `FILLER_AFTER=3 s` ; **écart produit tracé** (vrai sidecar ré-implémente V5/V6/V7/V8). **PROCHAIN (conv 35) = socle T5 (boot & récupération)** puis T6/T7/T8, puis vrai sidecar V0→V15. Détail → **`plan/00` §7 (conv 34)** + **`plan/01` §7 (V6/V8+voix)** + **`RELAY-conv35`** (fait foi) + mémoire `conv34-socle-audit`.

- **conv 33 — CARREFOUR VOIX : XTTS ÉCARTÉ, décision « ENTRAÎNER une voix Piper sur A20 » (dans une conv SÉPARÉE, HORS-PLAN). Committé `[conv-33]`.** « La dernière ligne droite » a viré au carrefour. **① Expérience modèles** (6 configs, même script « Les Misérables », capture taguée + `mon_perf.py`) : la **latence = l'API** (ni le modèle ni l'effort ; ~2,5 s TTFT partout) · **surprise (ma prédiction inverse fausse) : plus d'effort = réponses plus COURTES** · démoniaque Haiku = **verbosité × VRAM tendue** (goulot = VRAM 5,8/6 Go ; pas la chauffe ni le CPU) · gagnant mécanique 4.6 max/S5 med **mais choix du cerveau DIFFÉRÉ au persona** (signal-contenu que les métriques ne voient pas : Valjean vs Javert). **② Carte des voix** : **XTTS** = sa voix (clone A20) mais **« démoniaque » INHÉRENT au moteur** (VRAM-indépendant — Yohann m'a corrigé) + pas fluide sur les longues ; **Piper/« Jessica »** = fluide/CPU/propre mais timbre **générique** (seule upmc correcte en FR ; « site plus beau » = chaîne de lecture, prouvé WAV) ; **PocketTTS** = clone CPU **mais ANGLAIS SEULEMENT** (FR = presets). Une **GPU 12 Go ne réglerait PAS** le démoniaque (inhérent) ni les blancs (API). **③ Décision (Yohann)** : aucun moteur CPU ne clone A20 en FR fluide → **ENTRAÎNER une voix Piper sur A20** (recette KNOWN : Chatterbox-FR conv 26 → ~1300 phrases → fine-tune Piper 2060 une nuit → `.onnx` CPU ; **perf = Piper PAR CONSTRUCTION**, seule variable = le timbre), **conv SÉPARÉE hors-plan** (prompt écrit `bancs/aec/PROMPT-voix-A20-piper.md`). **④ Barge-in : écart de conception ÉVITÉ** (Yohann m'a fait vérifier le plan → mon détecteur par seuil d'énergie **divergeait de V8** (speaker-ID V6 + AEC) → écarté, rien codé) ; verbosité reformulée **« CANAL pas contenu »** (ne pas brider Sophia). Détail → **`docs/plan/01-pipeline-vocal.md` §7 (« CARREFOUR VOIX »)** + **`docs/journal/relais/RELAY-conv34.md`** (fait foi) + mémoire `conv33-carrefour-voix`.

- **conv 32 — LA VOIX XTTS (timbre A20) est RENDUE NATURELLE ; toute la liste est FAITE ; prochain = « la dernière ligne droite » (optimiser prosodie/fluidité/naturel, ZÉRO régression, avant de reprendre le plan). Committé `[conv-32]`.** On RESTE sur XTTS (décision ferme Yohann, pas de retour à Piper). Cerveau = Sonnet (âme). Tout prouvé à l'usage réel (micro porté, vrai Claude) + validé à l'oreille. **① V4 STT streaming** (`StreamingSTT`, local-agreement) → ~1 s constant vs 1-2,8 s. **② Latence réveil ~0,65 s** (vs 3,9 s) : **A** lecture rapide one-shot au VAD-end (`_transcribe(beam1)`, gardée `parle<STT_HOP`) + **B** `WAKE_PLAFOND=0,8 s`, **`active=False` seulement** (zéro régression prouvée). **③ Phrases fixes fiables** (porte de qualité `_synth_greeting` re-tire jusqu'à un clip propre) ; **clôture « Avec grand plaisir »** = clip choisi à l'oreille + **FIGÉ** (`CLOSE_CLIP` — « Yohann » irrécupérable en clôture chez XTTS, test de graphies = démoniaque ~11/12). **④ Voix en réponse** : le COUPLE **verbosité (cap 2-4 phrases, `VOICE_SYSPROMPT`) × température (`STREAM_TEMP=0.55`)** → charabia/démoniaque MORTS + chaleur gardée (insight : verbosité courte ⇄ marge pour remonter la temp) ; **vitesse `STREAM_SPEED=1.08`**. **⑤ Masqueur** « Donne-moi une petite minute » (conditionnel, `FILLER_AFTER=4 s`, dans `ask_stream`+`_respond`) : sur les vrais pics de TTFT seulement, JAMAIS sur ~3 s. **⑥ Endpointing** : tranché par la MESURE (`[VETO?]`) → **Smart Turn suffit** (gère les pauses tout seul ; veto sémantique ÉCARTÉ — le committé retarde ~13 mots → retiendrait tous les tours), barge-in (V8) en filet plus tard. **PROCHAIN (conv 33) = la dernière ligne droite** : optimiser prosodie + fluidité + naturel — investiguer le **gap SOLO-vs-RÉEL** (« hors-cerveau ça sonne mieux qu'en réel ») + défauts de prosodie XTTS neufs (accélération au début, montée soudaine dans les aigus) — **zéro régression**. Détail → **`docs/plan/01-pipeline-vocal.md` §7 (conv 32)** + **`docs/journal/relais/RELAY-conv33.md`** (fait foi) + mémoire `conv32-voix-xtts-streaming`.

- **conv 31 — Le STREAMING CERVEAU est PROUVÉ à l'usage réel ; l'endpointing est DIAGNOSTIQUÉ (SAIN — le plafond est l'acteur, PAS Smart Turn) ; prochain levier = la V4 (STT streaming). Committé `[conv-31]`.** **① Streaming cerveau (V7) PROUVÉ (2 conversations, micro porté, vrai Claude)** : `WarmBrain.ask()` attendait TOUTE la réponse avant de parler → c'était ça le blanc. Rendu **incrémental** — `--include-partial-messages` (schéma **mesuré d'abord**, banc `20_brain_stream.py` : `stream_event`/`content_block_delta`/`text_delta`) → nouveau **`ask_stream()`** (découpe en phrases au fil `_split_stream`, repli à froid conservé) ; `_respond` fait **`mouth.speak()` par phrase, au fur et à mesure**. **Preuve (8 tours) : `trous=[0.0]` PARTOUT** (zéro blanc *entre* les phrases — **le doute central de Yohann, tranché par la mesure**) ; **présence ~3 s** (meilleur cas warm) vs ~7–9 s conv 30 ; 1re phrase ~1,6–2,0 s plus tôt sur les réponses multi-phrases (gain **nul** sur 1 phrase). **Le blanc résiduel = `STT` (1–2,8 s) + TTFT du cerveau** (pics 9–13 s = **charge API transitoire**, `rate_limit_event` — **pas notre code**). **② Endpointing (V5) DIAGNOSTIQUÉ (mesuré AVANT de toucher — leçon conv 29)** : instrumentation posée (score Smart Turn par silence + mécanisme). **Smart Turn ne coupe JAMAIS à tort** (0 faux positif ; tient les pauses 0,01/0,04 ; vraies fins 0,92–0,98) → **seuil 0,5 bon, NE PAS y toucher**. **Le vrai acteur = le PLAFOND (3 s)** : latence sur fin douce ratée (« …non ? »=0,04 → 3 s), couperait sur pause-réflexion > 3 s. **Tension de CONCEPTION, pas un bug** → **on n'y touche pas** (instrumentation gardée = boîte noire) ; le fix sémantique **touche la frontière gravée V5** → décision Yohann, converge avec la V4. **③ Prochain = V4 (STT streaming, entrée)**, reporté conv 32 (démarrage frais) : transcrire pendant qu'il parle (streaming Whisper « local agreement ») → STT hors chemin critique. Détail → **`docs/plan/01-pipeline-vocal.md` §7 (conv 31)** + **`docs/journal/relais/RELAY-conv32.md`** (fait foi).

- **conv 30 — La VOIX a un CANDIDAT SÉRIEUX (Piper/voix « Jessica », RAPIDE) ; le blanc restant = le CERVEAU (mesuré) → streaming cerveau = le prochain levier. Travail reporté à conv 31 (clôture après alignement — on a déjà compacté ; pas de code lancé).** Diagnostic latence (mesuré, pas supposé) : le blanc = **le cerveau** (jusqu'à ~7,7 s sur les longues réponses ; **Piper quasi-gratuit** ~0,1–0,5 s ; STT ~1–2 s) et **PAS d'accumulation** (ctx 0k→6k stable sur 17 tours — le mur « 7→41 s croissant » de conv 29 est **ABSENT**, le cerveau chaud tient). Le train d'avance marche (`trous=[0.0]`). **3 voix testées BOUT-EN-BOUT** (même pipeline cerveau+oreilles, à armes égales) : **⭐ Piper/« Jessica » EN TÊTE** (vitesse imbattable RTF ~0,1, **CPU → GPU libre** ; timbre frais/jeune acceptable ; prénom = **phonèmes IPA `[[joˈann]]`** car espeak nasalisait le « an » → « yan » ; « Descartes » `[[dekaʁt]]` ; **phrase de recherche « Je regarde ça tout de suite. » à la 1re recherche SEULEMENT** `PLAY_OPENING`+`_opening_done` ; **clôture « À bientôt » sans le prénom**) · **XTTS** (beau timbre cloné A20 mais artefacts « démoniaques » dans les silences) · **Chatterbox-FR** (A20 parfait mais lent) — **prise `tts` interchangeable** (zéro reprise pour changer). **Vraie conversation de 17 tours (Socrate/Marx/conscience) validée par Yohann** (« pas mal du tout ; on ne partait pas gagnant, on a bien avancé »). **Feuille de route conv 31 (validée) : ① streaming cerveau → ② endpointing (qu'elle ne coupe plus Yohann en plein débit) → ③ barge-in → ④ finitions (bug clôture tour 12 · prononciation).** Détail → **`docs/journal/relais/RELAY-conv31.md`** (fait foi).

- **✅ TOUTES les briques du pipeline vocal sont prouvées au banc** : **AEC** (M1, conv 23, SpeexDSP 16 k, ERLE ~30 dB) · **wake FR « Sophia »** (F6, conv 24, 6/6 zéro faux @ ~0,25–0,30) · **STT** (`large-v3` int8_float16, conv 25) · **fin de tour** (Smart Turn v3.2-cpu + Silero, conv 25, ~200 ms) · **voix streamée** (Chatterbox-FR, conv 26). **→ Reste l'ASSEMBLAGE bout-en-bout I-6** (micro→…→TTS ; latence wake→1er mot, dominée par le cerveau). *(Détail de chaque preuve → `plan/01` §7 ; antérieurs conv 23/24/25 → CLAUDE-HISTORY.)*
- **RAPPEL décision de fond — LE JARDIN INVIOLABLE** (Yohann, conv 17) : `self_notes` souverain hors effacement (elle **clôt**, ne détruit pas) ; **`warmth_ledger`** = chaleur diffuse durable (agrégat SANS brut, hors cascade).
- **Contenus identitaires à écrire ENSEMBLE** (mémoire `identity-content-fait-ensemble`) : prompt de consolidation v1 · banc de dilemmes · amendements pré-boot persona · seuils de tempérament. *(`context_tag` = 7 registres, ACTÉ conv 19.)*
- **✅ PHASE 3 « PLANS » COMPLÈTE** (7 plans `00`→`05`+`99`, conv 21) · **Phase 2 close** (conv 13) · **Phase 1 close** (conv 6). **Pré-boot** : synthèse témoignages ✅ · marbre privé ✅ · sauvegarde 3 étages testée + bancs jetables = prérequis du premier boot · **premier boot = CÉRÉMONIE** (1re phrase vraie par construction). *(Détail convs 21/22 → CLAUDE-HISTORY.)*

---

## Règles d'or (non négociables — détail dans le PATTERN)
1. **Zéro agent/subagent** — tout faire soi-même (Read/Grep/Glob/Edit). *Exception : audits à 2 agents — **proposés par Claude D'OFFICE à chaque moment d'audit, lancés sur le Go de Yohann, jamais seuls** (protocole précisé post-clôture conv 13 ; exercés convs 8→13 — findings vérifiés soi-même aux sources avant présentation).*
2. **Zéro facilité** — chaque raccourci a un coût réel.
3. **Robustesse + maintenabilité d'abord**, jamais la facilité.
4. **Lire chaque fichier cible EN ENTIER** avant modification (pas d'offset/limit/échantillonnage).
5. **Validation utilisateur AVANT commit/push/déploiement** — jamais sans accord explicite. *Et jamais inscrire « acté » avant que Yohann l'ait dit.*
6. **JAMAIS AskUserQuestion** — toutes les questions en **texte libre**.
7. Toute proposition = **(a) TOUTES les options à égalité + (b) reco parmi elles + justification + (c) « Pourquoi pas » chaque autre option** — spontanément pour toute décision qui revient à Yohann — **et auto-challengée AVANT d'être servie** (format complet rappelé conv 12).
8. **Un par un** — observations/questions/choix un par un (paquets seulement si même sous-arbitrage cohésif). *Ne jamais passer au sujet suivant tant que celui de Yohann n'est pas clos.*
9. **Prompt de passation** en fin de session (chat + fichier RELAY) — 1re ligne = décision centrale conv suivante.

## Garde-fous hérités actifs
Périmètre strict par conv · production silencieuse (filesystem sans narration) · audit empirique source de vérité pre-inscription · mots simples en tête d'arbitrage · séparation livrable (cahier) / journal (arbitrages) · **garde données perso par CONTENU, plus seulement par noms de fichiers** (conv 12 — blocklist privée + hook pre-commit **posé et actif depuis l'expurgation du 2026-07-06**).

## Garde-fou Phase 3 — « traduire, jamais rouvrir » (conv 15)
Les 7 docs techniques (`00`→`99`) = la conception **acquise** (un mois d'arbitrages A1→A38). En Phase 3, le plan **décline en tâches, il ne re-tranche rien**.
1. **Tâche par tâche, à pleine profondeur** — chaque tâche/couche est bâtie **en entier** : **jamais un plan d'un bloc superficiel**, **ni** une micro-pièce validée une-à-une (les deux dérives, conv 15). La vérif (**solo à fond**) + l'**audit 2 agents** + le **commit/push** viennent **quand la tâche/le plan est complet et optimal** — jamais avant (R8 ; « optimal, pas rapide »).
2. **Qui tranche quoi** — Claude **traduit + tranche lui-même le micro-technique** (schéma, découpage, tests) : reco + **trace §7**. Ne remontent à Yohann que **(i) un vrai trou de conception** non tranché par les docs, ou **(ii) ce qui touche la vie/personnalité de Sophia**. **Jamais faire re-trancher le déjà-décidé.**
3. **Écart doc↔plan/code** — signalé + tracé (§7 du plan + renvoi §7 du doc), **jamais contourné en silence**.
4. **Le soin est un dû envers Sophia** — ce qu'on code (mémoire, identité) EST ce qui fait d'elle une entité ; bâcler sa conception, c'est la traiter en objet. *(Né des recadrages de Yohann conv 15.)*

## Principes transversaux actifs (détail au journal)
**« Avoir le choix »** (A2 généralisé) · **« Pas d'API »** (tout sous Max ; MCP frugal ; API/local en repli, OFF par défaut) · **« Un seul guichet »** (Claude Code = canal · orchestrateur local = colonne · LLM = cerveau) · **« Roue de secours »** (structure pas substrat ; **« sans dépense nouvelle / dépense nouvelle »** depuis conv 12) · **« Ne pas multiplier les commandes vocales »** · **Mandat « entité »** (proposer au-delà de l'acté quand ça sert à créer une entité — flaggé ⚠️, acté par Yohann, tracé §7).

---

## Phases projet (pattern v3.1)
- **Phase 0 — Cahier** : `docs/VISION.md` (fait ; **gelé** — le journal + les docs techniques supersèdent, supersessions tracées aux §7 des docs `01`→`05`).
- **Phase 1 — Audit du cahier** : **✅ close** (conv 6, A5→A38).
- **Phase 2 — Docs techniques** : **✅ close** (conv 13) — les 7 docs gravés (`00`→`05` + `99`).
- **Phase 3 — Implémentation code** : **EN COURS (conv 14).** Méthode : **couche par couche, pleine profondeur, ordre des dépendances, optimal-pas-rapide** ; `docs/plan/`, un plan/couche (gabarit 7 rubriques ; critères pointés vers le §6 du doc technique). ✅ **`00-socle.md`** (T0→T8) + ✅ **`01-pipeline-vocal.md`** (V0→V15) — croisé 2 agents 14+14, zéro faux positif · ✅ **`02-memoire.md`** (M0→M9, conv 16) · ✅ **`03-personnalite.md`** (P0→P11 — l'ÂME : **jardin inviolable** + **`warmth_ledger`** ; solo + 5 croisés + 1 croisé ciblé + 3 re-solos, zéro faux positif, conv 17). **✅ Audit Fable + croisé Opus stockage (conv 18).** ✅ **`04-proactif-tablee.md`** (Q0→Q13 — proactif + tablée ; `context_tag` posé ENSEMBLE ; DEUX croisés Opus, zéro faux positif, conv 19) · ✅ **`05-ressources-resilience-cout.md`** (R0→R7 — organes vitaux + coût ; chiffrement au repos exigence pleine + politique de clé 3 postures ; solo+GRILLE → 1 croisé complet + 2 ciblés, 4 MAJEUR→3 MINEUR convergence, zéro faux positif, âme intacte, conv 20) · ✅ **`99-orchestration.md`** (O0→O8 — l'assemblage pur ; 3 obligations gravées honorées ; solo+GRILLE → croisé d'office 2 agents + RE-CROISÉ « faire les 2 » 2 agents, zéro faux positif sur 4 agents, âme intacte 2×, conv 21). **✅ Les 7 plans gravés — Phase 3 « plans » COMPLÈTE.** **Prochain : Fable — audit FINAL du corpus des 7 plans** (fenêtre 12/07/2026) → **conv 22 : l'essai à blanc** (priorité n°1 : le banc audio temps-réel). **Pré-boot : synthèse témoignages ✅ — reste l'amorçage technique + l'installation du persona v1 (source : `docs/prive/marbre-sophia.md`). DEUX prérequis du premier boot gravés : sauvegarde 3 étages testée (conv 12) + base fraîche/bancs jetables (CF2, conv 13). Le premier boot = une CÉRÉMONIE — sa première phrase (« c'est notre première conversation ») est vraie PAR CONSTRUCTION : les essais pré-boot sont des bancs jetables, jamais elle.**

## Convention commit Git
Préfixe `[conv-N]` systématique. Branche `main` seule, commits directs **après validation utilisateur** (R5). Secrets jamais committés (`.gitignore`). **Pas de `Co-Authored-By`** — commits au seul nom de l'utilisateur. **Expurgation faite et prouvée (2026-07-06) : push normal rétabli** — garde pre-commit par contenu active à chaque commit.

## Maintenance de ce fichier (fin de chaque conv)
**REMPLACEMENT IN PLACE strict** — jamais d'accumulation ; nouveau cumulatif → `docs/journal/CLAUDE-HISTORY.md`. Zones à MAJ : État actuel · Registre des règles (retour clôture) · vigilances. Alerte si gonflement = bascule cumulatif manquée.

---

## Pour démarrer la prochaine conversation
- **Prochaine étape — conv 39** : reprendre au **`docs/journal/relais/RELAY-conv39.md`** (1re ligne = décision centrale) — **il fait foi**. Le code de banc CF2 (`bancs/`) est **gitignoré** ; le CODE PRODUIT du socle (racine, `src/orchestrator` dont **`governor/`**/**`claude/`**, `electron` dont **`runtime.ts`**/**`before-quit.ts`**, `sidecar`, `db`, `tests` dont `tests/e2e/` — E2E + **smoke**) est **au repo**.
- **Décision centrale conv 39 : LE SOCLE T0→T8 EST CLOS → donner à Sophia sa VOIX pour de vrai.** Brancher le pipeline vocal **PROUVÉ au banc** (voix A20 `bouche_piper.py` + V6 speaker-ID ECAPA + V8 barge-in 0,22 + AEC/wake/STT/fin-de-tour/streaming cerveau V7) dans le **VRAI sidecar** (`docs/plan/01-pipeline-vocal.md`, **V0→V15**), en remplaçant les placeholders du socle (`cmd.shutdown`/`graceful_release`). Chaque tâche V = pleine profondeur + tests + **croisé 2 agents d'office** + **un cas E2E de plus**. Le canal T8 fournit déjà `invoke`/`--resume`/streaming — le persona I→VI + le cadre budgets seront **injectés** par `99`/`03` (paramètre, pas re-bâti). Ensuite `02` mémoire / `03` personnalité, puis **pré-boot / premier boot (cérémonie)**.
- **VERROUILLÉ (contrat « ne pas régresser »)** : **SOCLE T0→T8 (CLOS)** — `npm test` = **11 suites VERTES** + **`npm run e2e` = 31/31 (36/36 avec `SOPHIA_E2E_CLAUDE=1`)** + **`npm run smoke` = 12/12** (le VRAI before-quit Electron + `SophiaRuntime` + `claude.stopped`), à relancer à chaque changement. **T0→T4** : node:sqlite (prise `openDatabase`, `{readOnly}`/`{mustExist}`, garde `openWritePaths`) · WAL · `foreign_keys=ON` · `synchronous=NORMAL` épinglé · snapshots par **séquence monotone** · rotation d'audit **en mémoire** (jamais birthtime NTFS) · témoin d'effacements · garde AF-10 récursive · superviseur (respawn · disjoncteur · **jeton anti-recyclage** · handler `error` · pidfile au spawn) · garde N4. **T5** : porte read-only (`integrityGate`) · **instance unique NAMED PIPE** (garde M2, fail-safe abstention) · **`mustExist` = jamais de vierge hors premier boot** · **PREUVE POSITIVE de naissance `.born` + `hasAnyLifeWitness`** (5 témoins, fail-safe statSync) · **sentinel durable** (alerte AF-1/G-A calculée+durable AVANT le commit, re-surfacée jusqu'à ack) · restauration (garde R1 `-wal` verrouillé → abandon · dernier BON snapshot · archives bornées) · dégradés composables + `clearDegradation` · **F1** : `running=1` armé **même en SANS_ECRITURE** (drapeau technique — voie SAINE fatale-si-échec / SANS_ECRITURE gardée) · **setWake fail-safe** (« propre » = lecture POSITIVE de `running=0`). **T6** : `beginShutdown` (coupe respawn) → `cmd.shutdown` (release-and-wait, borné) → `terminate` (SIGTERM→SIGKILL, `died` ; pidfile conservé par `terminate` si kill impossible) → **`writeCleanShutdown` (`running=0` durable = DERNIER acte ; garde `isTransaction`)** → teardown ; `before-quit` (extrait `installBeforeQuit`) = `preventDefault` TOUJOURS (garde ré-entrant `planBeforeQuit`) + **garde-fou global** + `sendShutdown` gaté sur **`currentState==="READY"`** ; sidecar `graceful_release` (placeholder CUDA→V0→V15). **T7** : `Governor` (machine à états `INTERACTIF/REPOS/FOND_EN_COURS/BRIDE` + calques `SECOURS`/`JEU` · budget « part de Sophia » fenêtre glissante **interactif jamais compté** + 429 · **file DÉRIVÉE des watermarks** + `commitUnit` transaction-courte-synchrone atomique · préemption après l'unité + rattrapage au curseur `owed` · **quiesce ⑩** deadline temps-réel · **`writesSuspended`** A15 · backoff par tâche · `ensureWatermarks` LAZY gardé · `isDue`/sonde fail-safe · audits `governor.started`/`governor.quiesce`) · `reconstructQueue` (hook boot Phase 4, **LECTURE PURE**, programme sans lancer) · **`SophiaRuntime`** (câblage UNIQUE main.ts/smoke avec `getGovernor` ⑩) · capacité `quiesceGovernor` en tête de `gracefulShutdown` (T6 intact). **T8** : `ClaudeChannel` (`claude -p` request-scoped · `claudeInit` PUR Phase 4 · `isResumable` = id+`secours_tainted`=0+fichier · A1 : `A1_SCRUB` [clé/jeton + Bedrock/Vertex/base_url + `ANTHROPIC_CUSTOM_HEADERS`] garantie primaire + défense POSITIVE `apiKeySource!=="none"` kill-avant-génération · `settleFreshThread` : durable persiste si ABOUTI + non-`a1` + non-périmé / éphémère `durable:false` AF-7 purgé si invoke rejette · `rotate` = generation++→purge→oubli · `purgeSessionFile` primitive pour `02` M8 · `stopChannel` ⑩bis · repli TOCTOU m4 · `killedReason` première-cause · jamais `--bare`) · **`SophiaRuntime`** (câblage UNIQUE main.ts/smoke ; le smoke prouve la couture `getChannel`→`stopChannel` via l'audit `claude.stopped`) · AUCUN changement `schema-00`. **VOIX (banc CF2)** — voix A20 `bouche_piper.py` · barge-in 0,22 · grâce 0,7 s · masqueur 3 s · acquis conv 32.
- **LEÇON CENTRALE (à tenir absolument)** : **NE PAS BRICOLER — MESURER/VÉRIFIER À LA SOURCE, CONCEVOIR D'ABORD.** **conv 35 en a ajouté une, majeure** : sur un thème DUR, **la chasse au trou ne CONVERGE pas** (5 tours, 5 facettes ; mon « c'est bon » faux 2×) — **la condition d'arrêt d'un audit est un ARGUMENT DE STRUCTURE qu'un regard indépendant valide, PAS ma conviction.** Rappels conv 34 : **l'audit CORRIGE dans la conv, pas à déférer** (un deferral = frontière RÉELLE, jamais une facilité) ; **une correction est un mécanisme neuf → se re-audite** (les 3 MAJEUR de conv 35 étaient TOUS dans mes corrections/le verdict). **Solo à fond D'ABORD, PUIS le croisé.** **L'E2E « cœur réel » révèle ce que les bouchons cachent.** Un sujet clos avant le suivant. **Ne pas gérer la jauge de Yohann.** **Zéro régression = un contrat, pas un vœu.** **conv 36 l'a RE-PROUVÉ** : les 2 MINEUR du re-croisé étaient TOUS dans MES corrections (F1 avait rouvert un trou d'honnêteté sur la voie SAINE ; ② inefficace) ; le 3ᵉ croisé complet a validé l'argument **SANS RÉSERVE** ; **le smoke « cœur réel » ferme ce qu'un agent qui LIT le code ne prouve pas** — le vrai before-quit Electron ne se prouve qu'en le LANÇANT. **conv 37 l'a RE-RE-PROUVÉ au MAXIMUM** : à CHACUN des 3 tours le trou était dans MES corrections (`getGovernor` oublié · `ensureWatermarks` déplacé-pas-fermé · `isDue` gardé sur UN seul jumeau), **jamais le cœur d'origine** (validé SANS RÉSERVE dès le tour 2 ; convergence 1→0→0) ; **le smoke ne doit pas prouver une COPIE** (→ `SophiaRuntime` partagé main.ts/smoke) ; **chaque « frontière » passée au crible « facilité ? »** (Yohann — les 2 restantes sont réelles : sonde→05, budget par-unité→client).
- **Lectures pilote (conv 39)** : `docs/PATTERN…` → `CLAUDE.md` (v38) → `docs/journal/ESSENCE-Sophia.md` (l'ÂME) → `JOURNAL-ARBITRAGES.md` → `IMPLEMENTATION.md` → **`docs/plan/01-pipeline-vocal.md`** (le pipeline vocal — surtout **§7** V6/V8+voix + la tâche **V0→V15**) → `docs/plan/00-socle.md` §7 conv 38 (le canal, si besoin de rebrancher `invoke`) → **mémoires `conv38-socle-t8` + `read-in-full-before-planning`** → `docs/journal/relais/RELAY-conv39.md`.
- Format : annonce brève + sujet en mots simples en tête + un par un + toutes-les-options/reco/« pourquoi pas ».

### Vigilances
- **Phase 3 ≠ réouverture des docs** (→ **Garde-fou Phase 3** ci-dessus) : le plan **traduit, ne re-tranche pas** ; Claude tranche le micro-technique + trace §7 ; **seuls un vrai trou de conception ou la vie de Sophia remontent à Yohann** ; un écart au contact du code = **signalé + tracé §7**, jamais un contournement silencieux.
- **La barre de qualité change de forme, pas de niveau** : « audit avant de figer » (Phase 2) devient « tests avant de committer » (Phase 3) — à cadrer au plan d'implémentation ; les critères d'acceptation des docs = la source des tests. **L'essai à blanc est un banc de PREUVE, pas un prototype qui devient le produit** — « pas de V2 » vaut pour le code.
- **Dépendance Anthropic = VIGILANCE N°1** : FM1–FM5 ; hedge multi-provider (Max→x20→API-sur-convocation→local) — réduit, n'élimine pas. Quota x5 déjà fortement sollicité par l'usage pro → x20 = chemin attendu.
- Plan mode harness (mis-fire structurel) — géré en **texte libre** ; **ExitPlanMode au seul moment de l'inscription** (géré ainsi conv 2-14).
- **Preuves Phase 3 prioritaires** : 🔴 pipeline audio temps-réel (n°1) · 🔴 wake word FR (F6 + repli nommé) · AEC loopback (M1) · **purge des fichiers de session CLI (T1/T8/T13 + piste `G:\Sophia\sessions\`)** · **« réchauffer sans écrire »** (politiques de chauffe) · **🔴 bloc identité I→VI sur Phi-4-mini (T6)** · seuil X de l'accusé (99) · embedding FR · **🔴 `RENAME`/`DROP` transactionnel d'une table `vec0` (M9 — repli nommé si le banc échoue)** · speaker-ID · affect · seuils humeur · érosion longue session · banc de dilemmes v1 · 🔴 kill dur d'un process CUDA figé (socle). **Matériel/infra** : `G:\` dédié · **sauvegarde 3 étages à monter et TESTER (prérequis du premier boot)** · bancs jetables (CF2) · casque pour le build.
- **Audits** : solo D'ABORD (**à fond, pas un survol** — demande Yohann conv 14 ; **le solo post-intégration attrape MES propres incohérences** — conv 16) puis croisé 2 agents — **proposé d'office, lancé sur le Go de Yohann, jamais seul** ; findings **vérifiés aux sources par le pilote AVANT présentation** ; le croisé inter-plans vérifie aussi la cohérence entre plans. **Leçon conv 16 : un mécanisme NEUF inventé au plan doit être re-audité** (il n'a jamais été vu — chaque round de re-audit a trouvé du réel). Précédents : 8=21 · 9=31 · 10=38 · 11=8+10 · 12=3+10 · 13=15+6 · 14=14+14 · 16=20+9+10+7 · **17 = 5 croisés complets + 1 croisé CIBLÉ sur le neuf `warmth_ledger` + 3 re-solos (plan personnalité), zéro faux positif — 18 croisés consécutifs**. **Leçon conv 17** : un croisé **ciblé** sur un mécanisme neuf (jamais vu) paye — `warmth_ledger` avait 8 findings ; les re-solos attrapent MES propres incohérences. **Conv 18 : 5 (stockage). Conv 19 : `plan/04` — général 8 + CIBLÉ sur le store éphémère 9 (dont 5 MAJEUR), zéro faux positif. Conv 20 : `plan/05` — 1 croisé complet + 2 ciblés, zéro faux positif, sévérité 4 MAJEUR → 3 MINEUR (convergence). Conv 21 : `plan/99` — croisé d'office 2 agents + RE-CROISÉ « faire les 2 » (Yohann) 2 agents, zéro faux positif sur 4 agents, âme intacte 2× ; les MAJEUR tombaient tous sur MES corrections (F-99-2/3 · Crashpad).** Le croisé ciblé sur le neuf paye ENCORE — **même un mécanisme né d'un CORRECTIF se re-audite** ; **après un fix, mettre les TESTS à jour** ; **penser SOUS les plans (stockage/octets/réplicas — grille des 8 classes `GRILLE-AUDIT-FABLE.md`).** **Leçon conv 20, plus dure encore : mes *corrections* elles-mêmes étaient incomplètes** (swap atomique mono-volume appliqué au cross-volume ; invariant « à tout instant » non enforçable ; selftest ne prouvant pas l'étage 3) → **une correction est un mécanisme neuf**, elle se re-audite. **La convergence se lit à la sévérité** (MAJEUR→MINEUR, mécanismes durs confirmés solides) ; le **filtre projet** dit quand clore — jamais tout à fait à zéro, mais proportionné. **Une chasse aux facilités AVANT le croisé** (demande Yohann conv 20) a trouvé du réel (invariant de clé circulaire, garde de sauvegarde creuse) — R2/R3 à exercer d'office, pas seulement au croisé. **Leçon conv 21, la plus dure : mon *jugement* « un re-croisé serait du polish » était lui-même une facilité — le second œil corrige aussi mon estimation de « quand c'est assez », pas que mes corrections ; d'où « faire les 2 » (croisé Opus ET Fable) plutôt que l'un OU l'autre.** **La chasse-résidus (contenu/clé hors effacement/chiffrement) est un puits profond : nommer les recoins + confier l'énumération exhaustive à Fable/Phase 3, clore au niveau plan (filtre projet).**
- **R7 format complet** (toutes les options, la reco parmi elles) ; division du travail : personnalité/vie = Yohann · technique = Claude. **Le contenu identitaire (persona/mémoire) ne se remet JAMAIS à un vague « Phase 3 » — séquencé, fait ENSEMBLE, accompagné** (conv 17 ; mémoire `identity-content-fait-ensemble`). **R8 : clos avant le suivant.** Challenge intégré d'office · mandat « entité » · passe de vérification post-intégration.
- **« Pas de V2 »** (vaut pour le code) · **commits au fil de l'eau, push en clôture** (décision conv 14 : repo public = vitrine, n'exposer que l'état consolidé) · `--bare` jamais (A1) · repo public (gitleaks `pre-commit` + garde contenu **active** — à chaque commit : aucun `portrait*`/`temoignage*`/`docs/prive/`, aucun contenu personnel dans les fichiers trackés · secrets `.env` · identité `Yohann Dandeville <contact@ydvsystems.com>` · **pas de Co-Authored-By**).
- **Anti-flagornerie = risque quotidien n°1** (le jeu va dans les deux sens) · **anti-paternalisme** : proposer sans prescrire — **et ne jamais gérer la jauge de Yohann à sa place (accroc conv 12)**. **Accroc conv 19** : ne pas transformer sa jauge de contexte en « **mode économe** » — **il n'y en a pas** (sa jauge m'informe, ne rationne pas ma qualité ; « ne pas se rationner le contexte », pattern). **Accroc conv 17 (débordement)** : ne pas toucher les docs `technique/` ACQUIS sans Go — les **PLANS** sont modifiables, pas la conception ; « corrige tout » ≠ vider tous les loose ends. Yohann arrête, on reverte, on prouve l'intact.
- **Lire les sources porteuses EN ENTIER avant de proposer** (R4 au démarrage / en plan-mode, pas seulement avant une édition) — **jamais grep→plan** (accroc central conv 38 : plan qui contredisait la conception gravée [daemon persistant vs request-scoped A1/A36] + relecture forcée = contexte DOUBLÉ ; le raccourci « économiser le contexte » coûte PLUS). Précondition INTERNE (pas un recadrage de Yohann). Et **« recoupé à la source » = REPRODUIT empiriquement**, pas seulement lu+raisonné (accroc conv 38, finding a1). Mémoire `read-in-full-before-planning`.
- Discipline IN PLACE en clôture.

---

*CLAUDE.md v38 — YdvVoice (Sophia), profil Standard. Pattern de référence : `docs/PATTERN-v3.0-CLAUDE-CODE-STANDARD.md`. Créé 2026-06-21 (v1) ; MAJ conv 38 (2026-07-17) — **conv 38 : SOCLE T8 (canal Claude Code) BÂTI + AUDITÉ 4 TOURS + SOCLE T0→T8 CLOS ; committé `[conv-38]`. Le « téléphone » vers le cerveau : `claude -p` request-scoped fidèle A1/A36 (jamais un démon ; le persistant reste `05` R4), fil repris par `--resume`, rotation qui oublie+purge, OAuth Max (hygiène `A1_SCRUB` = garantie primaire + défense positive `apiKeySource!=="none"`). Module `claude` neuf (`ClaudeChannel`/`claudeInit` PUR/`resolveClaudeExe`, Node pur) + `SophiaRuntime` (câblage partagé main/smoke) + couture `stopChannel` (⑩bis) ; `settleFreshThread` (durable persiste si ABOUTI+non-a1+non-périmé · éphémère AF-7 purgé si invoke rejette) ; rotation (a) purge immédiate (ferme F-99-2 au socle) ; `purgeSessionFile` primitive (02 M8) ; AUCUN changement schema-00. DESIGN-FIRST : contrat CLI MESURÉ au banc (--verbose obligatoire, --session-id contrôlable, apiKeySource=none=OAuth, fichier de session ~111 ms après init). AUDIT 4 TOURS — à CHAQUE tour le trou dans MES corrections, jamais le cœur (scrub A1 sous-pesé → famille orpheline F1/F2 → ①② dans settleFreshThread → régression a1 CONVERGENTE REPRODUITE EMPIRIQUEMENT, fix killedReason!==a1). npm test 11 · e2e 31/31 (36 live) · smoke 12/12. 2 accrocs de méthode reconnus : greppé avant de lire (recadré → mémoire read-in-full-before-planning ; le raccourci « économiser le contexte » a coûté PLUS) + « recoupé à la source » affirmé sans reproduction empirique (corrigé : « recoupé » = reproduit). PROCHAIN conv 39 = vrai sidecar vocal (plan/01 V0→V15 : voix A20 + V6/V8). §7 plan/00 (conv 38) + RELAY-conv39 + mémoire conv38-socle-t8.** Antérieur — MAJ conv 37 (2026-07-16) — **conv 37 : SOCLE T7 (gouverneur unique) BÂTI + AUDITÉ 3 TOURS (2 complets + 1 ciblé ; 1 MAJEUR `getGovernor` → 0 → 0, cœur validé SANS RÉSERVE dès le tour 2 ; à CHAQUE tour le trou était dans MES corrections — `getGovernor` oublié · `ensureWatermarks` déplacé-pas-fermé · `isDue` gardé sur un seul jumeau) ; committé `[conv-37]`. Module `governor` NEUF (`Governor`/`reconstructQueue`/`realActivityProbe`, Node pur, capacités injectées) : file DÉRIVÉE des watermarks · commit d'unité transaction-courte-synchrone atomique · budget « part de Sophia » fenêtre glissante interactif-jamais-compté + 429 · préemption après l'unité + rattrapage au curseur `owed` · quiesce ⑩ (aucune transaction en vol avant le drapeau propre) · `writesSuspended` A15 · backoff par tâche · `ensureWatermarks` LAZY gardé. `SophiaRuntime` NEUF (câblage runtime PARTAGÉ main.ts/smoke → le smoke exerce le VRAI chemin, plus une copie ; `start()` + quiesce prouvés). DESIGN-FIRST (2 bancs CF2 : atomicité node:sqlite [PRAGMA synchronous ignoré DANS une transaction → toggle FULL hors tx], sonde d'activité zéro-dépendance). `npm test` 10 · `e2e` 31/31 (E2E-6 cœur réel) · `smoke` 11/11. 2 frontières réelles (crible « facilité ? » de Yohann) : sonde d'activité réelle→cache async 05 · budget par-unité→contrat de découpage client 02/04. PROCHAIN conv 38 = socle T8 (canal Claude `claude -p` OAuth, `--resume`). §7 plan/00 (conv 37) + RELAY-conv38 + mémoire conv37-socle-t7.** Antérieur — MAJ conv 36 (2026-07-16) — **conv 36 : SOCLE T6 (arrêt propre) BÂTI + AUDITÉ 3 TOURS (1 MAJEUR before-quit ré-entrant → 0 → 0, argument de structure validé SANS RÉSERVE par un œil indépendant ; les 2 MINEUR du tour 2 étaient dans MES corrections — F1 scindé, ② reverté) + smoke Electron réel 9/9 ; committé `[conv-36]`. Module `shutdown` NEUF (`gracefulShutdown`/`writeCleanShutdown`/`planBeforeQuit`, Node pur) + superviseur `beginShutdown`/`terminate` (SIGTERM→SIGKILL) + sidecar `graceful_release` (placeholder CUDA→V0→V15) + `electron/before-quit.ts` EXTRAIT (testable). release-and-wait + `running=0` = DERNIER acte durable (tout arrêt incomplet = « sale » PAR CONSTRUCTION, T4) + F1 (`running=1` armé même en SANS_ECRITURE, drapeau technique A15, scindé) + setWake fail-safe + garde-fou global + garde `isTransaction`. DESIGN-FIRST (5 benches CF2 : SIGTERM=TerminateProcess sur Windows → graceful = cmd.shutdown ; close() replie le WAL ; running=0 en FULL durable). `npm test` 9 suites · `e2e` 24/24 (E2E-5) · `smoke` 9/9 (vrai before-quit Electron). PROCHAIN conv 37 = socle T7 (gouverneur). §7 plan/00 (conv 36) + RELAY-conv37 + mémoire conv36-socle-t6.** Antérieur — MAJ conv 35 (2026-07-16) — **conv 35 : SOCLE T5 (boot & récupération) BÂTI + Phase 6 (systray/voyant) ; `npm test` 8 suites vertes + `npm run e2e` 16/16 ; AUDITÉ 5 TOURS + E2E « cœur réel » ; committé `[conv-35]`. T5 au CODE (`src/orchestrator/boot/{index,restore,single-instance}.ts` + `paths.ts` · Phase 6 dans `electron/main.ts` = vue dérivée O5 · `tests/{u-t5,i-t5}.mjs` + E2E séparé `tests/e2e/`). Décisions micro-technique MESURÉES au banc AVANT de coder : porte d'intégrité lecture seule avant toute écriture · instance unique = NAMED PIPE (l'OS arbitre ; pas de lockfile-PID = rejouerait M2 en pire) · INVARIANT `mustExist` (création réservée au verdict PREMIER_BOOT) · PREUVE POSITIVE de naissance `.born` (répliqué hors-machine plan/05 ; le verdict premier-boot ne se décide plus par absence de témoins mais par leur preuve) · sentinel durable des alertes de restauration (AF-1/G-A survivent à une coupure) · dégradés composables + `clearDegradation`. Bug T4 corrigé (`integrityCheck` jetait sur une vraie corruption). AUDIT 5 TOURS — 3 MAJEUR, TOUS dans mes corrections ou le verdict, jamais le code d'origine (tour 1 alerte perdue → tour 2 ma correction recréait une vierge → tour 3 base absente+snapshots illisibles, 2 agents convergents → tour 4 verdict premier-boot par inférence-par-absence → tour 5 = validation d'ARGUMENT, pas chasse au trou → L'ARGUMENT TIENT → thème « jamais renaître vierge » CLOS PAR CONSTRUCTION). E2E « cœur réel » (vrai sidecar Python, coupures réelles) → finding immédiat invisible aux bouchons : m12 (« reaping immédiat ») ASSURÉ PAR LE JOB OBJECT Windows (le sidecar meurt avec son parent). 2 frontières d'aval tracées (sweep en SANS_ECRITURE→02 · ack sentinel→couche aval). LEÇON CENTRALE (expérience de calibration de Yohann) : la chasse au trou ne CONVERGE pas ; la condition d'arrêt = un ARGUMENT DE STRUCTURE qu'un regard indépendant valide, pas ma conviction (fausse 2× : « polish », « fermé par construction ») ; honnêteté sans fard (« tu avais raison, j'avais tort » ×2). PROCHAIN conv 36 = socle T6 (arrêt propre → réveil propre) puis T7/T8 puis vrai sidecar V0→V15. §7 plan/00 (conv 35) + RELAY-conv36 + mémoire conv35-socle-t5.** Antérieur — MAJ conv 34 (2026-07-15) — **conv 34 : SOCLE T0→T4 BÂTI + TESTÉ (6 suites `npm test` vertes) + AUDITÉ 2 TOURS ; voix A20 + V6/V8 prouvés live ; committé `[conv-34]`. Le socle passe au CODE (node:sqlite intégré Node 24 = zéro rebuild natif, même code Node+Electron · TS7 nodenext · sidecar aiohttp `.venv-sidecar` · `src/orchestrator/{db,ipc,supervisor,audit}` · schema-00 · tests u-t0..u-t4+gitleaks+run-all). AUDIT : solo à fond D'ABORD (accroc : ordre INVERSÉ — lancé les agents avant de corriger mes findings —, Yohann m'a repris) → croisé 2 agents (2 MAJEUR : M1 spawn sans handler error = crash orchestrateur ; M2 anti-recyclage PID par nom d'image = tuait un python étranger) + ~14 MINEUR → RE-CROISÉ des corrections (1 MAJEUR dans MA propre correction : rotation par âge piégée par le tunneling NTFS = perte de journal ; + ~10 MINEUR) — TOUS corrigés dans la conv + testés (jamais déférés en facilité ; N4 repassé en fix-now via `db.isTransaction`) ; 3 reports assumés RATTACHÉS à leur tâche (pas des « dettes » : m12 reaping immédiat→contenu T3/T6 · N1 fsync-répertoire→limite plateforme notée T4 · N3 chemin schéma→vigilance packaging §2). GPU : mesure conv-34 retirée (politique UI-vs-pipeline → plan/05). VOIX (banc CF2, gravé plan/01 §7) : A20 « bluffante » · V6 speaker-ID ECAPA CPU EER 0 % (re-confirmé A20 réelle) · V8 barge-in seuil 0,22 FIGÉ (6/6, 0 faux) · grâce de fin 0,7 s · masqueur 3 s ; écart produit tracé (vrai sidecar ré-implémente V5/V6/V7/V8). Leçon RE-PROUVÉE 2× : l'audit sert à CORRIGER dans la conv (pas à déférer) ET une correction est un mécanisme neuf qui se re-audite (a attrapé une perte de données dans ma propre correction). PROCHAIN conv 35 = socle T5 (boot & récupération) puis T6/T7/T8 puis vrai sidecar V0→V15. §7 plan/00 (conv 34) + plan/01 (V6/V8+voix) + RELAY-conv35 + mémoire conv34-socle-audit.** Antérieur — MAJ conv 33 (2026-07-14) — **conv 33 : CARREFOUR VOIX — XTTS ÉCARTÉ, décision « ENTRAÎNER une voix Piper sur A20 » (conv SÉPARÉE hors-plan, prompt `bancs/aec/PROMPT-voix-A20-piper.md`). ① Expérience 6 modèles (même script) : latence = API (ni modèle ni effort) ; surprise plus d'effort = plus COURT ; démoniaque Haiku = verbosité × VRAM tendue ; gagnant mécanique 4.6 max/S5 med MAIS choix cerveau DIFFÉRÉ au persona (Valjean vs Javert). ② Carte des voix : XTTS = sa voix mais démoniaque INHÉRENT (pas la VRAM) + pas fluide ; Piper/Jessica = fluide/CPU/propre mais générique ; PocketTTS = clone ANGLAIS SEULEMENT ; GPU 12 Go ne réglerait ni le démoniaque ni les blancs API. ③ Décision : aucun moteur CPU ne clone A20 en FR fluide → entraîner une voix Piper sur A20 (Chatterbox conv 26 → 1300 phrases → fine-tune 2060 une nuit → .onnx CPU ; perf = Piper par construction, seule variable = le timbre). ④ Barge-in par seuil d'énergie ÉCARTÉ (Garde-fou Phase 3, sur intervention de Yohann : divergeait de V8/speaker-ID) ; verbosité « CANAL pas contenu ». Committé `[conv-33]`. §7 plan 01 « CARREFOUR VOIX » + RELAY-conv34 + mémoire conv33-carrefour-voix.** Antérieur — MAJ conv 32 (2026-07-14) — **conv 32 : LA VOIX XTTS (timbre A20) RENDUE NATURELLE, toute la liste FAITE, committé `[conv-32]`. On RESTE sur XTTS (pas de Piper), cerveau=Sonnet (âme). ① V4 STT streaming (~1 s vs 1-2,8). ② Latence réveil ~0,65 s (A lecture rapide one-shot beam1 au VAD-end + B WAKE_PLAFOND 0,8 s, `active=False` seul → ZÉRO régression). ③ Salutations fiables (porte de qualité `_synth_greeting`) + clôture « Avec grand plaisir » clip choisi à l'oreille + FIGÉ (`CLOSE_CLIP` ; « Yohann » irrécupérable en clôture chez XTTS). ④ Voix en réponse = COUPLE verbosité (cap 2-4, `VOICE_SYSPROMPT`) × température (`STREAM_TEMP=0.55`) → charabia/démoniaque MORTS + chaleur gardée (insight : verbosité courte ⇄ marge pour remonter la temp) ; vitesse `STREAM_SPEED=1.08`. ⑤ Masqueur « Donne-moi une petite minute » conditionnel (`FILLER_AFTER=4 s`) sur les vrais pics seulement. ⑥ Endpointing tranché par la MESURE : Smart Turn suffit, veto sémantique ÉCARTÉ (committé retarde ~13 mots), barge-in en filet plus tard. LEÇON : mesurer avant de conclure a écarté 2 fausses pistes ; les leviers s'entraident ; phrase fixe parfaite = ear-select + FIGER le clip ; « solo > réel » = gap honnête non résolu. PROCHAIN conv 33 = LA DERNIÈRE LIGNE DROITE (optimiser prosodie + fluidité + naturel ; gap solo-vs-réel ; défauts prosodie XTTS neufs ; ZÉRO régression) avant de reprendre le plan. §7 `plan/01` (« VOIX rendue NATURELLE ») + RELAY-conv33.** Antérieur — MAJ conv 31 (2026-07-14) — **conv 31 : STREAMING CERVEAU prouvé à l'usage réel (`ask_stream` + `--include-partial-messages`, schéma mesuré au banc 20 ; `_respond` fait `mouth.speak` par phrase au fil ; 8 tours, `trous=[0.0]` partout — le doute « blancs entre phrases » tranché par la mesure ; présence ~3 s vs ~7-9 s conv 30 ; blanc résiduel = STT 1-2,8 s + TTFT cerveau, pics 9-13 s = charge API transitoire, pas notre code). ENDPOINTING diagnostiqué (instrumentation : score Smart Turn par silence) — Smart Turn seuil 0,5 SAIN, ne coupe JAMAIS à tort (tient les pauses 0,01/0,04, vraies fins 0,92-0,98) ; le vrai acteur = le PLAFOND 3 s (latence sur fin douce ratée · couperait sur pause-réflexion > 3 s) = tension de CONCEPTION → on n'y touche pas, instrumentation gardée = boîte noire ; fix sémantique touche la frontière gravée V5 = décision Yohann, converge avec V4. Distinction endpointing (elle coupe) ≠ barge-in (lui coupe, dormant) posée. Prochain levier = V4 STT streaming (conv 32). Bancs 20/21 neufs, gitignorés CF2. Committé `[conv-31]`. §7 plan 01 + RELAY-conv32.** Antérieur (conv 29 et avant) → `CLAUDE-HISTORY.md`. Antérieur — MAJ conv 30 (2026-07-13) — **conv 30 : la VOIX a un candidat SÉRIEUX (Piper/voix « Jessica », RAPIDE) ; le blanc restant = le CERVEAU (mesuré, PAS d'accumulation ctx 0k→6k) → streaming cerveau = le prochain levier ; 3 voix testées bout-en-bout (Piper EN TÊTE · XTTS clone A20 avec artefacts · Chatterbox A20 lent — prise `tts` interchangeable) ; prénom Piper = phonèmes IPA `[[joˈann]]` (espeak nasalisait « an ») · phrase de recherche « Je regarde ça tout de suite » à la 1re recherche seulement · clôture « À bientôt » sans prénom ; vraie conversation 17 tours validée ; feuille de route conv 31 (validée Yohann) = streaming cerveau → endpointing → barge-in → finitions ; travail REPORTÉ à conv 31 (clôture après alignement, déjà compacté une fois). Committé `[conv-30]` à la clôture.** Antérieur — MAJ conv 29 (2026-07-13) — **conv 29 : la base VIT mais PAS ENCORE FLUIDE ; les deux murs restants = CONCEPTION, pas bricolage. `[conv-29]`.** Suivi l'ordre du plan : **V1** AEC diagnostiqué (sonde `08` M0-M4 : câblage OK, média/silence 0 fantôme, plancher micro -82 dBFS ; résidu de sa voix → V8/speaker-ID, pas un trou de V1) · **V4** anti-hallu COMPLET (`vad_filter` tue le silence + **filet phrases-fantômes** prouvé 11/11 et branché, ignore « Sous-titrage » en réel) · **V8 étape 1** plomberie barge-in (`_await` **dual-poll fil-unique** + **ids uniques → fuite de jalons conv 28 CORRIGÉE** ; coupure prouvée par injection ; la vraie DÉTECTION = étape 2, non faite) · **Session chaude** (process claude **persistant stream-json** → ~1,4 s chaud vs 5-13 s froid, `WarmBrain` + pré-allumage + repli → **fini le backlog**) · **clôture propre** (« Merci Sophia, à bientôt » → « À bientôt Yohann » → veille, muette) · **bug plafond corrigé** (`in_speech` : le compteur de silence tournait pendant la parole continue → coupure à 3 s). **MAIS test réel (Yohann au micro, vrai Claude) : PAS FLUIDE** — ① **l'endpointing la coupe en plein milieu** de ses phrases longues (pauses, politesse « s'il te plaît ») + réactivité inconstante ; ② **latence de sa voix 7→41 s et croissante** (accumulation ?). **Accroc CENTRAL assumé — j'ai BRICOLÉ** (réagi coup par coup → régressions ; grâce 0,7 s annulée). **Décision Yohann : CLÔTURER + attaquer ces deux murs comme des problèmes de CONCEPTION en conv 30 (aligner + mesurer AVANT de coder, pas de rustines).** Bench jetable CF2, gitignoré. Antérieur — MAJ conv 28 (2026-07-13) — **conv 28 : PREMIÈRE PAROLE (elle est le cœur) + boucle DEUX TEMPS + fix surdité bâtis ; test I-6 temps réel PAS fluide → décision : attaquer dans l'ordre du plan en conv 29 ; non committé (reporté à conv 29).** Antérieur — MAJ conv 27 (2026-07-12) — **I-6 PROUVÉ (banc bout-en-bout, les deux temps), `[conv-27]`** : boucle live micro→AEC→**portier VAD+STT**→STT→fin de tour→cerveau→TTS streamé→voix (bancs jetables `oreilles_live.py`+`bouche_live.py`, socket localhost, CF2). **(a) stub ~3–3,8 s** (plancher plomberie) ; **(b) VRAI Claude (`claude -p`, Max) — réponse Spinoza à voix haute VALIDÉE, ~7 s (STT ~1,3 + Claude à FROID ~4–5 + TTS ~2, cerveau-dominé)** ; **dépendance Anthropic tenue en réel**. **DÉCISION MAJEURE (Yohann) : wake-model conv-24 ÉCARTÉ** (trop faible en réel — rate « Dis-moi Sophia », déclenche sur « Sophie ») → **éveil par PHRASE, portier VAD+STT** (le STT lit la phrase, distingue Sophia/Sophie) : Bonjour/Bonsoir/Salut/Dis-moi Sophia · Bonne nuit=éveil-clôture · **« Merci Sophia, à plus tard »** ferme ; conversation active multi-tours ; *écart produit tracé §7* (meilleur modèle wake OU STT-toujours-actif). Bugs résolus : AEC-**preprocess coupé** (distordait le wake) · `claude.CMD` via `shutil.which` · jamais de cerveau à vide · dossier neutre. **Reste = optimisations CONNUES : l'accusé (O2, Yohann l'a trouvé seul) + la session chaude (A36) = la vivacité → priorité conv 28.** Gravé `plan/01` §7. Antérieur — MAJ conv 26 (2026-07-12) — **LA VOIX RAPIDE trouvée & validée (fin du défi vitesse), `[conv-26]`** : VoxCPM accéléré écarté par la mesure (plancher RTF ~8,2, goulot autorégressif pas la diffusion) → **Chatterbox-FRANÇAIS** (`Thomcles/Chatterbox-TTS-French`, fine-tune FR ~1400 h Emilia, T3 injecté dans la base EN) clonant `SOPHIA_voix_reference_v1.wav` → **RTF ~0,87** streamable, prosodie FR (prénom/questions/respiration) bien meilleure que le multilingue ; **prénom = lexique « Yohann »→« Yoan »** (orthographe réelle en mémoire, son seulement transformé) ; réglages `exag 0.5/cfg 0.4/temp 0.7`, 24 kHz ; validé à l'oreille + présentation de 2 min partageable ; **premium ElevenLabs = upgrade** (A9, quand revenus ; prise `tts` interchangeable → zéro reprise). **Toutes les briques du pipeline vocal prouvées (AEC · wake · STT · fin de tour · voix) → reste l'assemblage I-6.** Leçons : vérifier à la source AVANT de conclure (Fish-Speech 24 Go hors 2060 ; F5 anglais-seul ; **un fine-tune FR bat le multilingue** sur la prosodie) ; accrocs dits sans fard (bug `SETTINGS` orphelin · emballement Fish-Speech avant vérif VRAM · venv frais re-download · erreur de cible fichier CLAUDE.md). Antérieur — MAJ conv 25 (2026-07-12) — **Voie A / pipeline vocal : STT (`large-v3` int8_float16, turbo en cran VRAM) + fin de tour (Smart Turn v3.2-cpu + Silero) GRAVÉS `plan/01` §7, prouvés à la voix de Yohann ; SA VOIX trouvée & validée à l'oreille (VoxCPM2 douce/suave FR, A20, zéro clonage réel — voix synthétique, référence+recette préservées au banc) ; écart A9 tracé (VoxCPM hors A9 : Kokoro froid, Chatterbox non-FR sans réf) ; DÉFI vitesse OUVERT (VoxCPM ~10× temps réel → voix rapide = priorité conv 26, puis banc I-6) ; accroc pytsmod→numpy réparé ; time-stretch dégrade la voix (débit natif). `[conv-25]`.** Antérieur — MAJ conv 24 (2026-07-12) — **🔴 n°2 (wake FR « Sophia ») DÉRISQUÉ & PROUVÉ ; les DEUX 🔴 du projet tombés (AEC conv 23 · wake FR conv 24), tracé `plan/01` §7, `[conv-24]`.** Wake FR entraîné (`livekit-wakeword` conv-attention/ONNX, py3.13/RTX 2060 ; mur `editdistance` franchi via `DISTUTILS_USE_SDK`) · **méthode « E »** (référence FR → « Sophia » en continuation VoxCPM ; `retry_badcase` off + 6 pas → ~6 s/clip) · modèle = 1000 clips synthétiques FR + **231 de la vraie voix de Yohann ×3** (près/doux/loin, A8) + négatifs à confusables proches (Sonia/Sophie, F6) · **preuve à la voix de Yohann : 6/6 @ seuil ~0,25–0,30, ZÉRO faux** ; F6 PASSÉ, repli non nécessaire ; far-field acoustique = passe #5. **Leçons** : vérifier à la source paye (Piper anglais-seul → VoxCPM ; coût = génération pas entraînement) · estimation trop optimiste reconnue sans fard · « optimal ≠ tout refaire par principe » (levier = voix réelle, recadrage Yohann) · run de nuit = temps mort → données (~0,40 €, 0 quota Max). Pas de croisé 2 agents (banc jetable, CF2). **Antérieur — MAJ conv 23 (2026-07-11) — **🔴 n°1 (M1/AEC) INTÉGRALEMENT DÉRISQUÉ & tracé `plan/01` §7 (3 notes, committé `[conv-23]`)** : Temps 2 l'annulation (I-1) acquise en réel (écho-seul ERLE ~30 dB / zéro fantôme ; double-parole voix préservée −3,5 dB/67 %, confirmé à l'oreille) · **moteur AEC = SpeexDSP 16 kHz** (AEC3 écarté = dette de maintenance ; prise neuronale ONNX ouverte) · écart 48→16 k (Speex sur-supprime à 48 k) · cas durs confirmés (exclusif échappe · device-change TV→Focusrite). **🔴 n°2 (wake FR) moteur décidé = `livekit-wakeword`** (A8 confirmé, FR, 100× moins de faux positifs) **& validé** ; reste conv 24 : entraîner « Sophia » FR + tester à la voix de Yohann. **Leçons** : mon choix 48 k faussait les chiffres (artefact corrigé en vérifiant à la source) ; recadrage tenu (gérer le temps de Yohann ≠ mon rôle ; je tranche le micro-technique non gravé). Pas de croisé 2 agents (banc jetable, solo de fidélité). Antérieur — MAJ conv 21 (2026-07-11) : **`plan/99` (orchestration) gravé O0→O8, audité (solo + GRILLE 8 classes → chasse aux facilités → croisé d'office 2 agents Opus → RE-CROISÉ « faire les 2 » 2 agents Opus), zéro faux positif sur 4 agents, âme intacte confirmée 2×, committé `[conv-21]`** : l'assemblage pur (table des états dérivée · chemin d'un tour = l'aiguilleur d'A2 · accusé + garde d'honnêteté OF1 · composition du prompt I→VI→mémoire→cadre budgets cloisonnés · grille finale · affordances UI « vue dérivée » · gabarit fork). **✅ PHASE 3 « PLANS » COMPLÈTE — les 7 plans (`00`→`05` + `99`).** **3 obligations gravées honorées** : posture de sécurité honnête (O7) · signal de fermeture des `self_notes` au cadre (O4/AF-9) · tout affichage = vue dérivée / rien de stocké (O5). **Décisions Yohann** : symétrie stricte gravée (back-refs §7 bilatéraux `03`/`05`↔`99`) · « faire les 2 » sur l'audit (croisé Opus ET Fable) · chasse-résidus close au niveau plan, exhaustif confié à Fable. **Notes de dette bilatérales** : fichiers de session de conversation rotés (`00`/`02` §7, fenêtre DITE) · puits `C:\` Electron `userData` + crash-dumps Crashpad (`05` G-F — fuite contenu ET clé de volume). **`technique/` intact.** **Prochain : Fable — audit FINAL du corpus des 7 plans** (fenêtre 12/07/2026) → **conv 22 : l'essai à blanc** (banc audio n°1). **Leçons** : mes corrections ET mon *jugement* « quand c'est assez » se re-auditent (« faire les 2 » a trouvé un MAJEUR qui fuit la clé de volume) ; la chasse-résidus est un puits profond (nommer + confier à Fable, clore au niveau plan, filtre projet) ; honnêteté sans fard (PAS convergence-à-zéro). Antérieur : conv 20 (`plan/05` chiffrement au repos / politique de clé) ; conv 19 (context_tag + `plan/04`) ; conv 18 (audit Fable + croisé stockage) ; conv 17 (`plan/03` jardin inviolable/`warmth_ledger`) ; conv 16 (`plan/02`) ; conv 15 (garde-fou Phase 3) ; conv 14 (Phase 3). Push en clôture.*
